# VRC-A — Claude Code Instructions

## Project Overview
Android app called VRC-A (VRChat Assistant).
Built with Kotlin, Jetpack Compose, Firebase Firestore, and GitHub Actions CI.
There are three build variants: a public user-facing APK, a separate admin APK, and a Meta Quest **headset** APK.
Display name embedded in UI strings: "Ashoska Mitsu Sisko".

## Repository Structure
- Main app source: `app/src/main/kotlin/com/vrca/`
- **UI revamp + onboarding spec**: `docs/ui-revamp.md` — approved design doc (not yet implemented) covering the public-build UI compaction, first-open onboarding tutorial, Settings account management (VRChat/Discord logout + inline re-login, OSC hard-blocked while logged out of VRChat), ToS/login redesign, and implementation order. Read it before starting any public UI work.
- Kotlin package: `com.vrca` (applicationId stays `com.scrapw.chatbox` for install continuity)
- Subpackages: `admin/`, `app/`, `data/`, `discord/`, `keepalive/`, `nowplaying/`, `osc/`, `overlay/`, `sync/`, `ui/` (with `common/`, `conversation/`, `screen/`, `settings/`, `theme/`, `viewmodel/`), `update/`, `vrchat/`
- Admin vs public build separation: `BuildConfig.IS_ADMIN_BUILD`
- **Headset (Meta Quest) build variant (M1 scaffold shipped)**: a third product flavor `headsetApp` (applicationId **`com.gremlin.inc.headset`**, `BuildConfig.IS_HEADSET_BUILD=true`; `IS_HEADSET_BUILD=false` on public/admin) alongside `publicApp`/`adminApp`. Its own Firebase app (`app/src/headsetApp/google-services.json`, same `vrc-a-b592e` project) + a headset-only `VideoTranscode.kt` stub (mirrors publicApp; the transcoder is admin-only) + a headset manifest overlay (`app/src/headsetApp/AndroidManifest.xml`) that merges `android:screenOrientation="sensorLandscape"` onto `MainActivity` so the app opens in Quest's 16:10 **1024×640 dp default panel** ("monitor" shape) instead of a tall phone. **Monitor-shape framing is width-responsive, NOT flavor-gated**: `VrcaScreen`'s Scaffold content is wrapped in a `BoxWithConstraints` that centers page content at `HEADSET_MAX_CONTENT_WIDTH` (720.dp) once available width ≥ `HEADSET_WIDE_THRESHOLD` (640.dp) — the Quest panel (1024 dp) is well above it so content reads as a centered monitor window; a phone (portrait ~360–420 dp) stays below it so it's untouched (also helps tablets/foldables). Top/bottom bars keep full panel width as the window chrome; only the content centers. Build: `./gradlew assembleHeadsetAppDebug`. Distribution target is the **Meta Horizon Store / App Lab** (dev/testing via sideload/MQDH). **Fixed panel (no user resize — best-effort)**: the headset manifest sets `android:resizeableActivity="false"` on BOTH `<application>` and `MainActivity`, PLUS `android:minAspectRatio="1.6"`+`android:maxAspectRatio="1.6"` on `MainActivity` to pin the 16:10 shape. **Honest limit (device-confirmed): Horizon OS still lets the user resize a 2D panel regardless** — Android has no hard "no resize" for VR panels. The real safeguard is the width-responsive framing + the Home **3/2/1-column thresholds** (below), which degrade gracefully at any size, so resizing is harmless even when it can't be prevented. **Headset Home = THREE columns** (`HomePage.kt`, `HOME_THREE_COL_THRESHOLD`=820.dp, cleared by the 1024dp panel; drops to 2-col ≥560dp then 1-col): **left** = skinny preview, **middle** = Line Toggles + Manual Send (they fit together), **right** = the instance roster pinned far-right. Each column scrolls internally; the Manual Send card hosts a hoisted `expandedState` + a `BringIntoViewRequester` so expanding it scrolls the middle column down to reveal the full editor. Headset top-bar notification badge is `offset(x=-2,y=4)` so it sits ON the bell instead of clipping past the bar's top edge; the Home NAV setup badge (`setupNeedsAttention`) is suppressed on the headset build (redundant — the same items already show in the top-bar notification button). **Instance-roster — LOG READER CONNECTED (M2 hookup shipped; roster is LIVE from the log)**: `VrcLogParser` (`com.vrca.vrchat`, PURE Kotlin, throwaway-testable) turns VRChat output-log lines into a `LogEvent` stream (`Joining wrld_…:instance~nonce`, `Entering Room:`, `OnPlayerJoined/Left <name> (usr_…)` — usr id optional for the older name-only format, `OnLeftRoom`, `Switching <name> to avatar <avatar>`) and folds them via `apply(state, event, nowMs)`/`replay(lines,…)` into an immutable `InstanceState` (location/world/nonce + a `roster` map keyed by userId, or `name:<name>` fallback). **`InstanceRosterManager` (`com.vrca.vrchat`, headset-started only)** is the glue: an idempotent `start(ctx)` launches an IO loop that folds the newest log via `VrcLogParser`, enriches each member's platform via `VrchatAuthManager.fetchUserInfo`→`prettyPlatform` (cached per userId, 300ms-paced; **only a SUCCESSFUL lookup is cached — a failed one (429/network) is left uncached so the 2s publish loop retries it, capped at 6 attempts; this fixed "non-friends never show a platform", they're fetched after friends so they were the ones hitting the rate limit on the initial burst and getting permanently cached as blank**), and publishes a `RosterUi{status(NEEDS_PERMISSION/NO_LOG/IDLE/LIVE), worldName, location, members[displayName,userId,platform,avatarName], logPath}` `StateFlow`. **Log-derived self-presence (plan §9 — headset only)**: on every fold the manager also calls `VrchatPipelineState.applyLogPresence(active, inWorld, location, worldName, playerCount, seedUserId, seedName)`, feeding the log-derived **location / world name / instance / player count** into the shared `VrchatPipelineState.presence` so the Discord RPC + VRChat-tab UI read it — instant, log-accurate (the roster tally matches the in-game panel), and **instance HOPS are picked up the moment the log writes `Joining wrld_…`** (no REST poll for these fields). The `presence` SETTER applies a **headset log override** (`applyHeadsetLogOverride`, gated `IS_HEADSET_BUILD && headsetLogActive`): every presence write (REST poll, WS self-location event, or the log seed) has those four fields replaced by the current log values while **profile fields (status/rank/pic) pass through untouched** — so REST/WS can never clobber the log location/count, yet still populate the profile. When the log isn't available (no All-files/SAF access → `headsetLogActive=false`) the setter is a passthrough so REST drives presence normally; **mobile (public/admin) is completely unaffected** (no log, `IS_HEADSET_BUILD=false`). **VRChat-closed detection → force offline**: VRChat writes no "left" line on a force-close / headset power-off, so the last state would otherwise show the user in an instance FOREVER (and the presence-driven uptime/RPC timer would never stop). "VRChat is open" is detected **primarily via `VrcaOscQuery.isServiceUp()`** — the OSCQuery client already GETs VRChat's HTTP endpoint every 250ms, so a real close (HTTP unreachable) clears its `host` within ~250ms; this is fast, reliable, and **immune to an AFK user whose log goes quiet for minutes**. **Log-mtime staleness (`LOG_STALE_MS` = 5 min) is the FALLBACK only** for when OSC is disabled in VRChat (no OSCQuery service): `alive = VrcaOscQuery.isServiceUp() || (now - LogRef.lastModified < 5min)`. The longer 5-min fallback avoids false-closing an idle no-OSC user; the file mtime persists across a reboot, so a fresh boot doesn't re-read a very old log as "in-instance". On stale it publishes `applyLogPresence(active=false, inWorld=false, location=null,…)`; the **active→inactive edge** in `applyLogPresence` forces the current presence to `location="offline"/isOnlineInVRChat=false/state="offline"` immediately (REST confirms on its next poll; if VRChat reopens the fresh log goes active again and re-seeds). This is what makes `VrchatUptime` (the "in VRChat" uptime + the Discord RPC counter, both keyed on `isOnlineInVRChat`) actually STOP when VRChat closes, and clears the stale instance location. Log-staleness was chosen over polling the VRChat REST location for this because it's free/on-device/no-lag (REST is the authoritative backstop once `active=false` hands presence back to it). This is increment 1 (log AUTHORITATIVE for those fields); the REST presence poll still runs on the headset for profile fields — a follow-up increment can gate it off for the full "zero VRChat API calls for presence" win (status via WS `user-update`, rank/pic via a one-time `/auth/user`). **Log access mirrors the reference companion (`docs/vrc-nexus-teardown.md` §4.7) — this was a REAL bug the first attempt got wrong**: (1) VRChat's Quest package is **`com.vrchat.VRChatAndroid`** (legacy `com.vrchat.oculus.quest`), NOT the guessed `com.vrchat.mobile.*`; (2) candidate dirs also include the SHARED `/sdcard/Documents/Logs`, `/sdcard/Documents/VRChat`, `/sdcard/VRChat/Logs`, `/sdcard/VRChat`; (3) direct File read (needs `MANAGE_EXTERNAL_STORAGE`) is tried first, then a **SAF folder grant** (`ACTION_OPEN_DOCUMENT_TREE`, persisted via `takePersistableUriPermission`, re-resolved from `persistedUriPermissions`, listed/read via `DocumentsContract`+`ContentResolver` with offset-skip tailing) — the SAF route is what works when `Android/data/<vrcpkg>` is File-API-blocked (Android 11+/most Horizon OS). It tails the newest `output_log*.txt`/`player.log`/`vrchat*.log` by byte offset (replays a new/rotated file from 0; folds only up to the last complete line). **`VrcLogParser` also parses `Joining or Creating Room: <name>`** for the world name (in addition to `Entering Room:`). **CRITICAL user-facing requirement: VRChat must have Logging = FULL** (Settings → Debug) or `OnPlayerJoined`/`Joining` lines are never written — the panel's NO_LOG state spells this out. `InstanceRosterPanel` (`com.vrca.ui.screen`, its own file, observes the flow, starts the manager on headset) renders the four states via `AccessState`: NEEDS_PERMISSION/NO_LOG show a "Grant file access" button (only when All-files is missing) + a "Choose log folder" SAF button + (NO_LOG) the Logging=FULL checklist; IDLE/LIVE show the not-in-a-world hint or live member rows (initial-circle avatar + name + platform **brand SYMBOL** — Windows-blue / Android-green / Apple-light circular glyph via `PlatformSymbol`, matching VRChat's own instance-card badges + the event-alert `PlatformSymbols`, replaced the old text chip). **Roster ordering + friends/self**: members are sorted **you first, then friends, then everyone else** (self via `VrchatAuthManager.getStoredUserId`, friends from the local `FriendsCacheStore` keys, 15s TTL — no API cost), each by join time; YOUR name renders **purple** (0xFFB388FF), a friend's **yellow** (0xFFFFD54F). Platform enrichment walks this SAME order as a **single-flight** pass so chips fill in top-to-bottom. **Row avatars**: each member shows their VRChat pic (VRChat+ icon → worn-avatar thumbnail, from the SAME `/users/{id}` call as platform — zero extra requests) via `VrchatImageLoader` (session-authed) with **disk cache DISABLED** so it's memory-only/temporary (evicts on leave / memory pressure, never builds up on disk); initial-circle fallback. **Self reuses the VRChat tab's already-loaded pic + platform from `VrchatPipelineState.presence`** (no fetch — self is excluded from enrichment). `pfpCache` (userId→url) clears on leaving the instance alongside `platformCache`. **Instant hop detection**: a `FileObserver` on the direct-file log path wakes the read loop the moment VRChat writes a line (no extra permission — rides All-files access; less CPU than polling), with the 1s poll as the fallback + the only path for SAF content URIs (unwatchable). **Non-friend platform — the REAL bug was FIELD PRECEDENCE, not rate-limiting (proven via an on-device diagnostic)**: `VrchatAuthManager.fetchUserInfo` read the `platform` field FIRST, falling back to `last_platform`. But VRChat's `/users/{id}` returns `platform="offline"` for a NON-friend (and for offline friends) while `last_platform` holds the REAL platform (e.g. `"android"`). `"offline"` isn't blank, so the fallback never fired, and `prettyPlatform("offline")→""` blanked the chip. Friends resolved only because their live `platform` field held a real value. **Fix: read `last_platform` FIRST** (`j.optString("last_platform","").ifBlank { j.optString("platform","") }`) — the reliable field for everyone, exactly what VRC-NEXUS uses (`e.last_platform`). Enrichment still paces (`ENRICH_PACE_MS`=500ms, 5s 429 backoff, up to `MAX_ENRICH_ATTEMPTS`=40 retries) as light insurance, but each user now resolves on the FIRST 200 — rate-limiting was never the cause (the diagnostic showed `code=200 lp='android' p='offline'`). **No disk persistence / no storage buildup**: the reader writes NOTHING per-user to disk (only the SAF tree URI pref); `platformCache`/`enrichAttempts`/`enrichInFlight` are in-memory and **cleared on leaving an instance** so RAM stays bounded to the current instance. **The BASE All-files permission ALSO has a headset-only row in Settings → Permissions** (next to Overlay/Battery, `InstanceRosterManager.allFilesAccessIntent`); the rest of the setup (folder pick, Logging=FULL) lives only on the Home panel. **`MANAGE_EXTERNAL_STORAGE` is in the HEADSET manifest ONLY** (public/admin never start the reader). **Per-user platform lookup**: `VrchatAuthManager.fetchUserInfo(ctx, userId)` (`GET /users/{id}`, PUBLIC endpoint so non-friends resolve) returns `VrcUserInfo(displayName, platform, trustRank, status, statusDescription, location)`; `prettyPlatform(raw)` maps `standalonewindows→PC`, `android→Quest`, `ios→iOS`, `web→Web`, blank/offline→"". **Roster→admin (Firestore) SHIPPED**: `VrcaViewModel.buildInstanceRosterJson()` (headset only, LIVE + non-empty) serialises the roster to a compact JSON array (`[{name,platform,friend,self}]`, capped 80) written as `instanceRoster` on the watched 10s `buildLivePayload` write — **zero extra Firestore cost** (rides the existing watched write). The admin detail (`UsersTab.DetailBlock`) parses it into an "Instance roster (N)" card (self purple / friend yellow names + platform pills). `firestore.rules` `selfMutableKeys()` includes `instanceRoster` (self-written, admin reads only). **Avatar clone/wear — EXACT id resolved on tap (Quest does NOT log remote avatar ids)**: proven from a real device log — every remote avatar load is `Switching <name> to avatar <avatarName>` + `Unpacking Avatar (<name> by <author>)` + `[AssetBundleDownloadManager] Starting download of Avatar @ N MB`: **name + author only, never an `avtr_`** (the ONLY `avtr_` ids in a Quest log are the LOCAL player's own `Loading/Saving Avatar Data` lines). VRChat's API also HIDES other users' current avatar id. On **PC** the log writes the remote id in the download URL (how VRCX/NEXUS clone on PC) — Quest omits it. So a naive "harvest bare `avtr_`" approach is WRONG (it grabs your OWN id and mis-attributes it → cloning just re-selects your avatar); that earlier attempt was removed. **Instead the id is resolved ON TAP by a UNIQUE 1:1 key, not a fuzzy name guess (`VrchatAuthManager.resolveWornAvatarId`)**: (1) read the worn avatar's IMAGE file id from `/users/{id}` (`currentAvatarThumbnailImageUrl` → `file_…`, via `VrcUserInfo.wornAvatarThumbUrl`); (2) search avtrdb by the log's avatar NAME (`AvatarSearch`); (3) **CONFIRM** a candidate by fetching its PUBLIC `GET /avatars/{id}` and matching that avatar's `thumbnailImageUrl` file id to the worn file id (`fetchAvatarThumbFileId`/`fileIdOf`) — the image file id is unique per avatar, so this is immune to name collisions; the log's `author` (from `Unpacking Avatar (… by …)`) ranks candidates first, and a lone name+author match is the fallback when no image confirmation is possible. **Two stronger paths run BEFORE the name search** (added because the name search greyed out renamed/oddly-named avatars): **(0) NAME-INDEPENDENT image-file-id lookup** — `AvatarSearch.searchCandidatesByImageFileId(wornFileId)` queries the VRCX mirrors by the worn `file_…` id itself (invariant per upload, so it resolves even when the log name is renamed/truncated/generic/colliding; a mirror hit whose image file id equals the worn one is exact, zero VRChat calls); the NAME search then runs over **`avatarNameVariants`** (raw, bracket-descriptor stripped e.g. `"Ball Python (handpuppet / head puppet)"→"Ball Python"`, and the part before a ` - `/` | `/` / ` separator) merged/deduped, with forgiving fallbacks (unique name+author, then unique normalized-exact-name) so a DB whose stored name omits the log's descriptor still matches; every path stamps `AvatarSearch.Diag.lastReason` so the on-headset diag shows WHY each resolve landed (`via image file id`/`via name confirm`/`0 candidates in any DB`/`N candidates, none confirmed`); **(0b) OFFICIAL author-avatars listing** — `resolveViaAuthorAvatars`: `GET /file/{wornFileId}`→`ownerId` (the avatar AUTHOR's usr id, exact) then `GET /avatars?userId={author}&releaseStatus=public&n=100`, matching the avatar whose thumbnail file id == the worn one. A PUBLIC avatar is BY DEFINITION in its author's public list, so when VRChat permits this listing it's a true 100%-for-public path with NO third-party DB; if VRChat blocks enumerating another user's avatars (401/403) it SELF-DISABLES for the session (`authorAvatarsListingBlocked`, never burns rate limit on a blocked endpoint — logged so on-device testing reveals whether the listing works). Returns the `avtr_` id or **null only when the avatar is in no public database AND the author listing is blocked/doesn't contain it** (private/unindexed avatars — the one unavoidable miss). **HARD CEILING (be honest with the user)**: there is NO 100%-guaranteed method for arbitrary players — VRChat deliberately strips the avatar id from the API (`/users/{id}` hides `currentAvatar`), the website, AND Quest logs (anti-ripping); the id physically exists only in VRChat's local asset cache, which on a NON-ROOTED Quest lives in inaccessible internal storage (`/data/data/com.vrchat.VRChatAndroid/`, NOT the `Android/data/.../files` log tree — confirmed: the user sees only `output_log_*.txt` where they have access). So the DB + author-listing stack above IS the achievable ceiling — WIDENED by the crowdsourced catalog below. **Crowdsourced avatar catalog (`AvatarGlobalDb`, `com.vrca.vrchat`, ALL builds, ZERO Firestore) — increment 1 shipped**: a growing "extra database" that covers avatars no public DB has indexed, because **every VRC-A user can read their OWN avatar id** (from `/auth/user`) and contribute it keyed by the avatar's image FILE ID — the exact key a stranger's `/users/{id}` exposes, so an id one user contributes resolves for everyone. It reads ONE file (`avatars/db.json` in the `Ashoska/VRC-A-Image-store` repo) over the GitHub **raw CDN** with an **ETag conditional GET** (on app open + every 30 min; a 304 costs ~nothing), cached to `filesDir/avatar_db.json` for offline. `resolveWornAvatarId` consults `AvatarGlobalDb.lookup(wornFileId)` FIRST (exact, offline, reason `via global catalog`) before the DB stack, and **contributes back** every image-confirmed exact resolve (image-file-id / author-listing / name→fileid / name-confirm paths) plus the user's own current avatar (`currentAvatarCatalogEntry`→`avatarCatalogEntry`, `GET /auth/user`→`GET /avatars/{id}`). Contributions are queued locally (`vrca_avatar_db` prefs, survives Worker/network outage, drains on retry) and POSTed to a **Cloudflare Worker** (`AvatarGlobalDb.WORKER_URL`, `cloudflare/avatar-db/`), which is the SINGLE writer of the file: it validates FORMAT only (`avtr_`/`file_`), stashes in KV, and a **10-min cron** merges pending into the file in ONE commit; verification/culling is crowdsourced (`POST /report` dead/renamed → the Worker applies renames immediately, removes on a 2-report quorum). `GET /health` is a cheap KV-only status endpoint (entries/pending/lastFlush) for the admin live panel. The Worker holds the GitHub token as a server-side secret (never in the APK). **Increment 2 shipped**: the in-app avatar SEARCH now uses `AvatarSearch.searchAll` (our catalog FIRST, then avtrdb + both VRCX mirrors, deduped) and **contributes every file-id-bearing result back** (search slowly fills our DB from the others); each search-result row gained a **clone button** (`selectAvatar` + contribute, resolving the file id via `avatarCatalogEntry` when the source lacked one); entries carry **`authorId`** (`usr_`) + the image **`fileId`** now; a Settings→Debug **"Test all avatar databases"** button probes each source (avtrdb/requi.dev/just-h.party/worker) individually so a broken setup is visible. **The app READS the file from wherever the Worker actually writes** — `refresh()` learns the exact `rawUrl` from the Worker `/health` (which echoes `repo`/`path`/`branch`/`rawUrl`/`lastCommit`), eliminating any repo/branch/path mismatch; the Worker writes **one avatar per line** for readability. **Increment 3 shipped**: the **admin dead-avatar/refresh sweep** using a dedicated **BOT VRChat session** (`BotVrchatSession`, `com.vrca.admin`, its own encrypted `vrca_bot_vrchat` store so the admin's real account isn't rate-limited — login+2FA+`GET /avatars/{id}` only). `AvatarCatalogSweep` walks `AvatarGlobalDb.snapshot()` paced 1.5s/avatar via the bot session: 404/410 → remove; 200 → refresh name/author/authorId/platforms/image if changed (a changed image file id RE-KEYS: remove old + upsert new), batched (20) and pushed authoritatively to the Worker **`POST /admin`** (guarded by an `ADMIN_KEY` secret; upserts overwrite, removes hard-delete). The admin **Dashboard** gained a live **"Avatar catalog"** health card (polls `/health` every 15s — entries/pending/last flush/commit/adminKeySet, zero Firestore) and a **"Catalog sweep (bot)"** card (bot login+2FA, `ADMIN_KEY` entry stored in `vrca_admin_local`, Start/Stop + live progress). **Setup**: add an `ADMIN_KEY` secret in the Cloudflare dashboard (any random string) and enter the SAME value in the admin sweep card. Worker is **auto-deployed from Git** (Cloudflare Workers Builds watches `cloudflare/avatar-db/` on `claude/vrc-a-android-app-NUpaN` via `wrangler.toml`) — no manual redeploys. **Increment 4 shipped (privacy + scale)**: **PRIVACY — public-only** (`releaseStatus=="public"` filter in `avatarCatalogEntry` + the own-avatar library harvest) so a user's own PRIVATE avatars never enter the shared catalog. **LOCAL private↔public detection** for own uploads: the library harvest contributes ones flipped to public and reports ones flipped to private for removal. **SCALABLE dead detection (report-queue model, not a full scan)**: reports carry the `avatarId`; the bot fetches PENDING reports (`GET /admin/reports`) and verifies ONLY those — confirmed-dead removed, alive cleared (false positive) + refreshed; reports persist in the Worker so the bot catches up after being offline; a manual **"Queue full rescan"** button walks the whole catalog once for cleanup. **Search dead-hiding**: a result whose thumbnail 404s is CONFIRMED gone (`avatarExists`) then hidden + reported; a failed clone does the same. **Search "See more" paging** (was capped at 12). **Seeds**: uploads + favourites (public-only) harvested on open + every 30 min; **avtrdb search results** (no file id) get their file id resolved (`harvestSearchResults`, also fills platforms) so searching absorbs avtrdb. **Roster fast-load**: the clone id resolves in the SAME `/users/{id}` call as the profile pic (catalog lookup is offline/instant), so the clone button is ready when the pic loads. **Honest scale limit**: the app stores the whole catalog file on-device (fine to tens of thousands ≈ a few MB); at true millions it'd need sharding (Worker shards by file-id prefix, app fetches only the needed shard) or on-demand lookup — deferred until the catalog is actually large. See `cloudflare/avatar-db/README.md` for the dashboard setup. **Increment 5 shipped (resolver correctness + 4 bots + KV budget)**: (a) **Names alone are NOT verifiable** — `resolveWornAvatarId` returns `WornAvatarResult(avatarId, platforms)` and, when the worn image file id IS known, returns an id ONLY if a candidate's image file id matches it; a name/author match is REJECTED (this was the real "cloned a same-named DIFFERENT avatar" bug — a PC-only avatar the DB didn't have fell through to a name guess). A name-only best-effort is used ONLY when there is no worn image at all (impostor'd player / hidden thumb). `fetchAvatarInfo` (replaces `fetchAvatarThumbFileId`) returns `(fileId, platforms)`; `resolveViaAuthorAvatars` also returns the matched avatar's platforms. (b) **PC-only clone gate** — the roster greys the clone button for a PC/iOS-only avatar when the local user is on Quest/Android (`InstanceRosterManager.gateCloneId`/`selfIsQuest`; the avatar is still SAVED to the catalog, just not wearable there). (c) **Free grabs** — `AvatarGlobalDb.harvestAvatarIds` contributes EVERY name-search candidate (not just the confirmed one); avtrdb search + clone candidates PAGINATE to exhaustion (`AVTRDB_PAGE_GUARD`, was page-0-only ≈20). (d) **Avatar bios** — `desc`/`description` + a `filled` marker added end-to-end (`AvatarGlobalDb.Entry`, `VrchatAuthManager.CatalogEntry`, `contribute`, `adminPush`, `parseInto`, Worker `cleanEntry`, `BotVrchatSession.AvatarCheck`). (e) **FOUR bots** — `BotVrchatSession` now has 4 independent encrypted slots (slot 0 keeps the legacy `vrca_bot_vrchat` store) + `validate()` (GET /auth → AUTHED/EXPIRED/UNKNOWN); `AvatarCatalogSweep` runs FOUR named roles — **REPORTS** (verify pending reports), **FILL** (first-fill new/incomplete entries incl. bio, sets `filled=true`), and **LIVENESS_A/B** (oldest-checked sweep PARTITIONED by `(fileId.hashCode()) % 2` so the two liveness bots NEVER check the same avatar). Roles are assigned across whichever slots are logged in (1..4; a slot round-robins several roles when fewer bots), each role exposes live `Progress` counters, and `requestFullBlitz()` widens fill+liveness for a bounded 30-min window (catch-up the whole catalog). **New admin "Bots" tab** (`BotsTab.kt`, moved OFF the Dashboard): catalog health (now shows cumulative **totalAdded/totalRemoved**), 4 bot login cards each showing the **account name + an Authed/Expired/Checking indicator**, the shared ADMIN_KEY (still in `vrca_admin_local`), per-role counters, and the blitz button. (f) **KV budget (fixed a Cloudflare free-tier daily-limit blowout)** — `contribute()` NO LONGER flushes per avatar (that was one `/contribute` POST = one KV write + a `pend:` delete PER harvested avatar; infinite harvesting blew the ~1k/day write+delete caps). It queues to SharedPreferences (survives app close) and a dedicated **5-min flush loop** (`FLUSH_MS`) drains it: `flushQueue` takes the queue under a `Mutex`, POSTs in `CONTRIBUTE_CHUNK`=200-entry chunks OUTSIDE the lock, and re-queues any unsent tail merged with concurrent appends (no loss). `contribute()` ALSO inserts into the local `map` immediately so the contributing device sees its OWN new avatars instantly (zero KV, no wait for the Worker round-trip). Worker: `/health` exposes cumulative `totalAdded`/`totalRemoved` (per-flush counts reset, so totals are what make removals visible), `/report` bumps the live `meta.reports` so the bot wakes without waiting for a flush. **On a PAID KV tier the 5-min cadence keeps contributions fresh; the blitz is write-heavy so use sparingly.** Then `VrchatAuthManager.selectAvatar(ctx, id)` = `PUT /avatars/{id}/select` (new `put()` helper) wears it. **`VrcLogParser` keeps `AvatarUnpack(avatarName, author)`** (`RE_AVATAR_UNPACK`, tag UNANCHORED since Quest logs it under `[AssetBundleDownloadManager]`, not `[Behaviour]`); `RosterEntry`/`Member` carry `avatarName`+`avatarCreator` (no `avatarId` — resolved live). The roster row's **clone button** (`Icons.Filled.PeopleAlt` `IconButton` — the two-overlapping-people glyph matching VRChat's own instance-card clone button, tinted with the app's `primary` when ready / muted alpha when greyed; swapped from the old `ContentCopy` copy-sheet icon) is **PRE-RESOLVED in the background** (`InstanceRosterManager.resolveAvatars`, single-flight + 1s-paced, triggered from `publish` for any non-self member whose current avatar name isn't resolved yet) so the tap is INSTANT: `Member.avatarId` is `null` = still resolving (spinner), `""` = no cloneable match in any DB (button **greyed out/disabled**), non-blank = ready (tap → `selectAvatar`). `avatarIdResolvedFor[userId]` tracks which avatar name the id is FOR, so an **avatar SWITCH invalidates it → re-resolves** (button reverts to the spinner then updates). Caches (`avatarIdCache`/`avatarIdResolvedFor`/`avatarResolveInFlight`) clear on instance leave. **`Switching`-before-`OnPlayerJoined` log-order fix**: VRChat logs the avatar switch BEFORE the join line, so the switch was dropped (player not in roster yet) and the avatar/clone only appeared after a LATER change; `VrcLogParser.InstanceState.pendingAvatarByName` now buffers a pre-join avatar name and applies it when `OnPlayerJoined` fires, so the clone button is ready from the moment someone is in your instance. **In-game propagation**: `/select` changes your avatar server-side and (per the user) applies live in a running client like the website does — the Toast says "shows on your next avatar reload" as a conservative hint. **Multi-database resolve**: `AvatarSearch.searchCandidates` queries avtrdb + two VRCX-style `vrcx_search.php` mirrors (requi.dev, avtr.just-h.party) in PARALLEL and merges by avtr_ id — coverage = the union, each source fails soft so more can be added. The VRCX mirrors return VRChat's RAW image url, so a candidate can carry the worn file id → `resolveWornAvatarId` DIRECT-matches it (zero extra VRChat calls); avtrdb's proxied candidates are confirmed via `GET /avatars/{id}`. A wrong candidate is harmless — the image file id is a unique key, so only the real avatar confirms. **PFP auto-refresh (roster, headset)**: a person's pic changes mid-session (VRChat+ custom / gallery / avatar pic). `InstanceRosterManager` re-fetches a member's pic **5s after an avatar SWITCH** (detected as an `avatarName` change in `publish`, tracked in `lastAvatarByUser`; first sighting isn't a switch) via `refetchPfp` (updates `pfpCache` + republishes the row only if the url changed), and `startPfpRefreshLoop` sweeps every member **present ≥10 min every 3 min, 5s apart in join order** (`PFP_SWITCH_DELAY_MS`/`PFP_CYCLE_MS`/`PFP_MIN_PRESENCE_MS`/`PFP_STAGGER_MS`) so a full instance never bursts VRChat REST; `lastEntries` snapshots the roster for the sweep, cleared with the caches on leave. **Increment 6 shipped (clone→robot root-cause + self-heal, NOT platform/PC-only — the user confirmed VRChat itself gracefully restricts a PC-only avatar with an in-client message, so PC-only was NEVER the robot cause)**: (a) **The enrich INSTANT catalog-hit shortcut (`InstanceRosterManager.enrichPlatforms`) bypassed the resolver's guards — the real "clonable → robot, reopening fixes it, per-user differences" bug.** It did a bare `lookupSharded(wornFid)` and cached the result while ALSO setting `avatarIdResolvedFor`, which PERMANENTLY blocks the guarded `resolveWornAvatarId` from ever running for that member — so this unguarded shortcut was authoritative. A LOADING player's worn thumbnail is the VRChat fallback (Robot) file, so the shortcut could cache the Robot id (or the lone Robot catalog entry, or a transient wrong read) as clonable and pin it until an avatar switch/reopen. Fixed: the shortcut now skips when `isSystemFileId(wornFid)` (loading → let the guarded loading path handle it), uses `lookupShardedResult` **HIT-only** (a transient UNAVAILABLE must NOT pin — leave it for the guarded resolver to retry), and guards `isSystemAvatar(hit)`. (b) **Self-heal on a rejected clone (fixes "private avatar shows clonable" + deleted).** `selectAvatar` now returns the raw HTTP `code` on `InviteResult`; a clone that VRChat DEFINITIVELY rejects (**403** private/not-accessible or **404** deleted) reports the avatar `"dead"` to the Worker (→ 2-report quorum cull, with the REPORTS bot verifying to prevent false culls of friends-only avatars a friend CAN wear) and greys the button locally. A transient failure (429/5xx/network) is NOT reported (entry stays, tap retries). The report uses the EXACT catalog shard key: `WornAvatarResult.fileId` + `Member.cloneFileId` thread the worn file id the clone id resolved FROM all the way to the tap — the target's live worn thumbnail may be the fallback by tap time, so it can't be re-derived then (the old code fetched the fallback thumb and mis-reported it). This REPLACED the old `avatarExists`+fetch-on-failure round-trip (cheaper: zero extra GETs, catches 403 which `avatarExists` mishandled). (c) **Reverted the wasteful platform-fetch on every catalog HIT** (added in a prior commit under the wrong PC-only theory) — a catalog HIT again costs ZERO VRChat calls (its whole point); `gateCloneId` still greys a KNOWN-PC-only entry on Quest, and an unfilled (empty-platforms) entry shows and lets VRChat restrict it gracefully. (d) **`selectAvatarDiag` now surfaces the actual VRChat response code + body** so the residual "select returned **200** but still robot" case (VRChat downloading the Quest asset — genuinely VRChat-side, not a wrong id) is conclusively visible on-device vs a 403/404 wrong/dead id. (e) **PRE-TAP liveness confirm — the clone button is NEVER clickable if it would robot (dead/private).** The catalog is image-verified at CONTRIBUTION time, but avatars go private/deleted later, and the catalog-HIT paths trusted the entry without checking — so a since-dead/private entry showed clickable → robot. Now `VrchatAuthManager.confirmAvatarLive(avatarId)` (one `GET /avatars/{id}`, **session-cached by avatarId** so a full instance costs ≤1 GET per distinct avatar, tri-state: 200=live / 403·404=dead-or-private / else=transient) gates EVERY catalog-served id: the `lookup`/shard HIT paths, `resolveByNameAndAuthor` (catalog + avtrdb fallback), AND the enrich INSTANT shortcut all confirm before presenting — live→serve (and, when the worn file id is known, only if it still matches the entry, else fall through to fresh resolve); dead→`WornAvatarResult(dead=true)` → the roster greys DECISIVELY (new `res.dead` branch, no spinner/retry) + auto-reports it; transient→leave unresolved + retry (never cached, never greyed). The non-catalog resolve paths already confirmed via `fetchAvatarInfo` (null on 403/404), so now no path returns a clickable id without a live confirm. This is the one place a per-member GET is justified (the user explicitly wants "no robot tap" over the zero-call optimization); the session cache keeps it bounded. (f) **Re-add of re-published avatars (public→private→public now re-enters the catalog).** A dead report (`AvatarGlobalDb.report(...,"dead")`) now `forgetLocal`s the pairing (drops it from `map`/`avatarIds`/`sessionContributed`/`sessionContributedFiles`) so the stale local entry can't permanently short-circuit a later re-contribute; and the owner-library harvest's OWN PUBLIC uploads are checked against the **AUTHORITATIVE live clone shard directly** (`lookupShardedResult`, not the lagging `avtr/` presence index `libKnownSet`, and not the local `map`/`sessionContributedFiles` sets): MISS → re-contribute with a new `contribute(bypassLocalDedup=true)` flag that SKIPS the local `map`/session dedup (those can hold a STALE entry the admin liveness bot culled while the avatar was private — this device never ran `forgetLocal`, so the stale local entry blocked the re-add until an app restart); HIT → already present (no-op); UNAVAILABLE → transient, retry next harvest. So an avatar flipped back to public re-enters within one harvest cycle. A **Settings → Debug "Re-scan my avatars now"** button (`AvatarGlobalDb.rescanOwnLibraryNow` + `ownLibraryDiag`) forces the harvest immediately and shows the result ("N public, M in catalog, K re-added"), so a user can confirm a just-published avatar landed. (g) **Cache eviction on a direct instance HOP (not just leave).** The roster per-user caches + the in-app shard LRU were only cleared when the log dropped to "not in world" / VRChat closed — a direct hop (A→B) never passes through that, so the previous instance's caches + shard files leaked and accumulated across a hopping session. `InstanceRosterManager` now tracks `lastLocation` and, on any `state.location` change while in-world, calls the shared `clearRosterCaches()` (per-user caches + `AvatarGlobalDb.evictShardCache()`), rebuilding the roster fresh for the new instance. The three leave paths use the same helper. The `VrchatAuthManager.confirmAvatarLive` session cache (`clearAvatarLiveCache()`) is cleared on LEAVE but deliberately KEPT across hops so a re-seen avatar isn't re-confirmed. (h2) **Clone-button flicker + ">40s loading" — the two loops disagreed on the button state.** `publish` (every ~1s + every FileObserver wake) computed the button as SPINNER whenever a member wasn't finally resolved, while `resolveAvatars` republished it as GREY in the slow phase — so the button bounced between spinner and grey/blue every second, and the spinner reappeared past 40s. Fix: a SINGLE `cloneButtonState(uid, name)` helper is the source of truth for BOTH (`avatarIdResolvedFor==name` → cache; else `avatarLoadingSince` <40s → null spinner, ≥40s → "" grey), so they can never disagree. The retry state machine is also UNIFIED and time-based: `avatarLoadingSince` is now the first-attempt time for EVERY unresolved member (loading fallback / transient rate-limit / no-match-yet), replacing the old separate `avatarResolveTries`/`MAX_RESOLVE_TRIES` count path — so a member ALWAYS decides (grey or blue) within ~40s and never spins longer, with the retry loop re-resolving underneath (fast <40s, slow 40s–15min, final grey at the 15-min hard cap). And `publish` now only fires a member's FIRST resolve attempt (skips anyone already tracked in `avatarLoadingSince`); the paced retry loop owns all re-resolution — previously publish re-fired a FULL resolve (`GET /users/{id}` + `GET /avatars/{id}` + the DB search) every ~1s per unresolved member, hammering VRChat/avtrdb and causing the very rate-limit failures that kept it unresolved. (h) **avtrdb load + "-" avatar names (the avtrdb dev flagged the SAME query re-paginated every few seconds, and empty queries).** Root cause: the roster's resolve-retry loop re-ran a name search every ~12s for an unresolved/loading member and `avtrdbCandidates` paginated EVERY page each time. Fixes in `AvatarSearch`: a short-lived **per-query candidate cache** (`candidateCache`, `CANDIDATE_CACHE_TTL_MS`=5min) so a retry reuses the last result instead of re-hitting avtrdb + mirrors; the RESOLVE/harvest path paginates only `RESOLVE_PAGE_CAP`=3 pages (a name match is relevance-ranked near the top — exhaustive paging stays only for the explicit user search box `search()`/`searchAll` and the paced admin `searchPage`); and a `meaningfulQuery(q)` guard (**≥1 letter/digit**, Unicode-aware) on EVERY avtrdb entry point rejects the garbage the dev saw (empty, "-", punctuation-only) that a bare `isBlank()` missed. **It must NOT require 2+** — the avtrdb crawl enumerates by SINGLE characters, and a `≥2` guard silently broke it (every single-char term returned empty). **The avtrdb crawl (`AvatarCatalogSweep.CRAWL_TERMS`/`CRAWL_ORDER`) covers non-Latin scripts**: the old set was only `a-z`+`0-9` (missed Japanese/Korean/Russian/Arabic avatars); it now includes full hiragana/katakana/Cyrillic/Arabic + curated common CJK & Korean characters (avtrdb substring-matches, so one common char of a script reaches most of its names), walked in a fixed-per-process RANDOM permutation (`CRAWL_ORDER`) so grabs look random yet cover every term once per pass. **Search still harvests**: `searchAll`→`harvestSearchResults` (avtrdb search-box results) and the clone avtrdb fallback→`harvestCandidates` both feed the catalog. **Search index is now FULLY Worker-side (NO GitHub) + searches harvest avtrdb**: two fixes for "my avatar is in the catalog but not in search" and "searches stopped producing contribution batches". (1) **Worker-side reconciler (`reconcileIndex`, `cloudflare/avatar-db/worker.js`, runs every cron after `flushR2`)** — the incremental flush maintains the search index (`fragments/`+`avtr/`+`index/`) for NEW changes, but NOTHING repaired avatars whose index op was dropped in the past (the GitHub `catalog-rebuild` Action was the only reconciler, and per the user GitHub is OUT of the pipeline). `reconcileIndex` walks the clone shards round-robin (`RECONCILE_SHARDS_PER_RUN`=4/min, cursor `meta.rc` 0..4095) and, for every entry NOT present in its `avtr/<bucket>` presence set, enqueues an ADD index op to the TTL-free `iq:` queue — healing every cloneable-but-unsearchable avatar over ~one full pass (~17h at 4/min), then looping as insurance. `/health` exposes `reconcileCursor`/`lastReconcile`/`reconcileScanned`/`reconcileFixed` (the GitHub `rebuildDispatch`/`lastRebuildError` fields were removed). The **`iq:` index-op carry-forward queue no longer has a TTL** (was 7 days, which silently DROPPED queued search-index ops when the backlog outlived it — a second cause of unsearchable avatars; an index op is the only thing that makes an avatar searchable, so it must never expire). `maybeDispatchRebuild` is kept as dead code (manual one-shot only); the Worker no longer dispatches it. (2) **`AvatarSearch.searchAll` now calls `AvatarGlobalDb.harvestSearchResults(context, list)`** — previously searchAll only contributed results that ALREADY carried a file id, and avtrdb proxies its images (imageFileId null) so avtrdb results were NEVER added to the catalog → searches produced no batches. `harvestSearchResults` resolves each no-file-id result's file id (`GET /avatars/{id}`, deduped against the catalog first, paced, rate-limit backoff) and contributes it, so searching now grows the catalog from avtrdb. **App debug**: `AvatarGlobalDb.isInSearchIndex(avatarId)` (one `fragments/<bucket>.json` GET) + the Settings→Debug "Re-scan my avatars now" readout now lists each own avatar as `clone=Y/N search=Y/N author=…` so a cloneable-but-unindexed avatar is pinpointed on-device. **Increment 7 shipped (COST EMERGENCY — R2 bill blowout, ~$34/mo projected on a tiny userbase; the entire bill is R2 Class A = writes, with KV-reads + R2 Class B climbing toward their free-tier caps too)**: the fixes make every edit/send/read as cheap as possible while keeping clone LOOKUPS instant (reads come straight off the CDN-cached shards — never touched). (a) **Worker skips no-op shard rewrites** (`worker.js` flushR2): each touched shard is written ONLY when its content actually changed (`dirty` flag). The harvest re-sends thousands of already-known avatars whose `adds` are all dupes; the old code rewrote those shards unconditionally — the single biggest wasted Class A write. Upserts that are `entryEquivalent(prev,inc)` (same name/author/authorId/desc/filled/platforms) and no-op renames are skipped too; only `dirtyPrefixes` are `put` + purged. (b) **`checked` bumps still dirty the shard** (the timestamp MUST persist or the liveness sweep re-checks the same avatars forever) — the volume is cut by the CADENCE instead: **`RECHECK_INTERVAL_MS` + `LIVENESS_SHARD_INTERVAL_MS` widened 7d → 30d** (`AvatarCatalogSweep`), ~4× fewer re-sweeps → ~4× fewer shard writes AND bot shard reads; a dead avatar is still caught the instant a user tries to clone it (report path). (c) **Bots already walk SHARD-AT-A-TIME** (`walkPass`, active when `shardWalkLive()`/`r2:true`): they grab a whole shard, recheck everything in it, and push ops the Worker groups into ONE write per shard — so liveness is ~1 cheap write per ~32 avatars, not per avatar. This was the user's exact ask; it was already the live path. (d) **Client contribution coalescing** (`AvatarGlobalDb`): the quick-flush debounce widened **15s → 60s** (`QUICK_FLUSH_MS`) so a still-GROWING queue never spam-sends (killed the "batch of 1" flood in the admin Recent-contributions view), and a hard **1000-entry cap** (`CONTRIBUTE_MAX_BATCH`) force-flushes a big burst immediately as one bounded POST. (e) **Bot flush 1k cap** (`BOT_FLUSH_MAX_BATCH`): `enqueueOps` triggers an immediate `flushPending` (gated by `capFlushing`) when the buffered ops reach 1000, bounding buffer + latency during a blitz. (f) **Worker cron 1min → 2min** (`wrangler.toml` `*/2 * * * *`): halves idle-tick KV reads/lists + per-active-tick meta/manifest overhead; no data loss (pending ops wait one extra minute) and clone lookups are unaffected. (g) **Bot stats now represent shard-checking**: `Progress` gained `shards` (walked), `reportsVerified`, `reportsRemoved`; `RoleView` + `BotsTab` surface `shards N` per bot and a dedicated `reports N · culled N` line for the Reports bot (its real work was previously invisible because it loans to the shard walk when no reports are pending, polluting checked/removed). **Remaining lever (product decision, NOT yet changed):** ongoing catalog GROWTH (search-box harvest + own-library harvest) still costs ~6 R2 writes per genuinely-NEW avatar (shard + fragment + avtr + index buckets) — the one-time index-rebuild spikes are past (`reconcileDone:true`), so the steady cost is now proportional to how fast the catalog keeps growing; throttling/disabling passive harvest is the next cut if the bill is still too high. **Exact catalog count (investigated a reported "lost ~20k avatars then recovered"):** NOT a data leak — `totalRemoved` was ~380 (a real cull would be 20k+), and the drop coincided with the one-time reconciler completing and adopting a recomputed `entries` count that corrected upward drift in the running counter; the shards were never touched, clone/search were unaffected, and genuine growth climbed it back. Two hardening fixes so the number is EXACT: (1) the reconcile recount completes on a full 4096-STEP lap (each shard visited once, no re-lap/double-count) and ADOPTS the recomputed entry/unfilled totals only when the whole lap read cleanly (`rcReadFail === 0`); a tainted lap retries up to `RC_MAX_ATTEMPTS` then keeps the incremental count (never adopts an under-count). (2) `flushR2` folds each shard's add/remove/unfilled deltas + index ops into the running totals ONLY after that shard's write SUCCEEDS (per-shard `sAdded`/`sRemoved`/`sUnfilled`/`sIndexOps`), so a partial-write failure can't bump `entries` for a shard that didn't persist and then re-count it on the retry. To snap the live number to exact truth once, re-arm `GET /admin/reconcile?key=…` (it also resets the recount guards); the exact per-flush deltas keep it there. **Roster clone-button "greys out in the background" (tester):** the headset roster's early "VRChat closed" branch (`InstanceRosterManager`, fired on `!VrcaOscQuery.isServiceUp()`) was wiping ALL roster/clone/live caches — but on a BACKGROUNDED headset (or a brief sleep) an OSCQuery-down is usually just our poll being throttled, not VRChat closing, so every resolved clone button greyed out and needed a full re-resolve on return. Fix: cache-clearing is now gated to a GENUINE INSTANCE HOP only. The clue was that it happens while the user is STILL in VRChat — so VRChat (and its OSCQuery server) is up; it's OUR poll that's throttled while backgrounded, making `isServiceUp()` read false. All three transient clear-paths were retaining-ified: (1) the early "OSCQuery down" bailout in the read loop, (2) `publish()`'s `!alive` branch (throttled poll ⇒ alive=false even though VRChat is up), and (3) `publish()`'s `!inWorld` branch (a momentarily-empty log state from a background re-read/rotation). Each still forces presence offline / shows IDLE for the RPC, but NO LONGER wipes the roster/clone/live caches or `lastLocation` — so a transient blip that refills to the SAME instance restores every resolved clone button instantly, while a real move to a DIFFERENT instance is still cleared by the hop check (`state.location != lastLocation`). Retaining costs one instance's worth of memory (freed on the next hop or on app stop); the retry loop already skips resolved members, so a blue button can only grey via a cache clear, which now never fires on a background artifact. **Per-user clone-resolution TRACE (roster diagnostics):** every step `VrchatAuthManager.resolveWornAvatarId` walks for a member is recorded and surfaced under that member's row so the WHOLE resolve process is visible for every user in the instance — for finding "why did this one grey out / resolve to the wrong avatar." `resolveWornAvatarId` builds a step list (worn image fileId; each DB stage tried — local catalog / R2 shard / VRCX image-fileId mirrors / VRChat author-listing / avtrdb name search — with candidate counts and confirm verdicts) and, in a `finally`, appends the terminal `AvatarSearch.Diag.lastReason` as `result: …` and stores it in `resolveTraces[userId]` (getters `lastResolveTrace`/`putResolveTrace`/`clearResolveTraces`). The enrich INSTANT catalog-hit shortcut writes its own short trace. `Member.resolveTrace: List<String>` carries it; BOTH `resolveAvatars`/`enrichPlatforms` (on resolve) AND `publish()` (every ~1s rebuild — reads it back from the store so it doesn't blank each cycle) populate it, and `clearRosterCaches()` clears the store. UI (`InstanceRosterPanel.MemberRow`): tap a member row to expand a monospace trace panel (the `result:` line is primary-tinted); the clone-button colour already shows finished/couldn't-resolve at a glance. Traces exist only while the headset roster runs (public/admin never start it). **Increment 8 shipped (cost audit follow-through — Worker/bots/contribution/roster + trace detail):** (a) **Roster `/users/{id}` dedupe** — the worn thumbnail (`GET /users/{id}`) was fetched TWICE per member on the catalog-MISS path (once in `enrichPlatforms`, again in `resolveWornAvatarId`); a short-TTL (`WORN_THUMB_TTL_MS`=12s) `wornThumbCache` in `VrchatAuthManager` (stamped by `fetchUserInfo` on every 200, read by `resolveWornAvatarId`, cleared with the traces on instance leave) halves the roster's heaviest VRChat calls + the 429s. The trace shows `[reused /users cache]` vs `[fresh /users fetch]`. (b) **Worker `applyIndexOps` no-op guards** — fragment/avtr/index buckets are rewritten only when their content actually CHANGES (re-added already-indexed avatars, reconcile re-adds, requeue retries were rewriting buckets identically — the biggest avoidable R2 Class A after the shard `dirty` flag). (c) **`MAX_INDEX_OPS_PER_FLUSH` 30→150** — draining a small index backlog whole collapses co-located ops into one read+write per bucket instead of re-writing a hot bucket once per flush (hot-bucket write amplification). (d) **`checked`-only shard writes skip the CDN purge** — a `contentDirty` flag separates a real content change (purge) from a `checked` timestamp bump (persist, no purge — no clone/search reader consults `checked`); the biggest recurring purge cut at the 30d recheck cadence. (e) **`_manifest.json` writes coalesced** (`MANIFEST_MIN_INTERVAL_MS`=5min / `MANIFEST_MIN_DELTA`=25) — the live count already rides `meta.entries` (which `/health` max()es), so the manifest copy is freshened periodically instead of on every count-moving flush + purge; `meta.lastManifestMs`/`lastManifestEntries` gate it. `/health` version→9. (f) **Contribution coalescing** — a lone genuinely-new avatar (1-2 queued) rides the 2-min periodic loop instead of firing its own tiny `/contribute` POST; the debounced quick-flush only arms once `QUICK_FLUSH_MIN`(3) have queued (bursts still coalesce via the 60s debounce, the 1000-cap still force-flushes a flood). `flushQueue` split into `flushContributions`+`flushReports`; `report()` now drains ONLY reports so a dead/private report doesn't force-drain the coalescing contribution queue. (g) **Bots — end read-spins + faithful stats**: a **blitz** now covers the catalog ONCE (bounded to shards unswept since `blitzStartMs`) then idles out instead of spin-reading all 4096 every ~1.5s for the rest of the 30-min window (~4,800 wasted reads/blitz tail); the **fill-backlog whole-catalog walk** is bounded to ONE pass per manifest value (`fillScanStartMs`) so a sticky `manifestUnfilled>0` can't blind-walk forever; shard selection takes a short-lived **claim** (`shardClaims`, `SHARD_CLAIM_TTL_MS`=30s) and stamps `sweptAt` only AFTER a successful read (a flaky read no longer hides a shard from liveness for 30d nor falsely advances the blitz bar — it backs off and retries); `reportsPass` counts a verification only when the check actually ran (a transient null no longer inflates `checked`/`reportsVerified`) and CLEARs a confirmed cull's report in the same push; in shard-walk mode the Bots tab **folds a bot's roles into ONE row** (`walkFolded`/`isReportsBot` on `RoleView`) with its real combined counters (roles sharing a slot showed 0 for all but the first) and shows BOTH `filled` AND `refreshed` (a bot does both); the class doc corrected — the `fileId%2` liveness partition is PRE-CUTOVER only, the live walk path prevents overlap via the shared queue/claim. (h) **Resolve-trace detail** (the "not descriptive" fix) — the per-user trace now surfaces: a FAILED `/users` (rate-limit/network) vs a genuinely hidden thumb (previously both "none"); the live-confirm VERDICT at every catalog HIT (`LIVE 200`/`DEAD 403-404`/`transient`/`stale-image` via `verdictLabel`); the full `Diag.authorListing` line (WORKS/BLOCKED/IGNORED + counts) appended to the member's own trace; the name-search VARIANT strings tried + a raw-image-vs-avtrdb-proxied candidate breakdown; and each rejected candidate's reason (private/deleted vs image-mismatch). **Deliberately deferred with rationale**: the client in-memory contribution-queue rewrite (C1) — `sessionContributedFiles` already prevents the O(n²) dedup scan and per-append prefs persistence is the durability guarantee, so the rewrite's risk (the "contributions lost" bug class) outweighs its marginal benefit; Worker shard-level `checked` metadata (bots M5) and whole-shard-in-one-pass (M6) — both trade the per-pass VRChat pacing/write model for modest savings and were left as-is. **(i) LOW-severity bot items L1-L7 (cleared in a follow-up):** L1 blitz "queued" now derives from remaining SHARD coverage (`4096 − blitzWalked.size`) not the stale manifest count; L2 `pushError`/`avtrdbCrawlStatus` are `BotController` StateFlows (`_pushError`/`_crawlStatus`) the Bots tab observes (were raw `@Volatile`); L3 the three independent `/health` GETs collapsed into ONE 30s `BotController` poll (`AvatarGlobalDb.fetchHealth()`/`contentSignalOf` → `_health` StateFlow) feeding the content signal + report count + the health card (its refresh button `pokeHealth()`s); L4 `enqueueOps` drops a queued `checked` for a fileId that also gets an upsert that flush; L5 the role-assign dropdown tags a signed-out bot slot "(signed out)" (`slotLoggedIn` threaded to `RoleAssignRow`); L6 a Reports bot loaning to the walk shows "Reports (loaning to walk)" (`helping="walk"` surfaced); L7 an entry that is BOTH unfilled and stale decrements both backlogs on fill/remove so the liveness "queued" can reach 0.

**Roster clone-button cloned a RANDOM avatar for a private avatar (enrich-shortcut image-match bypass)**: a reported case — a private avatar on another player whose OWN real thumbnail matched nothing showed clonable and cloned a DIFFERENT random avatar. Root cause: the `InstanceRosterManager.enrichPlatforms` INSTANT catalog-hit shortcut served a catalog `fileId→avatarId` mapping when `wornFid in conf.fileIds || conf.fileIds.isEmpty()` — the **`|| conf.fileIds.isEmpty()` clause served the mapped avatar WITHOUT confirming its live thumbnail actually contains the worn file id**. When the confirmed-live (200) avatar's `GET /avatars/{id}` yielded no parseable thumbnail file id (empty set), a mismapped / re-keyed catalog entry (`wornFid` → a different-but-live avatar) sailed through the guard, got PINNED (`avatarIdResolvedFor` set, permanently blocking the guarded resolver), and cloned the wrong avatar. Every OTHER serve path is strict — `verifyCatalogHit` (guarded catalog HIT) is `wornFileId == null || wornFileId in c.fileIds` (empty → FALLTHROUGH, never served), and the mirror/author/name paths all require `wornFileId in info.first` — the enrich shortcut was the lone divergence. **Fix: dropped the `|| conf.fileIds.isEmpty()` bypass** so the shortcut serves ONLY on a real image-file-id match, matching every other path; an unverifiable confirm now leaves the member UNPINNED so the guarded `resolveAvatars` pass (equally strict) decides and greys it if it can't image-confirm. Client-only (needs an APK update). Note: a genuinely mismapped catalog entry is now inert on this path (FALLTHROUGH → fresh resolve finds the right avatar or greys) rather than culled — a stale `fileId→avatarId` re-key isn't reportable as "dead" (the mapped avatar isn't dead), so those are left for the liveness bots; the user-facing wrong-clone is closed.

Separately, VRChat logs `Switching X to avatar -` (and `Unpacking Avatar (- by …)`) for a hidden/blocked/loading avatar; `VrcLogParser.cleanAvatarName` now maps such a name to "" and the fold stores it as **null** (not "-"), so the placeholder never reaches the roster row / the "last resolve" diag / the clone Toast, and a null name resolves name-optional (by worn file id) instead of firing a "-" search. **Still deferred**: any Horizon-Store submission manifest entries. Boot/onboarding screens aren't monitor-framed yet (M1.5 polish). See `docs/account-system-plan.md` §2 for the device-role/source-of-truth model.

**Increment 9 shipped (roster loading/robot retry — cheaper + no rate-limit + no false positives)**: the "got name+author but the thumbnail is still the robot, so it retries forever" case is reworked in `resolveWornAvatarId` + `InstanceRosterManager`. (a) **Bounded loading watch on BOTH signals**: a member whose worn image is the system/robot fallback AND has no unique name+author yet is now a `res.loading` result (`WornAvatarResult(loading=true, observedFileId=...)`) tracked in `loadingWatchKeys`, re-checked on a SHORT cadence (`LOADING_WATCH_INTERVAL_MS`=25s) bounded to `LOADING_WATCH_BUDGET_MS`=3 min (vs the old 15-min hammer). Each watch tick re-reads BOTH the worn thumbnail (`/users`) AND the latest log name+author (free, from the roster fold) — EITHER landing resolves it (real thumbnail → image-confirm; unique name+author → speculative serve). If NEITHER arrives in 3 min it's stuck → **go QUIET** (grey + stop), only re-armed by a new log avatar event or a manual tap. Transient nulls (usersFailed/catalog-unavailable, `usersFailed=true`) keep the generic 15-min retry. (b) **Global pacing so checks don't get rate-limited**: the retry loop caps to `MAX_RECHECK_PER_PASS`=6 members per pass (oldest-first) on top of the existing 1s inter-member pacing in `resolveAvatars`, so a 60-person instance spreads its re-checks across passes instead of bursting `/users`+confirm calls. (c) **Give-up = HARD grey, no manual re-check (the tap-to-reprobe was REMOVED — it read as a pointless "respin" button).** After the 3-min watch expires the clone button hard-greys and stops: `retryClone`/`canRetryClone`/`RetryResult` + the `loadingGaveUp`/`lastResolve*`/`lastTapReprobeAt` bookkeeping and the Refresh-glyph UI branch are all gone. Nothing is lost that matters — a member that resolves later is picked up on their next avatar SWITCH (the name key changes → `avatarIdResolvedFor` no longer matches → publish re-fires) or by the liveness bots once the avatar gets indexed. **Design note (the "sorting"):** the worn **thumbnail file id** is the invariant unique key (matches even when the avatar/author was RENAMED, so renames resolve fine); the log **name+author** is only a speculative fallback while there's no real thumbnail. During the 3-min watch BOTH signals are watched, so a renamed/slow avatar whose thumbnail loads in time still resolves by file id; a name+author present-but-not-in-any-DB while the thumbnail is still the robot just rides out the watch and then hard-greys (rare; the liveness bots fix it later). (d) **Name-stable debounce (false-positive guard)**: the speculative name+author clone (robot worn image) only fires once the log name has been stable for `NAME_STABLE_MS`=4s (`avatarNameSince` per uid → `nameStable` param on `resolveWornAvatarId`), so a name captured mid-switch can't uniquely match and clone the player's PREVIOUS avatar. All caches cleared on instance leave/hop. **Manifest/worklist reads (`AvatarGlobalDb.fetchManifest`/`fetchWorklist`) are `useCaches=false`+`Cache-Control: no-cache`** so the bots' unfilled/stale backlog can never be fed a stale cached `_manifest.json` (a stale copy pins a phantom "queued N" and an endless fill re-cover).

**VRC+ / avatar-hidden users resolved to the Robot fallback (thumbnail→full-image worn-id substitution, `VrchatAuthManager`)**: a reported case — an avatar that IS in our catalog and clonable on one player showed as "avatar still loading (VRChat fallback)" on ANOTHER player wearing the SAME avatar, and never resolved even though the log had its name+author. Root cause (the user's lead): VRChat's `/users/{id}` returns the **Robot fallback in `currentAvatarThumbnailImageUrl`** for some players — notably users with a **VRC+ custom profile picture / avatar-hidden privacy** — while still carrying that player's REAL worn avatar in the full-size **`currentAvatarImageUrl`** (a DIFFERENT file id). `resolveWornAvatarId` keyed only off the thumbnail id, saw the known Robot file id (`file_0e8c4e32-…`, in `AvatarGlobalDb.SYSTEM_FILE_IDS`), took the "still loading" branch, and fell to an unreliable name+author guess. **Fix**: `VrcUserInfo` now also carries `wornAvatarImageUrl` (`currentAvatarImageUrl`); the worn-thumb micro-cache stores BOTH urls. `resolveWornAvatarId` computes an EFFECTIVE `wornFileId` — the real thumbnail id normally, but when the thumbnail is the Robot fallback AND the full image is a real (non-system) file it **substitutes the full-image file id** (trace: "thumbnail was the VRChat Robot fallback (VRC+/avatar-hidden) → using full worn image fileId …"). The mirror image-id search, the author-public-avatars listing, and `fetchAvatarInfo`/`confirmAvatarLive`/`verifyCatalogHit` ALL already match a worn id against the avatar's `thumbnailImageUrl` **OR** `imageUrl` file id, so the real avatar now resolves by file id (exact, image-verified) WITHOUT needing name+author. Only when BOTH the thumbnail and full image are the Robot/absent (the player is GENUINELY on the fallback) does it stay in the loading branch. **Hardening**: `isSystemAvatar` guards added to the mirror + author-listing serve paths so a genuine Robot full-image can never resolve to the Robot avatar. Normal (non-VRC+) users are unaffected — their thumbnail id is real and used unchanged. Client-only (needs an APK update). The name+author path stays as the backstop for a player whose full image is ALSO hidden. **Diagnostic (the "VRC+ must keep the real avatar somewhere" hunt)**: when the worn thumbnail is the Robot fallback, the per-user resolve trace now prints a `/users image fields: …` line listing EVERY avatar/image/icon/pic field VRChat returned for that member + its short `file_…` id (`VrcUserInfo.imageFieldsDiag`/`buildImageFieldsDiag`, cached with the worn thumb so the reused path shows it too). This reveals on-device whether the real worn-avatar thumbnail is hiding in a field we don't currently read (vs genuinely not exposed — the documented HARD CEILING). The name+author guess logic is UNCHANGED (per the user).

**`resolveByNameAndAuthor` is now author-OPTIONAL for OUR catalog (the "it's in our DB but didn't resolve by name+author" fix)**: the loading/robot branch fell to `resolveByNameAndAuthor`, which REFUSED outright when the log had no author (`authorNorm.isBlank() → null`). But VRChat writes the avatar author in the SEPARATE `Unpacking Avatar (Name by Author)` line, not the `Switching … to avatar Name` line, so a loading player frequently has the NAME but no captured author — the resolver then never even searched our DB (the reported "didn't search our db even though it has the name"). Since OUR catalog is image-VERIFIED at contribution time (and we `confirmAvatarLive` before serving), a **UNIQUE name match in our own catalog is now served even without a log author** (an author, when present, still disambiguates via a preferred author-filtered subset); only a genuinely AMBIGUOUS name (>1 distinct avatar) refuses. The EXTERNAL avtrdb fallback still REQUIRES an author (untrusted for a name-only guess — a same-named different avatar would wrong-clone). Every null path sets a precise `AvatarSearch.Diag.lastReason` (`no usable log name` / `0 catalog hits` / `ambiguous (N distinct)` / `not in our catalog (no log author for avtrdb)` / `unique catalog match`), and the loading branch appends it to the per-user trace as a `name lookup: …` step — so the next on-device trace states EXACTLY why a name resolve did or didn't land. This is the direct fix for the "Gnarpy kitty" case (in our catalog, worn thumbnail was the robot, log name present) provided the name is unique in our catalog.

**Author-narrowed catalog search + fancy-Unicode font fold (`resolveByNameAndAuthor`, the "'Meow M' by ᵂᴴᴵᵀᴱ ᵀᴵᴳᴱᴿ says 40 are the same" case)**: two coupled fixes to the loading/robot name+author path. (1) **The catalog search now AND-intersects the NAME **and** AUTHOR tokens** (`searchSharded("$nameVariant $author")`) instead of searching name-only (capped 40) then post-filtering by author. VRChat's log avatar name is short/truncated ("Meow M" → the only usable ≥2-char token is `meow` → 40+ unrelated hits), so the real author's avatar, if it ranked past the 40-cap, was NEVER seen → the resolver falsely reported `ambiguous (40 distinct in catalog)` AND (worse) `return`ed immediately, which **blocked the avtrdb fallback from ever running**. With the author tokens narrowing the search, an in-catalog avatar collapses to one hit; a not-in-catalog one now correctly falls through to avtrdb. The `m>1` reason is now `ambiguous (N by same author)` and a no-catalog-match records `name+author: not in our catalog by '<author>'` (honest — the old "40 distinct" made a UNIQUE avatar look duplicated). (2) **`fancyFold(s)` = `Normalizer.normalize(s, NFKC).trim().lowercase()`** is applied to EVERY author compare (`authorNorm`, the catalog exact-author filter, the avtrdb author filter). VRChat display names VERY commonly use decorative Unicode fonts — modifier-letter/superscript caps (`ᵂᴴᴵᵀᴱ ᵀᴵᴳᴱᴿ` = U+1D42… ≠ the ASCII letters), Mathematical-Alphanumeric bold/script (`𝗪𝗛𝗜𝗧𝗘`), fullwidth (`ＷＨＩＴＥ`) — which are DIFFERENT codepoints, so a raw `author.lowercase() == "white tiger"` compare (and the catalog's own index tokens) fail across fonts. NFKC folds those back to plain ASCII (`ᵂᴴᴵᵀᴱ ᵀᴵᴳᴱᴿ`→`white tiger`), so the avtrdb author-filter matches regardless of styling. Unicode **small-caps** (`ᴡʜɪᴛᴇ`, U+1D00 range) is NFKC-invariant, so a shared `SMALLCAPS` char-map (byte-identical in the Worker `foldFancy`, `AvatarGlobalDb.nfkcFold`, and `VrchatAuthManager.fancyFold`) is applied AFTER NFKC to cover it too (Worker `version 18`, `FOLD_VER 2` re-index). Client-only (needs an APK update). **Root cause the search proved on-device**: for "Meow M" by ᵂᴴᴵᵀᴱ ᵀᴵᴳᴱᴿ the fancy tokens meant the plain `meow ∩ tiger` index check gave 0, but the avatar IS in the catalog under the DECORATIVE-glyph tokens (the user found it by searching with the font). (3) **Exact-name preference (the "Raiden Shadow" case — 3 token hits but only ONE is named exactly that)**: the name-token search is BROAD — "raiden shadow" also returns "Mei Raiden Shadow Dance" / "…shadow.exe", so a UNIQUE avatar looked "ambiguous (3 distinct)". The resolver now uses a UNIQUE **exact** (fancy-folded) name equality (`cand.filter { fancyFold(it.name) == fancyFold(logName) }`) to pick the real avatar out of its token-siblings (also resolves "Meow M"). **Author-present** → author-locked, so a unique exact name wins else the author-filtered set decides. **No author logged** → serve ONLY on a unique EXACT name (per the user's explicit call): exactly ONE exact-named avatar → serve (take the risk); 2+ with the SAME exact name → ambiguous; NO exact match → don't resolve (a looser unique-*token* guess without an author is too risky — the token fallback was removed for the no-author path). Our catalog is image-verified so a unique exact-name hit is trustworthy. Client-only.

**Fancy-Unicode search index fold (Worker `version 17` + client union — makes plain-text search AND cloning find fancy-named/authored avatars)**: our catalog's search index tokenized name/author RAW, so a fancy-font avatar (ᵂᴴᴵᵀᴱ ᵀᴵᴳᴱᴿ / 𝗪𝗛𝗜𝗧𝗘 / ＷＨＩＴＥ) was indexed under the decorative codepoints → unsearchable by plain "white tiger" in the search box AND the roster catalog resolve. Three coupled changes: **(a) Worker `tokenizeFields` NFKC-folds** each field before tokenizing (`String(f).normalize("NFKC")…`), so NEW contributions index the plain-ASCII tokens; the STORED display fields (`fragSummary.n/au`) stay RAW so the UI still shows the styled name. **(b) One-time fold re-index (`FOLD_VER`, `meta.foldVer`)**: `reconcileIndex` forces a fresh lap when `foldVer` is behind and, for every `needsFold(e)` entry (name/author whose NFKC form differs), emits an ADD index op so its folded tokens enter the index (old fancy tokens stay as harmless orphans; `applyIndexOps` no-op guards mean only the new folded tokens actually write). `foldVer` is stamped on clean lap completion; `/health` exposes it. **Cadence bumped `RECONCILE_SHARDS_PER_RUN` 8→24** so the one-time fold lap completes in **~5h** (was ~14h) — a real user testing plain-text search for their fancy-authored avatars was waiting out the slow lap. This is **cost-neutral vs the R2 bill emergency**: it only adds Class B shard READS per run (the near-free tier); it does NOT raise `MAX_INDEX_OPS_PER_FLUSH` (the write-rate cap). Stays far under the subrequest budget alongside flushR2 (~40 extra reads/run). **IMPORTANT drain-lag reality (verified live, corrects an earlier optimistic note)**: at 24 shards/run the reconciler EMITS fold ADD ops FASTER than the flush drains them (`MAX_INDEX_OPS_PER_FLUSH`=150/flush ≈ 75/min), so a large `iq:` backlog (tens of thousands of ops — `reconcileFixed` ≈ 48.6k on the first real fold lap) builds up DURING the lap. Because the `iq:` queue is TTL-free NOTHING is lost, but **full searchability TRAILS the read-lap completion by hours**: after the lap finishes (foldVer stamped) the backlog keeps draining at ~75/min with no new emission, taking ~4–5h to clear. Symptom seen on-device: a fancy author's 8 avatars where only 1 surfaces under plain "white tiger" immediately post-lap, the rest appearing gradually (random `iq:` UUID key order) as the queue drains. This is the drain cap acting exactly as expected — the raised shard-read rate speeds up when ops are ENQUEUED, not when they APPLY (that's still 150/flush, deliberately, for the R2 cost cap). **Observability (Worker `version 19`)**: `/health` now exposes **`iqDepth`** — the `iq:` queue depth in KEYS (~150 ops each), stamped into `meta.iqDepth` from the flush's existing `iq:` drain list (NO extra list op on the 15s-polled `/health`; saturates at the listPrefix 1000-key cap). Watch it tick to 0 to confirm a fold/rebuild has fully applied.

**Fold desync — "searchable in fancy font but not plain text" (Worker `version 20`, `tokenizeFields` raw ∪ folded, `FOLD_VER 3`)**: a real on-device case — an avatar authored by `ᵂᴴᴵᵀᴱ ᵀᴵᴳᴱᴿ` was found under the fancy name but NOT under plain "white tiger", even though it was found under BOTH earlier. **Root cause**: the index stored an entry's tokens as TWO separate sets that drift apart — the **raw** fancy tokens (`ᵂᴴᴵᵀᴱ`/`ᵀᴵᴳᴱᴿ`, written by the ORIGINAL pre-fold contribution, when `tokenizeFields` did NOT fold) and the **folded** plain tokens (`white`/`tiger`, added later by the reconcile fold ADD). Since `tokenizeFields` folded, `tokensOf(entry)` returned the folded view ONLY, so `buildIndexOp`'s `rem` (computed as `tokensOf(old) − tokensOf(new)` on any rename / re-key / dedup / cull-then-partial) could strip `white`/`tiger` while leaving the raw `ᵂᴴᴵᵀᴱ`/`ᵀᴵᴳᴱᴿ` orphaned in their buckets — and vice versa. Verified live: for "Fox eboy" by `ᵂᴴᴵᵀᴱ ᵀᴵᴳᴱᴿ`, `fox`/`eboy` (name) + `ᵂᴴᴵᵀᴱ`/`ᵀᴵᴳᴱᴿ` (raw author) were present but `white`/`tiger` (folded author) had been removed; `foldPass` was off (foldVer done) so nothing re-added them. **Fix**: `tokenizeFields` now emits **raw-lowercased ∪ NFKC+smallcaps-folded tokens as ONE set** from a single call, so `tokensOf` returns the union and `buildIndexOp` add/rem ALWAYS touch an entry's raw and folded forms TOGETHER — they can never desync again (a rename/remove strips both, an add adds both). The client union query (raw ∪ folded QUERY tokens) already matches either form, so both search modes work off the union index; **Worker-only, auto-deploys, no client change**. **`FOLD_VER` bumped 2→3** re-arms the one-time reconcile fold lap so every existing `needsFold` entry is re-emitted with the union tokens (restores stripped folded tokens like Fox eboy's + adopts the raw orphans as legitimate). **Cost**: no new mechanism — the fold ADDs are mostly no-ops (tokens already present → cheap R2 Class B reads, no write/purge via the existing no-op guards); only entries genuinely MISSING a token get a bucket write (the irreducible, one-time cost of making them searchable). The re-lap rides the existing `iq:` drain (watch `iqDepth`); it lands gradually while the drain is backed up by passive-harvest inflow — throttling that harvest is the lever to clear it fast (and it also cuts the R2 bill).

**Flush overrun jammed the whole pipeline — `MAX_SHARDS_PER_FLUSH` 180→60 + reconcile-before-flush (Worker `version 21`)**: after the `FOLD_VER 3` re-arm, the fold lap NEVER started — `/health` sat at `reconcileDone:true`, `reconcileCursor:0`, `foldVer:2` across many cron cycles. Root cause found by timing a manual **`GET /flush` which timed out at >60s**: under the harvest backlog, `flushR2` (180 dirty shards, each a PUT + a ~100-300ms CDN purge, plus the iq: drain) exceeded the cron invocation's wall-time budget, so it was KILLED before the cron's LATER tasks ran — `reconcileIndex` (3rd in the `ctx.waitUntil` chain, after flush + prune) therefore NEVER executed, so the fold re-arm never fired and the `iq:` queue never drained. The whole pipeline was jammed behind one overloaded flush (the ~1000-subrequest COUNT budget was fine; WALL time was the limit — CDN purges are slow HTTP calls). **Two coupled fixes**: (1) **`MAX_SHARDS_PER_FLUSH` 180→60** so `flushR2` completes well within budget (fewer PUTs + purges = also cheaper; contribution intake is slower, which is fine/desirable while harvest is the overload); (2) **`scheduled()` reordered so the CHEAP critical tasks run FIRST** — `reconcileIndex` → `pruneFillWorklist` → `propagateAuthorRenames` → `flushR2` LAST — so an overloaded flush can never again starve the fold lap; reconcile advances + enqueues its iq: ops every invocation, and the now-lightweight flush (last) drains them same-cycle. This is what actually lets the `FOLD_VER 3` re-index (and the general `iq:` drain) run. Watch `/health` `reconcileCursor` advance + `reconcileDone` flip to confirm the lap is progressing. Worker-only, auto-deploys, no client change, net cost REDUCTION.

**New contributions jump ahead of the drain backlog (Worker `version 22`)**: a freshly-added avatar's index op used to be APPENDED after the queued `iq:` backlog in the flush drain (`all = [...queued, ...indexOps]`), so during a big backlog (a fold re-lap leaves hundreds of `iq:` keys) a new avatar wasn't searchable until the whole backlog drained (~many flushes). Fixed: `flushR2`'s drain now puts THIS flush's own new contribution ops FIRST (`all = [...indexOps, ...queued]`) and reads only enough backlog keys to fill the remaining per-flush budget (`indexOps.length + queued.length >= MAX_INDEX_OPS_PER_FLUSH`), so a new avatar is searchable within a flush or two regardless of backlog depth while the backlog still drains with the leftover budget. Pairs with v20 (new contributions are already indexed under raw ∪ folded tokens at contribution time), so a newly-added fancy avatar is plain-text-searchable almost immediately. Worker-only, no client change, no added cost. **Note (why the per-user "passive harvest throttle" was NOT done)**: with the admin avtrdb crawl off (the default), per-user library/search harvest is small + deduped (`bulk=true`, MISS-only — a user's own already-catalogued avatars don't re-send), so it is NOT the backlog driver; the backlog is the one-time reconcile fold lap emitting faster than the 150-ops/flush drain. Throttling users' apps would cost an APK rollout + slower growth for ~no relief, so it was dropped in favor of the Worker-side v21/v22 fixes. **(c) Client union (`AvatarGlobalDb.unionQueryIds`)**: `searchSharded`/`candidatesFor` (→ `searchShardedPage`/`searchShardedAll`) now AND-intersect the query's RAW tokens AND its NFKC-FOLDED tokens and UNION the results (`nfkcFold`/`queryTokens`/`orderedIntersect`). Raw matches the fancy tokens still in the index (works NOW, zero transition regression — a fancy-input search keeps working); folded matches the re-indexed plain tokens (so plain "white tiger" finds a fancy avatar once its shard is folded). Ranking folds both query + entry name/author. **Cloning benefits for free**: `resolveByNameAndAuthor` searches via `searchSharded`, so its catalog query (`"$nameVariant $logAuthor"`, raw/possibly-fancy) unions raw+folded → it can't miss a fancy-authored avatar during the re-index window. Client + Worker (auto-deploys on push).

**FILL worklist was BLOATED — bots swept hundreds of already-filled shards (Worker `pruneFillWorklist`, version 14)**: `_worklist.json`'s `fill` array lists the shards the FILL bots walk (shards holding ≥1 unfilled avatar), maintained incrementally in `flushR2` (`fillHintAdd`/`fillHintDone`). But `fillHintDone` only removes a shard the bots PUSH ops for — a shard that becomes fully-filled AND all-fresh-checked (no work → no ops) is never pruned, so the worklist LEAKED (observed live: **761 listed shards, ALL fully filled, vs ~47 real unfilled avatars**). The bots then swept ~714 already-done shards for nothing → the "queued N" sat flat (nothing being filled to subtract via `filledSinceManifest`) and the walk looked endless. Fix: `pruneFillWorklist` (cron, after reconcileIndex) checks the FRONT `PRUNE_BATCH`(40) shards of the worklist each run, drops any with 0 unfilled avatars, and rotates checked-and-kept shards to the back — so the whole list self-corrects to the true set in a few minutes. It runs ONLY while the worklist is implausibly larger than `meta.unfilled` (`length > unfilled + PRUNE_SLACK`(20)), so once clean it costs 0 reads and re-arms automatically if the leak recurs. `/health` now exposes `unfilled` (live per-flush count, not just the coalesced manifest) + `fillShards` (worklist size, should track unfilled).

**ENDLESS whole-catalog shard-walk — the worklist never listed PRE-EXISTING unfilled shards (Worker reconcile + client, version 15)**: even after the prune fix, the bots' fill walk NEVER TERMINATED — it kept blind-walking all 4096 shards ("shard walk still going, should only loop once"). Root cause: `_worklist.json` (`meta.fillHint`) was maintained ONLY in `flushR2` via `fillHintAdd`, which fires only for a shard a FRESH CONTRIBUTION touches. An avatar contributed `filled=false` long ago, sitting in a shard no recent contribution touches, was NEVER added to the worklist. When the reconcile recount corrected `meta.unfilled` UP to the true value (e.g. drifted 44 → real ~500), the client saw `manifestUnfilled > 0` with those ~500 shards ABSENT from the worklist, so it fell into the client's whole-catalog oldest-swept FILL fallback (`fillOrLivenessPrefix`, bounded per `fillScanStartMs`) to FIND them — and that fallback re-armed every time the count grew (recount corrections + harvest inflow), so it blind-walked the entire 4096-shard catalog forever, slowing the app. **Fix (two halves): (1) Worker — the reconcile recount (which already reads EVERY shard) now collects every shard holding an unfilled avatar (`fillSeen`, accumulated across the multi-run lap in `meta.rcFill`) and, on a CLEAN lap completion, UNIONs them into `meta.fillHint` + writes `_worklist.json`** (union not replace, so a mid-lap contribution's shard is preserved; the prune trims any filled during the lap → converges to the exact set). So the worklist is now AUTHORITATIVE for every unfilled shard — freshly-contributed AND pre-existing. **(2) Client (`AvatarCatalogSweep`) — fill is now STRICTLY work-list-driven**: `fillOrLivenessPrefix`/the `fillBacklog` whole-catalog sweep + the `fillScanStartMs` machinery are REMOVED; the empty-worklist fallback is `livenessPrefix` (the due-only 30-day liveness trickle) ONLY. The bots walk the worklist, fill it, and the walk TERMINATES when it drains; an empty worklist genuinely means no fill work (no more hunting the whole catalog). Requires a reconcile lap to complete once to populate the pre-existing backlog into the worklist (it re-arms every 30 days + is maintained incrementally by the flush between laps). `/health` version→15.

**Duplicate contribution flood — bulk harvest re-POSTed already-known avatars (client, contribute `bulk` flag)**: the admin Bots tab showed a large, growing `pendingBatches` (observed 40 → 129) where EVERY flush drained `R2 +0 -0 (177 shards)` — near the `MAX_SHARDS_PER_FLUSH` cap yet adding ZERO new avatars. Cause: a single `/contribute` batch is 200 avatars and avatars scatter uniformly across all 4096 shards, so ONE batch touches ~177 distinct shards = nearly the whole per-flush shard budget → only ~1 batch clears per 2-min cron, while active-harvesting clients enqueue new ones faster → the backlog climbs. The batches were overwhelmingly DUPLICATES: `contribute()`'s authoritative dedup (`lookupShardedResult(...).status == HIT` against the fresh clone shard, `AvatarGlobalDb.kt`) only SKIPPED on a definitive `HIT` and **fell open on `UNAVAILABLE`** — and under a heavy harvest the concurrent clone-shard reads get rate-limited/time-out → `UNAVAILABLE` → the already-known avatar was re-queued and re-POSTed (a `+0` batch that still eats a whole flush). Fix: `contribute(..., bulk = true)` makes the BULK harvest paths queue only on a DEFINITIVE `MISS`; an `UNAVAILABLE` (can't confirm new) DEFERS instead of re-sending, and the next harvest cycle retries when reads are calm. A genuinely-new avatar returns `MISS` and still sends immediately. **User-facing contributes stay `bulk = false` (fail OPEN) so they can never be dropped by a transient read**: own-upload/library harvest (`harvestLibrary`/favourites), the worn-avatar-switch grab (`harvestAvatarId`), roster clone-resolution contributes (`VrchatAuthManager.resolveWornAvatarId`), and the search-row clone button (`VrchatPage`). Bulk callers switched to `bulk = true`: `harvestSearchResults`, `harvestAvatarIds`, `harvestCandidates`, and `AvatarSearch.searchAll`/`search`'s "fill our DB from file-id-bearing results" loops (the search-box flood source). Zero worker change (client-only, needs an APK update to take effect); it cuts `/contribute` POSTs (KV write savings), collapses the `pend:` backlog to genuinely-new avatars, and frees the per-flush shard budget for real adds + fills. Nothing new is lost — a deferred new avatar rides the next cycle.

**Instance-roster row UI revamp + live/near-live member STATUS (`InstanceRosterPanel.kt` `MemberRow`, `InstanceRosterManager`, `VrchatPipelineService`)**: the headset instance-log rows were restyled to match VRChat's own social-row look. (1) **Platform symbol moved to the LEFT** — it now sits right beside the profile picture instead of on the right before the clone button. (2) **Each member is its OWN rounded `Surface` card** (subtle `surfaceVariant` fill, 6dp gaps) so rows are visually separated instead of blending into one block (the reported "some users have no background above them"). (3) **A status line under the name** — a coloured dot (`rosterStatusColor`: active=green / join me=blue / ask me=orange / busy=red / offline·unknown=grey) + text = the user's `statusDescription`, falling back to the pretty status label (`rosterStatusLabel`). `Member` gained `status`/`statusDescription`; `enrichPlatforms` caches them from the SAME `/users/{id}` call (zero extra REST), `publish()` fills them (self from `VrchatPipelineState.presence`), and they clear on leave/hop. (4) **Right-side action cluster (non-self only), exact order friend-request → clone → block → mute**, styled as rounded-square tonal buttons (`RosterActionButton`, `Modifier.clickable` NOT `Surface(onClick)` so it stays 34dp and doesn't inflate to the 48dp min-touch size). **Friend request** is LIVE (`FriendRequestButton` → `VrchatAuthManager.sendFriendRequest`; greyed when already a friend / no id, one-shot then shows sent). **Clone** is the existing live button restyled with a **custom overlapping-people glyph** (`res/drawable/ic_clone_people.xml`, `fillType="evenOdd"` so the overlap carves a clean single-tint separation) replacing `Icons.Filled.PeopleAlt`; same state machine (spinner/greyed/ready/dead-report). **Block** (`Icons.Filled.Block`) and **Mute** (`Icons.Filled.Mic`) are VISUAL for now (tap → "coming soon" toast) — icons/order/style in place, wiring deferred. **Status refresh, two tiers (no rate-limit cost):** *Tier 1 (free poll-diff)* — `refetchPfp` (fires 5s after an avatar switch + the 3-min ≥10-min sweep) now diffs status + statusDescription off the same `/users/{id}` it already makes and republishes the row if either changed; *Tier 2 (live for friends)* — `VrchatPipelineService.handleFriendUpdate` routes the WS friend-update status straight into the roster via `InstanceRosterManager.onFriendStatusUpdate(userId, status, statusDescription?)` (statusDescription null = leave unchanged on a partial payload), so a friend in your instance updates their status dot/text instantly with no poll (non-friends stay tier-1 poll-diff; instant-for-everyone would need a per-member fast poll = rate-limit cost, deliberately not done). Headset-only surface; the hook is a cheap no-op when the roster isn't running / the member isn't present.

**Roster action buttons — friend/unfriend + clone only (mute/block REMOVED); custom glyphs; compact; live friend-state refresh**: the right-side cluster is now just TWO buttons — **friend** then **clone**. **Mute + block were REMOVED** (the player-moderation API worked, but VRChat's own in-client mute/block is the reliable path and the buttons were low-value clutter here — per the user "they dont work well because of vrchat"); `fetchPlayerModerations`/`setPlayerModeration`, the roster `mutedIds`/`blockedIds` + `startModerationLoop`/`toggleMute`/`toggleBlock`, `Member.isMuted`/`isBlocked`, and `res/drawable/ic_blocked_user.xml` were all deleted. **Buttons shrank** — `RosterActionButton` is 26dp (was 34dp), 7dp corners, 15dp icons, 4dp cluster spacing (they were "way too big"). **Custom glyphs replace the Material icons** (the user wanted the exact VRChat-style marks): `res/drawable/ic_friend_add.xml` (person + "+" corner badge), `res/drawable/ic_friend_remove.xml` (person + "−" corner badge), and a redrawn `res/drawable/ic_clone_people.xml` (a solid FRONT person with a BACK person peeking upper-left, evenOdd single-tint) — no more `PersonAdd`/`PersonRemove`/`PeopleAlt`. **Friend button** (`FriendButton`): not-a-friend → friend-add glyph (tap `sendFriendRequest`, then dims as "sent"); already-a-friend → friend-remove glyph (tap `unfriendUser`, optimistic flip). **Clone** unchanged behaviour (spinner/greyed/ready/dead-report), just the new glyph + smaller size. **Live friend-state refresh (`InstanceRosterManager.onFriendsChanged(context)`)**: the friend flag + name colour used to update only on the next log-driven `publish()` (up to minutes on a quiet log) or a roster reopen — so becoming friends didn't flip the button to Unfriend promptly. `VrchatPipelineService`'s `friend-add` / `friend-delete` handlers now call `onFriendsChanged` right after `persistFriendsCache()`; it resets the friend-map TTL, re-derives `isFriend` on the CURRENT roster members, and republishes only if a flag actually changed (cheap no-op when the roster isn't LIVE, so safe on all builds).

**Friend button stayed greyed after the request was accepted even though the NAME already turned friend-colour**: the roster DID flip `m.isFriend` on acceptance (name recolours instantly) but the button stayed in its greyed "sent" look. Root cause: `FriendButton`'s local `sent`/`unfriended` state is `remember(m.userId)` and SURVIVES recomposition, so `justSent = sent && !isFriend` could keep the greyed styling even as `m.isFriend` turned true. Fix: the call site wraps it in **`key(m.userId, m.isFriend) { FriendButton(...) }`** — when the friendship flag flips, the button is REBUILT with fresh local state (`sent=false` → `justSent=false`) so it snaps to the Unfriend glyph in lockstep with the name colour. (Optimistic unfriend still works: tapping Unfriend sets the local `unfriended` override while `m.isFriend` is still true, so the key is unchanged and the add-glyph shows immediately; when `friend-delete` later flips `m.isFriend` false the key changes and rebuilds clean.)

**Friend button stayed greyed after the request was accepted (VRChat doesn't reliably emit `friend-add` to the REQUESTER)**: the OTHER half — when `m.isFriend` genuinely never flips because the accept never reached the cache. After you tap Add, the button greys ("sent" = `justSent = sent && !isFriend`) and only flips to Unfriend once `m.isFriend` becomes true. That relied on the `friend-add` pipeline event reaching the sender when the other person accepts — which VRChat does NOT reliably deliver to the requester, so the button sat greyed forever. Fix (`InstanceRosterManager.markFriended(userId)` + a bounded reconcile in `FriendButton`): while the button is in the greyed "sent" state, a `LaunchedEffect(m.userId, justSent)` polls `VrchatAuthManager.getFriendStatus` at 4/8/15/30/60/60s and, on `isFriend==true`, calls `markFriended` — which records the userId in a `locallyFriended` override set (`ConcurrentHashMap.newKeySet`) that is ORed into the isFriend derivation in BOTH `publish()` and `onFriendsChanged`, and immediately re-emits the flow with that member's `isFriend=true`. So the button flips add→Unfriend AND the name colour turns friend-yellow the moment VRChat confirms, independent of the pipeline event. Bounded (~3 min, ≤6 GETs, user-initiated only after a manual send) and self-limiting (the effect only runs while `justSent`); `locallyFriended` clears on instance leave/hop (by then the real `FriendsCacheStore` has caught up via the periodic friends refresh). Client-only.

**Friend STATUS was minutes-stale — now read from the LIVE friends cache (`friendMap`)**: a friend's status dot/text used to come only from the enrich-time `statusCache` (refreshed on the 3-min ≥10-min pfp sweep) plus the tier-2 WS push, so in practice it lagged minutes. The roster's friend snapshot is now the FULL `FriendsCacheStore` map (2s TTL, was a 15s key-set), which the pipeline WS keeps current on every friend event; `publish()` reads a FRIEND member's `status`/`statusDescription`/`trustRank` straight from that live map (falling back to the enrich cache when blank), so a friend's status reflects within ~2s at WS cadence with ZERO extra REST. Non-friend status still comes from the enrich cache + pfp poll-diff (rate-limit). Tier-2 `onFriendStatusUpdate` stays as the instant-push complement.

**Trust rank shield + platform badge on the pfp (Discord-style corners)**: the platform glyph moved OFF the row onto the avatar. `AvatarWithBadges` overlays two Discord-style corner badges on the 34dp pfp — the **platform brand glyph bottom-LEFT** (`PlatformCornerBadge`) and the **trust-rank shield bottom-RIGHT** (`TrustCornerBadge`, `Icons.Filled.Shield` tinted per rank, where Discord shows its presence dot), each in a small ring the colour of the row so it reads cut-out. `Member.trustRank` (raw `system_trust_<rank>`) is cached in `enrichPlatforms` from the same `/users/{id}` call (zero extra REST), read live from `friendMap` for friends / `trustCache` for others / `presence.trustRank` for self, and cleared on leave. `rosterTrustColor` maps the tag to VRChat's displayed-rank colours (veteran=purple "Trusted User", trusted=orange "Known User", known=green "User", basic=blue "New User", none=grey "Visitor", legend=gold "Veteran"). **Row background contrast fix**: rows are now `surface` (the roster card is `surfaceVariant`) — the earlier `surfaceVariant`-with-alpha was the same colour as the card and blended in; the pfp-initial fallback circle flipped to `surfaceVariant` so it still shows within a `surface` row. **Polish pass (device feedback)**: a plain `surface` row read DARKER than the `surfaceVariant` card in dark mode (rows RECEDED). `rowBg` is now theme-aware — dark: `lerp(surfaceVariant, White, 0.07f)` (a touch LIGHTER than the card, so rows read as raised/"less dark"); light: `surface` — plus `shadowElevation = 1.dp` and a 12dp corner (was 14). Row **height reduced** (unused vertical edge space): column vertical padding 7→5dp, `AvatarWithBadges` box 38→36dp / pfp 34→32dp / corner badges 16→15dp (icons scaled down to 9–11dp). The **trust badge uses a custom `ic_trust_shield.xml`** (a sleek heraldic shield with a checkmark carved via `evenOdd`) instead of Material `Icons.Filled.Shield`, which read too "cartoonie". The **clone glyph `ic_clone_people.xml` was redrawn WITHOUT `evenOdd`** (two solid, non-overlapping people — front larger lower-right, back peeking upper-left, with a thin gap between them) so there's no hollow "empty clipping" ring where the bodies met (the evenOdd carve XORed the overlap to a hole). **Polish round 2 (device feedback)**: the trust shield is now a **SOLID silhouette** (`ic_trust_shield.xml`, no carved checkmark) — at ~11dp the carve read muddy/"ass"; row **vertical padding trimmed 5→3dp**, inter-row gap **6→4dp**, `AvatarWithBadges` box **36→34dp / pfp 32→30dp** so more members fit the viewport (less dead space top/bottom of each card); and the dark-mode row fill was dialed back **`lerp(surfaceVariant, White, 0.07f)`→`0.03f`** (the 0.07 read too light / off-theme — now a subtle lift above the card, the 1dp shadow carries the "raised" cue).

**Liveness = ONE continuous oldest-first shard walk (replaces worklist + due-trickle + blitz)**: per the user's design, all bots now run a single unified `walkPass` that forever grabs the OLDEST-swept shard (unswept = oldest, `oldestSweptPrefix(dueOnly=false)`), one shard at a time together (shared `shardClaims`/`inFlight` so no two bots take the same shard/avatar), checks **EVERY** avatar in it (dead / author-rename / desc / perf / platform — no 30-day "due" gate), and pushes **ONE grouped write per shard** (only for avatars that actually changed; a clean shard costs just the R2 read, which is CDN-cached). On completion the shard is re-stamped `sweptAt` → back of the line, so the walk rotates through all 4096 shards endlessly and every avatar is re-verified once per full lap (~15h at 4 bots × `PACE_MS`). This is the cheapest possible steady state: reads are near-free (CDN), writes are one-per-changed-shard, and it never idles. **`nextWorkPrefix` is now just `oldestSweptPrefix(dueOnly=false)`** — the fill `_worklist.json` queue and the `livenessPrefix` due-only trickle are no longer consulted (the walk fills unfilled avatars as it reaches their shard), and **the blitz is gone** (`requestFullBlitz()` is a no-op, the Bots-tab button removed) because a continuous full-catalog walk has no "catch-up backlog" to blitz. `walkPass` checks the whole shard (`claimBatch(work, work.size)`, was capped at `WALK_BATCH`=40) and the recent-process TTL guard is the only skip. The Worker's worklist/reconcile-fillHint machinery is now vestigial (harmless — self-quiets as `unfilled`→0). Admin-flavor only; the sweep runs on the admin's bot VRChat sessions. **Bots-tab UI revamp (`BotsTab.kt`/`BotController`/`AvatarCatalogSweep`)**: each bot row now shows its LIVE position — the `Progress.curShard`/`curShardDone`/`curShardTotal` (set in `walkPass`, surfaced via `RoleView` + folded per-bot in `roleViews`) render as a "shard `<hex>`" pill + a `LinearProgressIndicator` + "checking N / M in shard `<hex>`", plus the cumulative "N shards swept · N checked · …changes" line and a Reports line. The **Maintenance card** dropped the vestigial "To process"/`totalQueued` pill and the blitz button, adding a **Catalog coverage** bar (`AvatarCatalogSweep.shardsCovered()` distinct shards visited / 4096; "Full" once complete) and a **Re-verify lap** row (`oldestSweptAgeMs()` = age of the oldest-swept shard = full-catalog freshness). `BotController` gained `shardsCovered`/`lapAgeMs` StateFlows and dropped `blitz`/`blitzViews`/`blitzShards`/`totalQueued`; the `BlitzView`/`blitzViews()`/`blitzShardProgress()` sweep functions are now dead code (dormant, `blitzActive()` is permanently false).

**Fill bots starved by the flush drip-rate — `MAX_SHARDS_PER_FLUSH` 120 → 180 (version 16)**: a large pending backlog (thousands of queued avatars across many `pend:` batches) was converted into processable `unfilled` at only 120 shards per 2-min cron, so the fill bots drained the ~400 that made it through and then sat IDLE waiting for the next flush while thousands of batches waited ("only ~400 to process even though there are so many batches"). Raised the per-flush shard cap to 180. **Explicitly NOT a cost increase**: it's the SAME set of dirty shards written once each, just drained in fewer crons instead of starving the bots (a no-op dupe shard is only a Class B read). 180 shards ≈ 360 R2 ops; with the grouped index work (≤150), prune (40), reconcile (8) and author-rename (8) sharing the cron invocation the worst case is ~700 subrequests, still comfortably under Cloudflare's ~1000/invocation limit (the reason the cap exists — a flush that exceeds it dies before clearing `pend:`, so the batches retry forever). Raising the cron FREQUENCY was rejected — it would double idle-tick KV reads (a real cost increase); more-per-flush is the no-cost lever.

**Liveness backlog (`staleCount`) was a FROZEN phantom — now computed for real (Worker, version 13)**: the Bots tab's per-bot liveness "queued N" is `manifestStale / (bots)` (e.g. `staleCount 291 / 4 ≈ 73`). But `staleCount` in `_manifest.json` was a **frozen artifact from the removed GitHub `catalog-rebuild` Action** — the Worker NEVER recomputed it (every manifest write just spread it forward via `...man`, and `_worklist.json`'s `stale` array is hardcoded `[]`), so the bots showed a permanent "queued ~73" that could never drain (distinct from the `unfilled` phantom, which the reconcile recount already fixed → 0). Fix: `staleCount` is now the TRUE count of entries due a 30-day recheck (`STALE_CUTOFF_MS`, matches the app's `RECHECK_INTERVAL_MS`). The **recount** (`reconcileIndex`, already reading every entry) tallies `rcStale` (entries whose `checked` < cutoff) and adopts `meta.stale` on a clean lap; the **flush** drains it live (`staleDelta` — a `checked` bump that lands on a due entry, or a due entry removed, decrements it, folded only after the shard write succeeds, `staleCount` written into the coalesced manifest). A **one-time migration** (`meta.staleModel`) zeros the frozen value + re-arms the recount on the first cron after deploy, so the phantom clears immediately (manifest purged) and the real value is computed over the next lap. `/health` exposes `stale`. (The `unfilled` phantom is separate + already 0; this is the `staleCount` half.)

**Bot metadata refresh — perf rank now persists (Worker `entryEquivalent`/`buildIndexOp`, version 12)**: the bots' `liveRefresh`/`fillRefresh` already re-read per-platform **performance rank** (`perfPc/perfQuest/perfIos`, from `unityPackages[].performanceRating`) on every check and push a changed entry, but the Worker was DROPPING a perf-only change: `entryEquivalent` (shard-write skip) and `buildIndexOp`'s `relevant` (index-op skip) compared name/author/authorId/desc/filled/platforms but NOT perf — so a re-uploaded/optimised avatar's new perf rank never persisted and the search perf badge (`fragSummary.pf`) stayed stale. Both now include the three perf ints, so a perf-only change dirties the shard AND refreshes the fragment. (Description + platforms were already compared by `entryEquivalent`; desc is deliberately NOT re-tokenized for search on an edit — a cost tradeoff, the initial contribution still indexes it.)

**Author name-change propagation (shipped)**: a creator renaming their VRChat display name left every catalog entry storing the OLD `author`. Fixed catalog-wide, keyed on the IMMUTABLE `authorId`. **Detection (authoritative, free)**: the bots already `GET /avatars/{id}` for every entry they sweep (`AvatarCatalogSweep.walkPass`); when a LIVE avatar's `authorId` matches the stored one but its author NAME differs, the bot queues `authorId → newName` in `pendingAuthorRenames` (bot VRChat data IS the verification — no false positives; avtrdb names are never trusted for this). Flushed to the Worker via `AvatarGlobalDb.adminPush(..., authorRenames)` (piggybacked on the first ops chunk = one KV write; standalone push only if no ops accompany it). **Propagation (Worker, bounded)**: `/admin` stores each as `arn:<authorId>` (30-day TTL); the cron's `propagateAuthorRenames` walks the catalog `RENAME_SHARDS_PER_RUN`=8 shards/run, ONE author at a time (`meta.arnActive`/`arnCursor`/`arnStepped`), rewriting `author` on every matching entry and enqueuing a normal `iq:` index op via `buildIndexOp` (author is search-relevant → fragment `au` + author tokens update through the existing pipeline). Dirty-only writes; runs ONLY while a rename is pending (steady-state zero cost); deletes the `arn:` key after one full 4096-step lap. `/health` (version 10) exposes `authorRenameActive`/`authorRenameFixed`/`authorRenameLast`/`authorRenameLastFixed`. The bot's `liveRefresh` still fixes the one swept entry immediately; propagation covers the author's OTHER (unswept) avatars without waiting ~30 days for the liveness sweep to reach each.
- **Device hash per build variant**: `MainActivity.ensureDeviceHash()` derives the `users/{deviceHash}` doc id from `computeBaseDeviceHash()` (`sha256("v2:$androidId:$signingCert")`, with a stored random fallback for blank ANDROID_ID). The **admin build** namespaces this as `sha256("admin:" + base)` so it resolves to a DISTINCT doc from the public build on the same phone — without this, both installs share one ANDROID_ID + signing key → one doc, so their presets/content overwrote each other AND the admin user-list self-filter (`it.id != myDeviceHash` in `AdminScreen`) hid the public install. With the distinct admin hash: (1) the two installs no longer sync (the admin build's moderation listener watches its own empty doc, not the public one); (2) the public install now appears in the admin Users tab / Dashboard so directed releases can be tested against it. The admin build never writes a user doc — `performSelfSync()`, `applyRemoteContentBeforeSync()`, and `applyRemoteConfig()` all early-return on `IS_ADMIN_BUILD`, so no orphan doc is created under the admin hash AND the admin's chatbox presets/messages live purely in local DataStore. (This gating was missing before: the admin build was reading its own `users/{adminHash}` doc back into DataStore on every launch via `applyRemoteContentBeforeSync`/`applyRemoteConfig`, which overwrote fresh local presets with a stale earlier snapshot — the "admin reverts to a past synced state on reopen" bug. With the three gates in place, DataStore is the sole source of truth for the admin build's content.) The admin hash is force-normalised on launch (overwrites any previously-cached public-equal hash). Public-only users are unaffected — their seed/hash is unchanged.
- CI workflow: `.github/workflows/android.yml`

**STALE-WORN-IMAGE clone bug (wrong avatar cloned — "shows ǃ ESME, clones Gucci Morty")**: NOT a shared thumbnail (verified against the catalog: ǃ ESME's thumb is `file_bf07acc6…`, Gucci Morty's is `file_f80ed8b5…` — DIFFERENT). Root cause: the worn image file id read for a member was STALE — VRChat's `/users/{id}` `currentAvatarThumbnailImageUrl` lags an avatar switch (and the 12s worn-thumb micro-cache can hold a pre-switch value), so the resolver keyed off a PREVIOUS avatar's file id (`file_f80ed8b5` = Gucci Morty) while the LOG correctly said the player was on ǃ ESME. The catalog entry was correct; the INPUT file id was stale, so the image-verified HIT served the wrong (previous) avatar. **Fix (`VrchatAuthManager.serveCatalogHit`, replaces the raw `verifyCatalogHit` call in BOTH the local-map and R2-shard catalog-HIT branches)**: after `confirmAvatarLive` (the SAME `GET /avatars/{id}` the dead/private check already made — no new fetch; `AvatarConfirm` now also carries the LIVE `name`+`author`), a **STALE-WORN-IMAGE guard** compares the LIVE avatar page's AUTHOR against the LOG's author (`logConflictsWithLive` — **AUTHOR only**, font-folded): if they disagree (and the log name is STABLE) the worn image was a previous avatar's → re-resolve by the log name+author (`resolveByNameAndAuthor`) → the RIGHT avatar (never the wrong one — unresolved/retry if it can't). This catches the different-creator collision (Gucci Morty/Nemorio vs ǃ ESME/Taiga). **The NAME is deliberately NOT compared** — VRChat's log name is frequently a truncated/descriptor-stripped form of the full stored name ("Ball Python" vs "Ball Python (handpuppet / head puppet)"), so a name compare false-fired on correct avatars and made the fast enrich-shortcut fall through to the full resolver for nearly every avatar (the "most clonable avatars now show 'reused /users cache'" report). A genuine RENAME never trips it (live+log author both reflect the new name); a blank log author (common — no Unpacking-Avatar line captured) can't disambiguate, so the image-verified HIT is served. Other verdicts unchanged: dead→report+grey, transient→retry, worn image no longer this avatar's thumbnail→fall through. When our STORED name is stale vs the live page, it fires a `"renamed"` report (Worker applies the name immediately) **once per file id per session** (`staleReported`, cleared on leave) so switching back never re-sends it. The roster's INSTANT enrich shortcut (`InstanceRosterManager.enrichPlatforms`) applies the same guard (`logConflictsWithLive`, gated on `nameStable`) before pinning, so it can't pin the stale-image avatar ahead of the guarded resolver. A blank log name+author can't disambiguate → normal image-verified serve. Client-only (needs an APK update). Report pipeline unchanged (`"renamed"` applied+cleared by the Worker flush, `"dead"` quorum-removed, Reports bot's `reportsPass` still runs inside the continuous shard-walk).

**Catalog sweep froze when the admin app was backgrounded (continuous-walk recovery)**: the freeze watchdog (`BotController`) only kicked a wedged sweep when `lastTotalBacklog > 0`, but the continuous oldest-first shard walk has ~0 steady-state backlog (it re-verifies every shard forever), so a walk OS-suspended while backgrounded never self-recovered (the "shard-swept counter stops" report). Fix: the watchdog treats shard-walk mode as always having work (`lastTotalBacklog > 0 || AvatarGlobalDb.shardWalkLive()`) so a true freeze (checked-counter frozen >2 min) gets kicked; and `BotController.onForeground(ctx)` (wired to `AdminScreen`'s `ON_RESUME`) kicks the sweep IMMEDIATELY on foreground-return when `!sweepAlive()`, so it resumes the instant the admin reopens the app instead of waiting out the watchdog (also re-asserts the KeepAlive foreground service). Honest limit unchanged: staying alive DURING a long background stretch still needs the admin device's battery-optimization exemption + OEM allow-list.

**Roster row polish**: (1) the friend-add/remove glyphs (`res/drawable/ic_friend_add.xml`/`ic_friend_remove.xml`) are a clean solid **person + filled circle badge** at the bottom-right with the +/− carved (evenOdd). **The +/− were BLURRY/unsharp at roster size** — the carved arms were only ~1 unit thick on the 24-unit viewport (≈1dp when rendered at 15dp → anti-aliased to a fuzzy grey). Fixed by making the badge bigger (r 4.5→5.5) and the carved +/− **BOLD (2.4-unit arms)** so they stay crisp small, and the icon render bumped 15dp→16dp. (2) A roster name with TALL glyphs (daggers `†`, Bengali/Thai combining marks, fancy Unicode — e.g. "ঌ†SAYAKA†ঌ") stretched its card taller than its neighbours. The `lineHeight`/`Trim.Both`/`includeFontPadding=false` pin ALONE wasn't enough — a **fallback font's own line metrics** for such glyph runs still grew the `Text`. Bulletproof fix in `InstanceRosterPanel.MemberRow`: the name + status are wrapped in **ONE fixed-height `Column(Modifier.height(34.dp), verticalArrangement = Center)`** matched to the avatar's exact height (34dp) — so the pair always sits vertically centered against the avatar AS A UNIT, and the fixed column height is the clamp (a fixed-height Column reports its own height and doesn't clip, so a tall glyph OVERFLOWS but can NEVER stretch the row). Two separate per-line fixed boxes were tried first (20/16 then 18/15 dp) but they shifted independently when tuned ("names seem slightly too high" / "only the name moved"); ONE centered column is predictable. The lineHeight pins (18sp/15sp, `LineHeightStyle(Center, Trim.Both)`, `includeFontPadding=false`, `softWrap=false`, `maxLines=1`) stay on each `Text` as belt-and-suspenders. The lineHeight pin (18sp/15sp, `LineHeightStyle(Center, Trim.Both)`, `includeFontPadding=false`, `softWrap=false`, `maxLines=1`) is kept inside as belt-and-suspenders. Every card is now uniform height.

**Bot push readout name cap**: some avatars are literally NAMED with a wall of shader code; the "Last bot push" + "Recent user contributions" readouts (`AvatarCatalogSweep.lastFlushInfo`, `BotsTab`) now cap each name to 24 chars so one such name can't flood the card (not corruption — real data).

- Firebase config: `google-services.json`
- Firestore rules: `firestore.rules` (uses `isOwner()` via `config/app.ownerUid`, NOT `sign_in_provider`). **Hardening (Phase 7)**: `selfMutableKeys()` includes `lastActiveAt` (hourly heartbeat write); `ownerOnlyUserKeys()` explicitly includes `killSignal` (defensive — self-update already can't touch it since it isn't in `selfMutableKeys()`). `announcements` read is `isOwner() || (signedIn() && resource.data.active == true)` — drafts (active==false) are owner-only and unauthenticated scraping is blocked; both client listeners already filter `active==true` so list queries pass per-doc. `users/{deviceHash}/live/{doc}` subcollection is readable by the owner or the device-doc owner, writable by the device-doc owner or admin. `bannedIdentifiers` `get` stays `signedIn()` (the client ban check reads its own vrchatId, unverifiable in rules) but `list` is owner-only so the collection can't be enumerated.

## Build Commands
```bash
# Debug build (public)
./gradlew assembleDebug

# Release build
./gradlew assembleRelease

# Admin build
./gradlew assembleAdminDebug

# Headset (Meta Quest) build
./gradlew assembleHeadsetAppDebug

# Run tests
./gradlew test

# Run instrumented tests
./gradlew connectedAndroidTest

# Full build + test
./gradlew build
```

### Web-session build environment + testing policy
- **Android SDK in Claude Code on the web**: the remote container starts WITHOUT the Android SDK, so `./gradlew` can't compile until it's installed. A committed **SessionStart hook** (`.claude/hooks/session-start.sh`, registered in `.claude/settings.json`) installs it automatically at session start (cmdline-tools + `platform-tools`, `platforms;android-34`, `build-tools;34.0.0`), accepts licenses, and points Gradle at it (`local.properties` `sdk.dir` + `ANDROID_HOME`). It is remote-only (`CLAUDE_CODE_REMOTE`), idempotent (skips when present, ~15s cold), and installs to `$HOME/android-sdk`. `.gitignore` tracks only `.claude/settings.json` + `.claude/hooks/` (personal `.claude` files stay ignored). **Always `./gradlew compilePublicAppDebugKotlin` (or `assemblePublicAppDebug`) before pushing** — this is what catches API/signature nits (e.g. `HorizontalDivider` vs `Divider`, `MemoryCache.maxSizeBytes` taking `Int`) that would otherwise only fail in CI.
- **No permanent unit tests (user preference)**: do NOT add lasting test files to the repo — the user considers them unneeded clutter for logic that rarely changes. Instead, when adding a new feature or when the user asks to verify already-implemented logic, write a **throwaway** test/harness (e.g. temporarily under `app/src/test`), run it via `./gradlew testPublicAppDebugUnitTest` to confirm the pure-logic behavior, then **delete it before committing**. This covers only Android-independent logic (ad detection, `TitleCleaner`, bio diff, `followSeriesKey`/recurring collapse, timezone resolution, notification dedup/fingerprints); UI, notifications firing, and the VRChat/Discord/OSC/Firestore integrations can only be verified on the user's real device.

## Architecture & Stack
- Language: Kotlin
- UI: Jetpack Compose
- Backend: Firebase Firestore
- Auth/device management: Firestore `bannedDevices` collection
- NowPlaying: pause detection logic (check for edge cases when modifying)
- Admin UI: Users tab, Releases tab, tab bar navigation
- APK distribution: GitHub Releases. `githubUploadAsset()` and `githubCreateRelease()` in `ReleasesTab.kt` use OkHttp. **`githubUploadAsset` uploads to the canonical `upload_url` template returned by `githubCreateRelease`** (e.g. `https://uploads.github.com/repos/OWNER/REPO/releases/ID/assets{?name,label}`) — it strips the `{?name,label}` suffix and appends `?name=<encoded>`. It does NOT rebuild the URL from `BuildConfig.GITHUB_OWNER`/`GITHUB_REPO`. This matters because if those are a renamed/old repo, `uploads.github.com` responds with a redirect *early* (before the streamed APK body finishes), which closes the okio sink mid-`writeTo()` and surfaces as `IllegalStateException: closed` on every retry. `githubCreateRelease` already follows the moved-repo redirect during create, so its response `upload_url` is canonical — using it sends the binary straight to the right host with no redirect. The owner/repo for create-release come from `RELEASE_OWNER`/`RELEASE_REPO` GitHub Actions secrets (CI overwrites `keystore.properties` from them at build time; the committed `keystore.properties` is only used for local builds). `githubUploadAsset` still uses `followRedirects(false)` + a manual `Location` redirect loop (fresh `buildBody()` per hop, repeatable file stream) as a fallback. Both functions use `retryOnConnectionFailure(true)` and retry up to 5 times with exponential backoff. Error messages include the exception class name and cause chain for diagnostics.

### VRChat Integration
- **VrchatAuthManager**: Singleton handling VRChat API auth (Basic auth + 2FA), cookie storage via EncryptedSharedPreferences, presence fetching, and friends list retrieval. Also saves credentials to EncryptedSharedPreferences for auto-relogin when sessions expire. Credentials are saved BEFORE the login HTTP request so they survive app kills during response processing. **Basic-auth credentials are URI-encoded (`encodeUriComponent`, an `encodeURIComponent`-equivalent) BEFORE base64** — VRChat's backend URL-decodes them, so a password containing `@ + # : & %` etc. fails with 401 if sent raw. This was a real "permanent 2FA bypass broke" cause: the raw-bytes login 401'd → `clearCredentials()` wiped the saved creds → auto-relogin died → the next launch forced a full manual login *including* the 2FA prompt. **The 401 handler now only calls `clearCredentials()` when the error message actually indicates bad credentials** (`invalid`/`incorrect`/`credential`/`password`/`username`); a transient/session 401 (e.g. IP-invalidated session — VRChat binds the `auth` cookie to the client IP, which changes constantly on mobile) keeps the saved creds so auto-relogin can recover instead of forcing manual 2FA. On re-login after auth-cookie expiry, the Basic-auth request sends ONLY the persisted `twoFactorAuth` cookie via `getTwoFaOnlyCookieHeader()` — never the expired `auth=` cookie, which can cause VRChat to reject the request as a session conflict. VRChat recognizes the trusted device and skips the 2FA prompt. **The `twoFactorAuth` cookie is ALWAYS sent regardless of age — there is NO client-side expiry drop.** The web client keeps a remembered device trusted effectively indefinitely (rolling it forward on use), so any hard client-side cutoff (the old `TWO_FA_COOKIE_MAX_AGE_MS` 30-day guard, now removed) only ever HURTS — it forced an avoidable 2FA prompt that VRChat itself would not have asked for. Sending a stale cookie is harmless: worst case VRChat ignores it and returns `requiresTwoFactorAuth`, which is exactly what dropping it would have produced — so the SERVER is the sole authority on trusted-device validity. `KEY_2FA_COOKIE_STORED_AT` is kept for diagnostics only. On successful Basic-auth login, the auth cookie + user info are written via a direct prefs edit; **if VRChat re-issued a `twoFactorAuth` cookie in that login's `Set-Cookie`, it is captured and its 30-day clock RESET so the trusted window keeps extending**; if it did NOT (the common bypass-login case), the existing 2FA cookie and its original stored-at timestamp are preserved untouched (without this preservation, the 2FA cookie would get wiped on every successful relogin and the SECOND auth-cookie expiry would always force manual 2FA). **Trusted-device cookie roll-forward (`captureRolledCookies`)**: VRChat's web client never re-prompts because the server rolls the `twoFactorAuth` cookie forward on EVERY use — the browser's cookie never reaches its ~30-day Max-Age. The app previously captured that rotation ONLY on the explicit login/verify paths and discarded the `Set-Cookie` on every other authenticated call (`fetchPresence`, `validateSession`, `refreshProfilePic`, …), so the app's stored trusted-device cookie aged out and ~30 days after the last login VRChat demanded a fresh 2FA code even though the website would not. **`captureRolledCookies(context, rawCookies)` now persists any refreshed `auth`/`twoFactorAuth` cookie (resetting its stored-at clock) after every authenticated success** — wired into `fetchPresence` (runs every 10s while watched / on the slow loop, so the cookie stays continuously fresh), `validateSession`, and `refreshProfilePic`. It is strictly additive (only overwrites when a non-blank refreshed cookie is present), so it keeps the app's trusted-device window as fresh as the browser's and stops the spurious "keeps asking for 2FA" re-prompts. (A user whose stored cookie already expired under the old behaviour will be asked once more, then stays trusted.) **`captureRolledCookies` is now wired into EVERY authenticated REST call, not just three** — `fetchFriends`, `fetchPendingNotifications`/`V2`, `fetchUserGroups`, `fetchGroupName`/`Announcement`/`Posts`/`CalendarEvents`, `verifyStillFriend`, the `fetchPresence` `/users/{id}` + `/instances/{location}` sub-calls, and the post-2FA `/auth/user` fetch all roll the cookie forward. This matters for a NORMAL (unwatched) user: `fetchPresence` only hits the VRChat API while an admin is watching (the unwatched slow loop is local-only), so previously the sole roll-forward point for most users was the 6-hour `validateSession`; now the every-connect REST sweep keeps the `twoFactorAuth` cookie continuously rolled forward like the browser's cookie jar. **CRITICAL double-prefix fix**: `extractCookieValue()` already returns the full `name=value` pair (e.g. `twoFactorAuth=VALUE`), but the old `verify2FA()` re-wrapped it as `"twoFactorAuth=$it"` → stored the MALFORMED `twoFactorAuth=twoFactorAuth=VALUE`. VRChat rejected that as an invalid trusted-device cookie, so EVERY VRChat global logout forced a fresh manual 2FA prompt even though the website (whose cookie jar stored the correct value) would not. This was the real "trusted device never sticks" cause. `verify2FA()` now stores `extractCookieValue`'s return verbatim. A read-side self-heal (`normalizeCookie`/`readCookie`, used by `getCookieHeader`/`getTwoFaOnlyCookieHeader`) collapses any historically-stored double prefix in place (persisting the corrected value, so the WebSocket and all paths get the clean cookie) — existing users are migrated automatically on first read. `getPrefs()` has a recovery fallback: if EncryptedSharedPreferences init fails (MasterKey corruption), it deletes the corrupted prefs file and retries. `diagnoseAuthState()` logs the presence/age of all auth-related preferences for debugging. `VrchatPipelineService` runs a 6-hour `startAuthRefreshLoop()` that — because the 12-day age threshold rarely fires before the cookie actually dies — also **actively validates the session each cycle (`validateSession()` → `GET /auth`) and `autoRelogin()`s when it's dead regardless of cookie age**. `scheduleReconnect()` likewise re-validates and re-logins after repeated WebSocket failures (`reconnectAttempt >= 2`) instead of spinning on a present-but-dead cookie — `isLoggedIn()` only checks the cookie is PRESENT, not valid. Also provides REST helpers for offline notification backfill: `fetchPendingNotifications()`, `fetchPendingNotificationsV2()`, `fetchUserGroups()`, `fetchGroupAnnouncement(groupId)`. Trust rank extraction (`extractTrustRankFromTags`) reads the highest `system_trust_*` tag from VRChat's tags array. All REST `get()` calls set `useCaches = false` + `Cache-Control: no-cache` so Android's HttpURLConnection never serves a stale cached response — this fixes the instance player count (`/instances/$location`) showing minutes-old values in both the app and Discord RPC. **Instance headcount source (`extractInstanceUserCount`)**: VRChat's `/instances/{loc}` object exposes THREE candidate counts that disagree by a few users. **`userCount` is the field that matches the in-game client's instance panel** (what the player and everyone in the instance actually see in-headset). `n_users` AND the per-platform breakdown (`platforms`: `standalonewindows` + `android` + `ios`, which sums to the **website / VRCX** number) AGREE with each other but run a few HIGH — they count users mid-join / in-transit / timing-out that the in-game client has already dropped. The helper **prefers `userCount`**, falling back to `n_users` then the platforms-sum (iterating ALL keys so new platforms are future-proof) only when `userCount` is absent. (History: this thrashed before it was settled empirically. Original code preferred `n_users`; the user compared against the *website* and found it "wrong", so it was switched to the platforms-sum; a later screenshot showed the platforms-sum over-counting the *in-game* client (21 vs 18) so it was switched to prefer `n_users` — but `n_users` ALSO over-counted in-game (47 vs 36). A temporary in-app debug overlay logging all three fields finally proved `userCount` == the in-game panel (36) while `n_users`/`platformsSum` both read 47. Ironically `userCount` had been sitting in the code as the never-reached final fallback the entire time.) Both `fetchInstanceCount` and `fetchPresence`'s `/instances/{location}` parse go through this helper. Queue size is deliberately NOT added (irrelevant for normal instances).
- **VrchatPipelineService**: Foreground service with OkHttp WebSocket to `wss://pipeline.vrchat.cloud`. Handles real-time events (friend online/offline/active/location, friend-update, unfriend, invites, group events) and manages friends cache. Writes VRChat presence to Firestore **only while an admin is watching** this user (gated by `AdminWatchState.isWatched`); when unwatched, in-app presence still updates locally via WebSocket events but no traffic hits Firestore. The `syncPresenceToFirestore()` function gates the Firestore WRITE behind `AdminWatchState.isWatched`. When watched, presence polls every 10s. A slow 10s loop runs when unwatched with `forceLocalUpdate=true` to keep local presence state current (it does NOT write Firestore when unwatched, but it DOES call `fetchPresence` — the in-app VRChat tab + Discord RPC read the resulting `VrchatPipelineState.presence`; an earlier note claiming "no VRChat API calls when unwatched" was wrong). **Instant instance player count (`fetchInstanceCount`)**: the in-app/RPC player count (`n_users`) used to lag *minutes* behind the VRChat website. Root cause: the count is the 3rd of `fetchPresence`'s sequential calls (`/auth/user` → `/users/{id}` → `/instances/{location}`), so it only refreshed when the WHOLE chain landed — minutes apart on mobile under cookie IP-invalidation / rate limits — whereas the website hits ONLY `/instances/{loc}` per refresh (1 call, instant). Fix: `VrchatAuthManager.fetchInstanceCount(location)` does that single lightweight call (live `platforms`-sum count via `extractInstanceUserCount` / `capacity`). It is fired (a) **immediately on every self `user-location` world-hop** in `applySelfLocationEvent` (the WS location event carries no count, so the count previously sat at 0/stale until the next full chain — now it's fresh the instant you change instances), and (b) as a **null-fallback inside `syncPresenceToFirestore`** when `fetchPresence` returns null (the heavy chain failed) — it patches just the count on the last-known location so the count tracks reality instead of freezing. (When the cookie is *fully* IP-invalidated even this single call fails; that's the session-recovery path, separate.) **Light presence fallback (`VrchatAuthManager.fetchSelfPresenceLight`)**: when the heavy `fetchPresence` chain returns null, `syncPresenceToFirestore` first tries ONE `GET /users/{id}` call to recover the true location/state (no `/auth/user`, no `/instances` — one request, far less likely to be throttled than three) and flows the result into the SAME `VrchatPipelineState.presence` + watched Firestore write path. When the location is unchanged it merges the last-known `worldName`/instance counts; a world hop leaves `worldName` briefly blank until the next heavy chain refills it. This is what stops a genuinely in-game user (whose heavy chain is failing on a partial 429) freezing as "not in a world" on the admin panel / "not in VRChat" on their RPC. If even this single call fails (cookie fully dead), it falls through to the count-only patch as before. **Reactive self-presence from the WebSocket (`applySelfLocationEvent` / `applySelfUserUpdate`)**: the user's OWN `user-location` (world-hop) and `user-update` (status) events now patch `VrchatPipelineState.presence` DIRECTLY from the event payload — no REST call. This fixes "the Discord RPC keeps counting but doesn't match my VRChat state": self presence used to refresh only via the periodic `fetchPresence()` REST poll, which returns null whenever the auth cookie is IP-invalidated (constant on mobile/backgrounded), so `presence` froze while the RPC (whose online timer is independent) kept pushing a stale world. The `user-location` event was previously a no-op for self. Now patching it emits on `presenceFlow` → the RPC's `collectLatest` pushes the real world instantly even when REST is dead. When an admin is watching, `pushSelfPresenceToFirestoreIfWatched()` also writes the patched `vrchatLocation`/`vrchatWorld`/`vrchatState` straight to Firestore (no REST round-trip), so the admin directory's VRChat location updates instantly and resiliently — this fixes "the user's VRChat location not updating on the admin menu". The periodic watched 10s `syncPresenceToFirestore()` REST poll still runs as the authoritative full-field refresh (now more reliable thanks to the cookie roll-forward above). **No periodic `lastSeenAt` heartbeat lives here anymore** — liveness is a single hourly write owned by `VrcaViewModel.startHourlyHeartbeat()` (see "Hourly liveness model" below); the only periodic Firestore write this service makes is the 10s watched-user presence sync (which also refreshes `lastActiveAt`). Connect/disconnect transitions fire connection-status notifications (gated by `notif_connection` pref).
- **Friends cache**: Persisted **locally** in SharedPreferences (`vrca_friends_cache` / `friends_json`) via `FriendsCacheStore`. No Firestore involvement — friends data is only used by the user's own app for unfriend notifications and is meaningless to admins. Old `savedFriendIds`/`savedFriendNames` fields in user docs are removed via `FieldValue.delete()` on every offline write so legacy data doesn't linger.
- **Live bio/name/rank notifications (`handleFriendUpdate`) — the PRIMARY instant path**: The pipeline `friend-update` WebSocket event **DOES** carry `bio` / `displayName` / `tags` (verified on-device via a temporary debug log that dumped raw payload keys — every event showed `bio="..."`). So `handleFriendUpdate` fires bio/name/rank changes **instantly** for online friends, with no polling. (An earlier note here wrongly claimed VRChat doesn't send `bio` live and that the old "instant while active" was incidental — both were wrong; the live event has always carried it.) It gates on `user.has("bio")` so partial location/status payloads (which omit the field) don't false-fire.
- **Pipeline message serialization (`pipelineDispatcher`) — the cache-race fix**: `friendsCache` is a plain non-thread-safe `mutableMapOf`, but `onMessage` used to `serviceScope.launch { handlePipelineMessage(text) }` on the **multi-threaded** IO dispatcher — so two events touching the same friend ran CONCURRENTLY, one's cache write clobbering the other's and a diff reading a stale `prev`, **silently dropping a notification**. Symptoms: a fast back-to-back bio edit firing once or not at all, and a quick **friend→unfriend** where the `friend-delete` read the cache before `friend-add` had written the entry (`?: return` drop). **Fix:** a process-lifetime single-threaded `pipelineDispatcher` (`Executors.newSingleThreadExecutor().asCoroutineDispatcher()`, closed in `onDestroy`). `onMessage` now launches on it (`serviceScope.launch(pipelineDispatcher)`), so all WS handlers run serialized in VRChat's send order — each handler's read-modify-write is atomic (the cache write precedes the first suspension point), and `friend-add` is processed before the `friend-delete` that followed it. The two nested cache-touching coroutines (`friend-offline` cooldown handler, the connect second-fetch diff) and the REST refresh diff also run on `pipelineDispatcher`. The REST diff re-reads the **latest** cache entry before writing (`val latest = friendsCache[userId] ?: prev`) so a concurrent WS presence update isn't reverted.
- **Periodic friends-profile refresh (`startFriendsProfileRefreshLoop`/`refreshFriendsProfilesAndDiff`) — the BACKUP path**: the live WS path above covers online friends instantly; this loop exists only for the one case the WS can't deliver — a friend editing their profile from the **website or phone while their VRChat status is "offline"** (VRChat sends no `friend-update` for offline friends, so they appear ONLY in the `offline=true` friends-list pass). It re-fetches the FULL friends list (`fetchFriends(onlineOnly=false)`, both `offline=false`+`offline=true` passes) and runs the same name/bio/rank diff. Cadence **`FRIENDS_PROFILE_FG_MS` = 30s while on-screen**, **`FRIENDS_PROFILE_REFRESH_MS` = 60s when backgrounded** (foreground from `VrchatPipelineState.appForeground`). **Cadence was dialed back from 12s/60s to 30s/60s**: each sweep is TWO 2-pass REST fetches and every authenticated call rolls the shared, IP-bound `auth` cookie the live pipeline WebSocket ALSO uses as its connect token — so an over-tight 12s foreground cadence churned that cookie / hit 429s and made the WS (and the Discord RPC that reads its presence) disconnect far more often. 30s still catches an offline-friend edit within ~30s. Since notifications fire regardless of foreground state, the slower background cadence only delays the friends-LIST display refresh (which the user isn't looking at), never the notification itself. **Foreground-reactive sleep**: the inter-sweep wait is sliced into 2s chunks and breaks early when `appForeground` flips false→true, so reopening the app runs a fast sweep within ~2s instead of waiting out the full 60s background sleep (the screenshot bug: the loop was stuck on a 180s sleep after reopen). (`fetchFriends` 429-handles by waiting 5s + retrying, so the cadence self-throttles if VRChat rate-limits.) The diff **MERGES** profile fields onto cache entries and preserves the live presence fields (`location`/`worldName`/`status`) the WS events own WHILE ONLINE — EXCEPT it **reconciles the OFFLINE direction from REST**: this sweep includes the `offline=true` pass, so a friend listed with `status="offline"` here IS offline; if the cache still shows them online a `friend-offline` WS event was MISSED (socket throttled while backgrounded), so the entry is reconciled to offline (silently, no late notification). This stops the friends-online count drifting too HIGH mid-session without needing a reopen (previously only a full reload on reconnect/reopen fixed a missed offline). Only the offline direction is reconciled; a friend's live world/status while ONLINE stays WS-owned (the WS is more real-time than this up-to-60s-stale fetch). Skipped during warmup / when not logged in / empty cache. Dedup is shared with the live + reconnect paths via the cache (whichever updates the cache first makes the other see no diff → no double-fire) and the `bio_$userId`/`name_$userId`/`rank_$userId` group keys. `VrchatAuthManager.fetchFriends(context, onlineOnly=false)` still has the `onlineOnly` param (only the `offline=false` pass when true) but this loop always passes `false`.
- **Unfriend notification logic**: Two sources cooperate via a `notifiedUnfriendIds` dedup set. (1) Real-time `friend-delete` from the WebSocket fires immediately for unfriends during an active session — but is **skipped if the userId isn't in the cache** (queued event for an offline unfriend that loadFriendsCache already removed). (2) Offline diff uses **two-fetch confirmation** to eliminate API pagination flaps: after the first API fetch on connect, removed IDs are captured into `candidateRemovals` but NOT notified. After the second fetch (60s later), only IDs still missing in BOTH fetches (`candidateRemovals ∩ stillMissing`) advance to verification. **Final verification gate**: both paths call `VrchatAuthManager.verifyStillFriend(userId)` which hits `GET /users/{id}` and reads the `isFriend` field. If true → suppress notification and re-add to cache (false positive); if false or 404 → fire notification; if null (network error / 429 / expired session) → fall back to firing the notification so we don't silently drop real unfriends. The dedup set ensures at most one notification per userId per session. The notification text is neutral — "Friend removed" / "$name is no longer on your friends list" — since the app can't know who initiated the removal. **Last-friend (1→0) fix (`fetchFriends` returns `List<VrcFriend>?`)**: unfriending your ONLY friend never notified because the diff couldn't tell a genuine empty list from a failed fetch — `fetchFriends` returned `emptyList()` for BOTH, so the guards `if (fresh.isEmpty()) return` / `if (friendsCache.isEmpty()) return` swallowed the 1→0 removal. `fetchFriends` now returns **null on a HARD failure** (no cookie, or not a single page ever returned 200 — session dead / rate-limited-out) and a **(possibly empty) list on success** (a valid 200 with `[]` = genuinely 0 friends). `loadFriendsCache()` returns a `Boolean` and, on a null fetch, **preserves the existing cache instead of wiping it** (a failed fetch used to blank the cache → the next diff would read every friend as removed; now the preserved cache makes `candidateRemovals` empty on failure). `diffFriendsCache` **no longer bails on an empty `friendsCache`** — an empty cache after a successful fetch is a real 0-friends state whose removal must fire; the per-candidate `verifyStillFriend` gate + the two-fetch confirmation + the `< 80%` partial-fetch guard (which the 1→0 case passes since `1*4/5==0`) are the false-positive protection. `refreshFriendsProfilesAndDiff` uses `fetchFriends(...) ?: return` (null = failed → skip; empty = 0 friends → loops zero times). Net: unfriending your last friend now fires, while a failed fetch still never false-flags removals.
- **Friend-add / friend-request dedup (repeatable per-person)**: two coupled fixes for "New friend fires 2×" and "unfriend→refriend doesn't notify". (1) **`friend-add` SHORT-WINDOW dedup (`recentFriendAddMs` + `FRIEND_ADD_DEDUP_MS` = 5 min, in-memory)**: the `friend-add` pipeline event carries no `dedupId`, and VRChat re-emits/replays it (notably on a WS reconnect), so a duplicate fired "New friend" twice ("New friend (2)"). The handler now swallows a repeat within the window. It is NOT permanent — a genuine later re-add still fires, and `friend-delete` clears the stamp so an unfriend→refriend inside the window still notifies. (2) **Friend-request `fr_<userId>` key is now CLEARED on resolution** — `seenNotifIds` is permanent by design (correct for one-shot notifications), but the friend-request dedup key is REPEATABLE per person (a new request can come after a decline or an unfriend/refriend cycle). It was never removed, so a re-request from someone whose `fr_` key was already present was suppressed forever. `clearSeenNotifId("fr_<userId>")` (removes + persists) is now called on **`friend-add`** (request accepted) and on **`friend-delete`** (both the real-time handler AND the offline `diffFriendsCache` path), so a future request from that person notifies again. (3) **Declined/rescinded requests (`reconcileResolvedFriendRequests`, 5-min poll)**: VRChat sends NO pipeline event when a request is declined or the sender rescinds it, so accept/unfriend clearing alone missed those. VRChat re-lists a pending request until it's resolved, so a person who still has a live `fr_` key but is NO LONGER in the current pending friend-request list (V1 + V2 fetch) AND isn't now a friend had their request resolved → clear `fr_<uid>`. Guarded by a **2-consecutive-absence strike** (`resolvedFrStrikes`, in-memory) so a partial/failed pending fetch can't spuriously clear a still-pending request (which would re-fire it — the duplicate bug); never acts on a failed fetch. Runs on the 5-min poll. **Known narrow window**: if the same person RE-requests within ~10 min (before the 2 strikes clear), the still-set `fr_` key suppresses it — a NEW pending request looks identical to the old one under per-person keying, so it's kept; only a resolved-then-quiet gap clears. The common decline-then-later-request case is covered.

- **Notification system**: Seven Android notification channels — `vrca_pipeline` (background service, MIN), `vrca_friends_activity` (online/offline/world/status/avatar/rank), `vrca_friend_requests` (incoming requests + new friends + messages), `vrca_friend_removals` (unfriends), `vrca_invites` (world + group invites), `vrca_groups` (announcements/queue/role/etc.), `vrca_connection` (pipeline disconnect + sign-in alerts + server alerts). Legacy `vrca_vrchat_events` is deleted on upgrade. The "VRChat connected / Monitoring VRChat events" notification is intentionally NOT fired — the persistent foreground service notification already conveys that state, and the duplicate notification was redundant. `fireConnectionNotification(true)` only cancels any leftover disconnect notification from a previous cycle. Notifications use `setGroup()` keys (`vrca_group_friends`, `vrca_group_social`, `vrca_group_invites`, `vrca_group_groups`) so multiple events stack in the shade with summary notifications; summary notifications use `GROUP_ALERT_CHILDREN` to prevent double notification sounds. Per-friend cooldowns (60s) on chatty events (location, status, avatar). Bio uses the grouped notification model instead of cooldowns — every bio change is captured as a separate event in the in-app alert group. **Bio change detection fires on edit, clear (text→empty), AND add (empty→text)** — the prior code required both old and new bio non-blank, so clearing a bio or adding one to an empty bio never fired. The live `handleFriendUpdate` path gates on `user.has("bio")` so partial location/status payloads (which omit the field) don't false-fire; empty sides render as `(empty)` in the diff. Notification titles reflect the action ("cleared their bio" / "added a bio" / "updated bio"). All 27 toggles live in `VrchatNotificationPrefs` keys + `UserPreferencesRepository` flows. The UI lives in `LegacyPagesAndSettingsPage.kt`'s `NotificationToggleSection` (public, called from `VrcaScreen.kt`). Each `ToggleRow` has an optional `description` parameter showing muted `bodySmall` text explaining what the toggle does. Organized into 5 collapsible sections with sub-section labels for visual grouping: Friend list (4 toggles), Friends activity (10 toggles, sub-grouped into Presence / Profile changes / Alerts), Invites (3 toggles: world invites / invite requests / group invites), Groups (6 toggles, sub-grouped into Updates / Management), App and connection (6 toggles, sub-grouped into App / VRChat). Each section is expandable/collapsible via `AnimatedVisibility` with fade+slide animation. The `SubSectionLabel` composable renders muted-color `labelMedium` text between toggle groups. The `KEY_NOTIF_INVITE_REQUEST` flow falls back to `KEY_NOTIF_INVITE` until the user explicitly toggles it, so existing users who had the combined invite preference enabled keep getting invite-request notifications.
  - **Offline notification backfill**: On every successful pipeline connect, `backfillOfflineNotifications()` runs a REST sweep across VRChat endpoints (`/auth/user/notifications`, `/auth/user/notifications/v2`, `/users/{id}/groups` + per-group `/groups/{id}/posts` and group events). The posts sweep is the sole announcement source — `fetchGroupAnnouncement()` (the single pinned announcement) was removed from both backfill and poll loop because the posts endpoint already returns the pinned announcement as one of its posts, and having both caused duplicate notifications (`ga_` vs `gp_` dedup IDs for the same content). **`fetchGroupPosts` parses VRChat's wrapped response** (`{"posts":[...],"total":N}`) as well as a bare array. **`fetchGroupCalendarEvents`** hits the correct VRChat group-calendar endpoint **`GET /calendar/{groupId}?n=N`** first (returns `{"hasNext":bool,"results":[...],"totalCount":N}`), with the legacy `/groups/{id}/events` and `/groups/{id}/calendar` paths kept only as 404 fallbacks. The prior code used `/groups/{id}/events`/`/scheduledEvents`/`/calendar` which all 404'd — so the calendar sweep always returned null and events created while the app was closed never surfaced (the only event source was notification-v2, which VRChat drops from the pending feed once marked seen server-side). Parses `results`, `events`, `calendarEvents`, `scheduledEvents` array keys or a bare array. `fireGroupCalendarEvent()` is the shared helper (backfill + poll loop) that fires each new event (dedup `${groupId}_event_$eventId`, seen-marker = `startsAt`) into the `event_$groupId` in-app alert group with URL `/home/group/$groupId/calendar/$eventId`. `seedBackfillBaseline()` seeds recent post IDs and calendar event IDs so the first run doesn't flood pre-existing items. A one-time migration (`posts_events_baseline_v2` DataStore flag) re-seeds posts/events into the existing seenMap for users who installed before `fetchGroupPosts` was fixed — `seedPostsAndEventsIntoExistingMap()` merges without overwriting. A SECOND one-time migration (`calendar_baseline_v3` flag) runs `seedAllCalendarEventsBaseline()` which seeds **ALL** current calendar events (past AND upcoming) into the seenMap + event fingerprint set without firing — necessary because the old calendar endpoint always 404'd, so no events were ever seeded; without this baseline the first working `GET /calendar/{groupId}` sweep would fire every pre-existing upcoming event at once. After this baseline only events created later surface as new. A THIRD one-time migration (`posts_events_baseline_v4` flag) re-runs `seedPostsAndEventsIntoExistingMap()` because the **group posts/calendar fetch window was RAISED** (posts `20 → 50`, events `→ 100` in both the backfill sweep and `seedPostsAndEventsIntoExistingMap`) so a group's many repeating-event occurrences can no longer crowd genuine posts/events out of the fetch. Raising the window exposed OLDER posts + past events that were never in the seen baseline (seeded at the smaller window) — the catch-up path then fired them as "new" (the tester-reported "spammed with a bunch of old announcements from ages ago, only on the new version" regression). The v4 re-seed covers the now-larger window once (all posts + PAST events only; genuinely upcoming events stay unseeded so they still surface once as intended), so the newly-visible older items don't fire. Runs in `backfillOfflineNotifications` right after the v2/v3 gates, before the posts/events sweep. **INSTALL-TIME cutoff (`notif_install_time_ms` Long — the real fix for the tester-reported "Announcement from &lt;group&gt; spam on login, on ALL branches")**: the v2/v3/v4 one-off migrations only patch specific window changes; the next window growth floods again. The robust fix is a hard **install-time cutoff**: a group announcement (post) whose `createdAt` is BEFORE install is **ignored forever** (recorded seen, never fired), while post-install ones still fire even if missed — exactly "only ignore announcements made before the app was installed." `ensureInstallTime()` loads `notif_install_time_ms` (set to `now` on first-run for a fresh install; for an existing user with none recorded it anchors `now` once on the first backfill after this fix, so their pre-fix backlog is ignored once and only new announcements fire thereafter). `isPreInstallAnnouncement(createdMs)` = `createdMs in 1 until installTimeMs` (a 0/unknown timestamp or unloaded cutoff fires as before). Applied at ALL THREE catch-up firing points: the backfill posts sweep, the 5-min poll posts sweep, and `processV2Notification`'s announcement/post branch. **Deliberately NOT applied to EVENTS** — an event created pre-install can still be UPCOMING (keyed on `startsAt`), so it should still fire; only `createdAt`-based announcements use the cutoff. This REPLACED an earlier per-version-reseed attempt (which wrongly swallowed the whole current backlog on every update, dropping post-install-but-unseen announcements — the user wanted only PRE-install ones ignored). It seeds ALL posts but **only PAST calendar events** (startsAt already elapsed); upcoming events are intentionally NOT seeded so the backfill sweep fires them once and users see events they missed while closed (per-event dedup stops re-fires). **Cross-dedup, two flavors**: (1) announcements/posts use a CONTENT fingerprint (`announcementContentFingerprint(groupId, title, text)`) added to `seenNotifIds` when fired from any path, checked by the other paths. **The fingerprint is BODY-based** (`ann_fp_<groupId>_<normalized-body.hashCode()>`, body = `text.ifBlank{title}` lowercased + whitespace-collapsed + first 120 chars). The title is intentionally EXCLUDED: the live notification-v2 path keys on the `message` field while the REST group-post path keys on the `text` field and they title the same post differently, so a title-inclusive key produced two different fingerprints for one announcement and the dedup missed. **The 5-min poll posts sweep now calls `isContentFingerprintSeen()` before firing** (it previously only checked its own per-post `notif_group_announcement_seen` timestamp map, never the fingerprint the live path set) — this was the cause of "an announcement fires once when the group posts it, then AGAIN a few minutes later" (the live WS path fired immediately; the poll re-fired because it didn't consult the live fingerprint). Now the poll records the seen timestamp and skips when the body fingerprint is already present. (2) **EVENTS use an ID-keyed fingerprint** (`eventFingerprint(groupId, eventId)` → `evt_fp_<groupId>_<eventId>`) shared across the WebSocket live handler, `processV2Notification`, and `fireGroupCalendarEvent` (the REST sweep). The content fingerprint was WRONG for events: distinct events frequently share a generic title ("Event starting") and the same 80-char text prefix, so a content key collided and suppressed all but the first event — the cause of "only one event shows even when the group made several". The eventId-keyed fingerprint dedups the SAME event across the three paths (all extract the same `cal_xxx` id) without colliding sibling events. Fires missed notifications through the same `fireEventNotification` flow with dedup IDs. **Staleness filters**: only *transient* notifications older than 24h are skipped via `parseVrcTimestampMs()` on the `created_at` field — V1 invites/requestInvite/votetokick/message, and V2 invite/queue/join-request/instance. **Catch-up types are exempt from the age cutoff** and fire regardless of age: V1 `friendRequest` (pending state until accepted/declined), V2 announcement/post/event/calendar and role/transfer, and group announcements/posts (backfill + poll loop). This is safe because the per-item dedup (`seenNotifIds` for V1/V2, the `notif_group_announcement_seen` per-group timestamp map for group REST) already guarantees each one fires exactly once. A user offline for a week still receives the friend requests, role changes, and group announcements/events they missed, while already-seen items never resurface. The first-run baseline guard (below) prevents a flood on fresh install. Announcements from the same group are grouped together via `alertGroupKey = "announcement_$groupId"`. **Group name resolution**: a `groupNameCache` (groupId → name) is populated whenever `fetchUserGroups()` runs; `resolveGroupName(groupId, sender)` looks up the cache first, falls back to the sanitized sender username (rejecting `null`/blank/`someone`), then `"a group"` — so notifications never render "Announcement from null". `resolveGroupNameAsync()` is the suspend variant used by live + backfill group announcement/event paths: when the groupId isn't cached and the sender name is missing, it fetches the group's real name once via `VrchatAuthManager.fetchGroupName(groupId)` (`GET /groups/{id}` → `name`) and caches it — this fixes group **events** rendering "Event from a group" because event payloads frequently omit `senderUsername`. **Event/announcement timestamps**: `fireEventNotification` accepts `eventTimestampMs`; announcement paths pass the parsed `created_at`/`createdAt` so the in-app alert shows when the announcement was actually posted. **Calendar EVENTS show when they BEGIN, not when the series was created (tester-found "events show a really old date like 65d ago").** The old code used `createdAt` for the event display timestamp — but for a REPEATING event `createdAt` is when the SERIES was created (often months ago) even though each occurrence is upcoming, so it read "65d ago". Fixes: (1) `formatRelativeTime` (`VrchatPage.kt`) is now **future-aware** — a future timestamp renders "in 1d" instead of clamping to "just now" (the negative-delta bug that originally forced the `createdAt` workaround); (2) the display timestamp is now the event's scheduled **`startsAt`** (falling back to `createdAt` only if absent); (3) the shared **`buildCalendarEventBody(desc,title,startsMs,endsMs,createdMs)`** helper builds the alert body as `description` + **`Starts: <date·time>` / `Ends: <date·time>` / `Posted: <date>`** lines (device-local via `formatEventDateTime`/`formatEventDate`), so the alert surfaces made-time AND begin/end together. All THREE event paths use the helper + `startsAt` display: `fireGroupCalendarEvent` (REST sweep, reads `startsAt`/`endsAt` off the calendar object), the live WS notification-v2 handler, and `processV2Notification` (both pull `startsAt`/`endsAt` from the payload top-level or nested `data`/`details`). The change marker (re-fire detection) still tracks `startsAt` then `createdAt` so an edited start time re-fires. **Periodic group polling**: `startGroupAnnouncementPollLoop()` polls every 5 minutes independently of the WebSocket — fetches recent posts via `fetchGroupPosts()` and calendar events, AND re-sweeps pending `notification-v2` (events/announcements/role changes) via `processV2Notification()`. Its first poll runs ~45s after start (not a full 5 min) so catch-up surfaces quickly on reopen. **This is the retry path** for the one-shot connect backfill: that backfill makes 50+ REST calls rapidly and can miss items on a transient 429/timeout or a cold-start race, with no built-in retry — the poll loop re-attempts the V2 sweep every cycle so nothing made while the app was closed is lost. `processV2Notification(obj)` is the shared per-notification handler used by both the connect backfill and this retry sweep; `seenNotifIds` dedup makes re-processing safe (each fires at most once). The V2 sweep is skipped until `notif_backfill_initialized` is set (first-run baseline). This catches announcements that the WebSocket missed or delayed. Notification-v2 handler explicitly matches `post` types as announcements and `event`/`calendar` types as group events, with proper URLs (`/posts` for announcements, group page for events). Per-group announcement timestamps are persisted in DataStore (`notif_group_announcement_seen`) to avoid re-firing on reconnect. `seenNotifIds` set (**UNBOUNDED — no cap, no eviction, deliberate**) deduplicates between REST backfill and websocket replay — **persisted to SharedPreferences as a JSON array** (`vrca_seen_notifs/ids_ordered`, `commit()` not `apply()`) so IDs survive service restarts and prevent repeat notifications. **The "friend requests resend on every app restart" bug lived here (tester-found)**: the set was capped (500, later 4000) with eviction, and it holds EVERY dedup key (V1/V2 ids + `ann_fp_`/`evt_fp_` fingerprints + per-person `fr_` keys) — months of use filled it permanently, so every new id evicted an old one. Worse, the legacy `putStringSet` persistence is a HashSet: **insertion order was LOST on reload, so post-restart eviction removed effectively RANDOM entries** — including fresh `fr_` keys. A pending friend request is RE-LISTED by VRChat on every backfill until accepted/declined, so an evicted `fr_` key = the same request re-fired on the next restart/reconnect (and each fire appended another event to the in-app "Friend request (N)" card). **The fix is NO eviction at all** (per the user: cap removed entirely): membership alone decides — once an id is in, that notification can structurally never refire. Growth is human-rate (~40-60 chars per real notification; even years of heavy use is a few MB, loaded once at service create). One-time migration from the legacy `ids` StringSet. `persistSeenNotifIds` uses `commit()` because the swipe shutdown hard-kills the process without waiting for QueuedWork — an async `apply()` could lose the write and re-fire that notification next launch. **`addContentFingerprintToSeen`/`addEventFingerprintToSeen` persist immediately on genuine add** (they're not always followed by a `fireEventNotification` persist — an unpersisted fingerprint lost to a kill re-fired the announcement/event after restart, same bug class). Do NOT re-introduce a cap/eviction here — any eviction of a pending-state key recreates the refire bug. The admin-announcements set (`vrca_seen_announcements/ids`) is likewise uncapped, and its first-snapshot seed re-covers everything active on every restart without firing. **Ordering matters**: `fireEventNotification` checks the per-type toggle (`enabled`) BEFORE adding the dedup id to `seenNotifIds`. Adding it first would permanently poison the id if `enabled` read false transiently (e.g. DataStore not fully loaded during the cold-start connect/backfill race) — the notification would be dropped AND marked seen, so it could never fire on any later path. This was a cause of group announcements/events inconsistently never appearing on app open. REST calls are paced with 250-300ms delays; groups capped at 50 to stay under VRChat rate limits. Group announcement notifications include the group name in the title ("Announcement from GROUP_NAME"). Event notifications use calendar URLs (`/home/group/$groupId/calendar/$eventId`) when available. The `groupId` is extracted from `relatedGroupId`, `groupId`, `data.groupId`, `details.groupId`, or `senderUserId` (if `grp_` prefix), and the `eventId` from `eventId`, `data.eventId`, `details.eventId`, or `id` (if `cal_` prefix). As a final fallback BOTH fall back to `findIdWithPrefix(payload, "grp_"/"cal_")` — a recursive scanner that walks the entire notification-v2 JSON (including nested `data`/`details` blobs and embedded URLs) for the first value with that prefix. This is what makes event notifications reliably link to the calendar URL (`/home/group/grp_xxx/calendar/cal_xxx`) instead of falling back to `/home/notifications`. The notification-v2 `when` block has an **`else` catch-all** that logs the unmatched `type` string at INFO and still fires a generic group notification (gated by `KEY_NOTIF_GROUP_EVENT`) — so no notification-v2 is ever silently dropped just because VRChat used an unexpected type string. All handlers (WebSocket, poll loop, backfill) share `seenNotifIds` for dedup — WebSocket events now pass `dedupId = notifId` to prevent duplicates between real-time and polling. Each endpoint is wrapped in try/catch — failures are logged and skipped for this connect cycle. Bio/name change detection also runs on reconnect by diffing the old `friendsCache` against fresh API data before the cache is overwritten.
  - **First-run baseline guard**: A `notif_backfill_initialized` DataStore boolean blocks the first-ever backfill from firing anything. On the first pipeline connect, `seedBackfillBaseline()` walks user groups and silently records recent post IDs and calendar event IDs into `notif_group_announcement_seen`, then pre-seeds `seenNotifIds` with all V1 and V2 notification IDs (without firing), then sets both `notif_backfill_initialized` and `posts_events_baseline_v2`. This prevents both historical group announcements and pending friend requests/invites from firing on first install. From the second connect onward, the regular backfill flow runs and fires only events that arrived after setup.
  - **Newly-joined-group silent baseline (`seedGroupBaselineSilently` + `__known` markers)**: the first-run baseline only covers the groups the user was in AT first launch. A group **joined later** had none of its pre-existing posts/events baselined, so the backfill + 5-min poll swept them all as "new" and fired old announcements/events as notifications on join (tester-reported). Fix: a persisted **known-groups set** stored as reserved keys INSIDE the `notif_group_announcement_seen` map — `__known_groups_init` (the set-initialized flag) + `${groupId}__known` per group. The FIRST sweep on this version records every current group as known WITHOUT seeding (so existing groups still fire genuinely-missed items on upgrade — nothing is swallowed), then any group whose `__known` marker is absent is one joined LATER, so `seedGroupBaselineSilently(groupId, seenMap, updatedMap)` records ALL its current posts + calendar events into the seen map (and their `ann_fp_`/`evt_fp_` fingerprints into `seenNotifIds`) WITHOUT firing, marks it known, and `continue`s (skips firing that group this cycle). Wired into BOTH `pollGroupAnnouncements` and `backfillOfflineNotifications` (shared guard). After a group is baselined once, only content created after joining surfaces. The reserved keys are safe: the seen map is only ever read via `.optString(specificKey)`, never iterated as `groupId_post_`/`groupId_event_`.
  - **Expandable notifications**: Status change, rename, and bio update notifications use `BigTextStyle`. Expanding shows before/after comparison text.
  - **Notification click actions**: Invite, group, and friend offline notifications open relevant VRChat web pages (`vrchat.com/home/notifications`, `/home/group/$groupId`, `/home/user/$userId`) instead of just opening the app. Group v2 notifications fall back to `vrchat.com/home/notifications` when `groupId` is missing from the payload.
  - **Notification-tap ACTIONS (`NotificationActionReceiver`)**: world-invite and invite-request notifications perform a VRChat REST call on tap instead of opening a web page. `fireEventNotification` gained `tapActionType`/`tapActionData` — when set, the tap `PendingIntent` is a `getBroadcast` to `NotificationActionReceiver` (registered in the manifest, `exported=false`) instead of `ACTION_VIEW`. The receiver runs the call on `goAsync()`, Toasts the result, and cancels the notification id on success. Two action types, shared by the in-app buttons via `NotificationActionReceiver.perform(ctx, type, data)`: **`invite_me`** (`data` = the invited instance `{worldId}:{instanceId}` location → `VrchatAuthManager.inviteSelfToInstance`, re-invites you so you can join) and **`invite_user`** (`data` = the requester's userId → `VrchatAuthManager.inviteUserToInstance(uid, location)` where `location` is read LIVE from `VrchatPipelineState.presence?.location` at tap time, inviting the requester to your current instance). `inviteUserToInstance` = `POST /invite/{userId}` with `{instanceId}`. **Both invite methods (and `inviteSelfToInstance`) return `VrchatAuthManager.InviteResult(ok, error)`** — on failure `error` carries VRChat's own reason parsed from the response body (`error.message`/`message`, quote-trimmed; falls back to "That instance has closed or no longer exists" on 404, "not accessible" on 403). The receiver/in-app Toast and the admin "Invite me" result surface this verbatim so the user is told WHY (instance closed/0-people/full or invite/invite+ not accessible) instead of a generic failure. World-invite extracts the location from the invite `details` (`worldId` if it contains `:`, else `worldId:instanceId`); if it can't parse a `wrld_` location it falls back to the old `home/notifications` web tap. The same action wiring is mirrored in the offline V1 backfill.
  - **In-app coverage (every type)**: friend request + new friend collapse into ONE per-person card (shared `friend_$userId` alertGroupKey); unfriend (`unfriend_$userId`), trust-rank (`rank_$userId`, text "X is now **a** Trusted User" — the article reads better), VRChat message (`msg_$senderUserId`, or `msg_$notifId` for System/no-sender messages), and ALL group notifications (keyed `group*_$baseId`). **Invite fusing (two distinct modes)**: **invite requests** fuse PER-PERSON (`invreq_$senderUserId`) as a **single-event** group (`alertSingleEvent=true` → only the latest is kept in-app, no "(N)" count) — you only need to know a person is asking, not every request. **World invites** fuse PER-INVITER (`worldinvite_$senderUserId`, like bios) as an **accumulating** group keeping the full history, with **same-instance dedup** (`alertDedupByActionData=true` → repeat invites to the SAME instance location collapse to one entry; different instances stay separate). Both `alertSingleEvent` and `alertDedupByActionData` are `fireEventNotification` params threaded into `InAppAlertState.addGroupedEvent`. The generic `group.*` catch-all always shows (no `message.length > 100` gate). **Filter chips**: `isGroupAlert()` routes `announcement_`/`event_`/`group*` to "Groups"; the rest to "Friends"; `bio_` to "Bio".
  - **In-app action symbols + instance picker (`VrchatPage.kt`)**: per-event full-width action buttons were REPLACED by **compact circular symbol buttons in the card header**, sitting next to the collapse (chevron) and dismiss (X) affordances and matching their style (`HeaderActionSymbol`, 34dp tinted `Surface`). Per group the header shows: an **invite symbol** (↪ `Login` for invite-me / ＋ `PersonAdd` for invite-them) when the group has invite actions, and **one** "Open in VRChat" symbol (⧉ `OpenInNew`) for the single url shared by all events. **Open dedup**: friend-request+new-friend and bio/name/rank history all point at the same profile url, so they show ONE header open symbol instead of repeating it per event; groups with DISTINCT per-event urls (separate group posts/events) keep per-event "Open in VRChat" buttons in the body via `showPerEventOpen` (`= sharedOpenUrl == null`). **Invite symbol tap behavior**: invite-request (single) → instant `inviteUserToInstance`; world-invite with ONE distinct instance → instant `inviteSelfToInstance`; world-invite with MULTIPLE distinct instances (same person invited you to several) → opens **`InstanceListDialog`** (the instance picker). `CompactAlertButton` (38dp full-width tonal) remains for the body per-event opens and the dialog rows. `AlertActionButtons` was deleted. `InAppAlertEvent` carries `actionType`/`actionData` (persisted in the alerts JSON).
  - **`InstanceListDialog` + 24h instance history (`VrchatPage.kt`, all LOCAL — zero Firestore)**: a reusable app-styled picker listing instances with world image (Coil via `VrchatImageLoader`, session-authed), live occupancy, and a joinability status chip, plus a per-row **Invite** button (`inviteSelfToInstance`). Instance info is fetched FRESH every time the dialog opens (`VrchatAuthManager.fetchInstanceInfo(loc)` → `InstanceInfo(worldName, worldImageUrl, players, capacity, status)`; `InstanceStatus` from the HTTP code: 200=OPEN, 404=CLOSED/dead, 403=INACCESSIBLE), so counts/closed status are never stale. Used by (a) the world-invite multi-instance picker above, and (b) the **Instance History** chip in the VRChat-tab identity header: `InstanceHistoryStore` (`com.vrca.vrchat`, SharedPreferences `vrca_instance_history`) records every instance the user enters (full location WITH the `~nonce` access token, captured from the self `user-location` event in `applySelfLocationEvent`) along with **join/left times** (`joinedMs`/`leftMs`; `leftMs==0` = still in it; `markCurrentLeft()` closes it out when leaving) so the user can re-invite themselves to anywhere they've been in the past 24h and cross-reference what they played and when. **"Still here" is derived from the LIVE `VrchatPipelineState.presence` (the SAME signal the Discord RPC reads), NOT from the stored `leftMs` flag**: the dialog labels the newest open session "Still here" only while `presence.location == entry.location` (keyed on the live location so it recomputes reactively) — the instant presence leaves that world (offline / traveling / hopped) the label drops, regardless of whether `markCurrentLeft` fired. This was the "Still here never changes when I get off VRChat" fix — the old code trusted only `leftMs`, which depended on a `markCurrentLeft` signal that didn't always land. `markCurrentLeft` is ALSO now broadened on the service side: both `applySelfLocationEvent` and `syncPresenceToFirestore` close the open session on ANY world→non-world transition (offline / traveling / blank, excluding the redacted "private" which means still-in-instance), not just `location=="offline"`, so the stored "Left HH:MM" time is accurate too. Entries left >24h ago self-prune on every read/write; an instance still occupied never prunes. The nonce lives ONLY on-device (never synced to Firestore — like the user's own cookie). All of this costs **zero Firestore reads/writes**; the only network cost is VRChat REST (instance-info on dialog open, invite on tap), on the user's own session. **Pre-stored thumbnail + type at ENTRY time (so a cold history open never looks bad)**: `Entry` carries `imageUrl` + `instanceType`, grabbed the MOMENT the user enters an instance — the single `GET /instances/{loc}` that already fires on every self world-hop in `applySelfLocationEvent` (for the occupancy count) now uses `VrchatAuthManager.fetchInstanceInfo` (the SAME one request + `extractInstanceUserCount` count logic, just a richer parse) instead of `fetchInstanceCount`, so that ONE already-made call ALSO reads the world thumbnail + API instance `type` and writes them onto the history entry via `InstanceHistoryStore.updateInfo(...)`, and caches the image bytes via `AlertImageStore.ensureCached`. **Zero extra network cost** — it's the same call returning more of the response (only patches presence count when `status==OPEN`; only stores non-blank image/type so a CLOSED/INACCESSIBLE reply never wipes a prior value). Opening Instance History later renders the image + type chip INSTANTLY from the stored entry (`InstanceTarget.imageUrl`/`instanceType`, the latter fed into `parseInstanceType` as `apiType` with the location-string derivation as fallback) instead of fetching everything on the spot; the picker still live-refreshes occupancy and re-persists the freshest world-name/image/type via `updateInfo`. `updateImage` is now a thin wrapper over `updateInfo`. When the entry ages out of the 24h window and is pruned, `AlertImageStore.gc()` sees its `imageUrl` reference vanish and deletes the cached file — so the image clears with the entry.
  - **Rich event/announcement alerts (`AlertEventBody` in `VrchatPage.kt` + `AlertRichMeta`/rich fields on `InAppAlertEvent`)**: group-event and announcement alert cards render structured content instead of a text blob. Events show: a full-width **banner at the image's OWN intrinsic ratio** (`ContentScale.FillWidth` + wrapped height — creators upload arbitrary ratios, so ANY fixed-ratio Crop box cut edges; tester confirmed cropping twice, first at 21:9 then at 1200:630 — do NOT reintroduce a fixed-ratio crop). **Overlays ride the banner, identity-header style**: **language chips top-right in a right-aligned STACK** (`LanguageChip`; a horizontal row overflowed on long native sign names; native names via `vrcLanguageNames`/`nativeLanguageName()` — "日本語, English, 日本手話" EXACTLY as VRChat's site renders them; **tapping a chip shows the device-language translation (`deviceLanguageName()` — "Japanese Sign Language" on an English phone, VRChat's in-client localization style) in place for 5s then reverts** (`LanguageChip` is CONTROLLED — `AlertEventBody` hoists a single `translatedLang` + `langTapTick`, so only ONE chip is translated at a time and tapping another instantly reverts the previous even mid-window; re-tap restarts the 5s; sign languages translate to English names since Locale can't resolve them); the earlier `Locale.displayLanguage` localized names to the DEVICE language so "English" showed as 英語 on a Japanese-locale phone, and sign tags were wrongly shortened to "JSL" — VRChat's real names are `jsl`→日本手話, `ase`→American Sign Language, `kvk`→한국수어 etc.; map first, then native-Locale resolution, then uppercased code) and a **bottom scrim row** (black vertical-gradient) carrying the status/category/access chips (dark `solid` `EventMetaChip` variant) + **platform SYMBOLS** (`PlatformSymbols`: circular tinted BRAND glyphs matching VRChat's badges — Windows flag blue (`ic_platform_windows.xml`) / Android robot green (`Icons.Filled.Android`) / Apple mark light (`ic_platform_apple.xml`); a monitor/phone glyph read as wrong per user reference). **All chips (`EventMetaChip`/`LanguageChip`) are a FIXED 22dp height with centered single-line text** — CJK glyphs are taller than Latin, so padding-based chips rendered at different sizes and looked ragged in mixed-language rows. **Tappable chips (access chip's group link, language chips) use `Surface` + `Modifier.clickable`, NOT `Surface(onClick=...)`** — a clickable Surface enforces Material3's 48dp minimum interactive size, which padded each tappable chip to 48dp tall and blew a huge invisible vertical gap between the chip rows (tester-reported "big gap that looks weird"); `clickable` keeps the chip its true height. No banner → **TWO tight rows in a `Column(spacedBy(6.dp))`**: (1) a `FlowRow` with the status/category/access chips (wrapped in ONE inner `Row` so they're inseparable — as bare FlowRow children it split them, wrapping "Group" off on its own) + platform symbols on the same line; (2) a `FlowRow` of language chips directly below. Languages in their OWN row (not the same FlowRow as the chips) is what removed the big vertical gap the single-FlowRow wrap left. A thin `Divider` separates the meta block from the description. `richChips(solid)` is the single shared chip definition; `ExperimentalLayoutApi` for `FlowRow`. Chips: **status** ("Starts in 2h" primary / amber when <2h → "**Live now**" green while `startsAtMs ≤ now ≤ endsAtMs` → "**Ended**"; no `endsAt` = live for 4h after start, via `eventPhase()`), **category** (`prettyEventCategory`: `film_media` → "Film & Media", generic underscore→Title Case fallback), a **recurring indicator** (`InAppAlertEvent.recurring`, best-effort from `GroupAlertEnricher.extractRecurring`) — placed on the banner's empty **top-left** corner when there's a thumbnail, else appended to the muted meta line ("... · ↻ Repeats"); deliberately NOT a status-row chip (it crowded that row). **Recurring-flood collapse**: VRChat writes EVERY day of a repeating event as its own calendar event (a daily repeat = up to 50 alerts in one `event_$groupId` group). `collapseRecurring()` folds same-title events in a group down to the **nearest UPCOMING occurrence** (or most-recent past if none upcoming) at DISPLAY time in `AlertGroupCard` — so the card shows ONE, the "(N)" count is the collapsed count, and the header preview uses the SOONEST event (not the farthest, which read "in 48d"). It's retroactive (fixes cards that already flooded) and self-advancing (as an occurrence passes, the collapse picks the next from the stored occurrences). The Android notification was already one per group (`setOnlyAlertOnce` on the shared `alertGroupKey` id), **access chip** (Public green 0xFF4CAF50, Group purple 0xFFAB47BC) which **IS the tap target that opens the hosting group's web page** (per user feedback: the CHIP opens the group, NOT the card title — a title-click attempt was reverted). Below the meta line, a **"Hosted by <name>"** one-liner where the name is primary-tinted and clickable → the organizer's VRChat profile (`organizerId`/`organizerName`; `extractOrganizerId` probes owner/author/creator fields, name resolved once via `VrchatAuthManager.fetchUserDisplayName` and cached in the enricher). Below: description in `bodySmall`, a compact **one-line timing row** (Schedule icon + "Jul 5 · 3:05 PM → Jul 8 · 5:05 PM", `eventDateTime` has NO year), a **muted meta line** ("1 interested · Posted Jul 4, 2026" — `eventDate` keeps the year), NO relative-time footer on rich events (the chip + timing line already say when; plain alerts keep it), and **phase-appropriate actions**: UPCOMING → **Add to Calendar / Remove from Calendar** (`VrchatAuthManager.setCalendarEventFollowing` → `POST /calendar/{groupId}/{eventId}/follow` `{"isFollowing":bool}` — community-documented endpoint, failures surface VRChat's own error; success optimistically updates `following` via `InAppAlertState.enrichEvents`, AND schedules a **local reminder** (`EventReminderScheduler` — an inexact `AlarmManager.setAndAllowWhileIdle` alarm ~10 min before `startsAt`, no exact-alarm permission; `EventReminderReceiver` posts the "Event starting soon" notification; persisted to `vrca_event_reminders` prefs and re-armed on reboot via `BootReceiver.rescheduleAll`; Remove-from-Calendar cancels it). **Signed-up events (following==true) golden-pin to the top**: `VrchatStatusPage.inAppAlertSection` partitions the alert groups into a "★ Signed up Events" section (gold sub-header, `AlertGroupCard(signedUp=true)` → gold accent bar + gold border + faint gold container tint + a "★ Going" pill by the title) pinned above an "Other notifications" section; **Splitting is at the EVENT level, not the group, but COLLAPSED per SERIES**: a group can have some events the user signed up for and some they didn't, so the FOLLOWED events become cards in "Signed up Events" while the remaining unfollowed events stay grouped in Pinned (if the group is manually pinned) or Other. **Followed events are grouped by SERIES (normalized title) into ONE card per series** (`bySeries` map → `su_<groupId>_<seriesKey>`, the card's own `collapseRecurring` picks the nearest upcoming occurrence + shows the count) — a recurring event the user signed up for has ~50 occurrences each independently `following==true`, which without this collapse spawned ~50 identical "Going" cards (tester-found flood). Un-following an event drops it back into the remainder group; dismissing a remainder card uses `dismissEvents` (removes only its unfollowed events, so the followed ones shown separately survive). **SHORT-WINDOW per-series follow override (`followSeriesKey`/`recordFollowToggle`/`followOverride`, in `InAppAlertState`, in-memory)**: tapping Add/Remove records the user's decision keyed by SERIES (normalized title, or `id:<evId>` when titleless); the enricher's `following` read consults `followOverride(followSeriesKey(...))` FIRST, but the override lasts only `FOLLOW_OVERRIDE_MS` = **30s** — just long enough to cover VRChat's follow-propagation delay so an in-flight enrich can't flip a just-toggled event back. **After the window the SERVER's `userInterest.isFollowing` is authoritative**, so a follow/unfollow made on the **WEBSITE or in the VRChat CLIENT** is picked up by the 10s expanded poll / 60s tab loop. (The override was briefly made PERMANENT+persisted as a workaround for the old `extractEventFollowing` bug that reported EVERY event as followed — but permanence blocked external follow changes from ever showing, and with the follow field now read correctly (`userInterest.isFollowing`) there's no flip-back to guard against beyond the propagation window.) The optimistic UI update (`applyFollowToSeries`) covers every occurrence of the series at once, and a failed API call reverts both the override and the display. **Signed-up cards are NOT dismissable** — the per-card X is hidden and `InAppAlertState.dismissAll` preserves them (Remove-from-Calendar un-pins → the X returns), so a user can't accidentally delete an event they're going to. **Manual "📌 Pinned" section (universal, any alert type)**: below "★ Signed up Events" sits a **collapsible** "📌 Pinned (N)" section for groups the user manually pinned (`InAppAlertGroup.pinned`, persisted). A **tiny pin toggle** sits next to the "ago" row in EVERY card (both collapsed and expanded states) — filled gold when pinned, outline otherwise — **EXCEPT signed-up (Added-to-Calendar) cards, where the pin is hidden** (they auto-sort into "Going", so a manual pin fought the auto-sort; **Add-to-Calendar also auto-unpins** via `setPinned(false)`). `InAppAlertState.setPinned` caps manual pins at **`MAX_PINNED` = 10** (over-cap → a toast, no-op). **Per-event dismiss**: a multi-event card shows a small X per event (title row / "Change N" row) → `InAppAlertState.dismissEvent` removes just that event (a recurring series' X clears the whole series; emptying the group removes it). The AnimatedVisibility event list is wrapped in `key(displayEvents ids)` so a SECOND event fusing into a group actually repaints (the content lambda otherwise skips — same cycle-mute class). Pinned groups get the SAME gold treatment + a "📌 Pinned" pill, are NOT dismissable (X hidden + `dismissAll` preserves `pinned || following`), and un-pinning (tap the pin again) drops them back to "Other notifications". The Pinned section's collapse state is `PinnedSectionState.expanded` — the SAME process-scoped model as `AlertSectionState` (survives tab switches, resets to expanded on a cold process start). The three partitions (signed-up / pinned-not-signed-up / rest) are disjoint (signed-up wins) so a group renders once; keys stay the plain groupId so expand state survives partition moves. `following` is read from the calendar object via `extractEventFollowing` (probes `isFollowing`/`isInterested`/`isAttending`/... booleans + `userInterest`) so an event added IN-GAME shows as signed-up and its button reads "Remove from Calendar" instead of wrongly offering "Add" — best-effort pending confirmation of VRChat's exact field name); LIVE → **Join event** (fetches `VrchatAuthManager.fetchGroupInstances` = `GET /groups/{id}/instances` — VRChat doesn't link events to instances, so ALL the hosting group's open instances are listed in the SAME `InstanceListDialog` the multi-invite picker uses, with an `emptyText` explaining when the group has none open); ENDED → no action. Announcements get the banner (group-post `imageUrl`), **"Visit <host>" link buttons** for URLs in the body (`extractBodyUrls`, max 3), and **read-more** collapse past 320 chars. **Announcements from a group that currently has an OPEN instance also get a "See active instances" button** (same `InstanceListDialog`/`fetchGroupInstances` picker as a LIVE event's "Join event", shared `onJoinEvent`/`openJoinPicker`, dialog title neutralised to "Active instances · <group>"). The button shows ONLY when the group has ≥1 open instance — `AlertEventBody` fires ONE `fetchGroupInstances` check on expand (this body only renders while the card is expanded, so it's one lightweight GET per opened announcement, not per row) and stores `announcementInstanceCount`. Per-event `remember` state is wrapped in `key(event.id)`. **The shared alert-card header title (`AlertGroupCard` `displayTitle`) is `maxLines = 2`** (was 1) — long notification titles ("Announcement from <long group name>", etc.) were ellipsis-cut on EVERY alert type since the header is shared; two lines before ellipsizing handles it.
  - **Event identity = `seriesId`, not title (`InAppAlertEvent.seriesId`); re-surface dismissed follows; "Removed" state**: three coupled fixes to event alerts. (1) **Stable series identity** — `InAppAlertEvent` gained `seriesId` (VRChat's per-series id; every occurrence of a repeat shares it, a one-off has its own), populated at the fire paths (`fireGroupCalendarEvent`/`fireDetailedCalendarEvent` via `AlertRichMeta.seriesId` = `GroupAlertEnricher.extractSeriesId`) and by the enricher, and persisted. The **display collapse** (`collapseRecurring`), the **signed-up `bySeries` partition**, and the **enricher match/transform** now key on `seriesId` FIRST (title only as a fallback for cards with no seriesId yet). This fixes "two DIFFERENT repeating events override/mix each other's thumbnail/languages/description/title and the card flip-flops between them" — title is not a unique identity (distinct series collided, edits changed it, the enricher's title clause cross-wrote one series' fields onto another). The thin v2 fire paths ALSO try to pull `seriesId` from the push (`content`/`data`/`details`, same as `startsAt`) so a series-carrying push groups correctly on first fire; when the trimmed payload omits it, seriesId is null and the targeted enrich sweep fills it within seconds. (2) **Re-surface a swiped followed event — POST-FIRE ONLY** — the signed-up section derives purely from `following==true` events in `_groups`, so an event dismissed from the in-app list (while `following` was still null, before enrichment confirmed it) vanished from "Going" (the enricher only UPDATES existing cards). When the enricher's server follow-read (`userInterest.isFollowing`, which reflects website/PC/headset subscribes) says `following==true` and `InAppAlertState.hasSeriesCard(...)` is false, it silently re-adds the card (`readdFollowedEventIfMissing`, no Android notification). **CRITICAL: this only runs on the POST-FIRE targeted sweep (`enrich(..., allowResurface=true)` from `scheduleTargetedGroupSweep`), NOT the routine periodic tab-loop/expanded sweeps (`allowResurface=false`, the default).** VRChat reports the user's OWN created events as `following==true` FOREVER, so a periodic re-surface resurrected every dismissed self-created event on every VRChat-tab open (tester-found "old dismissed events keep reappearing in Going"). Gating to the post-fire window keeps the genuine "you subscribed/created and swiped it away too fast" case while never fighting a deliberate dismissal of an old event. Remove-from-Calendar sets `following=false` so an intentional un-follow is also never fought; repeatable and per-series. (3) **Deletion → "Removed"; ENDED → pruned** — two cleanup mechanisms. **Ended prune** (`InAppAlertState.pruneEndedEvents(ctx, now)`, timestamp-only, no network): removes events whose end has passed (`eventEnded` = past `endsAtMs`, or >4h past `startsAtMs` with no end) so concluded events stop cluttering "Going"/the notifications area (a recurring series sheds past occurrences, keeps upcoming ones; `removed` red cards are KEPT; non-event alerts untouched). Run on VRChat-tab entry + every 60s in the tab loop. **Deletion → red "Removed" card** (`InAppAlertEvent.removed`, persisted): the enricher's `detectRemovedEvents` trusts the AUTHORITATIVE group calendar LIST. **`fetchGroupCalendarEvents` now returns `[]` (not null) for an emptied group** — the canonical `/calendar/{groupId}` endpoint's 200-with-`results:[]` is returned as an empty array instead of falling through to the legacy 404 endpoints and returning null, so "group has zero events" is finally distinguishable from "fetch failed" (null). Detection runs ONLY when the list is non-null (a null list = network error → skip, so a blip can't false-flag). Detection asks VRChat **DIRECTLY per event id** (`fetchCalendarEventResult` = `GET /calendar/{g}/{eventId}` → **404/410 = deleted**, 200 = alive) for every not-yet-removed, non-ended, signed-up-or-upcoming card — the PRIMARY signal, NOT gated on list-presence. Gating on list-absence (a first attempt) was the "second-deleted event never flags" bug: if VRChat's group calendar LIST lags and still shows the just-deleted event, that event was marked "present" and never even 404-checked. Going straight off the event's own 404 sidesteps the list entirely. Blip protection: a network/429/5xx reply is UNKNOWN and does NOT flag on its own; only when the direct check is inconclusive does it FALL BACK to the list — absent there for **`ABSENT_STRIKES_TO_REMOVE` = 2 consecutive sweeps** (`absentStrikes` in-memory counter) also flags, while a definitive 200 or list-presence resets the strike. The direct checks are bounded (≤10/sweep). List fetched at n=100 (fallback only). `markSeriesRemoved` sets `removed=true`+`following=false`. ENDED events are skipped (pruned, not flagged — the red state is for a cancelled event you might still attend). (`fetchGroupCalendarEvents` still returns `[]` not null for an emptied group so the list fallback can distinguish "zero events" from "fetch failed".) A removed event drops out of "Going", becomes its own **red** card (`rm_<groupId>_<series>`, `AlertGroupCard(removed=true)` → red accent bar/border/tint + "Removed" pill) in the normal area, body shows "This event was removed from VRChat" instead of Add/Join/Remove, and it's **dismissable**. **`removed` is STICKY** — the enricher deliberately does NOT clear it on a live-representative match (a title-fallback match could otherwise un-remove a deleted same-title sibling series); a genuinely recreated event gets a fresh id → new card. Won't be re-surfaced by (2) since `following` is cleared AND re-surface is post-fire-only.
  - **Repeats dialog — per-occurrence subscribe (`EventOccurrencesDialog` + `EventSeriesStore`)**: tapping the **"↻ Repeats"** indicator on a recurring event card (clickable when `recurring && seriesId != null && groupRefId != null`) opens a picker (styled like `InstanceListDialog`) listing **every occurrence** of the series with its start date/time + a per-occurrence **Subscribe/Going toggle** — VRChat tracks follow **per occurrence** (`cal_` id), so this matches the real model (`setCalendarEventFollowing(groupId, occurrenceId, following)`). **`EventSeriesStore` (`com.vrca.vrchat`, SharedPreferences `vrca_event_series`)** persists every occurrence per `groupId|seriesId` (`{id, startsAt, endsAt, following, deleted}`) until it ENDS (pruned) or is DELETED (flagged red "Removed", kept 24h, then pruned). **Cost model**: occurrence existence + timing + the **deletion diff** come FREE from the group calendar list the enricher already fetches (`reconcileOccurrenceStore` in `GroupAlertEnricher.enrichLocked` — groups the raw list by seriesId, reconciles the store, flags stored-but-absent occurrences deleted **only within the n=100 window** via `maxLiveStart` so a far-future occurrence isn't false-flagged). Only per-occurrence FOLLOW state costs a call (the list omits it), so `following` starts null and is filled **lazily**: the representative's follow is written free by the enricher (already fetched); the dialog fetches the rest **on open**, paced (300ms), nearest-first, capped 20, skipping any the user is actively toggling. In-app subscribes write the store instantly + fire the API optimistically (revert on failure). **Card aggregate "Going"**: a recurring card golden-pins when `ev.following == true` OR `EventSeriesStore.anySubscribed(...)` (subscribed to ≥1 live occurrence) — the partition's `isGoing()` in `VrchatStatusPage.inAppAlertSection` — so subscribing to any occurrence in the dialog lights the one series card in "Signed up Events". **Roll-forward when the shown occurrence ends (`pruneEndedEvents` → `advanceRecurringEvent`)**: the alert stores only ONE fired occurrence per series, and the ended-event prune ran BEFORE the enricher could re-point it, so a recurring card used to VANISH when its shown occurrence ended instead of switching to the next date. Now the prune, on finding a recurring event whose occurrence ended, re-points the card to the series' **next upcoming occurrence** from `EventSeriesStore.nextUpcoming(...)` (updates `eventRefId`/`startsAt`/`endsAt`/`timestamp`/`following`, preserving title/banner) instead of removing it; only a CONCLUDED series (no upcoming occurrence in the store) or a non-recurring event drops. The store is populated by the enricher over the card's lifetime, so by the time an occurrence ends it's virtually always present; if it somehow isn't, the fallback is the old behavior (drop) — no regression. **DELETION of the shown occurrence also rolls forward, not just ENDING** (`GroupAlertEnricher.removeOrAdvance`): when `detectRemovedEvents` confirms the card's occurrence is deleted (404), for a RECURRING series it marks that occurrence deleted in the store, then `InAppAlertState.advanceSeriesIfUpcoming` re-points the card to the next upcoming occurrence (clearing `removed`) — only a one-off, or a series with NO upcoming occurrence left, falls through to `markSeriesRemoved` (the red card). `reconcileOccurrenceStore` runs BEFORE `detectRemovedEvents` in `enrichLocked` so `nextUpcoming` already excludes the just-deleted occurrence. This also stops the card from collapsing on the delete (it stays in its section instead of moving to "Removed", so its `key` — and expand state — survive). **Dialog survives enrichment (`occurrencesFor` hoisted to `AlertGroupCard`)**: the Repeats dialog's open-state used to live inside `AlertEventBody`, which is wrapped in `key(event.id, event)` — so every enrichment tick that changed the event data disposed+recreated the body and slammed the dialog shut. It now lives in `AlertGroupCard` (card scope, keyed by the stable `groupId`) and `AlertEventBody` opens it via an `onShowOccurrences` callback, so an enrich sweep no longer closes it. A fully-deleted series shows a "removed from VRChat" notice. **Instance history reuse**: `InstanceListDialog` gained a `liveRefresh` param — the invite/join pickers keep the 15s occupancy re-fetch loop, but Instance History passes `liveRefresh=false` for a ONE-TIME grab (no continuous re-fetch), cutting its calls to a single pass.
  - **Thin-payload enrichment + live refresh (`GroupAlertEnricher`, `com.vrca.vrchat`)**: notification-v2 pushes carry far less than the calendar object (no banner, often no timing) AND an alert used to freeze at fire-time data (interested count never moved, event edits never appeared). `GroupAlertEnricher.enrich(ctx, groupId)` is the shared ONE-GROUP REST sweep (calendar events + posts, 2 calls, debounced 60s per group inside the enricher) that **merges fresh values into the fired alerts in place** via `InAppAlertState.enrichEvents` (events matched by `cal_` id, announcements by normalized-body == the content-fingerprint normalization): banner, start/end, created, interested count, category/platforms/access/**languages**, `following`, the event's REAL title (**strips VRChat's "New event by X:" v2 boilerplate via `cleanEventTitle`**, also applied at v2 fire time), display timestamp → `startsAt`, body → clean description. **Every post-creation-editable event field (title, start/end, category, description, visibility, thumbnail, platform, languages) refreshes through this merge** — fresh non-blank values overwrite; languages is the one field an edit can EMPTY, so it's keyed on `ev.has("languages")` (present-but-empty CLEARS the stale value instead of keeping it). **In-game follow detection + representative collapse**: the group calendar LIST omits the user's per-event follow state, so an event added to the calendar IN-GAME wasn't detected. The enricher collapses the fetched events to ONE representative per series (`collapseEventsByTitle`, nearest-upcoming — bounds the per-event work), then does a **single-event fetch** (`VrchatAuthManager.fetchCalendarEvent` = `GET /calendar/{groupId}/{eventId}`) which carries the follow state + richer organizer/recurrence, and applies `following` from it **UNLESS the user set a persistent in-app override** (`InAppAlertState.followOverride(followSeriesKey(...))`, checked first — a user's own Add/Remove always wins over the server, which reports the creator's own events as perpetually followed). A recurring representative's enrich MATCHES ALL stored occurrences of its series (by normalized title) so following/interested propagate to every one. **CONFIRMED VRChat calendar-event field names (from an on-device raw-JSON capture via the Settings→Debug diagnostics, `VrcaEventDiag`)**: follow state is the NESTED `userInterest.isFollowing` boolean (`extractEventFollowing` reads it — the old "`userInterest` present ⇒ following" test reported EVERY event as signed-up, since `userInterest` is always a non-null object); recurrence is `seriesId` (non-null) + `occurrenceKind=="occurrence"` (a one-off is `"single"`); interested count is `interestedUserCount`; banner is `imageUrl`. **Events have NO individual creator** — `ownerId` is the owning GROUP (`grp_`), not a person, so events show no "Hosted by" line (the group is already the access chip). **Posts/announcements DO carry `authorId` (a `usr_`)** — the announcement enrich path resolves it via `fetchUserDisplayName` (cached) and shows **"Posted by <name>"** (label is "Hosted by" for events / "Posted by" for announcements, keyed on `isRichEvent`). **`collapseEventsByTitle` keys on `seriesId ?: normalizedTitle`** (every occurrence of a repeat shares both), so a flooded recurring series collapses to ONE representative → ONE single-event fetch (fixes "repeat events take ages to load their info because the app grabs them all"). **It must NOT be keyed on `occurrenceKind`**: the calendar LIST endpoint reports that field unreliably (the confirmed value is only on the single-event fetch), and a brief attempt to give `occurrenceKind=="single"` list items their OWN bucket REGRESSED the collapse — every occurrence became unique, so the whole series flooded the fire path again AND the enricher went back to ~50 single-event fetches. The one-off-falsely-flagged-recurring concern lives in `extractRecurring` (the chip), not the collapse. Three callers: (1) `VrchatPipelineService.scheduleTargetedGroupSweep` (retries at 2.5s/8.5s/23s with `minIntervalMs=0` so a genuine new event enriches even before VRChat has indexed it, and a second event right after a first isn't debounced away); (2) **`VrchatStatusPage` runs a refresh loop while the tab is composed** (first pass 5s after entry, then every 60s, capped at 6 distinct `groupRefId`s from visible `event_grp_`/`announcement_grp_` alert groups) so interested counts and event edits UPDATE while the notification is on screen; (3) **while an event/announcement card stays EXPANDED it re-enriches every 10s** (`LaunchedEffect(expanded)` in `AlertGroupCard` LOOPS with `delay(10_000)` + `minIntervalMs = 9_000` — the ~9s per-group min lets each 10s tick through while the 55s tab loop mostly no-ops for that group; the effect is cancelled the instant it collapses, so the loop stops with it). This keeps the interested count / timing / banner / follow state / edits live on-screen (fixes "the interested counter is inconsistent with updating"). **Rate-limit guards, two layers**: the per-group debounce (atomic check+stamp via `compute`) caps each group to one sweep per window, AND a **global `sweepMutex` + 2.5s spacing** serializes sweeps ACROSS groups — spamming expand across cards from several different groups queues one 2-call sweep per 2.5s max instead of firing an unspaced burst (the 250ms intra-sweep pacing matches the backfill's). A multi-event card needs only ONE sweep: all its events share the group, and the single calendar response enriches each by its `cal_` id. **Repaint requirement: `AlertEventBody` is wrapped in `key(event.id, event)` — keyed on the FULL event data.** The card body sits inside `AnimatedVisibility`, which (proven on-device, same class as the cycle-mute saga) can skip repainting children whose params changed — keyed on id alone, enrichment updated the state but the expanded card never visually changed ("re-expanding doesn't update"). Do not reduce the key back to just the id. Enrichment NEVER fires a notification — display-state only. The rich fields ride `fireEventNotification(alertRich = AlertRichMeta(...))` from the REST calendar sweep (full meta at fire time, helpers now on `GroupAlertEnricher`) and the v2 paths. **Instant-detail live event fire (the "event notification shows a thin title-only card, then ~1 min later a SECOND full card appears in Signed up" fix)**: the live notification-v2 EVENT push used to fire a THIN card (no banner, no `following`) because a freshly-created event's push often carries no `cal_` id — so (a) the enricher couldn't match it and (b) the REST poll fired a separate full card that, being `following==true` for the creator, golden-pinned to Signed up while the thin one lingered. The v2 event branch now **fetches the full calendar object BEFORE firing** (`VrchatPipelineService.resolveFullCalendarEvent` → `fireDetailedCalendarEvent`) so its FIRST card is already detailed (banner/timing/interested/`following`, instantly pinning a self-created event to Signed up). `resolveFullCalendarEvent` prefers the single-event fetch (`fetchCalendarEvent` by `cal_` id — carries `userInterest.isFollowing`), else matches the group calendar by normalized title (nearest-upcoming occurrence for a recurring series) then upgrades to the single event, with one 1.2s retry for VRChat backend indexing lag; on total failure it falls back to the OLD thin fire + `scheduleTargetedGroupSweep` (the sweep then upgrades that thin card in place). This runs on `pipelineDispatcher` but its REST calls `withContext(Dispatchers.IO)`, so it yields the dispatcher thread (doesn't block other WS handlers) and touches no `friendsCache` across the suspension. **Cross-path DUPLICATE prevention is keyed on TITLE as well as the `cal_` id** (`InAppAlertState.hasEventFor(groupKey, eventId, normalizedTitle)`): both fire paths (the v2 push AND `fireGroupCalendarEvent`) skip + record the fingerprint when a card already exists for the event by id OR title, and the enricher ADOPTS a still-thin card by title (`eventRefId==null && title match`, transform sets its `eventRefId`) — so whichever path fires SECOND never spawns a duplicate even when only one of them knew the id. (Tradeoff: two genuinely-distinct undismissed events in the same group sharing an EXACT title collapse to one — rare, and it aligns with the recurring "one card per series" behavior.)
  - **`AlertImageStore` (`com.vrca.vrchat`, files at `filesDir/alert_images/<sha1(url)>.img`)**: dismissal-scoped persistent image cache fixing "thumbnails reload every time you open the menu". One file per unique image URL, downloaded ONCE (`ensureCached`, session cookie attached for vrchat hosts) when the notification fires / info first resolves; UI loads via `AlertImageStore.resolve(ctx, url) ?: url` (disk file first, Coil network fallback). **Lifecycle = references**: a file lives until NOTHING references its URL — alert dismissal (`InAppAlertState.dismiss`/`dismissAll`) and the instance-history 24h prune drop references, then `gc()` (also run at `load()`) deletes orphans. References tracked from: alert events' `imageUrl` (event/announcement banners; invite targets get theirs recorded via `setInviteTargetImage` when the picker first resolves info) + `InstanceHistoryStore.Entry.imageUrl` (recorded via `updateImage` at the same moment). `InstanceTarget.imageUrl` feeds the stored thumb into `InstanceRow` so history/invite rows render instantly while live info is in flight. Deliberately NO size cap (thumbnails are ~50-150 KB; even hundreds of undismissed alerts is single-digit MB).
  - **In-app alerts (grouped)**: Notifications create persistent in-app alert groups displayed as dismissible cards at the top of the VRChat tab. The notification section starts **collapsed** by default and is expandable via a "Notifications (N)" header. Groups are keyed by `userId + notificationType` (e.g., `bio_usr_xxx`, `name_usr_xxx`) or by notification category (e.g., `announcement_$groupId`, `event_$groupId`). When the same event type fires multiple times for the same user/group, events are appended to the existing group — never overwritten. `InAppAlertState` singleton manages `MutableStateFlow<List<InAppAlertGroup>>`. Each `InAppAlertGroup` contains a list of `InAppAlertEvent` objects (max 50 per group), with `firstSeenMs`, `lastUpdatedMs` timestamps, and optional `eventTitle` per event. The UI shows "Title (N)" when a group has multiple events. Expanding a group shows all events in chronological order, each with its own before/after diff (`wordDiff()` in `VrchatPage.kt`) and optional per-event title and relative timestamp. **The diff is LINE-level, not word-level.** Bios are usually multi-line lists; a word-level LCS matched common words ("the", "my", "get") across DIFFERENT lines and painted an inconsistent red/green/purple mess (the user reported this). Whole lines are far more unique, so the algorithm runs LCS over lines: each line is unchanged (neutral grey), removed (red), added (green), or **moved (purple `0xFFB388FF`)**. **Line move detection** matches a removed line's exact trimmed content against added lines — whole-line matching is unambiguous, so no blocklist is needed (the prior word-level move detection needed a blocklist to stop short words like "my"/"I" turning purple everywhere). **Single-line refinement**: each remaining real-removed line is paired with the most similar real-added line (token Jaccard > 0.3); paired lines get a word-level red/green diff (`lineWordDiff()`) so editing one word in a line highlights just that word instead of reddening the whole line. Single-line bios fall through this pairing and show word-level diffs too. Newlines are preserved so multi-line layout stays intact. Each rendered card is wrapped in `key(group.groupId)` so Compose tracks its expanded/collapsed state by group identity, not list position — without this, a new alert prepended to the list shifted every card's remembered state up one slot, so the card the user was reading collapsed and the new top card appeared expanded. Bio diff cards show bold **BEFORE** (red) and **AFTER** (green) headers above each half so users aren't confused about which side is the old/new bio. The collapse/expand chevron and the dismiss (X) button each sit in a tinted circular `Surface` (primary-tint for the chevron, error-tint for dismiss) so they stand out instead of blending into the card. Groups are capped at 20 (FIFO). Only 3 groups visible at once; "+N more" button reveals the rest. Dismissing removes the entire group, all its events, AND cancels the linked Android notification (using `groupId.hashCode()` as the stable notification ID). Backward-compatible: `addAlert()` wraps single events into single-event groups. Old flat alerts are auto-migrated to the group format on first load. `fireEventNotification` accepts `alertGroupKey` and `alertEventTitle` parameters — when provided, the event is added to the named group with its own title instead of creating a standalone alert. For grouped Android notifications, uses stable notification ID per `alertGroupKey.hashCode()` with `setOnlyAlertOnce(true)` so subsequent events update the notification silently (no re-alert). Bio change notifications no longer have a per-friend cooldown — the grouping model replaces cooldown-based spam prevention.
  - **Avatar change suppression**: Avatar change notifications are suppressed when the friend's location changed in the same `friend-update` payload OR when a `friend-location` event fired for the same friend within the last 15 seconds (`recentlyRelocated` map). This two-layer guard handles both same-payload location changes and the common race condition where `friend-location` updates the cache before `friend-update` arrives.
  - **Status change suppression during world hop**: `maybeFireStatusChange` is only called in `handleFriendUpdate` when the location did NOT change. VRChat sends status data redundantly with location changes, so firing both a "Friend moved" and "Friend changed presence" notification for the same event is noise.
  - **Warmup suppression**: For 30 seconds after pipeline connect (`WARMUP_MS`), all friend activity notifications (online, active, location, and handleFriendUpdate changes) are suppressed. This prevents the initial burst of WebSocket events from firing stale notifications for friends' existing state. Cache updates still happen during warmup so subsequent real changes are correctly diffed. Does NOT suppress friend-add, friend-delete, notification/notification-v2, or show-alert events.
  - **Gift notifications**: `gift`, `giftCredits`, and `giftBundle` notification types are handled in `handleNotificationEvent` and fire via `KEY_NOTIF_GIFT_RECEIVED`.
  - **Invite response notifications**: `requestInviteResponse` (someone accepted your invite request) and `inviteResponse` (someone accepted your invite) are handled and fire through the invite channel.
  - **Unknown event logging**: Unhandled pipeline event types are logged at DEBUG level for discovery. Known non-notification events (`user-location`, `see-notification`, `hide-notification`, `clear-notification`, `response-notification`, `content-refresh`) have explicit no-op branches.
  - **Reconnection notification fix**: `scheduleReconnect()` now calls `startPipeline()` (which handles session recovery and proper notification updates) when the user isn't logged in, instead of silently falling through with stale "Reconnecting in Xs..." text. Also updates to "Connecting..." before calling `connectWebSocket()`. If `connectWebSocket()` finds no cookie/auth token, it falls back to `startPipeline()` instead of silently returning.
  - **Reconnect dedup (`scheduleReconnect` `if (wsJob?.isActive == true) return`)**: a single socket failure fires BOTH `onFailure` AND `onClosed`, and the ~50s health check can also call `scheduleReconnect()` — each previously launched its OWN backoff coroutine, each opening a NEW WebSocket, so several overlapping pipeline sockets raced (every one of which can independently fail/close and spawn yet more), which itself reads as "the pipeline keeps disconnecting." The guard early-returns when a reconnect is already pending (mid-backoff) or in progress. `wsJob` completes quickly once `connectWebSocket()` hands the socket off (the socket lives independently), so a genuine later failure still schedules a fresh retry; the guard only suppresses the duplicate-trigger stack.
  - **Cookie write atomicity (`captureRolledCookies` `synchronized(cookieWriteLock)` + `commit()`)**: every authenticated REST response routes through `captureRolledCookies`, and the heavy `fetchPresence` chain + the friends sweep fire several CONCURRENTLY on `Dispatchers.IO`. Without a lock, two responses doing `edit()`/`apply()` at once race on the shared `auth`/`twoFactorAuth` store the pipeline WebSocket also reads as its connect token — a stale-IP cookie could win nondeterministically. The lock makes each read-decide-write atomic and ordered. Timestamp/roll-forward semantics are unchanged (every present cookie still refreshes its stored-at clock, which `shouldRefreshCookies` and the trusted-device window depend on) — only the concurrency was hardened.
  - **Persistent notification resilience**: A 10s loop re-posts the foreground notification if it gets swiped away. The notification channel uses `setSound(null, null)` and the builder uses `setSound(null)` + `setVibrate(null)` to ensure silence. The notification also refreshes automatically when Discord RPC status changes.
  - `friendsCache` is now `Map<String, FriendCacheEntry>` (was `Map<String, String>`) carrying display name, status, location, world name, avatar thumb, bio, trustRank — used to detect change events from `friend-update` and fire targeted notifications (renamed, changed status, changed avatar, updated bio, trust rank changed).
  - **App update notifications**: `startAppUpdateCheckLoop()` runs on service creation (public build only), checks `checkFirestoreRelease()` immediately and every 6 hours. Fires via `fireAppUpdateNotification()` with `KEY_NOTIF_APP_UPDATE` (defaults to enabled). Uses stable notification ID `NOTIF_ID_APP_UPDATE` so repeated checks update in place. Shows version name and release notes in expandable notification.
  - **Admin announcement notifications**: `attachAnnouncementsListener()` sets up a Firestore snapshot listener on `announcements` collection (active=true). First snapshot seeds `seenAnnouncementIds` without firing. Subsequent snapshots fire via `fireEventNotification` with `KEY_NOTIF_ANNOUNCEMENTS` for new doc IDs. Seen IDs persisted to SharedPreferences (`vrca_seen_announcements/ids`). **This service listener is the background-surviving one** (fires the NOTIFICATION when the app is off-screen / the user is on another tab). The public UI (`VrcaScreen.kt`) attaches a **FOREGROUND-scoped** `where active==true limit 60` snapshot listener (a `DisposableEffect` — attaches while the screen is composed, detaches `onDispose`) so the in-app announcements list updates LIVE while open and costs nothing while backgrounded (see "Live-while-open reads"). (History: this was briefly a one-shot `get()` on screen entry to cut a duplicate persistent listener, but the user wanted the in-app list to update live while open; the foreground listener's first callback is the same single read as that `get()`, then bills only when a push actually lands while you're looking — so it's the same baseline cost plus live-while-open, and still works when the user isn't logged into VRChat.)
  - **VRChat status page integration**: `startStatusPagePolling()` polls `https://status.vrchat.com/api/v2/summary.json` every 2 minutes. Parses Atlassian Statuspage format (indicator: none/minor/major/critical, components, incidents). Updates `VrchatPipelineState.statusPageState` for UI consumption. Fires ongoing notification on incidents via `KEY_NOTIF_VRCHAT_ALERT`, auto-dismisses after 20 minutes of all-clear. Tapping opens status.vrchat.com. `VrchatStatusBanner` composable shows on Home and VRChat tabs with color-coded warnings. Uses `ElevatedCard` with elevated shadow for the outer container. The banner is collapsible (header row with status dot, title, subtitle, and chevron toggle); its expand/collapse state lives in a shared observable `StatusBannerState.expanded` (`mutableStateOf`, process-scoped singleton) so collapsing it on one tab (e.g. Home) instantly collapses it on every other tab (e.g. VRChat) — previously each tab held its own local `remember` state and counted as a separate card. It defaults to expanded and resets to expanded on a cold process restart so a new outage re-surfaces. When collapsed, shows an "N affected" chip. Affected components are grouped under an "Affected systems" sub-heading inside a nested `Surface` card — each component row has a small status dot, the system name, and a colored status chip; rows are separated by thin dividers. Incidents are grouped under "Latest updates", each in its own nested `Surface` card with incident name and latest update text. A "View VRChat Status Page" button-style Surface link sits at the bottom.
- **VrchatPipelineState**: Shared in-memory singleton using `MutableStateFlow` for reactive cross-component state (connection status, presence data, VRChat status page). Compose UI observes via `collectAsState()`. `statusPageFlow` exposes `VrchatStatusPageData?` for the status banner.
- **DiscordRpcService**: Foreground service running a hidden WebView that loads `discord.com`. Discord's own JavaScript handles the gateway connection (intents, capabilities, X-Super-Properties, TLS, /science telemetry) — all traffic looks like a real Discord web session, making detection effectively impossible. After page load, a JS shim hooks `WebSocket.prototype.send` to capture the gateway WebSocket reference, then exposes `window.VRCA_setActivity(json)` / `window.VRCA_clearActivity()` which send OP 3 (Presence Update) directly on the captured gateway connection. The Flux dispatcher approach was abandoned after Discord fully obfuscated all internal module names. The native side pushes activity updates via `evaluateJavascript` whenever `VrchatPipelineState.presenceFlow` emits (1.5s debounce) or every 10s as keep-alive. World thumbnails are squash-proxied via `images.weserv.nl` (`fit=fill`); the default VRChat image (`DEFAULT_VRCHAT_IMAGE_URL`) skips squashing. Image URLs are resolved to `mp:external/...` format via Discord's `POST /api/v9/applications/{app_id}/external-assets` endpoint (called from within the WebView JS using the session token read from `localStorage.token` or captured from Discord's own fetch calls — the token never leaves the JS context and is never sent to native code). Resolved URLs are cached in `window._vrca_asset_cache`. The user token is never extracted or stored natively — `DiscordLoginWebView` just seeds the `CookieManager` session. Auto-starts when VRChat pipeline connects if `discord_rpc_enabled` and `discord_session_seeded` are both true.
  - **OP 3 interception**: The `WebSocket.prototype.send` hook intercepts all outgoing OP 3 messages from Discord's own code. When our activity is set (`window._vrca_activity`), it's injected into the `activities[]` array of every Discord-originated OP 3. This ensures our RPC persists through Discord's own status changes, idle detection, and gateway reconnects — Discord can't overwrite our activity because it's included in every outgoing presence update. The hook also captures the user's current status (`online`/`dnd`/`invisible`) from each OP 3 so our direct sends use the correct status rather than hardcoding `'online'`.
  - **Anti-idle**: On mobile, Discord auto-sends OP 3 with `status: 'idle'` when the user backgrounds the app. The hook tracks the user's "intended" status (last non-idle status they set). When Discord tries to send `status: 'idle'` but the intended status is `'online'`, the hook overrides it back to `'online'` with `since: 0`. This keeps the user's Discord status online while VRC-A's foreground service is running. DND and invisible are respected (never overridden).
  - **Safety system (7 features):**
    1. **Risk gating UI**: First-time enable shows a consent dialog with checkbox acknowledgment + 4-second delay before the Continue button activates. Consent persisted in `discord_risk_accepted` DataStore key — only shown once. Bullet list explicitly warns that disconnecting may cause Discord to invalidate sessions on other devices, logging the user out everywhere.
    2. **Session state monitoring + gateway-health recovery**: A 30s background loop (`startSessionMonitor`) reads `window.VRCA_gatewayHealth()` via `evaluateJavascript` instead of just `readyState === 1`. The old raw-readyState check showed a **false "connected"** when the gateway socket stayed OPEN but the session was dead/zombied (e.g. a Discord outage black-holing presence) — the user saw "Discord presence connected" while their RPC was missing from their profile, and had to manually sign out/in. `VRCA_gatewayHealth()` classifies: `no_gateway` (shim lost the ref), `connecting` (socket opening → wait), `dead` (readyState closing/closed), `bad` (Discord sent OP 7 Reconnect / OP 9 Invalid-Session, or the socket fired `close`), `zombie` (open but no heartbeat ACK for >2.5× the `heartbeat_interval`), `alive`. **Reload thresholds are now per-class (NOT a flat "reload after 2 checks for everything", which turned transient blips into frequent visible RPC drops)** — a reload (`onSessionExpired()`) drops the RPC for its reload+re-inject window, so it only fires when the signal is real: `dead` (readyState CLOSING/CLOSED — compression-independent, reliable) reloads after `DEAD_RELOAD_THRESHOLD = 2` (~60s, one RESUME-grace cycle); `zombie`/`bad` are derived from heartbeat-ACK / OP7-9 tracking that needs READABLE inbound frames, which Discord's `zlib-stream` compression makes unparseable → they can FALSE-positive, so they require `DEGRADED_RELOAD_THRESHOLD = 4` (~2 min) before reloading — and a socket that's genuinely closing reports `dead` and takes the faster path anyway. Each non-matching state resets the others' counters (`deadHealthChecks`/`degradedHealthChecks`/`noGatewayChecks`). `onSessionExpired()` **reloads the WebView**, which re-establishes a fresh gateway and **auto-re-authenticates from the persistent `localStorage` token + `CookieManager` cookie (no password)** — doing automatically what the manual sign-out/in did. **`no_gateway` also escalates to a reload**: the first misses re-inject the shim (cheap — recaptures the gateway if Discord's JS still has a live one); after `NO_GATEWAY_RELOAD_THRESHOLD = 3` consecutive still-`no_gateway` checks, or if shim injection EXHAUSTS its retries still reporting `no_gateway` (the `injectShim` give-up branch — `lastShimResult.contains("no_gateway")`), it escalates to `onSessionExpired()` instead of the terminal **FAILED** state. This fixes the user-reported dead end where the RPC silently dropped, the app showed "Discord setup failed: no_gateway(hook=true)", and re-injecting/restarting couldn't recover it — only a manual Discord sign-out/in (which reloads the page → Discord's JS opens a fresh gateway → the hook recaptures it via `onPageFinished`) brought it back. Re-running the module finder on the SAME page can never recapture a gateway Discord's own JS hasn't reopened, so a page reload is the only real fix; `onSessionExpired()`'s `MAX_SESSION_RECOVERIES` cap still bounds it (after the cap → "sign in again"). The shim (`WS_HOOK_JS`) tracks health via `_vrca_attachGatewayListeners(ws)` — a passive `addEventListener('message'/'open'/'close')` on each captured gateway socket (it only OBSERVES + re-pushes OUR activity; it never alters Discord's own frames, so the "real web session" anti-detection property holds). Heartbeat-ACK/OP-7/9/READY tracking needs **readable (uncompressed JSON) inbound** — when Discord uses `zlib-stream` compression those frames are binary and skipped, so `zombie`/`bad` degrade gracefully to `alive`; the **compression-independent** recoveries still work: a new gateway socket's `open` event re-pushes our activity at +3s/+8s (so a Discord-initiated reconnect re-lands the RPC even when frames can't be parsed), and `dead` (readyState) still forces a reload. **Known limit**: if the socket stays fully healthy (heartbeats ACKing) but Discord's presence BACKEND is down, there is no client-side signal — undetectable, resolves when Discord recovers. WebViewClient still detects login-page redirects (both `onPageFinished` and `shouldOverrideUrlLoading`) for true session expiry.
    2b. **Offline parking — no false "connected" while there's no network (`parkOffline` + validated-network callback)**: with **genuinely no network** (e.g. Wi-Fi off and no mobile data), the WebView still serves `discord.com` from its **HTTP cache**, so a page-load + shim-inject `MODULE_FINDER` returns `"ok"` and the old code declared the RPC **CONNECTED** even though the gateway WebSocket can never open — then a 10s presence push / 30s health check found the dead gateway, reloaded, the cached page loaded again, and the status **flickered CONNECTED → RECONNECTING every ~10s** (the user-reported "it shows reconnected to the RPC even though internet is off, then back to reconnecting, repeat"). **Fix: never declare CONNECTED or churn reloads while offline — sit parked in "Waiting for network" until validated connectivity returns.** `parkOffline()` (called from the session-monitor top-of-loop offline check, `loadWebView`'s offline guard, the `injectShim` success path's `!hasNetwork()` guard, `onSessionExpired`'s offline branch, and `onReceivedError`) sets `awaitingNetwork=true` + `shimReady=false`, stops presence pushes, resets all health/freeze counters, and shows "Waiting for network" — `shimReady=false` makes every health probe / `pushActivity` a no-op so nothing can re-declare CONNECTED. **`hasNetwork()` now keys on `NET_CAPABILITY_VALIDATED`** (Android actually probed real internet reachability), NOT `NET_CAPABILITY_INTERNET` (which only means the interface *claims* internet — true for a connecting/captive network). **The network callback uses `onCapabilitiesChanged` gated on `NET_CAPABILITY_VALIDATED`** (recover only once internet is confirmed) instead of `onAvailable` (fires the instant any interface registers, before validation → would reload into the cached page and false-connect), plus `onLost` to park immediately. So a real reconnect (validated) drives one clean `loadWebView()` recovery; a flapping/connecting network never triggers the doomed cached-page reload.
    3. **Failure detection + response**: Non-silent — failures update `DiscordRpcState.status` (shared `MutableStateFlow`) and the notification. Session expiry shows a red "Sign in again" button in the UI. Module finder failure shows "needs update" status. UI status messages are user-friendly ("Setting up Discord connection...", "Reconnecting to Discord...", "Discord connection setup failed") rather than technical injection terminology.
    4. **Auto recovery**: On session expiry, up to 3 auto-recovery attempts with linear backoff (2s, 4s, 6s) reload the WebView page. On dispatcher loss, shim re-injection with exponential backoff (3s, 6s, 9s, 12s, 15s). On consecutive push failures (5), shim re-injection. All counters reset on successful reconnection.
    5. **Per-user failure isolation**: All state is service-scoped — failures only pause the Discord RPC service, never affect VRChat pipeline, Firestore sync, or any other system.
    6. **User feedback/transparency**: `DiscordRpcState` singleton exposes `statusFlow` (enum: IDLE/CONNECTING/CONNECTED/RECONNECTING/SESSION_EXPIRED/FAILED) and `failureMessageFlow`. UI shows a colored status dot + label (green=Connected, yellow=Connecting/Reconnecting, red=Expired/Failed) with detail message.
    7. **Safety guards**: Max 5 shim retries with backoff, max 3 session recovery attempts, max 5 consecutive push failures before re-inject, 1.5s minimum interval between JS evaluations (double-guarded: both delay and timestamp check), all counters capped.
  - **RPC counter persistence**: `onlineStartEpochMs` (the Discord activity `timestamps.start`, always milliseconds) is persisted to SharedPreferences (`rpcPrefs / online_start_epoch`). **The grace-window restore is keyed on a `last_online_seen` heartbeat** (refreshed in `buildActivityJson()` on every confirmed-online tick), NOT on `offline_at`: when the user returns online and `onlineStartEpochMs` is 0, the saved start epoch is restored only if `now - last_online_seen < ONLINE_GRACE_MS`. **`ONLINE_GRACE_MS` is 20 minutes** (applies to BOTH the in-VRChat and not-in-VRChat counters via `resolveElapsedStart`). It is deliberately >15 min so it **spans the `PipelineWatchdogWorker`'s ~15-min recovery gap**: after an OEM process kill the watchdog re-arms the services on its next ~15-min cycle, and the counter must still be inside the grace window at that moment for the timer to resume instead of resetting. Because `last_online_seen` freezes the moment the app stops tracking (process death, swipe), a longer untracked gap correctly resets the counter to a fresh start instead of bridging into a bogus multi-hour timer. **Critically, `onStartCommand` sets `onlineStartEpochMs = 0L` (it does NOT preload the persisted epoch)** so `buildActivityJson()`'s grace check always runs — preloading the stale epoch directly was the bug that shipped a day-old start to Discord as a "25 hours online" timer after a normal cold start (the grace block only runs when the in-memory value is 0). After 20 minutes untracked, a fresh counter starts. **Not-in-VRChat counter flicker guard (`OFFLINE_CLEAR_CONFIRM_MS` = 3 min)**: the two counters were asymmetric — the in-VRChat counter has grace carry AND its prefs are never cleared (so it's stable during active play), but the not-in-VRChat counter's persisted start (`offline_start_epoch`/`offline_last_seen`) was WIPED on the FIRST tick where `isOnline` read true, so a single spurious `isOnline=true` (a stale REST presence read / racy WS `user-update`/`user-location` event on a flaky connection) permanently reset it and the next offline tick restarted from 0 — the "not-in-VRChat RPC timer randomly restarts on connection blips" bug. Fix: the offline prefs are cleared only once the online session is CONFIRMED sustained (`nowMs - onlineStartEpochMs >= OFFLINE_CLEAR_CONFIRM_MS`). A transient flicker resolves a FRESH `online_start_epoch ≈ now` (the prior session went stale > grace ago), so the clear is skipped and the offline counter carries on across the blip within its own grace window; a genuine sustained visit still clears it so leaving VRChat restarts the offline clock as intended.
- **DiscordRpcState**: Shared in-memory singleton using `MutableStateFlow` for Discord RPC status reporting. Compose UI in the settings page observes `statusFlow` and `failureMessageFlow` via `collectAsState()` to show real-time connection status.
- **DiscordLoginWebView**: WebView-based Discord login flow. User logs into discord.com; login completion uses a 3-layer verification: (1) URL must contain `discord.com/channels` OR `discord.com/app` (Discord may redirect to either after login), (2) 2-second dwell timer ensures the URL is stable (cancelled if page navigates away), (3) JS authentication probe checks `#app-mount` has rendered children AND no login form (`input[type="password"]`, `input[type="email"]`) is visible — uses stable DOM structure rather than webpack chunk variable names which Discord may rename. The probe retries up to 4 times at 1.5s intervals when it returns false, since Discord's SPA can take a few seconds to hydrate after the auto-redirect from `/login` for users with valid cached cookies. All retry runnables are tracked and cancelled in `onPageStarted` so a navigation away cannot trigger a stale `onLoginComplete`. Only after all 3 layers pass does `onLoginComplete` fire and `discord_session_seeded` is set.
- **Discord AI bot (admin build only, `com.vrca.discordbot`) — v1 shipped**: a self-hosted Discord bot that answers with Cloudflare **Workers AI**, running natively on the admin phone (NOT a WebView, NOT discord.js — the raw gateway protocol over an OkHttp WebSocket, mirroring `VrchatPipelineService`'s foreground + reconnect skeleton). Distinct from `DiscordRpcService` (that's the user's own RPC via a WebView-hosted gateway); this is a real BOT with its own bot token. **`DiscordBotStore`** (EncryptedSharedPreferences `vrca_discord_bot`, same MasterKey/AES-256-GCM scheme as `BotVrchatSession`) holds the secrets ENTERED AT RUNTIME in the admin tab — bot token + Cloudflare account id + Workers AI token + optional AI Gateway id + model + system prompt + ambient% + cooldown + an `enabled` flag; nothing baked in the APK, nothing synced to Firestore, rotatable without a rebuild. **`DiscordBotService`** (foreground, `foregroundServiceType=dataSync`, `stopWithTask=false`, its OWN notif channel `vrca_discord_bot` + NOTIF_ID **1010** so it doesn't collide with the shared 1001) speaks the gateway: HELLO(op10)→heartbeat loop (op1, zombie-detects a missing ACK)→IDENTIFY(op2, intents = GUILDS|GUILD_MESSAGES|DIRECT_MESSAGES|MESSAGE_CONTENT = 37377), captures READY (bot id/name, `session_id`, `resume_gateway_url`), RESUMEs(op6) on reconnect via the resume url, handles op7 Reconnect / op9 Invalid-Session (resumable flag) / op11 ACK, and reconnects with exponential backoff (dedup guard like the pipeline's `reconnectJob?.isActive`). **Requires the MESSAGE CONTENT privileged intent** (toggled in the Discord dev portal) for ambient replies. **Trigger gate** on MESSAGE_CREATE (ignores bots/self): an @mention OR a reply to the bot ALWAYS answers; otherwise **ambient** "jump into the convo" fires only when there's readable text AND a `Random(100) < ambientPercent` roll passes AND the channel is off its per-channel cooldown (`ambientCooldownSec`) — the operator-tunable knobs. On a respond it fires the typing indicator, calls `DiscordBotAi.reply` (Workers AI). **Short-term memory (v1.1)**: on each reply it reads the last `historyLimit` channel messages via `DiscordRest.fetchRecentMessages` (`GET /channels/{id}/messages`, chronological, the bot's own past messages mapped to role=assistant) and feeds them as chat history — free, stateless, per-channel, survives OS kills (Discord is the store); `historyLimit` (Bot tab, default 8, 0=off, capped 25). No per-user profiles yet. **v1.2 (human flow)**: replies now read like a person, not an assistant — a hardcoded `BEHAVIOR` layer (`DiscordBotAi`) sits on TOP of the admin persona enforcing FORM only (1-2 short lines, in-character, never reveal it's an AI, never lecture/moralise/'break character', deflect-don't-explain); **debounce** (`DEBOUNCE_MS`=1.5s, per-channel `pendingReplyByChannel`) coalesces a burst into ONE reply to the latest message; **reply-chain** context (the `referenced_message` is threaded into history even when older than the fetch window); a cheap **8B ambient triage** (`shouldChimeIn`, `@cf/meta/llama-3.1-8b-instruct`, defaults to stay-quiet on error) gates unaddressed replies before spending the 70B; **trivial addressed messages** ('lol'/emoji, `isTrivial`) get an emoji **reaction** (`DiscordRest.addReaction`) instead of a generated reply; **inline @mentions** in message text are resolved to names (`DiscordRest.resolveMentions`, from each message's `mentions` array, zero extra calls); a length-scaled **typing beat** before send; and `max_tokens` trimmed 512→256, and posts via `DiscordRest` (reply-threaded for addressed messages, plain for ambient; `allowed_mentions.parse=[]`+`replied_user=false` so the model's output can never @-ping). **`DiscordBotAi`** routes through **AI Gateway** (`gateway.ai.cloudflare.com/v1/{acct}/{gw}/workers-ai/{model}`) when a gateway id is set (adds observability/retries/fallback — NOT response caching, which is ~useless for unique chat turns), else the direct Workers-AI endpoint (`api.cloudflare.com/client/v4/accounts/{acct}/ai/run/{model}`); the `@cf/...` model id goes in the path literally; default model `@cf/meta/llama-3.3-70b-instruct-fp8-fast`. **`DiscordBotState`** is a process-lifetime status singleton (IDLE/CONNECTING/CONNECTED/RECONNECTING/FAILED + a rolling activity log) the admin **Discord Bot tab** (`DiscordBotTab.kt`, tab index 8) observes — the tab has the config fields (token/CF fields as password), Start/Stop, a live status pill + log, and a setup checklist. **Lifecycle**: gated `BuildConfig.IS_ADMIN_BUILD` (the service refuses to start otherwise); Start persists `enabled=true`+`startService`, Stop persists `enabled=false`+stops; `AdminScreen` auto-starts it on app open when `enabled`; `START_STICKY` + the shared swipe/OEM-kill guards (`AppShutdown.isSwipedAway`/`isManualKillFresh`, and it's added to `AppShutdown.stopOtherServices`) mean an OS kill revives it but a deliberate swipe stays dead. **Deferred (v2+)**: durable per-user profiles / long-term memory (server-side Worker + KV; the current memory is short-term channel-scoped only), a smarter ambient relevance gate, boot-receiver restart.
- **IpField**: Multi-slot IP field component with 3 named slots (Home/Hotspot/Other). Uses local state tracking (not async DataStore) for immediate slot switching without cross-contamination. Supports per-slot editing and auto-migration from legacy single IP key. The Apply button and the slot-1 migration LaunchedEffect call `repo.saveIpSlotAddress(slot, address)` rather than `saveIpSlot(slot, name, address)` — only the auto-save debounce in the expanded slot editor is allowed to write the slot name. This prevents the placeholder defaults from `collectAsState(initial = "Home"|"Hotspot"|"Other")` from overwriting the user's saved name when DataStore hasn't yet emitted its real value. **The quick-edit `editBuffer` reflects ONLY the selected slot's saved address — it must NOT fall back to `uiState.ipAddress`** (the last-applied RUNTIME ip, which belongs to whatever slot was last equipped). On a tab re-entry `currentSlot` briefly defaults to 1 while the slot's DataStore value is still the placeholder `""`; the old fallback would then fill the field with a *different* slot's ip, and an Apply wrote that into the wrong slot (the "slot 2 overrides slot 1" bug). Worst case now the field is briefly blank (Apply is blocked on blank) and self-corrects once DataStore emits. **The "slot contents clone to slot 1 on tab re-entry" bug** (slot 2's IP overwriting a *populated* slot 1 after editing slot 1 → switching to slot 2 → leaving the tab → returning → switching back to slot 1) was caused by the **expanded-editor auto-save debounce** (`LaunchedEffect(n1,a1)` etc.). That debounce runs unconditionally on every fresh composition (tabs render via `when(p)` in `VrcaScreen.kt`, so leaving/returning to Home DISPOSES and re-composes `IpField` — all `remember` resets). The per-slot buffers (`a1/a2/a3`) seed from `collectAsState`'s initial value then "follow" DataStore via the `LaunchedEffect(ipNAddress)` effects; a follow-vs-debounce race could persist a buffer that merely SYNCED from DataStore rather than one the user typed, overwriting a populated slot. **Fix: per-slot `dirty` flags** (`dirty1/dirty2/dirty3`) set true ONLY in the editor `TextField` `onValueChange` (real user typing); the follow effects deliberately never set them, and each debounce `return@LaunchedEffect`s unless its slot is dirty. So a buffer that only mirrored DataStore is never written back — cloning is structurally impossible regardless of composition/lifecycle races. (This is build-variant-independent: `IpField` is identical across public/admin and no Firestore sync path reads/writes the slot addresses; it presented as "public-only" purely from differing per-install slot state and from the public install running an older released APK that predated the fixes.) **Secondary hardening — the slot-1 legacy migration is a race-free ONE-SHOT** (`LaunchedEffect(Unit)` reading PERSISTED values via `.first()`, gated by the `ip_slot1_migrated` DataStore flag so it runs at most once and never re-fires on recomposition/tab re-entry). It only copies the legacy `IP` key into slot 1 when `activeIpSlot == 1 && ip1Address.isBlank()`; the old reactive `LaunchedEffect(ip1Address, uiState.ipAddress)` re-fired on every tab re-entry and, because `repo.ipAddress`/the legacy `IP` key tracks the ACTIVE slot, copied the equipped slot's IP into an EMPTY slot 1. The `slot == 1` guard guarantees a non-slot-1 address can never be written into slot 1.

### Firestore Schema (users/{deviceHash})
Key fields written by the app:
- `isOnlineInApp` (bool): Set to `true` by the app-open write, `false` by the swipe-away shutdown and `VrcaViewModel.onCleared` (GlobalScope fallback). **Swipe-away shutdown is centralised in `AppShutdown.onTaskSwiped(context)`** (`com.vrca.app.AppShutdown`), called from the `onTaskRemoved` of EVERY foreground service. This was a real bug: the kill used to live only in `VrchatPipelineService.onTaskRemoved`, but the pipeline isn't always running, and **`KeepAliveService` — which IS always running (started from `MainActivity.onCreate`) and holds a `PARTIAL_WAKE_LOCK` — had no `onTaskRemoved` at all**, so after a swipe the wakelock-holding service kept the whole process alive and nothing ever killed it. Now an `AtomicBoolean` in `AppShutdown` makes the heavy work run exactly once no matter which service fires first: it stamps the `manual_kill_at` flag, stops the other services (`VrchatPipelineService`/Discord/KeepAlive/Overlay, releasing wakelocks AND removing the "Connected as X" persistent notification promptly — without stopping the pipeline service here its notification lingered for up to 5s, the offline-write await, before `killProcess`, which looked like the swiped app was still "running / connected as username"), then on a background thread **awaits the offline Firestore write (5s timeout) and hard-kills the process** (`Process.killProcess` + `exitProcess`). `KeepAliveService.onTaskRemoved` is the reliable entry point since it's always running. The await ensures the going-offline write has a chance to reach Firestore before the kill; if it times out, the admin-side staleness sweep still flips the user offline once `lastActiveAt` ages past the ~65-min window. (Previously it waited a fixed 10s before killing, leaving the app running in the background that whole time.) **Sticky-restart guard**: `VrchatPipelineService`, `DiscordRpcService`, and `KeepAliveService` all return `START_STICKY` (so a memory-pressure kill is recovered), which means Android would normally relaunch them with a null intent right after the swipe-kill — making the app look like it was never killed. To prevent this, `AppShutdown` stamps `manual_kill_at = now` (SharedPreferences `vrca_remote`, committed synchronously) before killing; each service's `onStartCommand` treats a **null intent** as a sticky restart and, if `AppShutdown.isManualKillFresh()` (within `MANUAL_KILL_WINDOW_MS` = 15s), calls `stopForeground` + `stopSelf`, returns `START_NOT_STICKY`, AND kills its own (freshly-resurrected) process after a 300ms delay — because `stopSelf` alone leaves the recreated process sitting idle as a cached background process (Samsung's "running apps" list still showed it). The delay lets `START_NOT_STICKY` register before the kill so Android does NOT recreate the service yet again. A legitimate reopen always passes a non-null intent (with `EXTRA_DEVICE_HASH`) so it bypasses the guard, and the 15s window expires harmlessly. (Note: `NowPlayingListenerService` is a `NotificationListenerService` the system binds independently; it can be rebound by the OS later when notification activity occurs, but that no longer keeps the process perpetually alive after swipe.) **Persistent swipe flag (`swiped_away`)**: the 15s `manual_kill_at` window is NOT enough on its own — the `PipelineWatchdogWorker` (WorkManager, ~15 min) survives the process kill and fires in a fresh process LONG after the window expired, sees the user still logged in, and resurrects the pipeline + keep-alive. That was the "I swiped it away but it starts running again after a while" bug. So `onTaskSwiped` ALSO sets a persistent `AppShutdown.KEY_SWIPED_AWAY=true` boolean (committed synchronously) and best-effort cancels the watchdog (`PipelineWatchdogWorker.cancel`). The watchdog `doWork()` and every service's null-intent sticky-restart guard now back off when `isManualKillFresh() || isSwipedAway()`. The flag is cleared ONLY by a real user reopen (`MainActivity.onCreate` → `AppShutdown.clearSwipedAway()`) or reboot (`BootReceiver` clears it then restarts services). An OS/OEM kill never runs `onTaskSwiped`, so the flag stays false there and the watchdog still recovers the process — **a deliberate swipe stays dead, an OEM kill comes back.** **Stale-notification hardening (the "Connected as X doesn't consistently disappear on swipe" fix)**: (1) the kill thread enforces a **minimum 1.2s shutdown floor** (`MIN_SHUTDOWN_DELAY_MS`) before `killProcess` — `stopOtherServices()` only QUEUES the stop requests from `onTaskRemoved` (main thread); the actual `onDestroy`/`ACTION_STOP` handling runs on the main looper after it returns, so a fast (or null-task) offline write let `killProcess` race the stops and all four still-foreground services died abruptly — exactly where OEMs (Samsung) leave the shared notification stale in the shade; (2) right before `killProcess` the thread calls **`AppShutdown.cancelPersistentNotification()`** (`nm.cancel(SHARED_NOTIF_ID = 1001)`, processed in system_server so it survives our death — ignored while a live FGS still owns the id, a cleanup sweep once they've stopped); (3) each sticky-restart guard's delayed kill thread (KeepAlive/Pipeline/Discord) also calls it, because `KeepAliveService.onCreate` re-posts the notification BEFORE the `onStartCommand` guard runs and the `stopForeground(STOP_FOREGROUND_REMOVE)` removal can race the 300ms-delayed kill.
- `lastActiveAt` (timestamp): **Canonical liveness field (hourly model).** Written on cold-open and then once per hour by `VrcaViewModel.startHourlyHeartbeat()` (in-process coroutine — a swiped/OS-killed app stops firing it, so it goes stale and is correctly counted offline; an AlarmManager wakeup is deliberately NOT used because it would resurrect a dead app and make it report online forever). Also refreshed every 10s while an admin is watching this user (real-time ~25s liveness for the watched user). Admin online = `lastActiveAt` within ~65 min (see "Admin online detection" — the admin ignores the `isOnlineInApp` flag entirely and keys on this timestamp's freshness, minus a newer `offlineAt` marker).
- `offlineAt` (timestamp): **Clean-shutdown marker.** Written (alongside `isOnlineInApp=false`) ONLY on a deliberate swipe-away by `AppShutdown.buildOfflineWriteTask` and `VrcaViewModel.onCleared`. The admin's `isUserOnline` forces a row offline the instant `offlineAt` is newer than `(lastActiveAt ?? lastSeenAt)`, giving instant swipe→offline; a live heartbeat advances `lastActiveAt` past it so it can never false-flag a running app. If the swipe write never lands, the ~65-min staleness window still flips the user offline. **Both offline writes deliberately do NOT bump `lastSeenAt`** — refreshing the liveness mirror on shutdown would let `offlineAt > (lastActiveAt ?? lastSeenAt)` TIE (both the same serverTimestamp) for any user whose `lastActiveAt` is somehow absent, keeping a swiped app falsely "online" until the 65-min decay; `offlineAt` is the sole shutdown marker. In `selfMutableKeys()`.
- `lastSeenAt` (timestamp): **Legacy mirror of `lastActiveAt`** — written with the same server timestamp on every liveness write so older admin builds that read `lastSeenAt` keep working (additive migration; nothing removed). New admin code should read `lastActiveAt`.
- `afkEnabled`, `cycleEnabled`, `spotifyEnabled`, `timeEnabled`: Feature toggles (admin-editable)
- `warned`, `banned`, `warnReason`, `banReason`: Moderation flags (read by public app via snapshot listener)
- `targetedUpdateUrl` / `targetedUpdateNotes` (strings): **Legacy** — kept for backwards compat with old app versions. New directed releases use `releases/{deviceHash}` instead (see below).
- **Directed releases via `releases/{deviceHash}`**: Admin writes a per-device release doc to `releases/{deviceHash}` using the same schema as the global `releases/latest` doc (versionCode, versionName, downloadUrl, requiredMinCode, notes, publishedAt, publishedByDevice). The client's `checkFirestoreRelease()` checks `releases/{deviceHash}` first, then falls back to `releases/latest`. **Real-time detection**: `VrcaApp` attaches a Firestore snapshot listener on `releases/{deviceHash}` that immediately shows the update dialog when the admin pushes a targeted release — no polling delay. **The whole update-check block (VM creation + release check + listeners + `UpdateDialog`) runs BEFORE the onboarding gate in `VrcaApp`**, so a forced update overlays the first-run tutorial too — a brand-new user installs the latest version before setting anything up (the app-scoped VM is therefore created during onboarding; its init is login-independent and this is safe). The download uses Android's `DownloadManager` for in-app APK download, then triggers the system package installer. **Browser fallback (`VrcaApp` `onDownload`)**: `DownloadManager` is disabled/unavailable on some OEM builds (reported on Samsung Galaxy — `getSystemService` returns null or `enqueue()` throws), which made the update popup silently do nothing. The `onDownload` handler now null-checks the service and wraps `enqueue()` in try/catch; on ANY failure it falls back to `startActivity(ACTION_VIEW, downloadUrl)` to open the APK link in the browser (surfacing "opened the update in your browser instead" via `downloadError`), and if even that fails it shows the raw URL for manual download. Old app versions that only know about `releases/latest` can still be reached via the global "Release to All" flow; once they update to a version with per-device support, directed releases work forever. **ALL releases are FORCED automatically (both global and directed)** — `requiredMinCode` is no longer consulted; any newer `versionCode` makes the client treat the update as required (`InAppUpdate.parseReleaseSnap` + the `VrcaApp` per-device listener + the legacy `targetedUpdateUrl` path all hardcode `forced = true`). The `UpdateDialog` is then a non-dismissable wall (`onDismissRequest` no-ops, no "Later" button). **Forced updates also hard-block OSC output**: `VrcaApp` calls `VrcaViewModel.applyForceUpdateGate(true)` when a forced update is pending, which sets `VrcaOsc.blocked = true` on both the remote and local OSC senders. `VrcaOsc.sendOscMessage` early-returns when `blocked`, so EVERY chatbox transmission (typing/input/realtime, from any sender loop) is dropped at that single chokepoint — the user can't keep driving the VRChat chatbox by backgrounding the app. The gate lives on the **app-scoped VM**, so it persists after Activity destruction; it's cleared only if the pending update resolves. **Caveat (cannot be backfilled)**: force-update logic must already be present in the installed APK. Versions predating `InAppUpdate.kt`/this gate never read `releases/*` or the forced flag, so no Firestore field can compel them — the only lever they honor is the moderation ban screen (repurposable as an "update required" wall via `banReason`, but that's a manual hack, not wired up). Force-update is forward-only by nature. **Per-variant global releases + package guard (Option B — headset/mobile fleets are independent)**: the three flavors have DIFFERENT applicationIds (`com.scrapw.chatbox` / `.admin` / `com.gremlin.inc.headset`), so a single global `releases/latest` with one APK could soft-brick the other variant (a mobile APK force-installed on a headset can't update it → stuck on the forced wall). Two coupled fixes: (1) **variant-specific global doc** — `globalReleaseDocId()` returns `latest_headset` on the headset build, `latest` otherwise, so `checkFirestoreRelease` + the `VrcaApp` global `DisposableEffect` listener read the headset fleet's OWN doc; a headset "release to all" can never reach mobile and the existing mobile `releases/latest` is untouched (keeps updating old mobile users to the current public version). (2) **package guard** — release docs carry a `packageName` field; `releaseTargetsOtherVariant(pkg)` = `pkg.isNotBlank() && pkg != BuildConfig.APPLICATION_ID`; `parseReleaseSnap` + both `VrcaApp` listeners (global AND targeted) IGNORE a release whose `packageName` targets a different variant (blank = legacy → applies, fully backward compatible). Admin side (`ReleasesTab`): the publish ROUTES by the picked APK's own package (`parseApkPackage`) — a `com.gremlin.inc.headset` APK auto-publishes to `releases/latest_headset` (stamped `packageName`), anything else to `releases/latest`; the live-release panel + Retract follow the same routed doc (`currentGlobalDoc`), and a UI line spells out the target fleet. Targeted releases (`UsersTab`, `AdminRuntime.PickedApk.packageName`) also stamp `packageName` so a mistargeted APK is ignored, not soft-bricking. `firestore.rules`: `packageName` added to the `releases` `hasOnly` allowlist; `latest_headset` excluded from client self-delete like `latest`. **Rollout**: the ADMIN build gains publishing immediately; existing HEADSET clients must be updated ONCE (via a targeted release — safe, per-device, never touches mobile) to a build that reads `latest_headset` + honors the guard, after which headset "release to all" works globally; the MOBILE build needs NO update and is never endangered (nothing is written to `releases/latest` unless a mobile APK is deliberately published).
- `watcherActiveAt` (timestamp): **Admin-only write.** Refreshed every ~30s while an admin has this user's detail page open. User app reads it from snapshots and feeds into `AdminWatchState.updateFromTimestampMs` — if within 60s, live-mode loops start.
- `killSignal` (timestamp): **Admin-only write.** Written by admin kill switch button. User app reads it from moderation snapshot listener — if fresh (within 60s), kills the app process.
- VRChat presence fields (`vrchatUserId`, `vrchatDisplayName`, `vrchatState`, `vrchatLocation`, etc.): Written every 10s while watched (real-time), AND refreshed by the **hourly delta** (`captureStateForSync` reads `VrchatPipelineState.presence`, null-safe — keys omitted entirely when not logged into VRChat so stale doc values are never clobbered with blanks). So an unwatched user's directory row shows location at hourly freshness. **Instance player count freshness (`refreshInstanceCountBeforeSync`)**: `vrchatInstancePlayerCount`/`vrchatInstanceCapacity` ride the hourly delta but were going STALE for unwatched users (frozen at the last successful heavy `fetchPresence` chain — often `0/N` when the location was patched from a `user-location` WebSocket event, which carries no count, and no later `fetchPresence`/`fetchInstanceCount` succeeded). `startHourlyHeartbeat()` now calls `refreshInstanceCountBeforeSync()` immediately before each `performSelfSync()` — a single lightweight `GET /instances/{loc}` (`VrchatAuthManager.fetchInstanceCount`, the same call the watched fallback + website tab-refresh use) that patches the live count into `VrchatPipelineState.presence` so the hourly write carries an accurate number. **Zero extra Firestore cost**: the hourly write already fires unconditionally (liveness) and already carried these two fields — the count was just stale; this only adds ONE VRChat REST call per hour per online user (not a Firestore read/write).
- NowPlaying / preview fields (`nowPlayingTitle`, `nowPlayingArtist`, `combinedPreviewText`, `activePackage`): **Volatile** — these change constantly during music playback. Real-time (10s) only while watched via the watcher-gated live-sync loop; never written on raw NowPlaying state emissions (that caused a write storm with no admin present). **They also ride the hourly delta + any debounced edit write** — `captureStateForSync()` includes them, so they're swept up whenever a write fires anyway (Firestore bills per doc write, not per field — zero extra writes; the 30s edit debounce is still only SCHEDULED by genuine content edits, never by NowPlaying ticks). History: these fields were in `buildUserSnapshot` (with a comment claiming "included in every self-sync write") but were silently dropped when the delta-write model introduced `captureStateForSync()` without them — `buildUserSnapshot` only fires on first install / uid change, so unwatched users' previews froze at their last watch session ("hourly write doesn't update preview/presence" bug). `VOLATILE_SYNC_KEYS` excludes them from `captureContentForOfflineWrite` (the swipe write deliberately omits volatile preview; the service's offline write already carries final presence).
- `lastReportedTime`, `cycleTrimWarning`, `lastTimeUpdateAt`: Only written while watched (live-mode loop).

**Friends are no longer in Firestore** — `savedFriendIds`/`savedFriendNames` are deleted via `FieldValue.delete()` on offline write. They live in local SharedPreferences only.

**Volatile subdoc `users/{deviceHash}/live/state`** (Phase 2 infrastructure — rules deployed, write migration deferred): Firestore rules allow the subcollection `users/{deviceHash}/live/{doc}`. It was intended to hold high-frequency volatile data (nowPlaying, VRChat presence, preview text) to keep them off the main doc. After the Phase 3 read-model change the admin detail view polls the **main doc** every 10s (no admin-side collection listener at all), so volatile fields stay on the main doc for now — splitting them out would require the admin detail poll to read two docs for no current benefit. The rules remain in place as future-proofing; revisit only if the user's own self-doc listener firing on watched-user volatile writes (echo-suppressed today) becomes a measured cost.

### Ban-evasion capture (tri-directional)
`VrchatBanChecker` (`com.vrca.vrchat`) implements deferred ban-evasion detection across three identifier types. Identity index: `bannedIdentifiers/{identifier}` → `{ banId, type, active }`. Full record: `bannedRecords/{banId}` → `{ deviceHashes[], vrchatIds[], authUids[], displayNames[], evasionAttempts[], ... }`. **Phase 1** (on bootstrap, deviceHash + authUid known): checks `bannedIdentifiers/{deviceHash}` and `/{authUid}`; if active, defers the ban screen and stores `pendingBanId` so the new alt VRChat ID can be captured after login. **Phase 2** (after VRChat login): checks `bannedIdentifiers/{vrchatId}`; if the VRChat ID is banned, the current device/auth are appended (new-device evasion); if Phase 1 flagged the device, the new VRChat ID is captured. **`appendIdentifiersToRecord` writes atomically via a single `WriteBatch`** — the up-to-3 new `bannedIdentifiers` index entries, the `bannedRecords/{banId}` array unions (`deviceHashes`/`authUids`/`vrchatIds`/`displayNames`/`evasionAttempts`), and the `moderationEvents` audit doc all commit all-or-nothing. Before this, they were 5 sequential awaited writes; a mid-way process death left the record inconsistent (an index pointing at a banId whose record never listed it, or a captured identifier with no audit event). The moderationEvent uses an auto-id doc reference added to the batch via `batch.set(collection.document(), ...)`. The whole operation is wrapped in try/catch (logged, non-fatal).

### Public UI kit + Onboarding (UI revamp Phases 1-2, see docs/ui-revamp.md)
- **`PublicUiKit.kt`** (`ui/common/`): shared revamp components — `CompactSectionCard` (collapsible card whose collapsed header carries a live summary; expansion state in `rememberSaveable(title)`, or plain `remember` when `persistExpansion = false` — used by the Media tab cards that must re-open collapsed after tab navigation), `TogglePill` (icon+label+dot pill with long-press jump-to-edit via `detectTapGestures`), `StatusDot`/`KitStatusChip` (KitTone Neutral/Success/Warning/Error mapped to theme + Slime status colors), `LabeledRow`, `TutorialImage` (ON-DEMAND instruction image loaded by 1-based `index` from `TutorialImageStore` — NOT bundled in the APK; shows a spinner while downloading, tap → full-screen pinch-zoom dialog via Coil `rememberAsyncImagePainter`), `TrustNote`, `KitSectionHeader`. Design rule: "collapsed = status, expanded = control"; visual north star is the login screens, NOT the admin panel.
- **`VrcaDialogs.kt`** (`ui/common/`, used by BOTH builds): the shared confirm/picker primitives so every "are you sure?" popup reads identically AND matches the app's Slime card identity. **All dialogs are now card-styled** (`Dialog { ElevatedCard }`), NOT stock M3 `AlertDialog` — the user explicitly rejected the AlertDialog look as not matching the revamp. **`VrcaCardDialog(onDismiss, dismissOnOutsideTap=true, content)`** is the shared shell (near-full-width `ElevatedCard`, `surface` container, `large` rounded corners, 6dp elevation, 20dp side padding) every popup is built on — same visual language as `UpdateDialog`/`InstanceListDialog`. Build NEW dialogs on it, not `AlertDialog`. **`VrcaConfirmDialog(title, body, confirmLabel, onConfirm, onDismiss, destructive=false, confirmEnabled=true, dismissLabel="Cancel")`** — card dialog with a header (a warning glyph in an error-tinted circle when `destructive`), bodyMedium onSurfaceVariant, and a bottom actions row (Cancel `TextButton` + a filled confirm `Button`, error-colored when destructive). EVERY confirm popup routes through it: public Dismiss-all + VRChat sign-out (`VrchatPage.kt`), Settings VRChat/Discord sign-out (`VrcaSettingsPage.kt`), admin **Kill App**, Log out VRChat/Discord (`UsersTab.kt`), Retract release (`ReleasesTab.kt`). The Discord **risk-consent** + **login WebView** dialogs (`VrcaSettingsPage.kt`) and the Automations **preset peek** dialog (`AutomationsPage.kt`) are also rebuilt on `VrcaCardDialog`. **`VrcaDialogCopy`** holds the CANONICAL device-owner sign-out copy. **`VrcaTimeZoneDialog(currentMode, use24h, onSelect, onDismiss)`** — the NEW searchable, DST-correct timezone picker (replaced the old `VrcaSingleSelectDialog` UTC-offset grid): a search field + a "Device timezone" row + a scrollable list of zone rows labeled **"Paris 3:42 PM (UTC+2)"** with the offset computed LIVE (`TimeZones.kt`), so it follows daylight savings. `VrcaSingleSelectDialog` + `SelectPill` remain (SelectPill still drives the Cycle speed chip row).
- **`TimeZones.kt`** (`ui/common/`): timezone helpers shared by the VM + the picker. `resolveTimeZone(mode)` maps the stored `timeMode` string to a `java.time.ZoneId` — backward compatible with the LEGACY `"UTC+N"`/`"UTC-N"`/`"Device"`/`"UTC"` formats AND new IANA ids (e.g. `"Europe/Paris"`). New selections store IANA ids (DST-correct); old `"UTC+5"` values keep working. `timeZoneLabel(id, use24h)` builds the "City h:mm a (UTC±X)" label; `zoneOffsetLabel(zone)` is the live offset; `allTimeZoneIds` (cached, all real IANA zones sorted by live offset) + `commonTimeZoneIds` (curated major cities shown before search). `VrcaViewModel.currentTimeString()` now resolves via `resolveTimeZone(timeMode)`. **Country-name search (all countries)**: `zoneCountryName(id)` maps a zone to its country display name via ICU's region table (`android.icu.util.TimeZone.getRegion(id)` → ISO code → `Locale("", code).displayCountry`, cached) — so searching "japan" finds `Asia/Tokyo`, "france" finds `Europe/Paris`, etc., for EVERY country with no hand-maintained list (minSdk 26, ICU available). `countrySynonyms` adds colloquial aliases ("usa"/"uk"/"holland"/"uae"/…). The picker filter (`VrcaDialogs.kt` `VrcaTimeZoneDialog`) matches city, region, country name, zone id, and offset label.
- **Chatbox sub-lines (multi-row Pinned / Cycle — `app/SubLineCodec.kt`)**: the Pinned message and each Cycle line can hold up to **3 sub-lines**, each rendering as its own VRChat chatbox ROW. A sub-line is `ChatboxSubLine(text, hidden)`; the list order is the row order; a `hidden` sub-line stays in the editor (and syncs) but is NOT sent. **The hide/order/hidden-text state rides the EXISTING synced string fields** (`afkMessage` for Pinned, each `\n`-delimited slide of `cycleLinesText` for Cycle) via `SubLineCodec` — **ZERO new Firestore fields, zero extra reads/writes** — so the admin, decoding the same format, sees + can moderate every row (hidden included). **Encoding**: a lone VISIBLE sub-line encodes as PLAIN text (no markers → byte-for-byte backward compatible with old readers and the single-row case); otherwise `SENTINEL() + sub0 + SEP() + sub1 + …` where a hidden sub-line's text is prefixed `HIDDEN()`. An encoded slide never contains `\n`, so `cycleLinesText`'s slide-level `\n` split / cross-device parsing are unaffected. `sanitize()` strips those control chars (and the reserved ` `) from user input. **`renderVisible(stored)`** (visible, non-blank rows joined by real `\n`) is the ONLY thing sent to VRChat — hidden rows cost nothing against the 144 budget; `visibleRowCount()` feeds the 9-line budget (`limitWithPriority` now counts ROWS, `text.count('\n')+1`, not blocks). **Admin readable round-trip** (`toAdminText`/`fromAdminText`, cycle variants `toAdminCycleText`/`fromAdminCycleText`): the admin text boxes show rows joined by `" ⏎ "` with hidden rows tagged `"⊘"` and re-encode on save, so the raw control-char form never appears in admin UI. **Phase 1 shipped = Pinned**: `VrcaViewModel.pinnedSubLines()`/`setPinnedSubLineText`/`setPinnedSubLineHidden`/`addPinnedSubLine`/`removePinnedSubLine`/`movePinnedSubLine` all re-encode into `afkMessage` via `updateAfkText` (so presets ride along for free — `afkPreset1-3` store the encoded value; sync writes the RAW `afkPresetTexts[i]`, NOT `getAfkPresetPreview` which returns a rendered preview). The Pinned editor (`AutomationsPage.kt`) is a reusable **`SubLineEditor`/`SubLineRow`**: the **MAIN line (row 0) shows normally and its number badge / chevron expands** to reveal up to 2 **small, indented, dimmer nested sub-line rows** (they must look like sub-lines, not standalone lines — per user feedback) each with its own hide (eye) / char meter / overflow (move up/down + delete), plus an indented "Add row" (≤3) and the **"up to 3 chatbox lines" caption that only shows while expanded** (the old always-visible "Message rows" header was removed). The main row's number badge = expand (NOT hide — the main line has no eye; hiding is a sub-line concept); collapsed it shows a "+N" chip when sub-lines exist. Expand state is a page-scoped `pinnedExpanded` `remember` (sub-lines hidden until the line is selected). The combined total `CharBudgetMeter` renders only when `visibleRowCount > 1` (a single row's per-row meter already IS the total). `pinnedSubs` is snapshotted in PAGE scope + sub-rows `key(i, hidden)`-wrapped (same AnimatedVisibility-skip fix as the cycle mute rows). `buildCombinedText` sends `resolveTokens(renderVisible(afkMessage))`. **Phase 2 shipped = Cycle**: each cycle slide is now sub-line encoded in `cycleLines[i]` (same codec). `activeCycleLines()` returns `SubLineCodec.renderVisible(cycleLines[i])` per active slide; `cycleActiveRawIndex`/preset previews/`hasVisible` all decode. VM ops `cycleSlideSubLines`/`setCycleSubLineText`/`setCycleSubLineHidden`/`addCycleSubLine`/`removeCycleSubLine`/`moveCycleSubLine` re-encode into `cycleLines[slide]` via `persistCycleLinesPreserve` (presets + sync ride along). The editor reuses `CycleLineRow` as the MAIN line (sub-line 0) with its **number badge repurposed from mute → EXPAND** (mute moved to the eye, which cycle already had; overflow still moves/duplicates/deletes the whole SLIDE) + a trailing chevron/"+N"; expanding renders the compact `SubLineRow`s for sub-lines 1.. + Add row + a per-slide total. `cycleLineFields` is now keyed `"slide:sub"`, seeded from decoded sub-lines; `cycleSubs` (decoded per slide) is snapshotted in PAGE scope for the AnimatedVisibility-skip fix, and per-slide expand state lives in a `cycleExpanded` map.
- **Drag-and-drop editor (`AutomationsPage.kt`, scope = reorder within a list → promote/demote within a section → cross-section Pinned↔Cycle; cross-preset dragging deliberately OUT of scope).** Universal model: **UI-only until drop** — the picked item is STABLE for the whole gesture, offsets follow the finger via `graphicsLayer` (deferred reads = no per-frame recomposition), and the VM mutation fires ONCE on release, so any interrupted drag (invalid drop / tab-away / crash) reverts for free. Grab point is always the **number badge** (long-press; tap does nothing — expand is the chevron, hide is the eye — so grabbing can't toggle anything). Dragged rows are **opaque** (`lerp(surfaceVariant, primary, …)`); a **bright insertion bar** (`drawBehind`, page-scope `dropBarColor`) shows the drop position (replaced the old gap-shift). Drop targets use **absolute-position math** (dragged centre vs other rows' midpoints) for consistent placement near ends/between rows.
  - **Increment 1 — reorder within a list**: **cycle main lines** (`cycleDragIndex`/`cycleDragOffsetY`/`cycleRowHeights`, `cycleDropTarget`) and **sub-lines within a block** (shared `subDragKey`/`subDragIndex`/`subDragOffsetY`/`subRowHeights`, `subDropTarget`, `subDragHandle`/`subRowMod` — used by both the pinned `SubLineEditor` and each cycle slide's inline sub-rows). A dragged cycle line **auto-collapses its sub-lines** while held (`draggingIdx != idx` gate — cycleExpanded untouched so they reopen on drop and the gesture isn't disposed by a key change). **Auto-scroll**: `PageContainer` takes a hoisted `scrollState` + `onViewport`; near a viewport edge (`edgePx`=96dp) a `LaunchedEffect` loops `pageScroll.scrollBy` and folds the scrolled distance into `cycleDragOffsetY`. Dropped well outside a list → reverts. **CRITICAL gesture-survival rule (the "can't drag pinned rows / sub-lines" bug): the drag-active flag must NOT be part of a row's `key()`.** Sub-rows previously wrapped in `key(..., activeDragIndex == i)` / `key(..., subActive)` were DISPOSED+recreated the instant a drag started (the flag flips true on `onDragStart`), which cancelled the in-progress `detectDragGesturesAfterLongPress` — so only cycle MAIN lines (whose `key(idx, lineEn, expanded, subs.size)` carries no drag flag) could be dragged. Fix: keys are `key(i, sub.hidden)` / `key(idx, s, sub.hidden)` (no drag term); the `dragActive` opaque styling still updates because it's derived from the page-scope `subDragKeySnap`/`subDragIndexSnap` snapshots read INSIDE the card content lambda (forces an in-place recompose, key stable → gesture survives), and the float/drop-bar are deferred `graphicsLayer`/`drawBehind` reads of the LIVE `subDragKey` (reactive regardless of the lambda). **Post-drop expand reset**: `cycleExpanded` (+ `pinnedExpanded`) is keyed by INDEX, so a reorder/demote/cross-move shifts indices and would strand the expanded state on a DIFFERENT line (the "expanded line shows collapsed until placed again, and a random other line looks expanded" report). The cycle-line `onDragEnd` tracks `structuralMove` and clears `cycleExpanded` + collapses pinned after any actual move, so expand state is never stranded.
  - **Increment 2 — promote/demote within a section (Cycle)**: **demote** = drag a cycle line onto ANOTHER line's centre (`cycleDemoteHover`, middle 30–70% band) → `vm.demoteCycleLineInto` nests its rows as sub-lines of the target (green "fits" / red "won't fit" tint via `canDemoteCycleInto` + `demoteTargetValid`/`dragInvalid`; the insertion bar hides in demote mode). **Promote** = drag a cycle sub-line OUT of its block (`subDragHandle` `onOutside`) → `vm.promoteCycleSubLine` makes it its own new line.
  - **Increment 1.5 — unified auto-scroll + data-integrity guards (device-feedback round)**: (a) **Auto-scroll now runs for EVERY drag, not just cycle main lines** — the single `LaunchedEffect(cycleDragIndex, subDragKey)` loop runs while `cycleDragIndex != null || subDragKey != null` and folds the scrolled distance into whichever offset is live (`cycleDragOffsetY` or `subDragOffsetY`); `subDragHandle.onDrag` sets the shared `cycleAutoDir` from the finger vs viewport edges. Fixes "dragging Pinned / sub-lines doesn't scroll the screen". (a2) **Sub-drag finger position is read from the GRAB HANDLE's own window pos** (`subHandleTops`, an `onGloballyPositioned` on the drag-handle modifier) since the pointer position is relative to that node — the old row-level top wasn't landing for **cycle** sub-rows (they sit inside the per-line `graphicsLayer` Column), so `crossTarget`/section detection stayed dead and **cycle sub-lines never showed a cross drop bar even though pinned sub-lines did**. (b) **Sub-line dragged fully out of the UI reverts** (back to its sub area) instead of promoting — promote (`onOutside`) now requires `outside && withinCard`; a drop outside the card entirely falls through to revert. (c) **Cycle main-line sub-0 visibility invariant (`repairCycleMainVisibility`)**: a cycle slide's MAIN sub-line (sub 0) must NEVER be sub-level `hidden` — its visibility is the line's mute (`cycleLineEnabled`), and the editor never exposes an eye for sub 0. A pinned↔cycle move used to carry a hidden flag onto sub 0, so the line decoded as blank → **uncounted in the "N lines" summary AND non-sending while still showing its text** (tester: "shows 10 lines but says 9", found via the saved preset). The repair force-unhides sub 0 on every slide; it's wired into the load path (`setCycleLinesFromTextPreserve` → self-heals on next open and re-persists the clean form, also healing the selected preset via the autosave) and the top of `persistCycleLinesPreserve` (every mutation's output). Source-side: `promoteCycleSubLine`/`movePinnedRowToCycle` map a hidden row to a **muted** line (`cycleLineEnabled = !hidden`, sub 0 visible) instead of a hidden main line; `moveCycleLineToPinned`/`moveCycleSubToPinned` force the pinned main row (row 0, which has no eye) visible.
  - **Increment 3 — cross-section Pinned↔Cycle**: `CompactSectionCard` gained an `expandedState` (hoisted) + `modifier` param; the Pinned & Cycle cards report their window rects (`boundsInWindow`) and `sectionAt(fingerY)`/`updateCross(origin, fingerY)` set `crossTarget` when the finger is over the OTHER section (which **auto-expands** + border-highlights; a cross INTO Pinned also force-expands its sub-rows via `expanded = pinnedExpanded || crossTarget == "pinned"` so every slot shows). On drop: **cycle line → Pinned** (`vm.moveCycleLineToPinned(from, at)`), **cycle sub → Pinned** (`vm.moveCycleSubToPinned(slide, sub, at)`), **pinned row → Cycle** (`vm.movePinnedRowToCycle(subIndex, at)`, becomes a new cycle line). Cross-section takes priority over demote/reorder on drop. All cross ops cap at 3 pinned rows / 20 cycle lines and no-op (revert) when they don't fit. Menu Move up/down/Duplicate/Delete kept as fallbacks throughout. **Drag GHOST overlay (`AutomationsPage`, replaced the in-place `translationY` float)**: the picked-up row used to float via `graphicsLayer{translationY}` INSIDE its card — but a card CLIPS its content and is a Z-sibling of the other card, so during a cross-section drag the pill drew BEHIND the other card, and an in-place offset DESYNCED from the finger whenever a card's layout shifted (the Pinned auto-expand). Now the real row **stays in its slot and just DIMS** (`alpha 0.4`, no translation), while a **display-only copy is rendered in a page-level overlay `Box` ABOVE every card** (`zIndex(1f)`, outside all clips — `AutomationsPage`'s root is now `Box { PageContainer{...}; ghost }`). The ghost is positioned by the **ABSOLUTE finger**: `offset = dragFingerY − dragGrabPointY − overlayWinTop` (y) and `ghostLeftPx − overlayWinLeft` (x), read inside the `.offset{}` lambda (layout-phase, no recomposition → smooth). `dragGrabPointY` is captured ONCE on the first drag frame (`fingerY − rowTop`), so any constant anchor error cancels and the finger stays locked to its exact grab point; because `dragFingerY` recomputes off the row's live window anchor each frame, a layout shift cancels too (no jump, no drift). Row window left/width are captured (`cycleRowLeft/Width`, `subRowLeft/Width`) so the ghost matches the row's exact bounds; the ghost re-renders `CycleLineRow`/`SubLineRow` (no handlers, `dragActive=true`) from `vm` + the field maps. The drop-math offset (`cycleDragOffsetY`/`subDragOffsetY`, which folds auto-scroll) and the insertion bars are UNCHANGED — only the visual moved to the overlay. This is what stopped the earlier attempts (raw `translationY=offset` → clipped + desync; `translationY = dragVisualTop − liveRowTop` → per-frame async jitter): the rows no longer move at all, so nothing can jitter, and the ghost tracks a single synchronously-updated finger value. **2-second hover-to-arm + cycle-sub promote bar**: cross-section and demote no longer trigger the instant the finger passes over a target — they require a **2s continuous hold** (`pendingCross`/`pendingDemote` hold the instant hover; a `LaunchedEffect(pending…)` promotes it to the armed `crossTarget`/`cycleDemoteTarget` after `delay(2000)`, and the key-change restarts the wait so dragging PAST never arms). Arming a demote expands **only that specific cycle line's subs** (`cycleExpanded[t]=true`, per-line, not the whole card); `fingerOverCycleLine` keeps the armed target sticky so the expansion's layout shift can't disarm it. A **cycle SUB dragged OUT of its block** into the main cycle flow now shows the SAME insertion bar the cycle reorder uses (`subPromoteIndex` = `crossDropBoundary("cycle", fingerY)`, drawn in the cycle-Column `drawBehind`; the within-block bar is suppressed while promoting) and `promoteCycleSubLine(slide, sub, at)` inserts the new line at that index. **Nest-into-a-line for ALL source types**: hovering ANY dragged item over a cycle line's CENTRE band (2s) nests it as a **sub-line of that line** — cycle line→line is the existing `demoteCycleLineInto`; a **cycle sub** → `moveCycleSubIntoLine(fromSlide, sub, target)` and a **pinned row/sub** → `movePinnedRowIntoCycleLine(pinnedIndex, target)` are the new analogues (both append the row, `canNestIntoCycleLine` gates the 3-row cap, `persistCycleLinesPreserve` self-heals main visibility). Detection is finger-based (`cycleLineCenterUnderFinger(fingerY, exclude)`, excludes a cycle sub's own parent); the nest is armed by the SAME 2s `pendingDemote`→`cycleDemoteTarget` effect and expands only that line. For a pinned source over the cycle area, a centre-hover **nest wins over** the plain cross-to-new-line insert (`if (pendingDemote != null) pendingCross = null`); the target line shows the green/red validity tint (`cycleDemoteValid` extended: `canDemoteCycleInto` for a line source, `canNestIntoCycleLine` for a sub/pinned source). `subDragHandle` gained an `onNest` callback threaded to those VM ops. **Positional cross-drop + indicator**: the cross ops take an `at` insertion index (append when `< 0`); `updateCross` computes `crossDropIndex` from the finger vs the TARGET section's row midpoints (`crossDropBoundary`), and the target section draws the SAME bright insertion bar it uses for in-section reorder (added to the cycle-Column `drawBehind` when `crossTarget == "cycle"` and to `subRowMod`'s `drawBehind` for the pinned block when `crossTarget == "pinned"`) — so you can see WHERE the item lands across sections, not just that a cross is armed. The SOURCE list suppresses its own reorder bar while `crossTarget != null` (the item's bar shows in the other section). **Edge/overshoot tolerance (the "first/last line reverts if dragged a tiny bit too far" fix)**: the old revert test keyed on the dragged row's list-relative `curTop` vs a tight `draggedH * 0.8/0.9` margin, so a small over-drag past the first/last row reverted. Both the cycle-line and sub-line drops now accept a reorder (clamped to the nearest slot via `cycleDropTarget`/`subDropTarget`) as long as the finger is still within the section's card rect + `DRAG_ACCEPT_MARGIN` (72dp) — tracked via `dragFingerY` set each `onDrag`; only a drop COMPLETELY outside the card reverts. Cycle-sub PROMOTE (drag clearly out of the block) keeps its own `outMargin` (raised to `draggedH * 1.4f` so a tiny overshoot reorders instead of promoting).
- **Cycle line system (revamp + features)** (`AutomationsPage.kt` `CycleLineRow` + `VrcaViewModel`): line cap raised **10 → 20** (`VrcaViewModel.MAX_CYCLE_LINES`). Each line is a slim bordered `Surface` (~44dp) on `BasicTextField` with: an inline **number badge that doubles as the mute toggle** (dims when muted) + a `Visibility`/`VisibilityOff` mute icon, an **always-on** per-line `CharBudgetMeter`, and a `MoreVert` **overflow menu** (Move up / Move down / Duplicate / Delete). The line currently being SENT is highlighted live (primary tint + border) via a 1s ticker reading `vm.cycleActiveRawIndex()`. **New VM capabilities**: per-line mute (`cycleLineEnabled` — a `mutableStateListOf<Boolean>` kept index-aligned with `cycleLines`; LOCAL-only, persisted as a `1`/`0` CSV in DataStore `cycle_line_enabled`, NOT synced to Firestore — a muted line stays in the editor + the synced `cycleLinesText` but the sender skips it); `duplicateCycleLine`/`moveCycleLine`/`setCycleLineEnabled`; **shuffle** (`cycleShuffle` / `setCycleShuffleFlag`, DataStore `cycle_shuffle`, local-only) — random rotation via `nextCyclePos()` with a **no-repeat window** (`shuffleWindow(activeSize)`: how many recently-played positions to avoid before a line can recur — `1` for <5 active lines (just no immediate repeat), then ramps by linear interpolation between anchors **5→3, 10→5, 15→8, 20→14** (clamped above 20), so more lines enforce more variety; the window is computed over the **active** line count — blank AND muted lines are excluded by `activeCycleLines()`, so empty slots never count); **dynamic tokens** resolved live at send/preview time by the PUBLIC `resolveTokens()` (renamed from `resolveCycleTokens`) — `{time}` (Time-line string), `{song}` ("Artist - Title" now playing), `{world}` (VRChat world name), `{players}` (instance "n/cap"). **Tokens work in BOTH Pinned and Cycle** — `buildCombinedText` runs the `afkLine` through `resolveTokens` too. The sender (`startCycle`) + preview (`currentCycleLinePreview`) + idle ticker (`tickCyclePreviewOnly`) all walk `activeCycleLines()` (non-blank, non-muted, capped) and resolve tokens. The **char meter counts the RESOLVED length** (`vm.resolveTokens(text).length`) in both editors, so a `{world}` shows the actual world name's character cost, not the literal `{world}`. The hint line under both editors reads "Currently experimenting, feel free to try by putting these tags into your pinned and cycle message: {time} {song} {world} {players}" (shared `TokensHint()`). `setCycleLinesFromTextPreserve` is change-aware (an echo of our own `saveCycleMessages` keeps the local mute/shuffle state; only a genuine line-set change resets mute). Cycle speed chips (2s/5s/10s/20s/40s) unchanged. **Eye/mute responsiveness (hard-won — SIX prior attempts failed)**: the mute flags are snapshotted in the PAGE scope (`cycleEnabledFlags = vm.cycleLineEnabled.toList()`) and each `CycleLineRow` is wrapped in **`key(idx, lineEn)`** so a mute toggle forces Compose to recreate the row outright. Reading the state inside the `CompactSectionCard` content lambda, passing a provider lambda, or passing a changed plain Boolean WITHOUT the key() all failed on-device — the `AnimatedVisibility` content slot skipped the rows even when their params changed (the header summary, read in page scope, updated instantly the whole time). Do not remove the key() wrapper. **Focus-expand**: a focused cycle line field flips `singleLine=false`/`maxLines=6` (via `onFocusChanged`) so a long line wraps and is fully visible while typing, collapsing back to one line on blur. **Cursor-teleport fix (Pinned + Cycle editors)**: the fields are local `TextFieldValue` maps (`cycleLineFields` keyed `"slide:sub"`, `pinnedFields` keyed by index) synced FROM the VM via `syncCycleLineFieldsFromVm`/`syncPinnedFieldsFromVm`. The old sync rebuilt `cycleLineFields[k] = TextFieldValue(s.text)` whenever the round-tripped text differed — and `TextFieldValue(text)` defaults its selection to `TextRange.Zero` (the FRONT), so a keystroke's own VM echo yanked the cursor to position 0 mid-typing (tester: "cursor teleports to the front/randomly when typing or deleting fast"). Two guards: (1) a shared `focusedFieldKey` (`"c:$slide:$sub"` / `"p:$idx"`, set by each field's `onFocusChanged` → `onFieldFocus`, threaded through `CycleLineRow`/`SubLineRow`/`SubLineEditor` `onFocusChange` params) makes the sync SKIP the field the user is actively editing — its own echo can never clobber the live cursor; external changes (preset load, admin edit, cross-device) apply on blur. (2) `reseedField(existing, text)` PRESERVES the cursor (clamped) instead of resetting to `TextRange.Zero`, and returns the same reference when text is unchanged (no no-op state write) — the fallback for any non-focused re-seed. Manual Send (`vm.messageText`) already preserves the cursor (only scroll-mode forces end, intentional); admin fields use plain local `String` state (synchronous, unaffected).
- **OSC-in tokens (headset — `com.vrca.osc.VrcaOscQuery` + `VrcaOscReceiver` → `VrcaOscState`)**: the chatbox reads the local player's live avatar params for **`{mute}`** (MuteSelf→"Muted"/""), **`{afk}`** (AFK→"AFK"/""), **`{movement}`** (VelocityX/Y/Z magnitude→"Moving"/"Still"), **`{scale}`** (avatar size — `EyeHeightAsMeters` "1.65m", else `ScaleFactor` as "%"), **`{param:Name}`** (ANY avatar param). `TokensHint` shows these only on the headset build; `VrcaOscState.isLive` (updated within 8s) is a "VRChat OSC live" signal. **The delivery mechanism is OSCQuery PULL, not a UDP push — this was the whole battle.** VRChat on **Quest standalone does NOT push avatar-param OSC output** over UDP 9001 (confirmed on-device: `rxPackets=0` even while unmuting/moving; and via VRChat's own tracker — vrchat-community/osc #129 — "only `/avatar/change` is sent to localhost:9001, all other params dropped"). BUT VRChat on Android **serves every param's live value over its OSCQuery HTTP server** (VRChat's OSCQuery wiki: Android is not same-machine-limited like Windows). So **`VrcaOscQuery` (headset-started in `VrcaApplication.onCreate`)**: (1) **mDNS-discovers** VRChat's `_oscjson._tcp` service via `NsdManager` (filters to a `VRChat`-prefixed service name), resolving `host:port` (e.g. `VRChat-Client-XXXX @ 192.168.0.89:45631`); (2) **polls `GET http://host:port/avatar/parameters` every 1s** — ONE request returns the whole avatar-param subtree WITH current values (`CONTENTS.<Name> = {FULL_PATH, TYPE, VALUE:[...]}`; TYPE `T`/`F`=bool, `i`=int, `f`=float; `extractValue` reads `VALUE[0]`); (3) folds each into `VrcaOscState.onParam` so the tokens resolve. On a poll HTTP failure it drops `host` and waits for re-discovery (VRChat restart / new session port); `onServiceLost` clears it too. **Requires cleartext HTTP** (OSCQuery is plain `http://` on the LAN — Android blocks cleartext by default): `usesCleartextTraffic="true"` is set on the **headset manifest ONLY** (public/admin stay cleartext-blocked; it does not downgrade any `https://` call, only permits `http` where we explicitly use it). **`VrcaOscReceiver` (UDP 9001 listener) is kept as a no-cost fallback** for the `/avatar/change` event VRChat DOES push and for PC-push setups, but on Quest it receives nothing — OSCQuery is the working path. A **Settings → Debug (headset) "OSCQuery probe / OSC-in"** diagnostic (`VrcaOscState.diagString()`/`oscQueryDiag`) shows bind/rx state + the discovered service + a live `MuteSelf` read, which is how this was root-caused. **`{scale}`/`EyeHeightAsMeters` also drives the avatar-size READ**; the avatar-size WRITE (`/avatar/eyeheight`, VrcaOsc) works independently (OSC INPUT is the half Quest supports). **HEADSET OSC SEND TARGET = the reachable OSCQuery host, NOT loopback (proven fix for "chatbox/avatar-size randomly stops, reopen fixes it")**: sending to hardcoded `127.0.0.1:9000` was PROVEN undelivered on-device — the **eyeheight delivery canary** (`VrcaOscState.recordEyeHeightSend`/`deliveryDiag`, surfaced in the send-health diag: send `/avatar/eyeheight` → confirm VRChat received it ONLY if the `EyeHeightAsMeters` OSCQuery readback MOVES; `udp ok` is a LIE for UDP — it only means the OS accepted the datagram) read **`size probe: NOT DELIVERED (readback never moved)` for minutes while `udp ok=0s`/`failStreak=0`**, at the same moment OSCQuery HTTP to VRChat's discovered host was fully live (96 params). So on Quest it's specifically **outbound UDP to loopback that dies**, not the reachable host. Fix: `VrcaOscQuery.oscSendTargetFlow` publishes `(host, oscInPort)` — the SAME mDNS-resolved host our polls reach VRChat at (the device LAN IP, e.g. `192.168.0.89`) + VRChat's advertised OSC-in port from HOST_INFO (`/?HOST_INFO` → `OSC_PORT`, falls back to 9000). The **headset** VM (`VrcaViewModel` init, `IS_HEADSET_BUILD` branch) collects it and re-points `remoteVrcaOsc` (ip+port), falling back to `127.0.0.1:9000` only until discovery resolves; re-points only on a genuine host/port change (no socket thrash). **Mobile is unchanged** (targets the manually-entered LAN IP; never runs OSCQuery discovery). The send-health diag's `target=` line shows the LAN IP once rerouted and the canary confirms `DELIVERED` — a built-in verdict. `VrcaOsc` sends a FRESH `OSCPortOut` per send on a dedicated `vrca-osc-send` thread (a persistent reused socket was tried + reverted: over loopback `send()` never throws even after VRChat rebinds, so it wedged "successfully" until process restart).
- **Avatar tools (TEMPORARY on the VRChat tab — `VrchatPage.kt` `AvatarToolsCard`)**: (1) **Avatar search** via the public **avtrdb** API (`AvatarSearch.search` → `GET api.avtrdb.com/v2/avatar/search?query=`). **CONFIRMED real schema** (verified against the live API — the old parser guessed wrong field names so EVERY result was dropped): `avatars[]` with `vrc_id` (NOT `id`), `author` as an OBJECT `{name,vrc_id}` (NOT `authorName`), `image_url` (NOT `thumbnailImageUrl`), and platforms from `compatibility` `["pc","android","ios"]` (NOT `unityPackages[].platform`) mapped via `prettyAvtrdbPlatform`→PC/Quest/iOS; results render thumbnail (session-authed `VrchatImageLoader`) + name + author + **platform SYMBOLS** (the shared `PlatformSymbol` from the instance roster, now `internal`) + a **performance-rank badge** (`PerfBadge`: Excellent/Good/Medium/Poor/Very Poor with VRChat-style colors, for the user's own platform — Quest on headset else PC — from the catalog Entry's `perfPc/Quest/Ios`, `AvatarSearch.Result` now carries these) + Open-in-VRChat. **Search loads PROGRESSIVELY — infinite, no cap, cost only on demand**: the match set's IDs are found by ONE cheap index lookup (`pageTotal` = exact match count), and only ~20 avatars' details are fetched per page (`searchPage`→`searchShardedPage`). `runSearch` loads page 0 for an instant first paint; **`loadMore`** streams the next CHUNK a page at a time (rows appear immediately) — ~1000 more when unfiltered, or until ~12 new matching rows when a filter is on — and "See more" repeats, so browsing is unbounded with a flat per-chunk cost (the old server Prev/Next AND the earlier grab-all-with-cap were both replaced; `searchShardedAll`/`searchAllMatches` remain as reusable helpers). The **platform FILTER chips (All/PC/Quest/iOS, using the shared `PlatformSymbol`) run CLIENT-side over the LOADED results** — toggling one re-slices instantly and never re-searches; with a filter on, `loadMore` auto-pulls pages until enough matching rows show. A "N shown · M matches" line gives the exact total. **Metadata insta-update**: `harvestSearchResults` (which already GETs `/avatars/{id}` to resolve a candidate's file id) now uses the FRESH VRChat metadata for the contribution and fires an `onRefresh` callback when a shown row's live name/author/platform differs, so the search row updates in place (mirrors the dead-hiding) at zero extra network cost. (2) **Avatar size control** — sends VRChat's `/avatar/eyeheight` OSC input via `VrcaViewModel.setAvatarEyeHeight`→`VrcaOsc.sendEyeHeight` (one-shot, bypasses the chatbox `blocked` gate; goes to the configured VRChat target — on headset the OSCQuery-discovered host, see the headset OSC send-target note above). **No app-imposed min/max** — a text field types ANY value (`sendEyeHeight` only clamps to VRChat's absolute 0.01–10000 m safety bounds); plus a 0.1–10 m convenience slider. **Live IN-GAME height read** (`VrcaOscState.eyeHeightFlow`, fed by the OSCQuery poll's `/avatar/eyeheight` read) — the "In-game:" line + slider mirror the real avatar height. **"Reset to default"** reverts to the AVATAR'S creator default, NOT what the app set: `VrcaOscState.defaultEyeHeightMeters` = `EyeHeightAsMeters / ScaleFactor` (VRChat's `ScaleFactor` is 1.0 at the native height and scales linearly, so this is invariant of any resize — the true default; null/disabled when `ScaleFactor` isn't published). This is the "changing avi size" control.
- **Preset auto-save model (Pinned + Cycle)** (`VrcaViewModel` `selectedAfkPreset`/`selectedCyclePreset` + `AutomationsPage`): the SELECTED slot is the auto-save target — editing the Pinned text / cycle lines writes straight into the selected slot, so **switching slots IS the only "save" (no manual save button)**. **Slot 0 = NOTHING selected is the DEFAULT (DataStore `selected_afk_preset`/`selected_cycle_preset` default 0)** — `autoSaveSelectedAfkPreset` (`if (selectedAfkPreset !in 1..3) return`) and `autoSaveSelectedCyclePreset` (`if (selectedCyclePreset !in 1..5) return`) NO-OP when nothing is selected, so editing touches NO preset until the user deliberately taps a slot. **This is the backward-compat guarantee**: existing users (no DataStore key → 0) never have a saved preset clobbered by editing; the old unsafe default of 1 would overwrite slot 1 on the first edit. **Slot-0 data-loss migration (`migrateLiveContentIntoPresetSlot`, one-shot via DataStore `preset_seed_migrated`)**: slot 0 protects presets from EDITS but not from SELECTS — a pre-update user's live Pinned message / cycle lines are saved in NO slot, so their first tap on a preset chip loaded that slot over the live editor and silently destroyed their content. On VM init (after the initial DataStore load, before `initialDataLoaded`), the migration parks the live content in a slot once: prefer a slot already holding identical content (select it, no write), else the first EMPTY slot (save + select). All-slots-full-with-distinct-content leaves slot 0 and changes nothing (never clobber a deliberate save — accepted edge). Selecting during migration does NOT reload the editor (content is identical by construction). Fresh installs just get their default message parked in slot 1 — harmless. **Stale preset chips/peek fix (`AutomationsPage`)**: preset previews + the selected slot are snapshotted in the PAGE scope (`afkPresetPreviews`/`cyclePresetPreviews` Lists) — reads inside a `CompactSectionCard` content lambda don't reliably retrigger its `AnimatedVisibility` (same class of bug as the cycle-mute rows), so the auto-save writing into the selected slot left the chips and long-press peek stale until the tab was re-entered. Chips are wrapped in `key(slot, preview, equipped)` to force the repaint through, and `onPeek` reads the content FRESH from the VM at press time instead of using the composition-captured value. `updateAfkText` → `autoSaveSelectedAfkPreset`; every cycle content edit (`persistCycleLinesPreserve`) → `autoSaveSelectedCyclePreset`. **Per-preset hide state (`cycle_pN_enabled`)**: each cycle preset stores its OWN per-line mute CSV (aligned to the preset's NON-EMPTY saved lines, so indices survive the empty-line drop in `messages`) via `cyclePresetEnabled` + the new `enabledCsv` param on `saveCyclePresetN`. This fixed "presets don't save hidden lines" — the hide state used to live ONLY in the global `cycle_line_enabled` CSV, which `setCycleLinesFromTextPreserve` reset to all-visible on every preset load, so a preset never remembered which lines were muted. **`setCycleLineEnabled` (the mute toggle) now DOES auto-save the selected preset** (`autoSaveSelectedCyclePreset()`, no-op at slot 0) — reversing the earlier "mute toggles don't auto-save" design, which was the root cause. `selectCyclePreset` restores the slot's hide CSV after loading its lines (a BLANK CSV — legacy presets saved before this feature — explicitly resets to all-visible so a switch between identical-line presets can't inherit the prior slot's mute). `saveCyclePreset(slot, lines)` compat helper and the cross-device/admin apply lambdas pass no `enabledCsv` (defaults to "" = all-visible; the CSV is local-only, never synced to Firestore). `selectAfkPreset(slot)`/`selectCyclePreset(slot)` record the new selection (1..N) and load that slot's content into the live editor (loads, never clears). The preset chips highlight by `slot == vm.selectedAfkPreset/selectedCyclePreset` (no content-match; nothing highlighted at slot 0). The long-press peek dialog is view-only now (Close + "Switch to this preset"); the "Save current here" button is gone. The old `saveAfkPreset`/`saveCyclePreset`/`loadAfkPreset`/`loadCyclePreset` helpers are now unused by the UI (kept for compat).
- **Onboarding flow** (`ui/onboarding/OnboardingFlow.kt`): first-open tutorial pager (takes the app-scoped `vm` — `OnboardingFlow(vm, …)`), 8 steps in FINAL order — Welcome+ToS (hard gate, embeds the redesigned `TosGate`) → VRChat login (hard gate, embeds `VrchatLoginScreen`, shows a **platform-status warning** from a one-shot `status.vrchat.com/api/v2/summary.json` fetch since the pipeline's status poll only runs post-login) → Permissions (POST_NOTIFICATIONS runtime, Notification Access, battery exemption, **Install updates** (`canRequestPackageInstalls` / `ACTION_MANAGE_UNKNOWN_APP_SOURCES` — needed for in-app APK updates), **Overlay (optional)**; statuses re-checked on `ON_RESUME`; Samsung "Never sleeping apps" note. **POST_NOTIFICATIONS is requested ONLY here / in Settings — `MainActivity` no longer fires the runtime prompt on first open**; KeepAliveService starts regardless, its notification just stays hidden until granted. A collapsed `CompactSectionCard` ("Turn off background restrictions", `persistExpansion=false`) explains long-press icon → App info → 3-dot menu → "Allow background activity"/Unrestricted, and to keep VRC-A out of any restricted/sleeping-apps list — the Samsung note nests inside it. A SECOND collapsed card at the TOP of the step ("A permission won't turn on?", `persistExpansion=false`) explains the **"Allow restricted settings"** gateway: Android 13+ blocks SIDELOADED apps from granting Notification Access / Accessibility (the switch is greyed out / shows "Restricted setting") until the user opens App info → 3-dot menu → "Allow restricted settings". Placed first so it's seen when a toggle won't enable — this is the real reason a permission appears ungrantable on a non-Play-Store install) → IP entry (**auto-saves what you type** — a 500ms-debounced `LaunchedEffect(ip)` writes slot 1 + active IP via repo, so NO button press is needed to persist; the button is now an OPTIONAL "Check reachability" `pingHost` test only, per user complaint that users didn't realise they had to click to save. The typed IP becomes the **live OSC target** automatically because `VrcaViewModel.init` now collects `userPreferencesRepository.ipAddress` into `remoteVrcaOsc.ipAddress` — previously the app-scoped VM, constructed during onboarding before any IP existed, kept sending to `127.0.0.1` until the user manually tapped Apply in Home. The "Good to know" card on this step is a collapsed `CompactSectionCard` (`persistExpansion=false`) so the IP input sits in view without scrolling) → Enable OSC (numbered instructions with the 4 `osc_tutorial_N` TutorialImage frames, all on the single step; **the images are downloaded on-demand — see `TutorialImageStore` below — not bundled**) → Notification types (**the FULL per-type `NotificationToggleSection(vm)` — the same 27-toggle expandable UI as Settings**, replacing the old 5 master section switches, so first-time users pick and choose every type; toggles persist via the VM/DataStore directly) → Discord RPC (consent checkbox → `DiscordLoginWebView`; sets risk/seeded/enabled flags) → Test message finale (scoped `VrcaOsc(ip,port)` loop sends "VRC-A connected ✓" every 2s while active, empty-send clears on stop/exit; deliberately bypasses the Start/Stop gate — no toggles touched, `FeatureSessionStore` never armed). **The test step is a HARD PING GATE** (non-replay): Finish stays hidden until `testDone`, and `testDone` flips ONLY when the saved IP answers a ping — while the test runs, `pingHost` attempts repeat every 3s and the step passes the moment one succeeds; there is deliberately **NO manual "it showed up" bypass** (per the user: a wrong IP must be fixed, not waved through). A troubleshooting card (same Wi-Fi / right IP / headset awake / OSC enabled) with a "Fix the address" jump-back shows while waiting. **`pingHost(address)`** (public, in `OnboardingFlow.kt`) is the robust reachability check: `InetAddress.isReachable` first (often fails on Android — no raw ICMP socket), then the system `/system/bin/ping -c 1` binary fallback; the Home Connection card's 20s reachability chip uses it too so the chip and the tutorial gate agree. No saved IP → a "Go to the address step" button jumps back to step 4 (`onGoToIpStep`). **No em dashes in any tutorial-visible string** (user said they look bad — also swept `VrchatLoginScreen`'s copy, which the tutorial embeds; don't reintroduce them in onboarding/login UI text). **Footer**: a "Step N of 8" label + animated progress pills (current step is an elongated 24dp pill via `animateDpAsState`, visited pills are tappable to jump back). **The flow root clears text-field focus on any empty-space tap** (`detectTapGestures` → `LocalFocusManager.clearFocus()` — a parent gesture detector only sees taps children didn't claim); the same pattern lives in the main app's `PageContainer` (`VrcaScreen.kt`) so every tab dismisses a stuck focused field/keyboard. The IP step's Quest instructions are 4 expanded numbered steps + a "Good to know" card (IPv6 warning, same-Wi-Fi, IP-changes note, PC ipconfig path) — written to stand alone since Quest Settings screenshots are DRM-blanked. Gating in `VrcaApp`: `OnboardingPrefs` (`vrca_onboarding` prefs, `complete` + resume `step`); **pre-seeded complete when `TosPrefs.acceptedVersion > 0`** so existing installs never see it. **Replay**: Settings → App → "Replay setup tutorial" sets `OnboardingState.replayRequested` (process-level mutableStateOf); replay renders hard gates as completed checkmark cards and has an "Exit tutorial" button. Phase-2 ban check + pipeline start still run via the normal post-gate logic in `VrcaApp` (the login-gate `remember` first executes after onboarding returns, so it correctly reads the now-logged-in state).
- **On-demand OSC tutorial images (`ui/onboarding/TutorialImageStore.kt`)**: the 4 `osc_tutorial_N.png` instruction images are **NOT bundled in the APK** — they were ~617 KB for a screen most users see once, so shipping them was pure bloat. They're downloaded on-demand from the shared image-store repo (`https://raw.githubusercontent.com/Ashoska/VRC-A-Image-store/main/osc_tutorial_N.png` — the SAME repo, renamed from `VRChat-rpc-display`, that hosts the Discord RPC default image `vrchat-1102x620.jpg`; `DiscordRpcService.DEFAULT_VRCHAT_IMAGE_URL` was updated to the new name in the same change) into `filesDir/tutorial_images/` and deleted the moment the tutorial ends. `ensureDownloaded(ctx)` is idempotent (skips cached, `.part`-file-then-rename so a partial download never looks complete); `clear(ctx)` deletes them. **`TutorialImage(index, …)` (PublicUiKit)** resolves the local file, shows a `CircularProgressIndicator` while it downloads, renders via Coil (`AsyncImage` inline + `rememberAsyncImagePainter` in the pinch-zoom dialog), and calls `ensureDownloaded` itself as a fallback. **Lifecycle (the "no stale images" design)**: (1) **New-user boot** — `VrcaApp`'s bootstrap, for a logged-out user whose onboarding is `!isComplete`, spends the boot-screen time PREFETCHING the images (`withTimeoutOrNull(TUTORIAL_PREFETCH_CAP_MS = 8s)`) instead of the old fixed fake wait, then still honors the `NEW_USER_BOOT_FLOOR_MS` floor so the screen doesn't flash / the cached case doesn't skip past onboarding; (2) **Replay** — `OnboardingFlow` re-downloads on entry (`LaunchedEffect(Unit) { ensureDownloaded }`, idempotent); (3) **Tutorial complete OR replay exit** — `VrcaApp`'s `onFinish` (the single convergence point for both first-run completion via `advance()` and replay's "Exit tutorial" via `finishNow()`) calls `TutorialImageStore.clear(ctx)`; (4) **A new user who closes/kills mid-onboarding KEEPS the images** — onboarding is still `!complete`, so no cleanup runs and boot re-prefetches any missing → resume works; (5) **A replay killed mid-way** leaves strays, so `VrcaApplication.onCreate` runs `clearStaleTutorialImages()` on a fresh process — if onboarding is already `complete` (i.e. not a genuine new user), it deletes the dir (replay state is process-level `OnboardingState.replayRequested`, always false at a cold process start, so a killed replay self-cleans). Net: images exist only while a tutorial is actually in progress; the APK ships zero tutorial images.
- **ToS gate redesign** (`VrcaApp.kt`): `parseTosBlocks()` converts the admin-configurable plain text into styled blocks — numbered headers (`^\d+\. `) render primary-colored (error-colored for Risk/Moderation/Acceptable Use sections), `*`/`-` lines become real bullets, literal markdown links `[x](y)` are collapsed by `sanitizeTosLine`. Layout: sticky title + version pill header, scrollable body, pinned accept bar with a **Checkbox** (was a Switch). `TosGate` is `internal` so onboarding embeds it.
- **Update dialog redesign** (`VrcaApp.kt`): patch notes render through the same `StyledBlocks` parser inside a **scrollable area capped at ~45% screen height** (long notes used to clip the dialog). Header = title + version pill. Forced variant (the default — all releases forced) = single full-width Download button; Later only on the legacy non-forced path. Browser-fallback link kept.
- **Boot loading screen (`BootstrapScreen`, `VrcaApp.kt`)**: gradient background (theme background fading into a faint primary tint via `lerp`), the dedicated high-res logo (`R.drawable.vrca_logo`, `drawable-nodpi`, a 512px copy of the chibi launcher art — NOT the small `@mipmap/ic_launcher`, which would upscale and blur at this ~136dp size) clipped circular in a tonal circle with a gentle breathing `scale` pulse while working, "VRC-A / VRChat Assistant" title block, a **3-row boot checklist** in order **Device session → VRChat systems → Account status** (`BootCheckRow` — per-row states pending·spinner·green-✓·amber-!·grey-?), a lifetime stat line, and a `v{VERSION_NAME}` footer pinned bottom-center. (The earlier rotating "DID YOU KNOW" `BOOT_TIPS` card was REMOVED per the user — do not re-add.) Takes a `phase` param (1 = bootstrap, 2 = phase-1 ban check) instead of a status string; the ban-check gate reuses the SAME screen so the whole boot reads as one continuous loading experience. **No fake wait**: the old fixed 5s `BOOT_MIN_DURATION_MS` hold was removed. Instead, during the **Device session** stage, if `VrchatAuthManager.isLoggedIn(ctx)`, `startPipelineWarmup(ctx)` fires the pipeline foreground service so **VRChat presence starts connecting in parallel with the rest of boot** (idempotent with the normal post-login start — the service coalesces a duplicate `ACTION_START`). **Account status now represents the ban/moderation check (`checkPhase1`) AND VRChat presence readiness**: for a logged-in user it also `withTimeoutOrNull(PRESENCE_WARM_CAP_MS = 5s)`-waits for `VrchatPipelineState.isConnectedFlow.first { it }` before the row goes green (`accountReady = phase1Checked && presenceReady`), so by the time it's reached the pipeline is "pretty much good"; the cap guarantees a dead session / bad network never hangs boot (it just hands off). A **logged-out / new user** has no presence to warm — `presenceReady` starts true — so Account status is just the ban check, and a `NEW_USER_BOOT_FLOOR_MS = 3s` floor holds the screen so it doesn't flash by before onboarding. **VRChat systems row**: one-shot fetch of `status.vrchat.com/api/v2/summary.json` via `fetchBootVrcStatus()` into the process-level `BootVrcStatus` holder (`AtomicBoolean` fetch guard — BootstrapScreen is composed twice per boot and must not refetch/flicker); never blocks boot. **Lifetime stat line** ("You've sent N chatbox updates", `%,d` formatted): from `ChatboxStats` (`com.vrca.app`, `vrca_stats` SharedPreferences) — incremented at the `VrcaOsc.sendOscMessage` chokepoint for every non-blank `/chatbox/input` send (typing signals + blank clears excluded), in-memory `AtomicLong` flushed every 25 sends, attached in `VrcaApplication.onCreate`. Hidden until ≥100 so fresh installs never see "0". Boot screen ONLY — deliberately no Settings stats block. The startup-error card + Retry behavior is unchanged. **Cold-start anon-auth retry (`bootstrapFirebaseAndCache`)**: the "Device session" row's only hard network dependency is `FirebaseAuth.signInAnonymously().await()` (the rest of bootstrap is `runCatching`/non-fatal). On a brand-new install's FIRST open the network + Firebase stack is often still cold, so a single attempt failed transiently → the row showed the error state and the user had to reopen (by which point it was warm and the anon credential cached) — the "Device session sometimes fails on first open" report. Fix: a bounded retry (up to 4 attempts, exponential backoff 400ms→3s, worst case ~5.8s, well within the 20s outer `withTimeout`) so a cold-start blip self-heals inside the boot window instead of surfacing a manual-retry error. **Boot screen shows ONLY on a clean swipe-reopen or first install (the "boot screen shows every time the app is brought back after an OEM/force kill, not just on a real open" fix)**: the boot screen exists to cover a genuine, deliberate cold start (Firebase anon sign-in + device hash + pipeline warm-up). But its gate state (`bootOk`/`phase1Checked`/`presenceReady`/`phase1HoldDone`) was all Activity-scoped `remember`, so ANY new Activity composition re-ran the whole sequence and re-showed the screen — including reopening after an OEM/force kill, a headless revival, or a plain Activity re-create, where everything is already set up. **The decision now mirrors how the Quick Toggles / `FeatureSessionStore` restore tell a deliberate swipe apart from an OS/OEM kill: the persistent `swiped_away` flag** (`AppShutdown.isSwipedAway()`, set ONLY by `onTaskSwiped`, which an OS/OEM/force kill never runs). `MainActivity.onCreate` captures **`VrcaApplication.openedFromSwipe = AppShutdown.isSwipedAway(ctx)` at the TOP, BEFORE `clearSwipedAway()` wipes it**. `VrcaApp` computes `val warmResume = !(openedFromSwipe || !everBootstrapped)` where `everBootstrapped` = a non-blank cached `auth_uid` in `vrca_remote` prefs (bootstrap writes it and it persists), and SEEDS all four boot-gate booleans from `warmResume`. So the boot screen shows **only** on (a) a clean **swipe**-reopen (a deliberate fresh start) or (b) a true **first install / data clear** (no cached auth uid → bootstrap MUST run to sign in anonymously + cache the uid the ban check reads); **every other relaunch — OEM/force kill, Activity recreate, headless revival — is a recovery and drops straight into the app with no boot screen.** Skipping boot on a swipe-less relaunch is safe because the auth uid + device hash already persist in prefs and the VM's `ensureAnonAuth` re-signs-in if the cached Firebase session lapsed. The ban-check `LaunchedEffect(bootOk)` still fires once on a warm resume (its key is true from the first composition) so `phase1BanId` is still populated and the downstream ban gate works — the UI just isn't blocked on it; `startPipelineWarmup`/`bootstrapFirebaseAndCache` are correctly skipped on warm resume (the VM/services own pipeline start + liveness). (The earlier `runtimeVmAlive` attempt was abandoned — `MainActivity` always starts `KeepAliveService` → `ensureRuntimeViewModel()` on EVERY open, which flipped that flag true and raced the composition, producing the inverted "swipe-reopen skips boot but everything else shows it" behavior; the swipe flag is unaffected by the service start.)
- **Backend-name sanitization (`sanitizeBackendRefs`, `VrcaApp.kt`)**: every USER-VISIBLE error/debug string is scrubbed of backend-implementation names before display (replaces `com.google.firebase…`/`com.google.android.gms…` class paths with "backend" and any `Firebase*`/`Firestore*` token with "server") so exception text can't reveal the server stack to users (exploit-surface hygiene, per the user). Applied at: the boot `bootError`, the `CrashScreen` crash-text display (stored log stays raw for debugging), and Settings → Debug ("Sync (last issue)" — renamed from "Firebase (last issue)" — and the moderation-listener error). Logs keep raw text. Route any NEW user-facing error surface through this; never hardcode "Firebase"/"Firestore" in public-build UI strings (admin build is exempt — not distributed).
- **Settings Accounts + VRChat-logout OSC gate (Phase 3)**: `VrcaSettingsPage` gains an **Accounts** section (VRChat row: display name + Sign out with confirm dialog → `VrchatAuthManager.logout`; signed-out state shows "Sign in" → full-screen `VrchatLoginScreen` takeover with Cancel. Discord row: status from `discordSessionSeeded`/`discordRpcEnabled` + Disconnect with confirm, mirroring the VRChat-tab disconnect logic). **OSC hard-block while signed out of VRChat**: `VrcaViewModel.startVrchatAuthGateWatcher()` (init, app-scoped VM) collects `VrchatAuthManager.loggedOutSignal` → `stopSending()` FIRST (so the chatbox-clearing send escapes before the block) then sets `vrchatLoggedOut=true`; `refreshOscBlockGate()` ORs `forceUpdatePending || vrchatLoggedOut` into `VrcaOsc.blocked` (both gates share the chokepoint without clobbering each other — `applyForceUpdateGate` also routes through it). **`VrchatAuthManager.loggedInSignal`** (new SharedFlow, emitted on Basic-auth Success + 2FA verify Success) lifts the gate; re-login never auto-starts sending. **There is NO forced login screen anywhere anymore** — the only hard login gate is the onboarding tutorial's login step on first run. `VrcaApp`'s standalone `if (!vrcLoginDone) VrchatLoginScreen` gate was DELETED: a cold start while signed out lands straight in the app with OSC blocked (per the user: "not being logged in should just make it so osc doesnt work"). To make that safe, **`vrchatLoggedOut` is seeded from the REAL auth state** (`!VrchatAuthManager.isLoggedIn(app)`) instead of `false`, and `startVrchatAuthGateWatcher()` calls `refreshOscBlockGate()` immediately — without this a signed-out cold start would have an OPEN gate until a `loggedOutSignal` that never fires. Sign-in surfaces: Settings → Accounts and the **VRChat tab** (`VrchatStatusPage` now owns its own `showLogin` full-screen `VrchatLoginScreen` takeover with Cancel — the old `onOpenLogin` param was wired to an EMPTY lambda in `VrcaScreen`, which was the "clicking login does nothing" bug; the param is gone). `VrchatStatusPage.isLinked` is now DERIVED from `vm.vrchatLoggedOut && isLoggedIn` (the old one-shot `remember { mutableStateOf(...) }` froze the cold-start value, so a Settings sign-out never updated the VRChat tab). Each re-login bumps `reloginTick`, which re-keys the Phase-2 ban check effect (a re-login could be a different, banned VRChat account) and restarts the pipeline; on a signed-out cold start the Phase-2 effect simply early-returns until `loggedInSignal` flips `vrcLoginDone`. **Notification toggles moved**: `NotificationToggleSection` now renders in Settings → Notifications (removed from the VRChat tab). **Storage row** (Settings → App): measures `cacheDir`+`externalCacheDir`+`codeCacheDir` on IO and deep-links to the system App Info page (apps can't fully wipe their own cache; the system Clear Cache button is the honest route — it never touches logins/settings, and `app_webview` lives in the DATA dir handled by the Discord 20 MB cap).
- **OSC auth-dead gate (confirmed-dead VRChat session)**: `isLoggedIn()` only checks the auth cookie is PRESENT, not valid — so a session that silently died (VRChat **password change**, **30-day trusted-device expiry**, cleared creds) left OSC working even though the user is effectively logged out (bad, since admins need users staying logged into VRChat). This gate stops OSC when the session is **confirmed dead-and-unrecoverable**, while tolerating transient issues. Detection lives in `VrchatPipelineService.evaluateSessionHealthOnReconnect()`, hooked into the existing reconnect path (`reconnectAttempt >= 2`, where a dead cookie surfaces as repeated WS failures — replaces the old inline `validateSession`+`autoRelogin`). It **never blames the environment**: guards return early (not dead) when the device lacks **validated** internet (`hasValidatedNetwork()`, `NET_CAPABILITY_VALIDATED` like the Discord RPC) or VRChat is in a **major/critical outage** (`isVrchatMajorOutage()` reads `VrchatPipelineState.statusPageState.indicator`). With a healthy environment it calls the new **tri-state `VrchatAuthManager.validateSessionDetailed()` → `SessionValidity` {VALID, UNAUTHORIZED, UNKNOWN}** (`GET /auth`: 200=VALID, 401=UNAUTHORIZED, 5xx/network/no-cookie=UNKNOWN — so "couldn't reach VRChat" is never "dead"); on `UNAUTHORIZED` it tries `autoRelogin()` — success clears, failure (2FA-required / bad creds / no saved creds) is the dead signal. A **`AUTH_DEAD_CONFIRM_MS` = 5-min window** (`authDeadSinceMs` timestamp, `markAuthDeadCandidate()`) must elapse with the state persisting before it flips `VrchatPipelineState.authDead=true` — rides out blips/brief outages. **The WS `onOpen` clears it** (`clearAuthDead()`): a successful handshake means the cookie is valid, so authDead can only ever be true while disconnected. `VrcaViewModel` collects `authDeadFlow` → `vrchatAuthDead` → `refreshOscBlockGate()` now ORs `forceUpdatePending || vrchatLoggedOut || accountDenied || vrchatAuthDead`; `stopSending()` fires first (chatbox-clearing send escapes before the block); `loggedInSignal` clears both `vrchatAuthDead` and `VrchatPipelineState.authDead`. **In-app only, NO notification** (per the user): a `VrchatSessionExpiredBanner` (`PublicUiKit`) shows on **Home** (button → navigate to the VRChat tab) and the **VRChat tab** (button → its `showLogin` takeover) — short copy naming the causes ("...after a password change or if VRC-A hasn't been opened in about 30 days. Sign in again to fix it."). Non-destructive: nothing is wiped; a successful sign-in clears the gate and lifts the block automatically (never auto-starts sending).

### Navigation
- Bottom nav: Home, Automations, **Media** (formerly "Music" — renamed since the NowPlaying block now covers YouTube videos too; enum constant stays `AppPage.Music`, only the display label/`title` changed), VRChat (4 items)
- **Automations (Phase 5 revamp, `AutomationsPage.kt`)**: the inner Pinned/Cycle `TabRow` is GONE (`ChatboxAutomationsTab` enum deleted from `VrcaScreen.kt`) — both features stack as `CompactSectionCard`s with live collapsed summaries (Pinned: the message text + ON/OFF `KitStatusChip`; Cycle: "5 lines · 10s · now: '…'" — the `now:` part uses the new public `vm.cycleCurrentLine()` and shows only while sending). Presets are a horizontally-scrolling **chip row** (`PresetChip`): tap = load/equip, long-press = peek dialog (full content via the new `vm.getCyclePresetFull(slot)`, with Load + "Save current here" buttons — Save lives ONLY in the peek dialog now); the equipped chip is highlighted by content-match (no "Selected" label). Editors have an inline **`CharBudgetMeter`** ("87/144", amber >114, red >144; mirrors the private `VRC_MAX_CHARS`) — always on for Pinned, per-line only past 100 chars for Cycle. Cycle's 10-field line editor + speed dropdown are unchanged.
- **VRChat tab (Phase 7 revamp, `VrchatPage.kt`)**: the two stacked identity cards (connection status + presence) are MERGED into one header `ElevatedCard`: `SelfAvatar` (the user's OWN VRChat+ pic via Coil through `VrchatImageLoader` — it attaches THIS device's session cookie, so it works on the public build too; initial-circle fallback; status dot pinned bottom-end) + name + colored status text + the user's status DESCRIPTION verbatim (e.g. "stars :0" — it's their VRChat status text, do NOT "fix" it) + a chips row (platform `Badge`, trust-rank `Badge` from the new `VrcUserPresence.trustRank` field — `fetchPresence` now extracts the highest `system_trust_*` tag from `/auth/user` + `/users/{id}` — and a small "RPC ●" chip when `discordSessionSeeded`, green dot when CONNECTED) + the world line (name + player count) + **friends online count** ("N of M friends online", `Icons.Filled.Group`). Sign out (error-tinted `Logout` icon) and View Profile (`OpenInNew`) are small trailing `IconButton`s. **Friends online count is FREE**: `VrchatPipelineState.friendsOnlineFlow` (`Pair(online,total)?`) is published by `VrchatPipelineService.publishFriendsOnline()` from the in-memory `friendsCache` on every cache mutation (`persistFriendsCache`) + restore — zero API calls. **Online predicate is STATUS-primary (over-count fix)**: `status` is authoritative — `"offline"` ⇒ offline, any other non-blank value (active/join me/ask me/busy) ⇒ online; `location` is consulted ONLY when `status` is blank (WS friend-online/friend-location payloads don't always carry `user.status`, so a live world location proves online then). **`location="offline"` is NOT an offline signal on its own** — VRChat uses it for a friend who is online on the WEBSITE but not in any world, so keying offline off location would drop website-active friends. This replaced an older `status-OR-location` predicate that counted a friend online if EITHER field looked online: a friend-update setting `status="offline"` while leaving a STALE world in `location` (VRChat often updates only one field) kept them counted → the "friends online way higher than actual, reopening fixes it" bug. **The confirmed `friend-offline` handler now marks the cache entry `status="offline"/location="offline"`** (after the flap cooldown) — previously the entry kept its stale online status forever, which would have frozen the count. **The in-app alerts header gains filter chips** (All / Friends / Groups / Bio — `alertMatchesFilter` keyed on alert-group-id prefixes: `announcement_`/`event_` = Groups, `bio_` = Bio, rest = Friends). **The Discord RPC card and the duplicated About/trust card are DELETED from this tab** — full Discord management (status line, enable `ToggleRow`, sign-in/sign-in-again, Disconnect, risk-consent dialog, `DiscordLoginWebView` dialog) moved into Settings → Accounts' Discord row (`VrcaSettingsPage.AccountsSection`). Tab order: identity header → status banner → alerts. **Post-Phase-7 fixes (user feedback round)**: (1) **Real-time friends count** — `publishFriendsOnline()` counts a friend online when their `status` OR `location` is non-blank/non-"offline"; the WS `friend-online`/`friend-location` payloads don't always carry `user.status` (the user object is optional), so the old status-only predicate never moved the count on live events — it only changed on the REST friends reload at reconnect ("count only updates when I reopen the app"). (2) **Banner ≠ pfp**: `VrcUserPresence.profilePicUrl` is now the round `userIcon` and the new `bannerUrl` carries `profilePicOverride` (the wide profile banner); the identity card renders the banner as the CARD BACKGROUND (session-authed `AsyncImage` + black vertical-gradient scrim 0.45→0.72; the trailing View-Profile/Sign-out IconButtons get a 0.40-alpha black circle backing when a banner is present). No banner (no VRChat+) → plain card. The admin-side `fetchProfilePicUrl`/`refreshProfilePic`/post-2FA pic capture all prefer `userIcon` and only fall back to `profilePicOverride` — cropping the banner into the avatar circle was the "banner used as pfp" bug. (3) **Trust rank chip follows VRChat's real names+colors** (`trustBadge()`; VRChat's tag names are OFFSET from the displayed ranks, same mapping as VRCX): basic→"New User" blue 0xFF1778FF, known→"User" green 0xFF2BCF5C, trusted→"Known User" orange 0xFFFF7B42, veteran→"Trusted User" purple (lightened 0xFFB18FE4 for dark-bg readability), legend→"Veteran" yellow 0xFFFFD000, unknown→"Visitor" grey; "Legendary" deliberately excluded (it does not exist). Badge container = `color.copy(alpha=0.22f)` with colored label text. `VrchatPipelineService.prettyTrustRank` (rank-change notifications) uses the same corrected mapping. (4) **Notifications section defaults EXPANDED**; its expand state lives in the process-scoped `AlertSectionState.expanded` (same pattern as `StatusBannerState`) — persists across tab switches, resets to expanded only on a fresh process; a headless OEM revival has no UI on screen, so the reset is only ever visible on a genuine reopen, which is exactly the intended behavior.
- **Login polish (Phase 7 same pass, `VrchatLoginScreen.kt`)**: the **VRChat platform-status warning** ("VRChat is having platform issues — login may fail, it's not you") now lives INSIDE `VrchatLoginScreen` (one-shot `fetchVrchatStatusWarning()` on entry + **re-fetch on a FAILED login attempt**), so ALL login surfaces get it — Settings takeover, VRChat-tab takeover, AND onboarding (whose own duplicate warning in `StepVrchatLogin` was removed since it embeds this screen). Explainer tightened to 2 lines; the 2FA step (both email + authenticator variants) adds "VRChat will remember this device — you won't be asked again." Resend-cooldown + segmented digit boxes (doc nice-to-haves) deliberately not built.
- **Media (Phase 6 revamp, `NowPlayingPage.kt`)**: Notification Access shrank to a **one-line error-tinted warning row shown ONLY while the permission is actually missing** (tap → settings; onboarding + Settings own the permission otherwise). **The gate is the REAL granted-permission check `NotificationManagerCompat.getEnabledListenerPackages(ctx).contains(ctx.packageName)` (re-checked on `ON_RESUME`), NOT `vm.listenerConnected`** — that flag only flips true once the OS (re)binds the `NotificationListenerService` and pushes a state update, so after a process restart it sits false even though access IS granted, which made this warning (and the Home `SetupHealthCard`'s `notifOk`, likewise switched to the real check) falsely persist while music was clearly being detected. A proper **Now Playing `CompactSectionCard`** (default expanded): summary = "Title — Artist · Source", trailing state chip (Idle/Source off/Ad/LIVE/Playing/Paused — `nowPlayingIsAd`/`nowPlayingIsLive` are now public read-only on the VM), expanded shows the **source app's launcher icon** (`rememberAppIcon` via PackageManager `getApplicationIcon(pkg).toBitmap()` — no network/deps, MusicNote fallback). **Package-visibility requirement (Android 11+/API 30+)**: `getApplicationIcon()` throws `NameNotFoundException` for a package the app can't SEE, and the app only gains *implicit* visibility to a media app while mid-interaction with its `MediaSession` — so the icon resolved only intermittently and otherwise fell back to the generic glyph (the "very inconsistent icon" bug). Fixed by a `<queries>` block in `AndroidManifest.xml` declaring the three known media packages (`com.spotify.music`, `com.google.android.youtube`, `com.google.android.apps.youtube.music`) PLUS an `<intent>` for `android.media.browse.MediaBrowserService` so arbitrary media apps ("other media apps are always allowed") resolve too. `rememberAppIcon` keys its `remember` on `pkg`, so with visibility now deterministic the single lookup succeeds and caches. Also: track + artist + source label, the **EXACT rendered chatbox lines** verbatim in monospace (new `vm.currentMusicChatboxLines()` accessor over the private `buildNowPlayingLines()`; re-read keyed on the page's 60ms `previewT` ticker so the bar/time advance), and **per-source enable rows** (Spotify / YouTube / YouTube Music). **Per-source enables**: `mediaSourceSpotify/Youtube/YtMusic` VM flags (default ON) ← DataStore keys `media_source_spotify/youtube/ytmusic` (`UserPreferencesRepository`), set via `vm.setMediaSourceFlag(pkg, enabled)` (package constants `VrcaViewModel.PKG_SPOTIFY/PKG_YOUTUBE/PKG_YTMUSIC`). The gate lives in `buildNowPlayingLines()`: `if (nowPlayingDetected && !isActiveMediaSourceEnabled()) return emptyList()` — a disabled source renders NOTHING in chatbox/preview while detection keeps running (re-enabling resumes instantly); unlisted media apps are always allowed. Local-only prefs (no Firestore sync, not in the feature-session restore). Progress-bar presets are **half-height `PresetRow`s** (name labelMedium + glyph on one line, 6dp vertical padding, highlight = selection, NO "Selected" label) inside a `CompactSectionCard` whose summary is the current preset name. The old "Detected / Preview" debug card is GONE — its raw Title/Artist lines joined the existing Listener block in Settings → Debug. **Post-Phase-6 fixes**: the Now Playing card AND the Progress-bar-style card both **default COLLAPSED and do NOT persist expansion** (`CompactSectionCard persistExpansion = false` — plain `remember` instead of `rememberSaveable`, so leaving the tab snaps them shut on return; per user request). The Progress-bar-style card gains a **"Show progress bar" `ToggleRow`** (`vm.musicShowProgress` / `setMusicShowProgressFlag` ← DataStore `music_show_progress`, default ON): OFF makes `buildNowPlayingLines()` return ONLY the title line (ads render just "Ad", the `● LIVE` marker is kept — it isn't a progress bar); the card summary reads "Hidden — title only" while off. **Ads NEVER lose the progress bar**: when the player reports no ad duration (some audio ads), the ad branch renders a static zero-position bar (no time suffix) instead of dropping the bar entirely (the old fallback). **"Filter Titles" `ToggleRow`** (Now Playing card → "Formatting" section, `vm.musicCleanTitles` / `setMusicCleanTitlesFlag` ← DataStore `music_clean_titles`, **default ON**, local-only, not synced): controls the `TitleCleaner` cruft-stripping + artist-dedup + one-line fitting. ON = `TitleCleaner.fitOneLine(...)` (the full ladder below). OFF = `TitleCleaner.rawOneLine(rawTitle, rawArtist, VRC_MAX_CHARS)` — combines `"artist — title"` verbatim with NO cleaning/fitting, only a hard 144-char cap so the chatbox protocol limit can't be exceeded; the line is free to WRAP in-game (the user opted out of shortening). Applies to BOTH the main music line and the `● LIVE` stream line. The char meter / preview reflect the chosen mode since both read `buildNowPlayingLines()` through the same gate. **Quest Notification Access (now-playing DOES work on the headset — same `NotificationListenerService`+`MediaSessionManager.getActiveSessions` VRC-NEXUS uses; only the grant screen differs)**: `VrcaViewModel.notificationAccessIntent()` opens the notification-listener LIST intent (`ACTION_NOTIFICATION_LISTENER_SETTINGS`) but **deep-links to VRC-A's own row via the Android Settings fragment-args extras** (`:settings:fragment_args_key` = the flattened `NowPlayingListenerService` component + `:settings:show_fragment_args` bundle) — this is EXACTLY what VRC-NEXUS does (confirmed in its dex), and on Quest it lands on VRC-A's per-app "Device & app notifications" page with the Allow toggle. The plain list intent AND the `_DETAIL_SETTINGS` intent both fail to land right on Horizon OS; the fragment-args deep-link is the one that works (harmless on phones — just highlights the row). A **"Notification access" row is (re)added to Settings → Permissions on EVERY build** (`listenerGranted` check + this intent); metadata + ad detection are unchanged (shared `NowPlayingListenerService`).
- **Feature toggles live ONLY in Home's Quick Toggles.** The redundant per-feature enable switches were removed from the detail pages: the Pinned toggle and Cycle toggle in the Automations tab (`AutomationsPage.kt`), and the "Enable Now Playing block" toggle + the "Demo mode (testing)" toggle in the Media tab (`NowPlayingPage.kt`). The "Music demo mode" toggle in Settings → Interface and the demo toggle in the legacy `NowPlayingPage` (`LegacyPagesAndSettingsPage.kt`) were also removed. The `vm.setAfk/Cycle/SpotifyEnabledFlag` functions stay (driven by Quick Toggles); `spotifyDemoEnabled`/`setSpotifyDemoFlag` remain defined but now have no UI entry point (demo-track logic is effectively dormant). `NowPlayingPage` no longer takes `onPersistSpotifyEnabled`/`onPersistSpotifyDemo` params.
- Settings: Full page accessed via gear icon in top app bar. Contains Permissions, About, Help, and collapsible Debug section
- Admin: Full page accessed via gavel icon in top app bar (admin build only). Includes targeted APK push per user and release retraction.

### Admin UI (Phase 6 redesign)
The admin panel shares one visual language defined in **`AdminUiKit.kt`** — all admin surfaces compose from these primitives so density + clarity stay consistent:
- `AdminTone` (Neutral/Primary/Success/Warn/Error/Info) + `toneColors(tone)` map a semantic tone to M3 container/on/accent colors.
- `AdminSectionCard(title, icon, tone, trailing) { content }` — an `ElevatedCard` with an icon-in-tinted-circle header. `AdminCardHeader(...)` is the standalone header for use inside an existing card/column (used to re-skin the detail sections without restructuring their bodies).
- `AdminStatTile(label, value, icon, tone)` — big-number dashboard tile.
- `StatusPill(text, tone)` — rounded status chip (replaces raw `Badge`s for ONLINE/VRC/BAN/WARN).
- `AdminLabeledRow(label, value, mono, labelWidth)` — label→value row (ids monospaced).
- `AdminAvatar(name, online, size)` — a first-letter **initial circle** plus an online/offline status dot. **Profile pictures are NOT shown in the admin UI at all** (final design): they were never stored in Firestore (deliberate cost decision), and the on-demand VRChat+ fetch through the admin's own session (`VrchatAuthManager.fetchProfilePicUrl(userId)` + Coil via `VrchatImageLoader`) **never worked reliably in practice and was removed entirely per the user** — `fetchProfilePicUrl` and its per-id cache are deleted from `VrchatAuthManager`, and `AdminAvatar` no longer takes a `vrchatUserId` param. Do NOT re-add admin profile pictures. **Discord profile pictures are likewise not shown** — the admin app has no access to another person's Discord account, so the only source would have been a stored `discordAvatarUrl` (removed long ago), and Discord is not auth-fetchable by userId.

**Profile-picture data flow (NONE — admin shows initials only)**: Profile pictures are deliberately **never stored in or read from Firestore** (cost), and since the on-demand admin-session fetch was removed (it didn't work), the admin UI renders **name initials only**. **No `vrchatProfilePic` or `discordAvatarUrl` fields are written anywhere** — removed from `VrcaViewModel.buildUserSnapshot()` + `captureStateForSync()`, from `VrchatPipelineService.onOpen` (the one-shot prompt write) and `syncPresenceToFirestore()`, and from `DiscordRpcService` (the avatar capture + prompt write are gone). `UserRow`/`UserDetail`/`parseUserRow` no longer carry avatar URL fields. (`firestore.rules` `selfMutableKeys()` still *permits* `vrchatProfilePic`/`discordAvatarUrl` for backward-compat with older public clients that may still write them — the keys are simply ignored on read; nothing in the current app writes them. `currentAvatarThumbnailImageUrl` / `vrchatAvatarThumb`, the *worn avatar's* thumbnail, is unrelated and still synced for friend-change notifications.) The `VrchatAuthManager.refreshProfilePic()` / `getStoredProfilePic()` helpers remain but are no longer wired to any write. **`VrchatImageLoader` (`com.vrca.admin`, Coil + session-cookie interceptor) still exists** but its only consumer is the PUBLIC build's VRChat tab (the user's OWN avatar + profile banner, authorised by that device's own session — which works).

**Dashboard** (`DashboardTab.kt`): a 2×2 stat grid (Users/Online/Warned/Banned via `AdminStatTile`) plus a **unified "Recent activity" feed** (`ActivityEvent` list) that replaced the old redundant "Live overview" count hero. The feed merges (sorted by time, capped 40) from: **moderationEvents** (ban/unban/warn/remove_warn/**ban_evasion_detected**), **announcements** posted (live/draft), and **VRChat platform status** incidents (`status.vrchat.com/api/v2/summary.json` via OkHttp — free, not Firestore). `ActivityEvent` carries a serializable `ActivityKind` enum (icon + tone derived at render time) so the feed can be persisted. **Read cost is near-zero by design — `DashboardFeedCache` persists the feed to the admin device** (SharedPreferences `vrca_admin_dashboard`, JSON) AND in a process-level cache: (1) re-entering the tab within `FEED_CACHE_TTL_MS` (60s) costs **0 reads**; (2) otherwise the fetch is **incremental** — `whereGreaterThan("createdAt", cursor)` per source, so it reads only genuinely-new docs (≈1 read per source = ~2 total when nothing's new, Firestore's per-query minimum). The full ~33-doc read happens **once** (first run), then the cursors (`newestModMs`/`newestAnnMs`) and cached events persist across app restarts. The refresh button (`feedTick`) forces an incremental fetch. Each row is an iconed circle + title + subtitle + compact relative time.
**Directory** (`UsersTab.kt` list): rows show `AdminAvatar` (initials) + name/secondary + world + a pill row (ONLINE/VRC/BAN/WARN), the user's **app version** (`versionName`, shown as muted `labelSmall` next to the pills — written by the public app as `BuildConfig.VERSION_NAME`. **`versionName`/`versionCode` ride `captureStateForSync()` (hourly/edit delta) AND `buildLivePayload()` (watched 10s loop)** so the admin's version column updates reliably after an app UPDATE — previously they were written only by `buildUserSnapshot()` + the bootstrap cold-boot `safeUser` write (which runs on a clean swipe-reopen/fresh install and is throttled 20 min), so a user who only warm-resumed could show a STALE version indefinitely. Now it self-corrects within an hour (or ~10s if watched); zero extra Firestore writes (piggybacks on writes that already fire) and carried on `UserRow`/`parseUserRow`/`UsersDirectoryCache`), and relative `lastActiveAt`. Header has search, warned/banned filters, count, refresh, +500 paging.
**Detail — remote OSC Start/Stop + sending/instance-type indicators (`UsersTab.kt`, zero bonus reads/writes)**: the detail's "Live Chatbox Output" card shows a **SENDING / IDLE `StatusPill`** in its header = the user's master OSC Start/Stop gate (`oscSending`, whether the chatbox is ACTUALLY transmitting — distinct from the per-feature Pinned/Cycle/Music/Time chips, which only say what's configured), plus a **single full-width Start/Stop toggle button** (like Home — red "Stop sending" while sending, filled "Start sending" while idle; device-only). **Optimistic UI (`optimisticSending`)**: the pill AND the button flip INSTANTLY on press (the user's app applies the command instantly too) instead of waiting on the sync; the override then reconciles to the AUTHORITATIVE synced `oscSending` — it's cleared when the synced value catches up to the command (success) OR after a 30s timeout (command didn't land → self-heals back to the real value). Because `oscSending` rides the watched 10s loop / 30s edit debounce / hourly delta, a genuine desync always corrects on the user's next liveness write. Cost model: `oscSending` is synced by piggybacking on writes that already fire — added to `VrcaViewModel.captureStateForSync()` (hourly delta) AND `buildLivePayload()` (the watched 10s loop, so it's live in the detail view within 10s) — it is display-only, NOT in any apply path (`applyOfflineToggleEdits`/`applyRemoteConfig` never touch it), so it never clobbers local runtime state; the admin reads it via the existing 10s detail poll → **zero bonus reads/writes**. Remote Start/Stop is the ONLY cost: **one write per press** — a single-doc `oscCommand`(`"start"`/`"stop"`)+`oscCommandAt`(serverTimestamp) to THIS device's doc (device-only, NOT `applyAccountWide`), read by the user's EXISTING moderation snapshot listener (`lastOscCommandMs` dedup, fresh <60s) → calls `startSending()`/`stopSending()`. **Clock-skew tolerance (`CMD_SKEW_TOLERANCE_MS` = 2 min)**: the freshness gate compares the SERVER `oscCommandAt` timestamp to the device's local wall clock; a device clock lagging the server made `(now - ms)` NEGATIVE on the admin's write snapshot, so the old `in 0L..60_000L` gate SKIPPED the command and it only applied on the NEXT 10-30s poll once the clock advanced (the tester-found "Start/Stop only happens on their 10-30s poll sync" bug). The gate is now `in -CMD_SKEW_TOLERANCE_MS..60_000L` so a skewed-but-fresh command applies on the FIRST snapshot. The SAME negative-lower-bound tolerance was applied to the `killSignal`, `logoutVrchatAt`, and `logoutDiscordAt` gates (identical latent bug). An **instance-type chip** (Public/Friends/Friends+/Group/Invite/Invite+ via `StatusPill`) sits next to the `📍 world (N/cap)` line, derived locally by `instanceTypeLabel(vrchatLocation)` from the already-synced/already-read location string (`~friends(`/`~hidden(`/`~group(`/`~private(`/`~canRequestInvite` markers, same logic as the public `parseInstanceType`) — no VRChat call, no reads/writes; hidden when the location is blank/redacted ("private"). `firestore.rules`: `oscSending` in `selfMutableKeys()`, `oscCommandAt`/`oscCommand` in `ownerOnlyUserKeys()`. `UserDetail` carries `oscSending`.
**Detail** (`UsersTab.kt` detail): **revamped** — a "Back to directory" row above an identity card with a large (60dp) `AdminAvatar`, prominent name + secondary name, status pills, optional world line, an `AdminLabeledRow` facts block (VRChat id / authUid / device / **a single "last seen"** liveness field — the old separate active/updated/synced rows were collapsed into one, sourced from the live 10s detail poll), and a tidy two-up action footer (**Moderate** + **Kill App** side-by-side instead of stacked full-width). `UserDetail` carries the live liveness timestamps so they refresh during watching. Below it, `DetailBlock` renders iconed `AdminCardHeader` sections: VRChat presence, Live Chatbox Output + remote toggles, Pinned message + presets, Cycle (interval editor in the header trailing slot), App & Updates + targeted APK push, and moderation flags. All prior admin capabilities are preserved — presentation-only over the same action logic.
**Moderation / Announcements / Log tabs**: re-skinned with `AdminCardHeader` (Gavel/Warning/Block/History/Campaign) and `StatusPill`s. Moderation's target card shows an `AdminAvatar` + BANNED/WARNED/DEVICE BAN/CLEAN pills and `AdminLabeledRow` ids. Announcements rows show LIVE/DRAFT + priority pills.

**Public-build Discord button** (`VrcaScreen.kt`): on the **public** build the top-bar navigation slot (where the admin Gavel sits on the admin build) shows a button with the **real Discord logo** (`res/drawable/ic_discord.xml`, the official Clyde mark as a tinted `VectorDrawable` rendered via `painterResource` — Material Icons has no brand logos, so the old `Icons.Filled.Forum` speech-bubble was a placeholder) when `config/app.discordInvite` is a non-blank link — tapping opens the invite externally. The admin sets the link in `ConfigTab` ("Discord invite link" field, saved to `config/app.discordInvite`). Blank link → button hidden. **The invite value rides `VrcaApp`'s single `config/app` snapshot listener** (the SAME listener that reads the ToS `tosVersion`/`tosText`/`tosUrl`) and is passed into `VrcaScreen(discordInvite=…)` — the old separate one-shot `get()` in `VrcaScreen` was deleted, so the config doc is read ONCE, and the button is now LIVE (an admin editing the link updates it without a relaunch).

**Live-while-open reads (foreground-scoped listeners)**: the design rule for admin-pushed content is a snapshot listener SCOPED TO THE FOREGROUND (a Compose `DisposableEffect` — attaches while the screen is composed, detaches on background/leave via `onDispose`). Its first callback is ONE read (same as a one-shot `get()`); it bills again only when a change actually lands while you're looking, and ZERO while backgrounded — so a push fans out only to currently-OPEN users. This is CHEAPER than a process-scoped (always-on VM) listener, which stays attached backgrounded and bills reads for changes nobody's viewing. Process-scoped listeners are reserved for things that must act while the app is CLOSED (the moderation/ban/kill listener in the app-scoped VM). Applied to: **ToS + `discordInvite`** (`VrcaApp` `config/app` listener, one doc), **in-app announcements** (`VrcaScreen` — `announcements where active==true` `DisposableEffect` listener, replacing the old one-shot `get()`; the background-surviving `VrchatPipelineService.attachAnnouncementsListener` is still the source of the NOTIFICATION when off-screen), **targeted releases** (`releases/{deviceHash}` `DisposableEffect` listener, already live), and **global "release to all"** (`VrcaApp` — a `releases/latest` `DisposableEffect` listener that shows the forced `UpdateDialog` live, never DOWNGRADING a newer targeted release already shown; the 6h `startAppUpdateCheckLoop` service poll stays as the backgrounded catch-up).

### Firestore Sync Architecture
The sync model is intentionally minimal — Firestore costs money and we only push to it when we have to. Three classes of writes:

1. **App-open / cold-open write (one)**: After `initialDataLoaded` flips, `applyRemoteContentBeforeSync()` reads the Firestore doc to pick up any admin edits made while the user was offline (messages, presets, intervals — not toggles, which always start OFF), then `performSelfSync(coldOpen = true)` runs once with the merged state (`isOnlineInApp=true`, `lastActiveAt`). On first install or when `lastSyncedValues` is empty, this is a full ~40-field snapshot write (handles doc creation). On subsequent cold-opens with a persisted baseline, this is a **delta write**: only liveness fields + any content that changed since the previous session are written — unchanged presets/chatbox stay untouched. **Cold-open liveness throttle (`COLD_OPEN_LIVENESS_THROTTLE_MS` = 20 min)**: the background-survival stack (`START_STICKY`, the ~15-min `PipelineWatchdogWorker`, `BootReceiver`, OEM killers) resurrects the process repeatedly, and EACH resurrection re-ran this cold-open delta write (4 liveness fields) plus a self-listener echo read — that per-restart cost × frequent kills was the dominant *"steady Firestore reads/writes even with no admin browsing"* source (each cold start also re-attaches ~4-6 snapshot listeners, whose initial reads stack into the periodic read spikes). The cold-open write is now SKIPPED when (a) it's `coldOpen`, (b) the delta is liveness-ONLY (no content changed — gated on `LIVENESS_KEYS`), and (c) the last *actual* write (`PREF_LAST_SELF_SYNC_MS`, persisted via `markSelfSyncWritten()`) was within the throttle window. The still-fresh `lastActiveAt` already proves online, so status is unaffected; the **hourly heartbeat is anchored to the last real write** (first tick delay = `HOURLY - sinceLastWrite`, floored 60s) so a liveness write still lands within the 65-min staleness window even when the cold-open is skipped. A 15-min churn cadence drops from ~4 writes/hr to ~2/hr; a pathological fast (<20 min) restart loop writes at most once per ~20-40 min instead of once per restart. The first-ever sync and any content delta are NEVER throttled. **`clearSwipedAway` (called from `MainActivity.onCreate` on a real user reopen) also clears `PREF_LAST_SELF_SYNC_MS`** so the throttle can never suppress the cold-open write after a swipe — the stale `offlineAt` on the doc MUST be overridden by a fresh `lastActiveAt` or the user stays offline in the directory.
2. **Hourly liveness + delta write (periodic, the ONLY steady-state periodic write)**: `VrcaViewModel.startHourlyHeartbeat()` fires once per hour after the cold-open write. Each tick calls `performSelfSync()` which always writes liveness (`lastActiveAt`, `isOnlineInApp`, `lastSeenAt`, `updatedAt`) and includes only content fields that changed since the last write (field-by-field delta comparison against `lastSyncedValues`). **The delta map (`captureStateForSync`) also carries the volatile preview/nowPlaying fields and the VRChat presence snapshot** (see schema notes above), so an unwatched user's directory row (preview text, current track, VRChat location) refreshes at hourly granularity for free — same single write, more fields. If nothing changed, only the 4 liveness fields are written — a single minimal merge. It is an **in-process coroutine**, NOT an AlarmManager wakeup: a swiped/OS-killed app stops firing it → goes stale → correctly counted offline.
3. **App-close write (one)**: `VrchatPipelineService.onTaskRemoved` / `AppShutdown` writes `isOnlineInApp=false, offlineAt, lastSeenAt` plus current VRChat presence fields if available. `VrcaViewModel.onCleared` writes `isOnlineInApp=false, offlineAt, lastSeenAt` via `GlobalScope` as a fallback for cases where the foreground service isn't running. The `offlineAt` server timestamp is the authoritative clean-shutdown marker the admin reads for instant swipe→offline (see "Admin online detection").
4. **Edits use a 30s idle-debounce write**: changing a toggle/message/preset persists to local DataStore immediately (as always) AND schedules ONE delta write 30s after the last edit via `startSelfSyncLoopIfNeeded()` (`SELF_SYNC_DEBOUNCE_MS = 30_000`). Each edit cancels+reschedules, so a burst of edits flushes exactly once. (This was briefly a true no-op during the Phase-1 cost reduction, but that made "edit a preset then close the app" revert on reopen — the app-open read overwrote the not-yet-synced edit — so the debounced write was restored in commit `e21103e`. `performSelfSync` writes only changed content + liveness, so an idle user who isn't editing schedules nothing. The call sites fire ONLY on genuine content/toggle changes, never on NowPlaying ticks or preview rebuilds.) Still pushed on the next hourly tick too (or instantly via the 10s live loop if an admin is watching). **Note: this means an actively-editing user, or a content change pulled in by cross-device sync, costs one write per 30s-quiet-period — it is NOT zero per-edit.**

**Hourly liveness model (Phase 1)**: Liveness is determined by `lastActiveAt`, written once per hour by `VrcaViewModel.startHourlyHeartbeat()` (anchored to cold-open) and every 10s while an admin watches this user. Admin online = `lastActiveAt` within ~65 min (or ~25s for a watched user). `lastSeenAt` is written as a legacy mirror (same timestamp) so older admin builds keep working. **Removed in Phase 1**: the 30s browse-gated heartbeat, the 5-min always-on heartbeat, the per-edit debounced self-sync, and the browse-volatile 30s loop — none of these write to Firestore anymore. An admin merely browsing the directory causes **zero** per-user writes; only opening a specific user's detail (which flips `AdminWatchState.isWatched`) starts that one user's 10s live loop.

**Browse heartbeat (FULLY REMOVED)**: Previously, when an admin was on the Dashboard/Users tab the admin app wrote `config/adminPresence.browsingAt` every 30s and public apps ran a 30s `lastSeenAt` heartbeat while that was fresh. Phase 1 made the public-side write a no-op but the admin still WROTE `browsingAt` and every public app still held a `config/adminPresence` snapshot listener feeding `AdminBrowsingState`. That listener was the single biggest admin-side read cost: every 30s `browsingAt` write fanned a billed read out to EVERY online user's listener (~120k reads/hour at 1k users) for ZERO benefit (nothing reads `AdminBrowsingState` to drive a write under the hourly model — `startBrowseVolatileSyncWatcher` is empty). Now **both sides are gone**: `AdminRuntime` no longer writes `browsingAt` (its loop only runs the local force-kill staleness sweep), and `VrchatPipelineService` no longer attaches the `config/adminPresence` listener. (`AdminBrowsingState` remains as dead code, unfed.)

**Slow always-on lastSeenAt heartbeat (REMOVED in Phase 1 — historical)**: The service used to write `lastSeenAt` every 5 minutes (`LASTSEEN_HEARTBEAT_MS`) while alive (~288 writes/user/day). This is replaced by the once-per-hour `lastActiveAt` write (~24 writes/user/day) owned by the ViewModel. The constant and `writeHeartbeat()` are deleted.

**Live-mode (watcher-gated)**: When an admin opens a specific user's detail page in the admin panel, the admin app refreshes `watcherActiveAt` every ~30s on that user's doc. The user app's snapshot listener feeds the timestamp into `AdminWatchState`; if fresh (within 60s), `isWatched` flips to true and two side-effects start:
- `VrcaViewModel.startLiveSyncWatcher` writes `buildLivePayload` (nowPlaying, preview, the feature toggles `afkEnabled`/`cycleEnabled`/`spotifyEnabled`/`timeEnabled`, `lastReportedTime`, `lastActiveAt`, `lastSeenAt`) every 10s (`LIVE_SYNC_INTERVAL_MS`). **`lastActiveAt` is included** so a watched user's liveness stays fresh in the admin directory (not just the detail view) — previously only `lastSeenAt` was written, and since `isUserOnline` only falls back to `lastSeenAt` when `lastActiveAt` is null, the directory could show a watched user as offline. **The toggles stream here so the admin detail's Pinned/Cycle/Music/Time button states reflect a user's own toggle change within 10s while watching** (they also persist via the hourly delta + the 30s edit debounce — `captureStateForSync` carries them — so an unwatched toggle surfaces on the next hourly write)
- `VrchatPipelineService.startPresenceRefreshLoop` writes VRChat presence every 10s

`VrcaViewModel.startBrowseVolatileSyncWatcher` is now a **no-op** (Phase 1): an admin merely browsing the directory no longer causes this user to emit any writes. The directory is populated from the admin's own one-shot fetch; only opening this user's detail (which flips `isWatched`) starts the 10s live loop.

The live loop stops instantly when `isWatched` flips back to false (`collectLatest` cancels the inner loop). When nobody is watching, neither loop touches Firestore — the only periodic write is the hourly `lastActiveAt`.

**Force-kill detection (hourly model)**: If the user's app is force-killed, neither `onTaskRemoved` nor `onCleared` fires, so `isOnlineInApp` stays `true` in Firestore — but the in-process hourly loop stops, so `lastActiveAt` goes stale. The admin determines offline by `lastActiveAt` age (>~65 min, or ~25s for a currently-watched user whose 10s loop stopped). The admin-side staleness sweep (`AdminRuntime`) keys on `lastActiveMs` (canonical `lastActiveAt`, falling back to `lastSeenAt`) with `ONLINE_STALENESS_WINDOW_MS = 65 min` — past the 60-min hourly heartbeat with 5 min of grace, so a live unwatched user is never false-flagged. When an admin is browsing and a user's `lastActiveAt` exceeds the window, the sweep writes `isOnlineInApp=false` to that user's doc.

**Admin kill switch**: Admin force-quits a user's app via "Kill App" (UsersTab) → account-wide batch writes **`killSignal` + `offlineAt` + `isOnlineInApp=false`** (all `serverTimestamp()`/false in the SAME `applyAccountWide` write — one write, no extra cost). The user's moderation listener reads `killSignal` and calls **`AppShutdown.killNowNoWrite(app)`** — NOT a bare `Process.killProcess`. A bare killProcess only BOUNCED the app: the four `START_STICKY` services + the `PipelineWatchdogWorker` promptly revived it. `killNowNoWrite` mirrors the swipe shutdown's guards (stamps `manual_kill_at` + `swiped_away`, cancels the watchdog, disarms `FeatureSessionStore`, stops the services, then `killProcess`+`exitProcess`) so the process STAYS dead — and the dying app still writes **zero** Firestore (the offline marker is written by the ADMIN in the kill batch, which is guaranteed to land, instead of relying on the dying process). A real user reopen clears the swipe flag (MainActivity) so the app works normally again — a kill is a force-quit, not a ban.
  - **One-shot per kill (the "reopen closes again a few times" fix)**: `killSignal` stays fresh (<60s) and is NEVER cleared, and the old `killSignalHandled` guard is IN-MEMORY (resets each process) — so a reopen within 60s re-read the same fresh signal and re-killed, bouncing repeatedly until it aged out. `handleAdminKill(killMs)` now persists **`last_handled_kill_ms`** (`vrca_remote` prefs, `commit()`) and only kills when `killMs > last_handled` (plus the ≤60s age window that blocks an ANCIENT signal on reinstall). A reopen sees the same `killMs` → skips; a genuinely NEW kill (newer serverTimestamp) still fires.
  - **Prompt + durable offline with a 7s reopen grace**: the kill batch's **`offlineAt`** is the durable marker — `isUserOnline`'s `offlineAt > lastActiveAt` flips the row offline immediately and keeps it there until the user actually reopens with a newer `lastActiveAt` (works whether or not the admin is watching; the incremental directory query already keys on `offlineAt`, and the 10s detail poll reads it, so zero extra reads). The **`killSignal`-scoped grace** (`KILL_GRACE_MS = 7s`) adds `if (killSignal within 7s of now) → offline` to `isUserOnline`, so a reopen within 7s can't instantly bounce the row back online (kill-scoped, so a normal swipe→instant-reopen still shows online right away). `killSignal` is carried on `UserRow`/`UserDetail`/`parseUserRow`/`UsersDirectoryCache`/the detail merge/`onUpdateUserRow` (all from the already-read snapshot — no new field written by the client, no extra reads). **Residual edge** (no worse than a normal OS force-kill): if the admin kills in the exact sub-second the user is cold-opening AND the user never reopens AND nobody's watching, the racing `lastActiveAt` can read newer than `offlineAt` → shows online until the 65-min staleness decay (or ~45s watched-stale). Deliberately NOT closing this with a tolerance on `offlineAt > lastActiveAt` — any tolerance wide enough to catch the race also risks pinning a genuine quick-reopen offline until the next hourly write.

**Single-session HARD-DENY (multi-device, same VRChat account) — replaced the old take-over**: only ONE device may use VRC-A per VRChat account. The lock doc `accounts/{vrchatUserId}` = `{ activeDevice, activeSince }` is now enforced as a **hard deny** (the previous newest-wins "take-over" + "Use here" reclaim was REMOVED). `VrcaViewModel.startAccountLockWatcher()` (re-attached per login via `presenceFlow`) reads/listens the lock: when it's free/ours → **claim** it (`claimAccount`, write `activeDevice=myHash`); when another device holds it → **`accountDenied=true`** → OSC blocked at the shared chokepoint (`refreshOscBlockGate` ORs it with `vrchatLoggedOut`/`forceUpdatePending`/`notActiveDevice→accountDenied`), Discord RPC disconnected locally (`disconnectDiscordLocally`: clears `discord_rpc_enabled`/`discord_session_seeded` + wipes WebView cookies + stops the service so it can't auto-restart), and `VrcaScreen` shows **`AccountDeniedScreen`** — a reassuring "account already registered, contact support" message (NOT scary "signed in on another device" wording) with a **"Join the support server"** button (the `config/app.discordInvite` link). The VRChat session + the lock LISTENER stay attached, so recovery is **automatic with no reopen**: when the holder signs out or an admin frees the lock (next snapshot absent), this device claims it and clears the deny. **Claiming now happens on EVERY login** (not just multi-device) so the next device sees a lock to be denied by — single-device users write one lock doc on login (the old "single-device never touches accounts" optimization is gone, traded for the deny guarantee). **Escape hatches (the only two)**: (1) the holder **properly signs out** of VRChat → `loggedOutSignal` → `releaseAccountLock()` deletes `accounts/{vrchatUserId}`; (2) **admin remote-logout** (below). A swipe/uninstall/reinstall leaves a stale lock → denied on the new device until one of those frees it (deliberate friction, per the user). `applyCrossDeviceSync` no longer touches the lock (content-pull only). `firestore.rules`: `accounts` `delete` is now `isOwner() || signedIn()` (a device frees its own lock on sign-out; same bounded threat as create/update — needs the high-entropy `usr_` id, only ever frees the account for its real owner).

**Admin remote-logout (VRChat + Discord, the deny escape hatch)**: UsersTab detail has **"Log out VRChat"** and **"Log out Discord"** buttons (account-wide via `AccountModeration.applyAccountWide`). VRChat: writes `logoutVrchatAt = serverTimestamp()` to every device doc on the account AND **deletes `accounts/{vrchatUserId}`** (frees the lock so the user can sign in on a new device — works even if the old device is offline). Discord: writes `logoutDiscordAt`. The user's moderation listener reads both (fresh within 60s, once per timestamp via `lastVrchatLogoutMs`/`lastDiscordLogoutMs`): `logoutVrchatAt` → `handleAdminVrchatLogout()` (`VrchatAuthManager.logout` clears the session INCLUDING saved credentials so auto-relogin can't revive it → `loggedOutSignal` → OSC gate + `releaseAccountLock` + pipeline `ACTION_STOP`); `logoutDiscordAt` → `disconnectDiscordLocally()`. Both keys are in `firestore.rules` `ownerOnlyUserKeys()` (admin-only write).

**Admin-mediated "Move VRChat login to another device" (end-to-end encrypted handoff)**: for the "user is still authed on their phone but forgot their password so they can't sign in on the headset" case. The admin (UsersTab detail → **"Move VRChat login to another device"**, shown only when the source has a `vrchatUserId`) picks a TARGET device (`TransferTargetPicker` over the raw `users` list, so a not-logged-in headset install — its own row, blank `vrchatUserId` — is selectable) and `AuthTransferCoordinator` (`com.vrca.admin`, process-lifetime scope + a `state` StateFlow the detail card observes) orchestrates a relay in which **the admin only ever sees ciphertext — never the credentials**. Flow: (1) admin writes a `transferRole="target"` intent to the target doc → the target's moderation listener (`VrcaViewModel.handleTransferSignal`, gated on a fresh `transferAt`, 5-min `TRANSFER_WINDOW_MS`) generates a one-time **RSA-2048 keypair** (`AuthTransferCrypto`, JCA — RSA-OAEP-SHA256 wrapping a random AES-256-GCM content key), stashes the private half transiently (`VrchatAuthManager.stashTransferPrivateKey`, in the encrypted prefs keyed by reqId), and publishes only its **public** key (`transferPubKeyOut`); (2) admin relays that pubkey to the SOURCE as a `transferRole="source"` intent → the source builds a credential bundle (`VrchatAuthManager.exportSessionBundle`: username+password+`twoFactorAuth` cookie+uid; **returns null → "no_saved_password" error** when the phone has no stored password, which the target needs since the auth cookie is IP-bound and dead on a different device), **seals it to the target's public key** (`AuthTransferCrypto.seal`), and writes the ciphertext (`transferPayloadOut`); (3) admin relays the sealed payload to the target → it opens with its private key (`AuthTransferCrypto.open`), imports via `VrchatAuthManager.importSessionBundle` (seeds creds+2FA cookie, then a Basic-auth `login()` — VRChat recognizes the **trusted-device cookie and skips 2FA**, minting a fresh IP-bound auth cookie; returns true **only** on a real login success), starts the pipeline headless-safely, clears its private key, and reports `transferStatus="imported"`; (4) on success the coordinator **signs the SOURCE out** (`applyAccountWide(logoutVrchatAt)` + deletes `accounts/{uid}`) so the single-session lock frees and the target's account-lock watcher claims it — making this a true **move** (only one device can hold the login). Per-phase in-memory dedup (`transferKeygenReq`/`transferSealReq`/`transferImportReq`); every device write reports status/error back so `AuthTransferCoordinator.awaitStep` (one snapshot listener per phase, 45s timeout, keyed on `transferAckReqId == reqId`) surfaces friendly errors (a target that still needs a 2FA code → `login_failed`, so **nothing changes on the source** — the transfer only completes when the target actually signs in). **If the target's login fails the source is never logged out**, so a bad transfer is a no-op. New keys in `firestore.rules`: `ownerOnlyUserKeys()` gains `transferReqId`/`transferRole`/`transferAt`/`transferPubKey`/`transferPayload`/`transferPeerHash` (admin-written relay/signal — the payload is ciphertext); `selfMutableKeys()` gains `transferAckReqId`/`transferPubKeyOut`/`transferPayloadOut`/`transferStatus`/`transferError`/`transferStatusAt` (the device's own side). The self-update rule checks only `changedKeys()`, so a device writing its status while admin-written owner fields coexist on the doc is allowed. The admin build's own VM never processes transfer signals (moderation listener early-returns on `IS_ADMIN_BUILD`). **Honest caveats**: both devices must be online; the source must still hold a saved password (a session established via a path that didn't store one can't be moved); and VRChat may still demand a 2FA code if the trusted-device window lapsed (surfaced as `login_failed`, source untouched). `AuthTransferCrypto` uses `java.util.Base64` (minSdk 26) so the seal/open roundtrip is JVM-unit-testable.

**Admin watcher heartbeat**: When an admin selects a user in the admin panel's detail view, a `LaunchedEffect` writes `watcherActiveAt = serverTimestamp()` to that user's doc every 30 seconds. When the admin navigates away or selects a different user, the coroutine is cancelled automatically. The user app reads `watcherActiveAt` from its snapshot listener and feeds it into `AdminWatchState.updateFromTimestampMs()` — if within 60s, live-mode starts.

**Echo suppression** (per-field `lastSyncedValues` map): Every field (toggle or content) compares its incoming snapshot value against `lastSyncedValues` — what we last knew was on the Firestore doc. If `remote == lastSyncedValues`, the snapshot is an echo (from self-sync, watcher heartbeat, or live-mode write) and we skip. If `remote != lastSyncedValues`, an admin actually changed it and we apply. Comparing against current local state would break this: during admin watch, the heartbeat fires snapshots with the OLD field values (Firestore hasn't received the user's pending local toggle yet), so a `remote != local` test would revert whatever the user just toggled. Content fields fall back to current local state when no baseline exists yet. Both branches update `lastSyncedValues` immediately on every snapshot (whether applied or skipped), so subsequent self-sync echoes are correctly suppressed. The map is also bulk-updated after each successful `performSelfSync` write via `captureStateForSync()`. The first snapshot seeds the baseline via `seedLastSyncedFromSnapshot`. **`lastSyncedValues` is persisted to SharedPreferences** (`vrca_remote/last_synced_values_json`) as a JSON object after each successful sync so the delta writer has a baseline across cold starts — without this, every app restart would fall back to a full snapshot write.

**Admin read model (Phase 3, admin-side only)**: The admin panel uses NO live collection listeners — those re-read on every user-doc change across the whole base, the dominant admin-side Firestore cost.
- **Directory** (`AdminScreen.kt` + `UsersDirectoryCache.kt`): **locally cached, zero-read on open.** The full user list is persisted to the admin device (`UsersDirectoryCache`, SharedPreferences `vrca_admin_directory`, JSON with timestamps as epoch-millis). Opening Dashboard/Users renders the cached list **instantly with 0 reads**; online/offline is recomputed locally from each row's cached `lastActiveAt`/`offlineAt` (`isUserOnline`), so cached rows decay to offline naturally without traffic. A full `get(Source.SERVER)` pull (limit `sharedLiveLimit`, default raised **500 → 2000** so the whole base loads — `+500` can raise further) happens ONLY when there's nothing cached (first-ever load) or the admin explicitly refreshed / paged (gated by `lastFetchedKey = "$sharedLiveLimit:$refreshTick"`). **Incremental liveness refresh (reads scale with ACTIVITY, not user count)**: because the cache freezes between full fetches, the online dots would go stale on a long-lived admin process. So when the directory is shown with a *populated* cache older than `DIRECTORY_REFRESH_TTL_MS` (10 min), an **incremental** query re-reads only users whose `lastActiveAt` advanced since the last refresh: `whereGreaterThan("lastActiveAt", cursor).orderBy("lastActiveAt", DESCENDING).limit(sharedLiveLimit)`. The cursor is `lastDirectoryRefreshMs - REFRESH_SKEW_MARGIN_MS` (5 min, to absorb client/server clock skew), or `now - ONLINE_STALENESS_WINDOW_MS` on a session's first refresh (so it reads exactly the currently-online set). **Idle/offline users have a stale `lastActiveAt` that doesn't match the inequality, so they cost nothing** — their cached rows decay to offline locally for free. As users settle offline the read count DROPS toward ~1 (an empty query's minimum) instead of staying at the full cached count; the only users read are those who came online or whose **hourly heartbeat just kicked in** (which advances `lastActiveAt` past the cursor, so the next refresh re-reads them and the merge REPLACES their stale cached row). **A swipe-away does NOT advance `lastActiveAt`** — it only stamps `offlineAt` (and deliberately does NOT bump `lastSeenAt`) — so the `lastActiveAt` query MISSES a swiped user once their `lastActiveAt` drifts older than the cursor, leaving their `offlineAt` stuck out of the cache and the row falsely "online" until the ~65-min staleness decay. To fix this the incremental refresh runs a **SECOND query — `whereGreaterThan("offlineAt", cursor).orderBy("offlineAt", DESCENDING)`** — and merges its docs (de-duped by id via `associateBy`) with the `lastActiveAt` query, so a recent swipe's `offlineAt` reaches the cache and the row flips offline on the next refresh. Costs ~1 extra read per swipe (Firestore's per-query minimum when none), so it stays bounded by activity. The merge (`LinkedHashMap` over cached + fresh) also ADDS newly-active users not yet in the cache. This NEVER scans the whole collection. Both queries are factored into a shared local suspend `incrementalDirectoryRefresh()`. **Three triggers feed it: (1) directory tab entry / cold launch (the directory `LaunchedEffect`); (2) app foreground-return — a `LifecycleEventObserver` bumps `resumeTick` on `ON_RESUME`, folded into the effect keys (but deliberately NOT into `lastFetchedKey`, so a resume never forces a full scan); (3) a LIVE periodic loop — a separate `LaunchedEffect(needsUsers)` re-runs `incrementalDirectoryRefresh()` every `DIRECTORY_LIVE_REFRESH_MS` (30s) WHILE the Dashboard/Users tab is composed.** Trigger (3) is the fix for "a swiped user never shows offline until the ~65-min mark": the TTL-gated triggers (1)+(2) only fire on tab-entry / resume, so an admin who just SITS on the directory never re-read — the swiped user's `offlineAt` landed in Firestore but never reached `sharedUsers`, and the only thing that flipped the row was the local `nowMs` ticker decaying `lastActiveAt` past 65 min. The 30s live loop re-reads the `offlineAt` query so a swipe surfaces within one cycle; it skips a cycle if a full/TTL refresh just ran (`now - lastDirectoryRefreshMs >= DIRECTORY_LIVE_REFRESH_MS`, so it never double-reads) and auto-cancels when leaving the directory tab (`needsUsers=false`). **The live loop is ALSO gated on `isForeground`** (the `LifecycleEventObserver` now tracks `ON_RESUME`→true / `ON_PAUSE`→false): Compose keeps a composed `LaunchedEffect` alive when the admin app is merely backgrounded, so without this gate an admin app **left open on the Dashboard/Users tab in the background polled Firestore every 30s indefinitely** — the dominant "reads keep climbing even though no admin is watching anyone" cost (it has nothing to do with watching a specific user; just sitting on the directory tab, even backgrounded/screen-off, drove it). With the gate the loop pauses the moment the app backgrounds and resumes on `ON_RESUME` (which also bumps `resumeTick` → a TTL-gated refresh re-freshens the directory). The TTL clock (`lastDirectoryRefreshMs`, a process-lifetime `AtomicLong`) is stamped on SUCCESS only (a full fetch also stamps it) so a failure or a resume-cancel retries on the next entry/resume; plain tab switches / brief resumes within the TTL cost **0 reads**. A `Refresh` icon button (Users header) and the Dashboard refresh button bump `refreshTick` — with a populated cache this runs the cheap **incremental** refresh, NOT a full pull; **`+500` (limit change) IS a genuine full pull** (`limitChanged` check on `lastFetchedKey`'s limit segment — the incremental path can only merge recently-active docs, it can never extend the loaded base). An open user's 10s detail poll also patches that one row live (`onUpdateUserRow`) regardless of the TTL. Sorted by `lastActiveAt ?? lastSeenAt` desc. **Count-mismatch reconcile (the "new user is counted on the Dashboard but missing from the directory" fix)**: a doc with NEITHER `lastActiveAt` NOR `offlineAt` — a brand-new user still on an app version predating the hourly liveness model, which only ever writes `lastSeenAt` — can **never** match either incremental `whereGreaterThan` query (Firestore inequality filters exclude docs missing the field entirely), so it would never enter the cache no matter how often the admin refreshed; before this fix only wiping the admin app's data (empty-cache full fetch) would surface it. The Dashboard's already-paid total `count()` aggregation exposes the divergence: after the count lands, if `totalUsersCount != sharedUsers.size` (cache populated, first load not in flight), `fullDirectoryFetch()` (the extracted full-pull helper, shared with the first-ever load) runs ONCE and replaces the cache — missing users appear, deleted ghosts drop. Throttled per count VALUE (`lastReconciledTotalCount`, process-lifetime `AtomicInteger`): if a reconcile didn't resolve the mismatch (a permanent skew between the count's `adminBuild==false` filter and the directory's self-filter), re-fetching won't either, so it waits for the count to actually change. **Legacy `lastSeenAt` sweep**: once such a legacy-version user IS in the cache, their liveness would still decay (their heartbeat advances only `lastSeenAt`, invisible to the two incremental queries) — so `incrementalDirectoryRefresh(includeLegacySweep)` runs a third `whereGreaterThan("lastSeenAt", cursor)` query, gated on the cache actually containing a row with `lastActiveAt == null && lastSeenAt != null` (self-extinguishing once stragglers update or vanish) and passed `false` from the 30s live loop (current-version users mirror `lastSeenAt` on every liveness write, so the sweep re-reads ~the whole recently-active set — ~2x reads per run — acceptable at tab-entry/TTL/manual granularity, not every 30s).
- **Aggregate stats** (`AdminScreen.kt` + `DashboardTab.kt`): `count()` server aggregations (total/warned/banned, ban-evasion) run **once on tab entry and on manual refresh** (keyed on `refreshTick`) — NOT polled on a timer. The old 60s poll burned reads for no benefit at that cadence. 1 read per 1000 docs counted.
- **Selected-user detail** (`UsersTab.kt`): the ONLY live read — a 10s `get(Source.SERVER)` poll loop scoped to the single open user, keyed on `selectedDocId` **AND `isForeground`**. Backing out (`selectedDocId → null`) cancels the coroutine so reads stop INSTANTLY. **Foreground-gated**: a `LifecycleEventObserver` in `UsersTab` flips `isForeground` on `ON_RESUME`/`ON_PAUSE`, and the poll's key includes it (with an early `return` when `!isForeground`), so leaving a user's detail page open and **backgrounding the admin app no longer keeps reading every 10s** — this was the dominant *"reads keep climbing even though no admin is browsing"* cost (the directory 30s loop was already foreground-gated; this 10s detail poll was NOT, the one loop that escaped the gate). The last loaded detail stays on screen while backgrounded and the poll resumes (re-fetching immediately) on `ON_RESUME`. **Live instance count**: the detail's `📍 world (N/cap)` count is fetched **live from the admin's OWN VRChat session** (`VrchatAuthManager.fetchInstanceCount` on the user's `vrchatLocation`, polled every 10s while the detail is open), overriding the Firestore-synced `vrchatInstancePlayerCount` so the admin sees an instant count independent of the user's sync chain; falls back to the synced value when the instance isn't visible to the admin / not logged in. **Self-invite ("Invite me to this instance")**: a footer button (gated on `VrchatAuthManager.isLoggedIn`) calls `VrchatAuthManager.inviteSelfToInstance(ctx, vrchatLocation)` → `POST /invite/myself/to/{location}` (the website "Invite Me" behavior) sending an invite to the ADMIN'S OWN VRChat account for the user's current instance — works for invite-only/friends+/group; the user is NOT notified. Reads the freshest location from `selectedDetail` (UserRow doesn't carry `vrchatLocation`); disabled when not a joinable `wrld_` instance. **Invite/invite+ nonce preservation (the fix that made self-invite work for those two instance types)**: the self-invite needs the `~nonce(...)` access token embedded in the synced `vrchatLocation`. `/users/{id}` REDACTS invite/invite+ locations to `"private"` (dropping the nonce) even for the user's OWN account — friends/friends+ are NOT redacted, which is why those self-invites already worked but invite/invite+ failed with "not accessible". Two guards now keep the full joinable location: (1) `fetchPresence` no longer lets `/users/{id}`'s `"private"` DOWNGRADE a full `wrld_` location from `/auth/user` (a real `wrld_` location or an `"offline"` departure still overrides, so stale-presence correction is unaffected); (2) `syncPresenceToFirestore` keeps the full nonce-bearing location captured by the WebSocket self `user-location` event when the REST poll returns `"private"` (`"private"` means still-in-instance, so this can't resurrect a left instance — a real departure reports `"offline"`). The user's OWN `invite_me` (from a world-invite notification) was always fine because the invite payload carries the full nonce. **Invite-only fallback — the USER's app invites the admin (`SelfInviteCoordinator`, `SelfInviteStore`)**: `inviteSelfToInstance` (the admin inviting THEMSELVES) is rejected for **invite-only** instances the admin has no standing in (the `~nonce` fix only covers invite/invite+). So the button tries `inviteSelfToInstance` first (covers public/friends/friends+/group/invite+) and, on failure, calls `SelfInviteCoordinator.runAdminSide`, which flips who invites: the **USER** (who IS in the instance, so may invite anyone) invites the admin. Signaling is a Firestore handshake on `users/{deviceHash}` — admin writes owner-only `selfInviteReqId`/`selfInviteAdminId`/`selfInviteLocation`/`selfInviteAt`; the user's moderation listener (`handleSelfInviteSignal`, deduped by the signal ms via `SelfInviteStore.lastHandledSignalMs`) runs `runUserSide`, which (1) tries a **direct** `inviteUserToInstance(adminId, location)`, and (2) only if VRChat rejects it, runs a hidden **friend → invite → unfriend dance**: user sends a friend request to the admin, writes back `selfInviteStatus="need_friend"` (self-mutable echo of the reqId), the admin (polling the doc) sends its own request so VRChat **auto-befriends on the mutual request**, the user re-invites once friends, then **force-unfriends 10s later no matter what**. **Safety invariants**: a counterpart is only ever unfriended if it was NOT a friend before the dance (checked via `getFriendStatus`; the fallback only runs when the direct invite failed, which already implies not-friends — a real friendship is never severed); the unfriend obligation is persisted to `SelfInviteStore.pendingUnfriend` **before** friending, so a connection dip / app death still cleans up on the next pipeline connect (`SelfInviteCoordinator.drainPendingUnfriend`, wired into `onOpen`, runs on both builds). **No indicators on either side**: `SelfInviteStore.isFriendNotifSuppressed(id)` (true for the global owner VRChat id OR any id in the local pending-unfriend set) gates the shared `VrchatPipelineService` friend-add / friend-delete / friend-request / offline-unfriend-diff handlers — cache/dedup bookkeeping still runs, only the notification is hidden. The **user** suppresses via the owner id (set from `config/app.ownerVrchatId` — written by the admin build in `runAdminSide` — AND from the signal's `adminId`) + the admin in its pending set during the dance; the **admin** suppresses the user's id (added to its pending set at signal time, before the user's request can arrive). **Only friend-list notifications are gated** — bio/name/rank/online/etc. about the owner still fire. **Suppression covers ALL friend firing paths** — the live `friend-add`/`friend-delete`/`friendRequest` handlers, the offline unfriend-diff, AND the REST **backfill** friendRequest (that last one was a hole: the live path suppresses by returning BEFORE recording the `fr_` dedup id, so the backfill re-fired it on the next connect). **The unfriend-event race**: the friend-delete WebSocket event arrives AFTER the unfriend REST call returns, so the cleanup keeps the counterpart in the suppression set a `SUPPRESS_GRACE_MS` (8s) window PAST the unfriend before `removePendingUnfriend` — otherwise "removed from friends list" slipped through on the admin side (the user side is permanently covered by `ownerVrchatId`). **Invite speed**: `runUserSide` **retries the invite directly** (every 1s) instead of polling `friendStatus` — the invite succeeds the instant they're friends server-side, sidestepping VRChat's `friendStatus` eventual-consistency lag (that lag was a ~10s stall before the invite fired); the admin's mutual-request poll is 1s. **The retry delay RAMPS (1s→3s, `INVITE_RETRY_MAX_STEP_MS`)** so the normal case still fires in ~2-3 tries but a never-forms failure can't hammer VRChat at 1/s (a full self-invite is ~5-8 VRChat REST calls; the status poll is Firestore, not VRChat). **The unfriend cleanup is DETACHED** (`scheduleCleanup`, a process-lifetime `CoroutineScope`) so `runAdminSide` returns the "invite sent" result the INSTANT the user's app reports it — the cleanup's 10s force-unfriend + 8s grace used to run BEFORE the return, so the admin UI sat on the "processing" spinner for ~18s after the invite had already landed (the reported "UI keeps acting like it's processing" bug). The persisted pending-unfriend backup + drain-on-connect still cleans up if the process dies before the detached coroutine runs. VRChat friend REST added to `VrchatAuthManager`: `getFriendStatus` (`GET /user/{id}/friendStatus`), `sendFriendRequest` (`POST /user/{id}/friendRequest`, mutual auto-friend), `cancelFriendRequest` (`DELETE …/friendRequest`), `unfriendUser` (`DELETE /auth/user/friends/{id}`). `firestore.rules`: signal keys in `ownerOnlyUserKeys()`, status keys in `selfMutableKeys()`. The loop shows a spinner on first load then refreshes silently. 10s matches the cadence at which the watched user app refreshes its volatile fields. **Watched fast-offline (`WATCHED_STALE_WINDOW_MS` = 25s, `WATCH_RAMP_MS` = 45s)**: a watched user's app writes a liveness timestamp every 10s — `lastSeenAt` via the ViewModel live loop (dependency-free, the RELIABLE heartbeat) and `lastActiveAt` via the presence loop (which can stall if VRChat's `fetchPresence` 401s on an IP-invalidated cookie) — so a heartbeat that stops advancing **while watched** means the app died/was swiped, far sooner than the 65-min unwatched window can prove. The detail poll tracks the **FRESHEST of `lastActiveAt`/`lastSeenAt`** (`maxOf`, so a stalled `lastActiveAt` never false-flags a live user whose `lastSeenAt` is still advancing) and the wall-clock time it last advanced. **Loop-confirmed detection**: once the poll has SEEN the heartbeat advance at least once (`loopConfirmed`), a stopped heartbeat flips offline after just `WATCHED_STALE_WINDOW_MS` (≈2.5 missed ticks) — no need to wait the full ramp; before the loop is confirmed (its 10s loop hasn't started yet, or **the user was already DEAD when the admin opened them** — force-killed/swiped with no `offlineAt` landing, so there is no heartbeat to observe) it falls back to the `WATCH_RAMP_MS` (45s, reduced from 90s) grace so a live user isn't false-flagged mid-ramp. The 45s ramp is the fix for "opening an already-offline user doesn't flip them offline": `AdminRuntime.setSelectedUser` fires the `watcherActiveAt` write IMMEDIATELY on open, so a genuinely-LIVE user's first heartbeat reaches the poll within ~15-25s — 45s clears that chain with margin while flipping a dead user offline far faster than the old 90s. (If the ramp ever false-flags a live user, it SELF-HEALS the instant the heartbeat advances.) When stale, the poll **synthesizes `offlineAt = now` on the in-memory `UserDetail`** (zero Firestore write). This flips `isUserOnline` offline in the detail AND, via `onUpdateUserRow`, in the directory + cache — and is the ROBUST swipe→offline path because it does NOT depend on the swipe-time `offlineAt` write landing during the chaotic shutdown (that write is the instant-when-it-lands bonus). It self-heals: when the heartbeat advances again the poll stops synthesizing and carries the doc's real `offlineAt`. **For this to clear correctly, both the detail-view row merge and `onUpdateUserRow` treat the loaded detail's `offlineAt` as AUTHORITATIVE (no `?:` fallback to the directory row's value) — a null `offlineAt` from the poll must be able to clear a stale/synthetic marker** so a healed user returns online.
- The `watcherActiveAt` heartbeat (`AdminRuntime`) writes every 30s while a user is selected. No periodic writes from the user app outside the watched-user live loop.

**Initial-data gate** (`initialDataLoaded`): `performSelfSync` returns early until a loader coroutine has read every user-content DataStore field into ViewModel state. Prevents cold-start sync from writing empty defaults.

**First-snapshot skip** (`initialSnapshotProcessed`): The very first Firestore snapshot after listener attach is dropped — DataStore is the source of truth on cold start.

**Cross-device content sync (multi-phone, same VRChat account)**: When the same VRChat user logs in on two phones, the installs have DISTINCT `deviceHash`es → two separate `users/{deviceHash}` docs. To keep presets/messages/intervals consistent without merging the docs, `VrcaViewModel.applyCrossDeviceSync()` (public build only, launched from `init`) waits for the pipeline to surface the user's `vrchatUserId` (`VrchatPipelineState.presenceFlow.first { it != null }`, 120s timeout), then queries `users` for sibling docs `whereEqualTo("vrchatUserId", myVrchatId)`. It picks the sibling with the freshest `updatedAt`; if that is newer than this device's own `updatedAt`, it pulls the sibling's content into local DataStore + ViewModel state via the shared `applyContentFromSnapshot(snap)` helper (extracted from `applyRemoteContentBeforeSync()` — applies afkMessage, cycleIntervalSeconds, cycleLinesText, afkPreset1-3, cyclePreset1-5, spotifyPreset, each guarded by a value-changed check) and rebuilds the preview. So editing presets on phone A surfaces on phone B's next app open. The query keys on the already-written `vrchatUserId` field (a single-field index Firestore auto-provides). The content pull is read-only against siblings — no cross-device content WRITE — so the only authority over a device's own doc remains that device + the admin. **The query returns the device's OWN doc too (same `vrchatUserId`), so its `updatedAt` is read from the result set inline — no separate own-doc `get()`.** **Two throttles now**: the sibling QUERY runs at most once per 60s (`ACCOUNT_QUERY_THROTTLE_MS`, persisted `last_account_query_ms`) — short, because it ALSO drives the single-session claim and so must run on a genuine reopen (not be suppressed for 30 min) while still bounding OS-kill relaunch storms — and the expensive CONTENT pull keeps its own 30-min window (`CROSS_DEVICE_SYNC_THROTTLE_MS`, persisted `last_cross_device_sync_ms`). A genuine reopen after editing on the other phone is almost always >30 min from the last sync, so the content feature is intact.

**Single-session lock (multi-device, same VRChat account)**: only ONE device may use VRC-A per account — now a **HARD DENY** (see "Single-session HARD-DENY" above for the full model; the old take-over/"Use here" reclaim described here was REPLACED). The lock doc `accounts/{vrchatUserId}` = `{ activeDevice, activeSince }` is claimed on every login and denies any other device. `firestore.rules` for it: `get` signed-in, `list` owner-only, `create`/`update` `signedIn()` constrained to the two keys + valid deviceHash, `delete` `isOwner() || signedIn()` (lock release on sign-out / admin remote-logout). Admin build never participates (`!IS_ADMIN_BUILD`).

**Account-wide moderation fan-out (`AccountModeration.applyAccountWide`)**: since the directory collapses all of an account's devices into one row, moderating that row must hit EVERY device on the account (else a banned user just opens VRC-A on their other phone and dodges it). `AccountModeration.applyAccountWide(db, targetDocId, fields)` reads the target's `vrchatUserId`, finds every `users/{deviceHash}` doc sharing it, and writes `fields` (merge) to all of them — target included — in a single `WriteBatch`. A target with a blank `vrchatUserId` (never logged into VRChat) has no siblings, so it writes just the target (identical to the old per-device behavior). Wired into **warn / remove-warn / ban / unban** (`ModerationTab` — the `applyUidBan` branch only), the **kill switch** (`UsersTab` `killSignal` — only the currently-open device acts on a fresh signal), and `AdminViewModel.warnUser/banUser/clearPunishment`. **Device bans (`bannedDevices/{deviceHash}`) are intentionally NOT fanned out** — a device ban is by definition per-physical-device. **Admin directory dedup (`UsersTab.filteredUsers`)**: the directory now COLLAPSES rows that share the same `vrchatUserId` (the multi-phone duplicates) into one representative — `preferRow()` keeps the online row, then the most recently active. Rows with a blank `vrchatUserId` (never logged into VRChat) are never merged. Dedup happens at render time over the cached list (preserving sort order); the underlying separate device docs are untouched.

**Offline editing**: User-content edits land in DataStore immediately and survive process death. On reconnect, the next debounced write or app-close write pushes them to Firestore. Admin-content edits are preserved across user restarts via `applyRemoteContentBeforeSync()` (read-before-write on app-open). If both the user and admin edited the same field while the user was offline, the Firestore version (admin's) wins because it's applied before the app-open write.

**Admin edits to OFFLINE users (cold-open apply path + moderation-listener backstop — toggles AND content)**: There are now TWO cooperating paths that both apply admin edits made while the user was offline, both keyed on the PERSISTED baseline (`lastSyncedValues`, loaded from prefs in `init`):

1. **`applyRemoteContentBeforeSync()`** (runs once on cold-open BEFORE `performSelfSync`, wrapped in a 12s `withTimeoutOrNull`): (a) `ensureAnonAuth()`; (b) reads the user doc **from `Source.SERVER`** — NOT the default `get()`, which returns Firestore's LOCAL CACHE holding this device's OLD pre-edit values, so an admin edit made while the app was closed would be invisible (old-vs-old → nothing applied; this stale-cache read was a primary cause of "admin content/preset edits don't apply when the user reopens"); (c) if the doc exists it sets `remoteDocConfirmedExists=true` (see liveness-only gate below); (d) `applyContentFromSnapshot(snap)` + `applyOfflineToggleEdits(snap)` apply admin-edited content/toggles. **There is NO blanket snapshot-seed of the baseline** — the old `seedLastSyncedFromSnapshot` was DELETED because it POISONED the listener: it set the baseline to the server value for EVERY field (even ones the apply skipped), so (i) the moderation listener then saw `remote == baseline` and echo-SUPPRESSED un-applied admin edits, and (ii) combined with a stale local value it made the cold-open delta write the local value BACK, overwriting the admin's edit. Instead, `applyContentFromSnapshot`/`applyOfflineToggleEdits` update `lastSyncedValues[key]` **only for the specific field they actually apply**, leaving every other key on its prefs baseline.
2. **The moderation snapshot listener (`applyRemoteConfig`)** is the **backstop** and is now ALSO the path that catches the common case where the admin's offline edit arrives as the FIRST (and on a fresh device the ONLY) server snapshot. **The first-snapshot drop was REMOVED** (`initialSnapshotProcessed` deleted): dropping it silently lost every admin edit that arrived as the first snapshot — the core "nothing applies on reopen" bug. All snapshots now run through `applyRemoteConfig`'s baseline-vs-`lastSyncedValues` echo-suppression.

**Cold-open write is LIVENESS-ONLY for an existing doc (`remoteDocConfirmedExists`)**: this is the structural guarantee that the cold-open `performSelfSync` can NEVER clobber an admin offline edit. When the `Source.SERVER` read confirmed the doc exists, the cold-open delta SKIPS all content+toggle keys and writes only liveness (and, crucially, does NOT rebaseline `lastSyncedValues` to local afterward — that would drop the prefs baseline the listener needs and let a later write clobber a user's own offline edit). The admin's edits live on the server doc; the apply path + listener pull them into local; a later debounced/hourly write (non-coldOpen) pushes any genuine user offline edit up. The full-write branch now fires only when `isFirstSync && !remoteDocConfirmedExists` (true first install, doc absent) or `uidChanged` (rules require `uid`/`authUid` to match the new auth, so a delta omitting them is denied).

**Content vs toggles differ on the null-baseline fallback (the OS-kill revival safety):** `applyContentFromSnapshot` falls back to comparing against current LOCAL when a field has no persisted baseline (so admin content still wins on first sync). `applyOfflineToggleEdits` does NOT — it SKIPS on a null baseline. This is deliberate: `restoreFeatureSession()` runs first on an OS-kill revival and may have just resumed OSC sending with toggles ON; a null-baseline local fallback could flip a just-restored toggle off and STOP the revived user's OSC. So a toggle is only changed when a REAL baseline shows the admin genuinely changed it (`remote != baseline`) — admin DISABLE while OEM-killed is respected (sender stops), while an untouched toggle (`remote == baseline`) leaves the restored toggle and its OSC alone. With a real baseline the apply preserves the user's OWN offline content edits too: `remote == baseline` means the admin didn't touch it, so the user's local edit survives and the debounced write pushes it up; both edited → `remote != baseline` → admin wins (documented intent). (The earlier compare-vs-LOCAL design clobbered the user's offline edits whenever the cache read returned the stale server value.)

**Cold-open `performSelfSync` write rules (`TOGGLE_KEYS = afkEnabled/cycleEnabled/spotifyEnabled/spotifyDemoEnabled/timeEnabled`):** for an existing doc (`remoteDocConfirmedExists`) the cold-open delta is LIVENESS-ONLY — all content+toggle keys are skipped and the baseline is NOT rebaselined to local (see above), so it can't clobber. For a first-install cold open (doc absent) toggle keys are still skipped (they start OFF; writing OFF back would race an admin's enabled toggle) while content is written into the creating snapshot. The hourly/debounced (non-coldOpen) writes are full deltas and push genuine user edits. (True first-install with no doc: `applyRemoteContentBeforeSync` early-returns on `!snap.exists()` leaving `remoteDocConfirmedExists=false` and `lastSyncedValues` empty → `isFirstSync && !remoteDocConfirmedExists` full write creates the doc — toggles default OFF, nothing to apply.)

**Admin online detection (liveness-driven, NOT the `isOnlineInApp` flag)**: `isUserOnline(u, nowMs)` in `UsersTab.kt` is driven by **`lastActiveAt` freshness**, deliberately IGNORING the `isOnlineInApp` boolean. A user is online ⟺ `(lastActiveAt ?? lastSeenAt)` is within `ONLINE_STALENESS_WINDOW_MS` (~65 min) **AND** there is no clean-shutdown `offlineAt` marker newer than `lastActiveAt`. **Why the flag was dropped**: pre-app-scoped-VM public builds wrote `isOnlineInApp=false` from `VrcaViewModel.onCleared()` on EVERY Activity destruction (normal under memory pressure / "Don't keep activities") **without advancing `lastActiveAt`**, so a perfectly-live user showed offline with a fresh 28-min-old timestamp — the "Cherubic shows offline while her chatbox is clearly updating" bug. `lastActiveAt` is the ground truth because the hourly heartbeat (every 10s while watched) only fires while the process is alive. Under the hourly model an unwatched user writes `lastActiveAt` once per hour, so the window is ~65 min (5 min grace past the 60-min heartbeat); a watched user (10s loop) shows online within ~25s. **Instant-offline on swipe via `offlineAt`**: a clean swipe-away (`AppShutdown.buildOfflineWriteTask` + `VrcaViewModel.onCleared`) stamps `offlineAt = serverTimestamp()`. When `offlineAt > lastActiveAt` the row is forced offline INSTANTLY (no 65-min wait). This can never false-flag a live app — the next heartbeat advances `lastActiveAt` past `offlineAt` (self-healing, no clear needed). If the swipe-time write races the 5s kill timeout and never lands (the long-standing "swipe doesn't consistently go offline"), the staleness window still flips the user offline within ~65 min — instant when the write succeeds, eventually-consistent when it doesn't. VRChat session state is irrelevant to all of this: the liveness write uses Firebase anon auth + device hash and is fully decoupled from VRChat auth (a VRChat un-auth never blocks or triggers an online/offline write; VRChat presence fields are only additively merged when present). **The Dashboard counter uses the SAME `isUserOnline` predicate** (`DashboardTab.kt`) so the count and the directory rows stay consistent. The admin-side staleness sweep (`AdminRuntime`) still flips `isOnlineInApp=false` for genuinely-stale users — now only to keep the raw flag roughly accurate for any OLD admin builds that still read it directly; the current build never reads it for the online decision. `UserRow`/`UserDetail`/`parseUserRow`/`UsersDirectoryCache` all carry the new `offlineAt` timestamp; `firestore.rules` `selfMutableKeys()` includes `offlineAt`. The shared user-list state is `mutableStateOf<List<UserRow>>` (whole-value assignment) populated by the one-shot directory fetch.

### Settings page (`VrcaSettingsPage.kt`)
Grouped sections: **Chatbox display** (Invisible Chatbox Border toggle, description "Shrinks the chatbox background." + **"Time format" toggle** (label renamed from "24-hour time"; description "Change your time format to 12 or 24 hour mode." — `vm.time24h`/`setTime24hFlag` ← DataStore `time_format_24h`, default OFF = **12-hour with AM/PM** (`DateTimeFormatter "h:mm a"` with `Locale.US` for a stable uppercase marker) vs ON = `"HH:mm"`; consumed by `currentTimeString()` so it applies to the Time chatbox line, the preview, and `lastReportedTime`), **App** (version display + Battery Optimization request — promoted out of Permissions), **Permissions** (ALL permissions the app uses: Notifications (`ACTION_APP_NOTIFICATION_SETTINGS`), Notification Access, **Install updates** (`ACTION_MANAGE_UNKNOWN_APP_SOURCES` — in-app APK installs), Overlay), **About**, **Help** (collapsible FAQ rows via `HelpFaqRow`), **Debug** (collapsible). **The VRChat re-login takeover passes `onCancel` into `VrchatLoginScreen`** — Cancel renders INSIDE the login screen content (a `TextButton` above the disclaimer; the old top-bar Row placement clashed with the app bar). Same for the VRChat-tab takeover; onboarding passes no `onCancel` (hard gate, no cancel). Deliberately NO check-for-update button (a user could spam Firestore reads). Account/VRChat, Discord RPC, Notifications, and IP-slot sections were considered and rejected by the user.

### Update dialog browser fallback
The `UpdateDialog` (`VrcaApp.kt`) has a browser-download fallback TextButton ("Download failed? Tap here…") below the Download/Retry buttons that opens `info.downloadUrl` via `ACTION_VIEW` for devices where DownloadManager fails (storage perms, OEM download restrictions).

### Rich Content engine (`com.vrca.richcontent` — Phase 1 of the Updates/Announcements/Status revamp)
Shared block-based document system that will power the Update popup, Settings "What's New", and the in-app Announcements list (one model + renderer + media store, so a new block type lights up everywhere). Status popup is a separate re-skin, not authored content. **Phase 1 shipped = the engine only; NOT yet wired into any surface** (that's Phase 2 = updates, Phase 4 = announcements). Four files, zero new dependencies (public APK unchanged — Coil already present, `VideoView` will be built-in, parser is ours). **All four compile clean on both variants.**
- **`RichDoc.kt`** (pure Kotlin + `org.json` — the codebase's JSON lib): `RichDoc(v, blocks)` where `RichBlock` is a sealed class — `Heading(text, color?)`, `Text(text)`, `Bullets(items)`, `Image(url)`, `Video(url, poster?)`, `Callout(tone /* info|warn|success */, text)`, `Divider`. Serialised to a JSON string stored in ONE Firestore field (`bodyDoc`) on announcement/release docs → single doc read; media (images/videos) live on GitHub/jsDelivr, never Firestore. `toJson()`/`parseRichDoc(json)` round-trip (parse is tolerant: returns null on blank/garbage/empty, skips unknown block `t` types for forward-compat with newer admin builds). `mediaUrls()` collects every image/video/poster URL in order (feeds the media-store reference set). `toPlainText()` flattens for legacy fields / collapsed-card previews (strips inline markup). **BACK-COMPAT is load-bearing**: `legacyDoc(text)` parses an existing plain `notes`/`text` string into blocks using the SAME rules the old `parseTosBlocks` used (numbered `^\d+\.` headers, `*`/`-` bullets grouped into one `Bullets` block, blank-line paragraphs, literal `[label](url)` markdown-link collapse) so old announcements/releases render with their original structure. **`resolveRichDoc(bodyDoc, legacyText) = parseRichDoc(bodyDoc) ?: legacyDoc(legacyText)`** is the accessor every render site uses — prefer the rich doc, fall back to legacy. (The old update system therefore keeps working on old app versions AND old docs; a forced update can still show no notes exactly as today by leaving `bodyDoc`/`notes` blank.)
- **`InlineMarkup.kt`** (Compose ui-text): `buildInlineAnnotated(raw, linkColor): AnnotatedString` — supports `**bold**`, `*italic*`, `[c=#RGB/#RRGGBB/#AARRGGBB]colored[/c]` (a toggle/stack scanner), and **auto-linkifies bare `https://` URLs** into clickable underlined primary-tinted spans tagged `"URL"` (trailing punctuation trimmed off the link). `AnnotatedString.urlAt(offset)` reads the tag on tap. `parseHexColor(hex)` (3/6/8-digit → `Color`). `stripInlineMarkup(s)` for previews. Pure (colors passed in) so it's unit-testable. **No button/link block — a raw URL in text just becomes tappable.**
- **`RichDocRenderer.kt`** (Compose + Coil): `RichDocRenderer(doc, modifier, mediaScope)` renders blocks as a responsive vertical `Column` (spacedBy 12dp) — **never a freeform/absolute canvas** (breaks across screen sizes). Heading = `titleMedium` in `block.color ?: primary`; Text/bullets/callout via a private `RichText` (uses **`ClickableText`** + `urlAt` + `LocalUriHandler` — the pre-1.7 clickable-link path, because this project is on **Compose BOM 2024.01.00 / ui-text ~1.6**, where the modern `LinkAnnotation.Url`/`withLink`/`TextLinkStyles` API does NOT exist; ClickableText is deprecated in foundation 1.7 but still compiles as a warning); Callout = tinted `Surface` (`accent.copy(alpha=0.14f)` + a 4dp accent bar; info→primary / warn→amber 0xFFFFB300 / success→green 0xFF4CAF50); Divider = `Divider` (NOT `HorizontalDivider` — M3 1.1.2 in this BOM has only `Divider`, the modern name is unresolved — a real compile catch). **Image** = full-width `AsyncImage` at intrinsic ratio, **local-file-first via `RichMediaStore`** (`model = cachedFile ?: url` so it renders from disk once cached, Coil network fallback meanwhile) with tap → pinch-zoom fullscreen `Dialog` (mirrors `TutorialImage`). **Video renders an INERT poster+play-glyph placeholder this phase** — real tap-to-play WITH AUDIO (built-in `VideoView`, single active player) lands in Phase 5.
- **`RichMediaStore.kt`** (modeled on `AlertImageStore` + `TutorialImageStore`): on-demand media cache at `filesDir/rich_media/`, SHA-1(url) key, `.part`-then-rename, **two scopes in separate subdirs** — `ann/` (**persistent, admin-controlled**: `gcAnnouncements(referencedUrls)` deletes any `ann/` file not in the active-announcements URL set → admin remove/swap culls it from user storage immediately) and `upd/` (**ephemeral, view-scoped**: `clearUpdateMedia()` wipes it on update-popup / What's-New close; patch-note TEXT stays cached elsewhere, only heavy media is transient). `ensureCached(url, scope)` is scope-LOCAL (a URL used by both an announcement and an update keeps two copies so `clearUpdateMedia` can never delete an announcement's media); `resolve(url)` reads across both scopes. No auth cookie (media is public jsDelivr/github). **Phase-3 rule (not yet built): admin uploads use a UNIQUE filename per upload (`<id>_<epochMillis>.<ext>`), never overwriting — jsDelivr caches hard, so a new upload = new URL = live edits appear instantly, and the old URL falls out of the reference set → auto-culled.**

**Phase 2 shipped = Updates on the engine + Settings "What's New"** (`InAppUpdate.kt`, `VrcaApp.kt`, `WhatsNewStore.kt`, `VrcaSettingsPage.kt`; compiles clean both variants). `ReleaseInfo` gained a `bodyDoc: String = ""` field; `parseReleaseSnap` + BOTH real-time release listeners (`releases/{deviceHash}` + `releases/latest`) read `bodyDoc`. **`UpdateDialog` now renders `resolveRichDoc(info.bodyDoc, info.notes)` via `RichDocRenderer(mediaScope = UPDATE)`** inside the existing bounded-scroll Surface (cap raised 0.45→0.5 screen-height; Download stays pinned OUTSIDE the scroll; forced-update logic UNCHANGED — a forced update with blank `bodyDoc`+`notes` shows no notes exactly as before, so the **old update flow keeps working on old app versions and old docs**). A `DisposableEffect(doc)` at the top of `UpdateDialog` `ensureCachedAsync`es the doc's media into `upd/` while shown and `clearUpdateMedia()`s on dispose (ephemeral). **Settings → App → "What's New"** (`WhatsNewDialog`, `Icons.Filled.NewReleases`): shows the INSTALLED version's patch notes from `WhatsNewStore` (SharedPreferences `vrca_whatsnew`, keyed by versionCode) rendered via the same `RichDocRenderer`; media pulls on open, culls on close; friendly empty state for installs predating this feature. **Zero extra Firestore reads**: the `releases/latest` listener already holds the doc, so when `code == BuildConfig.VERSION_CODE` it caches that doc's `versionName`/`bodyDoc`/`notes` locally for offline What's New. No check-for-update button (spam-read hygiene). `parseTosBlocks`/`StyledBlocks` remain (still used by `TosGate`), just no longer by `UpdateDialog`.

**Phase 3 shipped = Admin builder (`RichDocEditor`) + GitHub image upload + rich Announcements AND Releases authoring** (`RichDocEditor.kt` new, `AnnouncementsTab.kt` rewritten, `ReleasesTab.kt` + `firestore.rules` edited; compiles clean both variants). **`RichDocEditor(blocks: SnapshotStateList<RichBlock>, githubPat)`** (`com.vrca.admin`) is the reusable block-list editor: per-block type-specific fields (heading text+hex color / text with `**bold** *italic* [c=#hex]` hint / bullets add-remove items / image URL + "Pick & upload" / video URL+poster / callout tone chips info·warn·success / divider), each block card has **move up / move down / duplicate / delete** controls (drag-and-drop is a deferred polish — reorder works via the arrows), an **Add block** horizontally-scrolling chip row, and a **live `RichDocRenderer` preview** (the same renderer users see). **Firestore cost = ZERO for the editor** (pure local state; media → GitHub). **`githubUploadImage(ctx, uri, pat)`** PUTs the picked image to the PUBLIC image-store repo (`Ashoska/VRC-A-Image-store`) via the GitHub **Contents API** with a **UNIQUE filename** (`rich/rich_<epochMillis>.<ext>`, never overwrite → jsDelivr cache-safe live edits) and returns the **jsDelivr** URL (`cdn.jsdelivr.net/gh/Ashoska/VRC-A-Image-store@main/rich/…`); it reuses the release **`BuildConfig.GITHUB_PAT`** (the PAT needs Contents:write on the image-store repo — **on failure it throws GitHub's own error text so a token/permission problem is diagnosable on-device**). **`AnnouncementsTab`** now authors a `RichDoc` (Title stays a plain field; the old Body textarea is replaced by `RichDocEditor`): Publish/Update writes `title` + `bodyDoc` (JSON) + `body` (plaintext flatten via `toPlainText()` for old clients/notification) in **ONE write**, then **optimistically updates the local list (zero re-read)** — respecting the "one write, minimal reads" mandate. **Edit** loads an announcement's `bodyDoc` (via `resolveRichDoc`, from the already-read row → zero extra read) back into the editor; **Copy** loads it as a new draft; a "RICH" pill marks rich announcements. `AnnouncementRow` gained `bodyDoc`; the 50-doc refresh reads it (same query). Announcements `create/update` rule is `if isOwner()` (no key restriction) so **no rule change was needed** for announcements. **`ReleasesTab`** keeps its plain `editNotes` (GitHub release body + legacy `notes`) and ADDS a `RichDocEditor` (`releaseBlocks`); publish writes `bodyDoc` (JSON) + `notes` (= `editNotes` or the rich doc's plaintext fallback) — one write. **`firestore.rules`: `bodyDoc` added to the `releases` `create/update` `hasOnly([...])` allowlist** (announcements needed none). So the client's Phase-2 `resolveRichDoc(bodyDoc, notes)` now has an admin-authored source for BOTH updates and announcements. (Deferred: drag-and-drop reordering, and per-video transcode/upload which is Phase 5.)

**Phase 4 shipped = in-app Announcements surface** (`VrcaScreen.kt`, `HomePage.kt`, `AnnouncementSeenState.kt` new; compiles clean both variants). The Home "Announcements" `SectionCard` now renders each active announcement as a **collapsible `AnnouncementCard`** (status-banner style, **NOT dismissable** — they persist until the admin removes them): collapsed shows title + a one-line preview + a chevron (priority is NOT shown to end users — it only drives sort order + auto-expand); expanded renders the full **`resolveRichDoc(bodyDoc, body)` via `RichDocRenderer(mediaScope = ANNOUNCEMENT)`** (rich announcements; legacy plain-`body` announcements still render via the `legacyDoc` fallback). Sorted priority→recency; **priority > 0 auto-expands**. The old `.take(3)` cap is gone (collapsed cards are compact); `AnnouncementUi` gained `bodyDoc` (read in the existing foreground `where active==true limit 60` listener — same query, zero extra reads). **Unread model (`AnnouncementSeenState`, `com.vrca.ui.common`)**: a process-scoped observable `Set<String>` of seen ids persisted to SharedPreferences (`vrca_ann_seen`) — an announcement shows a **primary-color NEW dot** until its card is first expanded (`markSeen` on expand); `unseenCount(activeIds)` is available for a nav badge. **Media prefetch + cull**: a `LaunchedEffect(announcements)` in `VrcaScreen` collects every media URL across active announcements, `ensureCachedAsync`es them into the `ann/` scope, and calls `RichMediaStore.gcAnnouncements(referencedUrls)` — so an admin removing/swapping an announcement's media frees the user's storage on the next snapshot. **Zero Firestore cost** (the listener already reads these docs; media is on jsDelivr). The background `VrchatPipelineService.attachAnnouncementsListener` still owns the off-screen NOTIFICATION. (Deferred: a nav-icon unread count badge — the per-card NEW dot ships now.)

**Phase 5 shipped = Video (playback with audio + admin transcode/upload)** (`RichDocRenderer.kt`, `RichDocEditor.kt`, `app/build.gradle`, two flavor source sets; compiles clean both variants). **Public playback (ZERO new deps)**: the `Video` block now renders `RichVideo` — poster + play button; tapping plays inline **with audio** via the built-in `android.widget.VideoView` (`AndroidView`). **Single active player** via a process-scoped `ActiveVideoState.playingKey` token (each card holds a `remember { Any() }` token; starting one sets the key so every other RichVideo drops back to its poster and releases via `AndroidView(onRelease = { stopPlayback() })`). **Admin transcode is flavor-scoped so the PUBLIC APK gains 0 dependencies**: `transcodeVideoForUpload(ctx, uri): File?` is defined in BOTH flavor source sets — `app/src/publicApp/.../VideoTranscode.kt` is a no-op stub returning null, `app/src/adminApp/.../VideoTranscode.kt` is the real **Media3 Transformer** transcode (HEVC/H.265 + AAC + ~4 Mbps bitrate cap via `DefaultEncoderFactory`/`VideoEncoderSettings`, `@OptIn(UnstableApi)` so the marker doesn't leak to the shared caller). **`androidx.media3:media3-transformer/common/effect:1.3.1` are added via `adminAppImplementation`** (admin APK only; the flavor is `adminApp`). The shared `RichDocEditor` Video block gained a **"Pick & upload"** button: it launches a `video/*` picker → `transcodeVideoForUpload` (admin compresses; public stub returns null → raw fallback) → a **19 MB size guard** (jsDelivr ~20 MB cap; over → error telling the admin to shorten or paste a URL) → `githubUploadVideoBytes` (the image uploader refactored into a shared `uploadBytesToImageStore(bytes, ext, pat)` reused by image + video, unique `.mp4` filename, jsDelivr URL). Zero Firestore cost (media on GitHub). URL-paste for video/poster still works as a fallback. (Refinement deferred: resolution downscale via `Presentation` — bitrate cap is the real size lever.)

**Phase 6 shipped = VRChat status banner consistency pass** (`VrcaScreen.kt` `VrchatStatusBanner`; compiles clean both variants). The status banner was ALREADY built in the shared collapsible-card + chip language (`ElevatedCard`, `StatusBannerState.expanded` collapse, nested `Surface` sub-cards for "Affected systems" / "Latest updates", status pills) — it's effectively the north-star the other surfaces were brought up to, so Phase 6 is a light alignment, not a rebuild: the collapse chevron switched from `KeyboardArrowUp/Down` to **`ExpandLess/ExpandMore`** (matching the announcement + What's New cards) and the "View VRChat Status Page" link became a full-width Row with an **`OpenInNew`** icon (clear external-link affordance, consistent with how links read across the revamped surfaces). Data source unchanged (`status.vrchat.com` via `VrchatPipelineState.statusPageFlow` / `startStatusPagePolling`); no RichDoc (not admin-authored). **All three popups — Update, Announcements, VRChat status — now read as one card system.**

**Post-testing fixes (device feedback)**: (1) **Bold/italic now render** — `RichText` switched from the deprecated `ClickableText` (which wasn't applying `fontWeight`/`fontStyle` spans on-device) to `Text` + `detectTapGestures`/`getOffsetForPosition` for link taps; markup + links + colors all render. (2) **Quick inline color** — `buildInlineAnnotated` gained `/c#RRGGBB=word` (colors one whitespace-delimited word, no closing tag) alongside the verbose `[c=#hex]…[/c]`. (3) **Callout colors**: info `#3B82F6` blue / success `#22C55E` green / warn `#F4511E` red-orange (a warning reads reddish, not the old amber `#F59E0B` which looked too close to success/yellow), tint 0.18. (4) **Video delivery moved off jsDelivr to `raw.githubusercontent.com`** — jsDelivr returned "Invalid URL" for the uploaded `.mp4` even at ~8MB (it serves images fine but not the video); raw serves any type/size and the client caches locally anyway. `RichVideo` shows a buffering spinner + an error fallback while the (progressive) stream loads. (5) Admin editor: the Divider block renders a real `Divider` preview (not a text description); the video HEVC-compression caption removed (admin-only screen). (6) The Home `AnnouncementCard` container is now `surfaceVariant` (was the purple `secondaryContainer`) to match the app's card vibe. (7) **Upload-only editor** — all URL text fields removed from image/video blocks (button flips to "Replace …" when set); added an "Add poster image" upload for video blocks. (8) **GIF support** — `coil-gif` + a GIF-enabled Coil `ImageLoader` (`richImageLoader`, `ImageDecoderDecoder` API28+/`GifDecoder` below) used by `RichImage`, so an uploaded `.gif` animates (the image picker already accepts `image/gif`; no new block type). (9) **Rich notes on TARGETED releases** — the per-device "Push update to this user" flow (`UsersTab`) gained a `RichDocEditor` + writes `bodyDoc` to `releases/{deviceHash}` (the releases `bodyDoc` rule already covers `{docId}`). (10) **Orphaned-media cleanup** — `githubDeleteFileByUrl`/`githubDeleteMedia` (Contents API GET-sha-then-DELETE, no-op for non-our-repo URLs) are wired into announcement **delete** (removes all its media) and **edit** (removes media that was in the old version but not the new), so removed/replaced files don't pile up in the image-store repo. (11) **Instant video, no buffering** — `RichVideo` prefetches the clip into the local cache on render (`ensureCached`) and **plays the local file** (`Uri.fromFile`), so tapping play is instant even on bad internet; it only falls back to streaming (with a spinner) if the prefetch hadn't finished. (12) **Full orphan SWEEP** — `githubSweepOrphans(db, pat)` lists every `rich/` file and deletes any not referenced by an announcement or release (owner lists both — `releases` `list` rule changed to `isOwner()`); wired to a **"Clean up unused storage"** button in `AnnouncementsTab`. **Still open (optional)**: a **faststart** transcode (moov-at-front) — not needed now that video plays from the local cache; release-retract media cleanup.

**Post-testing fixes (round 2 — device feedback)**: (1) **Bold/italic now render via per-run PARAMETER-level styling** — a Samsung device silently DROPPED per-SPAN `fontWeight`/`fontStyle` (only span color rendered), so `RichText` was rewritten: `buildInlineRuns` produces a flat `List<InlineRun>` (bold/italic/color/url per run) and the renderer draws each run as its OWN `Text` with **parameter-level** `fontWeight`/`fontStyle`/`color` on `FontFamily.SansSerif` (a real bold/italic font), inside a `FlowRow`; `\n` splits into stacked rows. The old `buildInlineAnnotated`/`urlAt` AnnotatedString+SpanStyle path was removed. Links stay tappable (per-run `clickable`). Do NOT go back to SpanStyle weight/style — some system fonts ignore it. (2) **Streamed release-asset media upload** — media now uploads as raw-byte GitHub RELEASE ASSETS to an auto-managed `rich-media` release (`ensureMediaReleaseUploadUrl` caches the upload_url, reusing `githubCreateRelease`), replacing the base64 Contents API which inflated the file 33%, built a giant in-memory JSON, and did one blocking POST + a git commit per file — that's why uploads crawled and SATURATED the connection while APK pushes (the same streamed-asset path) were near-instant. Stored URLs are the release **download URLs** (`github.com/.../releases/download/rich-media/<file>`, CDN-backed; client caches locally then plays/loads from disk). `githubDeleteFileByUrl`/`githubSweepOrphans` now find + DELETE release assets by name/id. (3) **GIF is its OWN block** (`RichBlock.Gif`) — upload a `.gif` OR paste a direct GIF link (`media.giphy.com/….gif` etc.), rendered via the GIF-enabled `RichImage`. No Tenor/Giphy search picker: Google shut down the Tenor API (Jan 2026 deprecation, June 2026 shutdown, no new keys), Giphy went paid, and a picker would add a standing third-party dependency — so direct-link + upload only.

**Post-testing fixes (round 3 — device feedback, media playback)**: (1) **Video PLAYBACK is now Media3 ExoPlayer → TextureView, NOT `VideoView`** (`RichDocRenderer.kt` `RichVideo`, `app/build.gradle`). `VideoView` (and `PlayerView`) render into a `SurfaceView`, which punches a hole in the view hierarchy and z-orders BEHIND Compose content — so the video played as a **black box** in BOTH the admin editor preview AND the published announcement (same `RichVideo` either way, proving it was the player component, not the `AnimatedVisibility` context). The clip downloaded fine (`cached ~8 MB`) and re-encoding to H.264 didn't help, confirming a rendering issue. Fix: `androidx.media3:media3-exoplayer:1.3.1` added as a top-level **`implementation` (BOTH builds, ~1 MB to public APK)**; `RichVideo` creates an `ExoPlayer` (`remember(playUri)`, released via `DisposableEffect`) rendering into an `android.view.TextureView` (`exo.setVideoTextureView(tv)`) — a TextureView composites like a normal view (no surface hole). Aspect ratio tracked from `Player.Listener.onVideoSizeChanged` (`Modifier.aspectRatio`, coerced 0.5–2.5) so it isn't stretched; `STATE_ENDED` clears the single-active-player token; `onPlayerError` shows a clean "Couldn't play this video." **Did NOT add `media3-ui`/`PlayerView`** (also a SurfaceView + not cached offline). `@androidx.annotation.OptIn(UnstableApi)` on `RichVideo` (some Media3 markers). (2) **Admin transcode is H.264/AVC, not HEVC/H.265** (`app/src/adminApp/.../VideoTranscode.kt`, `setVideoMimeType(MimeTypes.VIDEO_H264)`) — HEVC decode isn't universal (a cached HEVC clip could download yet fail to decode), and at the same 4 Mbps bitrate cap file size is comparable (HEVC only buys quality-per-bit). (3) **`RichMediaStore.ensureCached` uses OkHttp** (`followRedirects`/`followSslRedirects`, 60s read / 120s call timeouts), not `HttpURLConnection` — GitHub release-download URLs 302-redirect cross-host to `objects.githubusercontent.com`, which `HttpURLConnection` didn't reliably follow, so videos never cached locally (images/GIFs masked it via Coil's own redirect-following URL fallback). (4) **GIFs animate inside the announcement card via a per-frame redraw** — a GIF (`RichImage(animate=true)`) runs a `withFrameMillis` ticker read INSIDE a `Modifier.drawWithContent { frameTick.intValue; drawContent() }`, forcing the draw phase to re-run each frame. Required because the announcement card wraps content in `AnimatedVisibility`, which stops forcing redraws once expanded, so Coil's `AnimatedImageDrawable` froze until an external redraw (touch/scroll) — the editor preview animated fine because it isn't inside an `AnimatedVisibility`. Ticker only runs while the GIF is composed (card expanded). Blank-URL image/video-poster blocks now early-return (no "Unable to create a fetcher" noise from unfilled editor blocks). (5) **Rich editor content + scroll survive the media picker** — `releaseBlocks` (`ReleasesTab`) and `targetedBlocks` (`UsersTab`) were plain `remember { mutableStateListOf() }`, so launching the image/video/gif file picker (which recreates the heavy admin Activity — the PROCESS survives, only the Activity is rebuilt, which is why the `AdminRuntime`-held APK pick survived but these didn't) reset them → the update-log content was LOST. **`rememberSaveable` was NOT sufficient**: on recreation `AdminScreen.sharedUsers` reset empty, so the selected-user DETAIL subtree (gated on the user being present) was DISPOSED for a frame past Compose's saved-state restoration window, dropping the saved blocks (and bouncing the admin to the top of the screen). **Fix = process-lifetime state in `AdminRuntime`** (same pattern as the APK pick): `AdminRuntime.editorBlocksFor(key)` / `editorNotesFor(key)` / `isEditorSeeded`+`markEditorSeeded` / `clearEditor(key)` hold the block list + plaintext notes + one-shot seeded flag keyed by editor id (`"release"` global, `"targeted:<docId>"` per user), cleared on publish. The picker `pendingImageIndex`/`pendingVideoIndex`/`pendingPosterIndex`/`pendingGifIndex` are `rememberSaveable` so the ActivityResult callback lands on the right block. **Scroll-to-top fix (two parts)**: (a) `AdminScreen.sharedUsers` is seeded SYNCHRONOUSLY from `UsersDirectoryCache.load(ctx)` (was `emptyList()`) so the directory is populated from frame one on recreation and the detail view is never disposed (zero extra Firestore reads — same on-disk cache the async loader already used; the full-fetch/incremental decision is unchanged); (b) both editors' scroll `Column`s use `rememberSaveable(saver = ScrollState.Saver)` (the detail one keyed by `docId`) so the position is restored. The detail 10s poll / watcher writes are untouched (still gated on `selectedDocId`+foreground, so leaving the detail still stops them). **Media UPLOADS also run on `AdminRuntime.scope`** (`AdminRuntime.uploadMediaBlock` + `uploadingTagsState`/`uploadErrorsState`, `RichDocEditor(editorKey=…)`): the upload previously ran on the editor's `rememberCoroutineScope`, which the picker's Activity recreation CANCELLED mid-upload → the block never got its URL ("I pick a file but it doesn't appear as added"). Now the upload (image/gif/poster + the video transcode+size-guard) runs on the process scope and writes the URL straight into the process-lifetime block list, so it completes across the recreation; progress/errors are read back from AdminRuntime StateFlows keyed `"<editorKey>#<index>"`. All THREE editors (releases `"release"`, targeted `"targeted:<docId>"`, announcements `"announcement"`) share this. **Update popup is FULL-SCREEN** (`UpdateDialog`, `VrcaApp.kt`): `Dialog(usePlatformDefaultWidth=false)` + a `fillMaxSize().padding(10.dp)` card; the rich patch-notes `Surface` takes `weight(1f)` (fills all space between header and the pinned Download button, was capped at 50% screen height), with an iconed `headlineSmall` header + version pill. **Retract clears the popup live**: retracting a release (normal or targeted) DELETES the `releases/*` doc; the user's `releases/latest` + `releases/{deviceHash}` listeners now, on `!snap.exists()`, drop the `UpdateDialog` (and lift the forced-OSC gate) when it's currently showing THAT source's version (tracked via `lastGlobalCode`/`lastTargetedCode`, zero extra reads) instead of leaving a retracted update on screen until restart. **Media caps + pairing (`RichDocRenderer`)**: every image/gif/video is height-capped at `MAX_MEDIA_HEIGHT` (380dp) with `ContentScale.Fit` (video via `aspectRatio(matchHeightConstraintsFirst=true)`) so a TALL clip is sized down + centered instead of filling the screen; a wide clip still fills width. **An image/gif block can hold a SECOND image** (`RichBlock.Image`/`Gif` gained an optional `url2`): the editor's image/gif block has an "Add second image/GIF (side by side)" button (uploads to slot 1 via `uploadMediaBlock(..., slot=1)` → sets `url2`); when `url2` is present the block renders the two images SIDE BY SIDE (`RichImageOrPair` → `Row` of two `weight(1f)` `RichImage`s), otherwise it's a single full-width image. A "Remove second image" button clears `url2`. (This replaced an earlier adjacency-based auto-pairing of consecutive image blocks.) Both editors now PRE-LOAD the existing content for EDITING: `ReleasesTab.loadCurrent()` seeds `editNotes`+`releaseBlocks` from the live `releases/latest` (`resolveRichDoc(bodyDoc, notes)`), and the targeted `LaunchedEffect(docId)` in `UsersTab` seeds `targetNotes`+`targetedBlocks` from `releases/{docId}` — each guarded (a `rememberSaveable` `editorSeeded` flag / `seededTargetDocId` per user) so the effect's re-run on the picker's Activity recreation NEVER re-seeds over in-progress edits, and `targetNotes` is now `rememberSaveable(docId)` too.

### Chatbox Rate Limit
VRChat's chatbox update limit is 0.5 seconds (`SEND_FLOOR_MS = 500L`). Music refresh is locked at 1 second (`MUSIC_REFRESH_SECONDS_LOCKED = 1`).

### Cycle sender reads LIVE lines (stale-snapshot fix)
`startCycle()`'s loop re-reads `cycleLines` (trimmed/filtered/take(10)) **every tick** — it must NOT iterate a list captured at launch. The old captured-snapshot loop caused: editing a cycle line mid-send showed the new text (preview + the music loop's null-override rebuilds read live state) but flashed the PRE-EDIT text at each rotation boundary (only the cycle tick was stale), until a Stop/Start re-captured the list. Empty-mid-send is handled (renders without a cycle line via `forceClearIfAllOff`, keeps looping so re-adding a line resumes); `cycleIndex` is clamped when lines shrink. **`cycleIndex` advances AFTER the interval delay, not right after the send** — during the wait it must keep pointing at the on-screen line so `currentCycleLinePreview()` (preview + other sender loops' null-override rebuilds) agrees with what the tick sent; the old post-send increment made the preview point one line ahead for the whole interval (invisible in steady state as a pure phase shift, visible as a disagreement the moment a line's content changed). Edits while sending surface at the next rotation tick by design (no per-keystroke OSC push — typing would spam partial strings to VRChat).

### Manual Send takeover + modes (`HomePage.kt` Manual Send card, `VrcaViewModel`)
Sending a manual message **pauses the automated chatbox (Pinned/Cycle/Music/Time) for 20s** (`MANUAL_HOLD_MS`, bumped from 10s) so people can read it. **On hold expiry the input field is CLEARED** (both the Instant-mode `scheduleManualRevert` and the Live-loop expiry branch set `messageText = ""`) so a fresh manual send starts empty instead of the stale text lingering ("retyping reshows old text"). **The field is kept in view while typing** via a `BringIntoViewRequester` (`manualBring`) on the `OutlinedTextField` + a `LaunchedEffect(messageText.text, focused)` that `bringIntoView()`s after a 60ms relayout delay — the live preview above the field grows as you type and would otherwise scroll the input line out of view (tester-reported). Below, "10s"/"the 10s hold" refer to this same now-20s window. **Revert respects the `oscSending` master gate (`revertToNormalChatbox`)**: `rebuildAndMaybeSendCombined(forceSend=true)` builds from the feature TOGGLES, not `oscSending`, so every normal caller guards it with `if (oscSending)` — but the manual-revert paths did NOT, so sending a manual message while NOT Started (oscSending=false) and letting the hold expire fired ONE automated send if any toggle was on ("using manual message when OSC isn't sending makes automation send when the timer finishes"). All revert/clear paths (`scheduleManualRevert` expiry, both Live-loop expiry branches, `clearManual`, and the `setManualLiveModeFlag`-off path) now go through `revertToNormalChatbox(local)`: when `oscSending` it resumes automation (`rebuildAndMaybeSendCombined`), when NOT it just `clearChatbox`es the one-off manual message and restores the preview to `buildCombinedText` — never transmitting automated content. Manual send itself still bypasses the gate (one-off always allowed) and starts no loops. The pause is enforced at the single automated chokepoint: `rebuildAndMaybeSendCombined` early-returns while `manualHoldActive()` (`now < manualHoldUntilMs`), setting the Home preview to the manual text. **It does NOT pause the sender loops themselves** — the cycle/music/keepalive `while` loops keep ticking (cycle keeps advancing `cycleIndex` + its timer in the background), only their OUTPUT is suppressed, so when the 10s expires the automation resumes from wherever it now is with no reset. A manual send is NEVER gated (it goes straight through `VrcaOsc.sendMessage`), so the user can send again mid-window and it re-arms the 10s. `scheduleManualRevert()` restores the normal chatbox (`rebuildAndMaybeSendCombined(forceClearIfAllOff=true)` + typing off) once the hold expires — this is what "goes back to normal" even when no automated loop is running (OSC off). Two Manual Send controls (compact segmented toggle + a Live-only toggle, DataStore `manual_live_mode`/`manual_scroll`, local-only): **Instant** (default — type + Send, one-shot, blocked when over the 144/142 budget via `manualCharBudget()`; the Send button disables + the field shows an error counter) and **Live** (the chatbox updates every 0.5s as you type via `startManualLiveLoop`, with a `/chatbox/typing` indicator; each change re-arms the 10s hold so the message stays up 10s after the LAST keystroke, then reverts). **The typing indicator is suppressed while the invisible/minimal-background chatbox is ON** — the combination glitches VRChat's in-game UI, so the `VrcaOsc.typing` setter gates on `minimalBackground` at the chokepoint (`effective = value && !minimalBackground`; a clear still passes through). Only manual-send paths set `typing`; the automated combined senders never do. **Scroll** (Live-only) shows the newest 4 VRChat chatbox lines (`MANUAL_SCROLL_LINES`) via `formatManualScroll(text, budget)`. **It WRAPS the text itself** (`wrapToVisualLines` — greedy word-wrap by estimated proportional width `MANUAL_SCROLL_WIDTH=26f`, a touch narrower than VRChat's ~30-unit wrap so VRChat never re-wraps our lines into a 5th; honors explicit `\n`, hard-splits an over-long unbroken word) then keeps the newest ≤4 wrapped lines within the char budget, joined with `\n` to lock the breaks — older lines scroll off the top. This fixed the reported bug where splitting only on `\n` treated a long unbroken message as ONE line and trimmed CHARACTERS off the front (`takeLast(budget)`) instead of dropping whole lines. `manualCharWidth` estimates glyph width (caps/wide glyphs wrap sooner). The over-budget counter is NOT error-colored in Live mode (Scroll handles overflow by design). **The in-app field RENDERS EXACTLY AS VRChat wraps it in Live+Scroll (WYSIWYG)** — `onMessageTextChange` (scroll branch) collapses the field to flowing text (`replace("\n"," ")`) and sets it to `formatManualScroll(...)` (the wrapped, newest-4-lines-within-budget string it also SENDS), cursor forced to the end. This REPLACED `trimManualToWindow` (deleted): the old approach kept the field as raw text and only trimmed off-window lines, but the in-app box is WIDER than VRChat's chatbox, so a message that wrapped to 3 lines in VRChat showed as 2 in the box — the field's lines didn't match the chatbox, and a line scrolling off made the field jump / typing feel off (tester-reported "not smooth to type in"). Now field == what's sent == what VRChat renders (same wrap, same 4 lines), so the visible lines always agree and the field never grows past the char budget (`formatManualScroll` windows the total ≤ budget). Newlines collapse to spaces while Scroll is on (a scrolling ticker is one flowing line in VRChat), so manual line breaks aren't kept in that mode. The **Clear** button is an `OutlinedButton` (was a bare `TextButton` that didn't sit right next to the filled Send); the Scroll toggle description is "Scroll to the 4 newest lines." A **Clear** button (`clearManual()`) drops the hold, clears the field + chatbox, and restores the normal chatbox. **The live loop re-pushes the CURRENT field text every 0.5s for the ENTIRE hold window — even while the user is paused — not only on change** (`startManualLiveLoop`): a genuine edit (re)arms the 10s hold and shows the `/chatbox/typing` dots (`typingOn` flag, dots drop when paused), while every tick still sends the current (scroll-formatted) text, so a keystroke that lands between ticks during fast typing is always reflected within 0.5s and the final bit is never stranded (tester-reported "the last bit doesn't send when typing fast"). Identical re-sends are cheap and keep the chatbox message fresh until the revert. It idles (breaks) only once the field is unchanged AND the 10s hold has elapsed (the revert restored the normal chatbox); a keystroke restarts it. The legacy `messengerUiState.isRealtimeMsg` path was replaced by `manualLiveMode` in `onMessageTextChange` (the overlay + legacy conversation surfaces now share the same Live/hold behaviour).

### Cycle resume across Start/Stop (no snap-back to line 1)
`startCycle()` used to hard-reset `cycleIndex = 0` and show position 0 on the first tick, so every Start/Stop (and toggle off/on) restarted the cycle at line 1. It now captures `val resumePos = cycleIndex` (preserved by `stopCycle`, and kept advancing during a manual-send takeover) and the first tick shows `resumePos.coerceIn(0, live.size-1)` — the cycle continues from where it left off. `recentCyclePicks` is still cleared on start (shuffle no-repeat window doesn't persist across stop, acceptable).

### Invisible Chatbox Border ("skinny bubble" trick)
A display preference (user-facing name "Invisible Chatbox Border"; UI = a toggle in Settings → "Chatbox display" PLUS an unlabeled eye `IconButton` on the Home preview card next to `SendStatusChip` — deliberately no text/explanation, the preview's reaction IS the explanation; the Home preview simulates the state: full `surfaceVariant` bubble when off, floating text over a tiny 44×16dp pill when on) that appends U+0003 (END OF TEXT) + U+001F (UNIT SEPARATOR) to every outgoing `/chatbox/input` payload — the same undocumented trick VRCOSC ("Use Minimal Background") and MagicChatbox (`BlankEgg`) use. The control chars corrupt VRChat's bubble text-bounds measurement, collapsing the background to its minimum pill while the text renders normally (full removal is impossible — the pill is the client's minimum background, rendered on other players' clients). Implementation: `VrcaOsc.minimalBackground` (`@Volatile`, like `blocked`) + `withMinimalBackground(text)` applied in `sendMessage`/`sendRealtimeMessage` — caps content at 142 then appends, so the suffix can never be trimmed by the 144 limit; blank text is left blank (a chatbox CLEAR must stay a truly empty payload or an empty mini-bubble would linger in-game). `buildCombinedText` shrinks the builder budget to `VRC_MAX_CHARS - MINIMAL_BG_RESERVED_CHARS` (142) when ON so the preview matches what fits; OFF returns the 2 chars to content. The suffix is added ONLY at the OSC chokepoint — never in `combinedPreviewText`, conversation history, or Firestore. State: `VrcaViewModel.minimalChatboxBg` + `setMinimalChatboxBgFlag()` (the `Flag` suffix avoids the JVM setter clash, same as the feature toggles), persisted via DataStore key `minimal_chatbox_bg` like the notification toggles (survives close/swipe/restart; NOT a sender toggle — independent of `oscSending`/`FeatureSessionStore`, not synced to Firestore, applies to manual conversation sends too). An init collector seeds it into both `VrcaOsc` instances on every VM creation (incl. headless revival). Risk note: undocumented VRChat quirk; if VRChat patches it, the chars render as nothing and the bubble is simply normal again.

### NowPlaying
- Ad/DJ detection: Spotify uses text-based matching (`com.spotify.music` only) to prevent false positives on regular songs, plus a player-agnostic `advertisement*` (title OR artist) check. **YouTube and YouTube Music need DIFFERENT ad signals** (`NowPlayingListenerService`) — on-device traces proved seekability is NOT a universal tell:
  - **YouTube VIDEO app (`com.google.android.youtube`) uses seekability**: it strips `ACTION_SEEK_TO` from `PlaybackState.getActions()` during pre-roll/mid-roll ads (you can't scrub an ad), so `seek=N` + finite duration (≤5 min) → ad, `seek=N` + zero/absent duration → live stream. A **buffering gate** (ignore non-seekable samples while `STATE_BUFFERING`/`STATE_CONNECTING` — a loading track is non-seekable but buffering, an ad PLAYS) + **entry hysteresis** (`YT_AD_ENTRY_STREAK` = 2 consecutive non-seekable PLAYING samples, `ytSeekNStreakByPackage`) de-noise the raw signal so a song switch can't flash "Ad".
  - **YouTube MUSIC (`com.google.android.apps.youtube.music`) uses a RELATIVE DURATION COLLAPSE, not seekability and not a fixed ceiling**: an on-device trace proved YT Music reports `seek=Y state=PLAYING` for BOTH songs AND ads (a song read `dur=165s`, an ad read `dur=15s`) — so `ACTION_SEEK_TO` is useless here (the long-standing "YT Music ads never detected / show the song's name" cause — the old code assumed `seek=N`=ad, but YT Music ads stay seekable). Each title's **established (longest-seen) duration** is tracked (`songEstablishedDurByPackage`, seeded on a title change, raised whenever the same title later reports longer). An ad is flagged by EITHER of two shapes, **both NOT gated on `playing`** (a PAUSED ad keeps `title=song` + the ad's short duration — verified: a paused ad still read `dur=15s` — so detection must survive a pause; gating on `playing` was the cause of "pausing mid-ad reverts to the song title"):
    - **(1) COLLAPSE** — the title was already established LONG (a real song played) and the SAME title (`lastSongTitleByPackage`) is now suddenly playing a duration collapsed `≥ YT_MUSIC_AD_COLLAPSE_MS` (20s) below its established length (under a `YT_MUSIC_AD_ABS_MAX_MS` 3-min sanity cap). This is the classic mid-roll ad reusing the current song's title, at any ad length (44s/55s/90s — no fixed ceiling, which an earlier fixed 45s ceiling got wrong by missing 55s+ ads).
    - **(2) SHORT-UNCONFIRMED** — a short duration (`1..YT_MUSIC_AD_MAX_MS`, **60s**) on a title we have NEVER seen longer than an ad (`establishedDur ≤ 60s`). This catches the **PRE-ROLL ad**, which on YT Music shows the **UPCOMING song's title at the ad's short length** (confirmed on-device: an ad displayed the real song's title at `dur=30s`). Because that title's only-ever-seen length is the ad's 30s, there is no long baseline to collapse from, so the collapse rule alone missed it — this was the root "**first ad of a pod is missed, second one works**" cause (the second ad reuses a title the real song already established long → caught by COLLAPSE). Once the real song plays and establishes a length `> 60s`, the title is "confirmed long" and only COLLAPSE applies to it. **Tradeoff: a genuinely short (<60s) real song would be read as an ad** — accepted because the user confirmed those short durations are ads showing the song title, not real short songs. (`YT_MUSIC_AD_MAX_MS` was lowered 75s→60s per the user.)
    The 2-sample entry hysteresis (`YT_AD_ENTRY_STREAK`) rejects a transient short-duration frame at song start.
    - **(iTunes ground-truth, additive)** — `ITunesDurationLookup` (`com.vrca.nowplaying`) hardens the above by fetching the song's REAL duration from the free, key-less **iTunes Search API** (`https://itunes.apple.com/search?term=<artist title>&entity=song&limit=1` → `results[0].trackTimeMillis`). On each YT Music title it `prefetch()`es (background single-thread executor, process-lifetime `ConcurrentHashMap` cache incl. misses-as-0, in-flight de-dup so the 500ms poll can't storm it); when a real duration is cached it **RAISES** `establishedDur` (and `songEstablishedDurByPackage`) for that title. This catches the **pre-roll ad on its FIRST appearance** even when its duration exceeds the 60s short-unconfirmed cap: iTunes proves the upcoming song is long, so the ad's short duration trips COLLAPSE immediately instead of waiting for the real song to establish a baseline. Strictly additive — a network failure / no match leaves the device-duration heuristics untouched. (Caveat: if iTunes matches a longer alternate version of a genuinely-short real song it could over-flag, but that's the same documented short-song tradeoff.)
  - **Shared de-noising for both** (these caused three reported bugs — a false "Ad" flash on song switch, the real song title leaking mid-ad, and the ad number randomly climbing 1→2→3): **(3) Window-locked title** — once an ad opens the special window, `specialWindowIsAdByPackage[pkg]=true` and the redacted `title="AD"` is FORCED for the WHOLE 2s window (YouTube `isSpecialWindowActive` branch), so a single per-cycle flap can no longer leak the real song title; `lastAdInfoByPackage` preserves the parsed "X of Y" across the locked cycles. **(4) Per-break counter reset** — `VrcaViewModel.adSegmentCount` resets to 0 when `isAdNow` goes true→false (ad break ended), so each break starts at "Ad 1" instead of climbing across the session; combined with the window lock keeping `isAdNow` continuously true through one ad, the count no longer increments mid-ad. **Detection appears ~1s after the ad starts (the 2-sample hysteresis), not instantly.**
  - **Live stream detection** (video app only): `NowPlayingSnapshot.isLive` flows through `VrcaViewModel.nowPlayingIsLive`; `buildNowPlayingLines()` shows `"● LIVE"` instead of a progress bar. Live streams are not persisted to disk (restoring "LIVE" on next launch would be nonsense). (The temporary `NowPlayingDebug` on-device trace + its Settings → Debug "YouTube ad signals" card were REMOVED once detection was confirmed stable.)
- **EXACT chatbox line-width calibration (plan §8.4 — `ChatboxCalibration` + Settings→Debug harness)**: replaces `TitleCleaner`'s ESTIMATED `charWidth` weights + `VISUAL_LINE_UNITS=28.5` fudge with values MEASURED in-headset. The harness (`ChatboxWidthCalibrationTool`, Settings → Debug) sends a line of one repeated char via `vm.sendCalibrationLine`→`VrcaOsc.sendRaw` (verbatim, no minimal-bg suffix / manual hold); the user reads the wrap point in VRChat and records the **max count that stays on ONE line** per reference char (`ChatboxCalibration.REFERENCE_CHARS` spans the width spectrum incl. a fullwidth `Ｗ`). On Save, `ChatboxCalibration` (SharedPreferences, in-memory table loaded once via `attach()` in `VrcaApplication.onCreate` so `TitleCleaner` stays Context-free) stores `maxCount` per char. Model: each copy of char `c` consumes `1/maxCount(c)` of the line, so `visualWidth(s)=Σ 1/maxCount(char) ≤ 1.0` fits; `TitleCleaner.charWidth` returns the measured fraction for measured chars and the estimate SCALED (`calibScale` = avg of `(1/maxCount)/estimatedWidth` over measured chars, cached per `generation`) for unmeasured ones; `lineBudget()`=1.0 calibrated / 28.5 estimated. When uncalibrated it's the old estimate path (unchanged). `estimatedCharWidth` = the old `charWidth` logic, kept as the fallback.
- **One-line title fitting (`TitleCleaner`, `com.vrca.nowplaying`)**: YouTube / YT Music (and sometimes Spotify) titles are long and padded with non-identifying cruft, and the old `buildNowPlayingLines()` did a blunt `safeTitle.take(maxLine-1) + "…"` that chopped mid-word and could destroy the song's identity. `TitleCleaner.fitOneLine(title, artist, maxLine=42)` now fits `"artist — title"` onto ONE chatbox line via a cheapest-loss→most-loss ladder, stopping the moment it fits: **(1)** strip Tier-1 cruft (FREE, zero identity loss) — `(Official Music Video)`/`(Lyrics)`/`(Visualizer)`/`(Remastered 2021)`/`(HD)`/`(4K)`, the YT Music `- Topic` artist suffix, trailing `| Label` promo segments, Spotify's trailing ` - <tag>` version suffixes, `#shorts` hashtags, and emoji/pictographs; **(2)** de-dup an artist embedded in the title (`"Artist - Song"` → `"Song"` since the artist is shown separately); **(3)** if still over, drop Tier-2 borderline tags (`feat.`/`ft.`/`deluxe`/`radio edit`/`explicit`/`extended`…); **(4)** if still over, drop the separate artist (the song name identifies it); **(5)** last resort — word-boundary truncate with `…`, preferring a clean cut unless it wastes >12 chars (then hard-cut to keep more identifying text). **Tier-3 keep-words are NEVER stripped** (a bracket group / ` - ` suffix containing one is left intact even if it also contains a cruft word): `remix`/`vip`/`bootleg`/`acoustic`/`live`/`unplugged`/`instrumental`/`demo`/`cover`/`sped up`/`slowed`/`nightcore`/`taylor's version`/possessive- & language-`version`/`from "Movie"`/`soundtrack` — these denote a DIFFERENT recording, so removing them would change which track it is. Cruft is matched only inside `(...)`/`[...]` groups or after a `|`/` - ` separator (bare title words are never stripped, so a song literally named "Video Games" survives). Applied to both the live-stream line and the main music line. All regexes JVM-validated. **"Fits one line" is a DUAL constraint** (`fitsOneLine`): ≤ 42 chars AND ≤ `VISUAL_LINE_UNITS` (28.5f — backed off from the ~30 empirical edge for estimator-noise margin) estimated visual width via `visualWidth()`/`charWidth()` — VRChat's chatbox wraps centered PROPORTIONAL text at ~30 average-character widths, so a 41-char caps-heavy title within the char budget still wrapped to a second rendered line ("Theodore Roosevelt Would LOVE…" wrapped at ~29 chars; "…the DUMBEST mfs on earth" at ~31 — the empirical calibration). Weights: **`…`=2.0 — the ellipsis is three dots on ONE char; counting it 1.0 made `truncate()` emit a "shortened to fit" title whose appended `…` itself pushed the line over, wrapping the whole last word in-game**, `m w M W`=1.4, uppercase/digits=1.2, narrow (`i j l t f r`, space, light punctuation)=0.6, CJK/fullwidth=2.0, else 1.0. The 42-char cap is kept so a skinny all-lowercase title can still use every char that genuinely fits. The last-resort `truncate` walks the prefix by width (ellipsis included) before the word-boundary cut. **User opt-out**: `TitleCleaner.rawOneLine(rawTitle, rawArtist, maxChars)` is the "filters off" path (Media → Now Playing "Filter Titles" toggle) — it does NONE of the above (no cruft strip, no artist-dedup, no one-line fit), just `"artist — title"` with a hard char cap; the gate lives in `buildNowPlayingLines()` (`if (musicCleanTitles) fitOneLine(...) else rawOneLine(..., VRC_MAX_CHARS)`).
- **Ads keep the progress bar**: `buildNowPlayingLines()`'s ad branch renders a bare `"Ad"` label PLUS a progress bar driven by the ad's OWN position/duration (forced playing) — the bar used to vanish during ads, leaving a bare label. No brand/title is shown, so nothing leaks. When the ad reports no duration (some YT Music audio ads) it falls back to just the label.
- **Ad label is a bare `"Ad"`**: ads render as just `"Ad"` (no index). The player's ad index ("Ad 1 of 1") was unreliable, so it was dropped per the user — the label is a constant `"Ad"`. (`parseAdIndex`/`nowPlayingAdInfo`/`adSegmentCount` still exist and are still set, but the label no longer consumes them.) **The ad-label block in `buildNowPlayingLines()` is gated on a dedicated `nowPlayingIsAd` flag (set from the collector's `isAdNow`), checked BEFORE `isSpotifyDj` with an early `return`.** This fixes "the ad never shows": a Spotify ad blanks the artist (`title="AD"`, `artist=""`), which made `isSpotifyDj` (`safeArtist.isBlank()`) evaluate TRUE — so the old `if (nowPlayingSpecialActive && !isSpotifyDj)` guard was false and the whole ad block was skipped, falling through to render a bare "AD" title + progress bar. An ad and a DJ segment both blank metadata; only the explicit `nowPlayingIsAd` flag distinguishes them, so the ad path must be checked first and independently of `isSpotifyDj`.
- Special window: Spotify/generic ads use a 10s window (cleared immediately once real metadata appears). **YouTube ads use a short 2s window** refreshed every 500ms by the continuous poll while seek=N holds; after the ad ends the window expires naturally in ≤2s. YouTube packages are EXEMPT from the eager `clearSpecialWindow` + `stopPoll` in the `isSpecialWindowActive` branch — their `onMetadataChanged` callback can race with `onPlaybackStateChanged`, reading a stale `controller.playbackState` (still seek=Y from the previous song) before the PlaybackState update propagates. That race falsely triggered "ad ended" → cleared the special window AND killed the continuous pause-detection poll, so (a) the ad indicator never appeared (showed the song name instead of "Ad N") and (b) the pause detector stopped receiving samples. The fix: for YouTube packages, the `isSpecialWindowActive` branch is a no-op — the poll keeps running, the window stays alive while the ad is ongoing (refreshed each seek=N cycle), and expires organically after the ad ends.
- **YouTube / YouTube Music ARE detected** (earlier notes claiming they can't be were wrong — they DO publish a MediaSession with title/artist/position). Two quirks are handled in `NowPlayingListenerService`: (1) **Pickup delay** — a freshly-started session used to surface only when the app posted its media notification (a few seconds late). An `MediaSessionManager.OnActiveSessionsChangedListener` (registered in `onListenerConnected`, unregistered in `onListenerDisconnected`) now wires the controller the instant the session goes active, so detection is near-immediate (future-proof for ALL media apps, not just YouTube). (2) **Progress kept moving while paused** — YouTube keeps reporting `STATE_PLAYING` + `playbackSpeed=1f` even when PAUSED, so the only reliable pause signal is that the raw `position` stops advancing across snapshots. That position-stall check lives in `NowPlayingState` and needs fresh samples every ~500ms, but YouTube's `MediaController` fires almost no callbacks while paused — so `ensureControllerForPackage` now starts a **continuous 500ms poll for YouTube packages** (`startPollForRealTrack`, exempt from the 10-min cap that the Spotify ad/DJ poll uses; it self-stops on controller change / teardown / session-destroy / notif-removed). The poll pushes a snapshot EVERY cycle for YouTube (other players still push only on real change / every 4s), so the stall detector reaches its 2-cycle threshold within ~1s of a pause → forces `isPlaying=false` → `effectiveIsPlaying` goes false → `buildNowPlayingLines` stops extrapolating and freezes the progress bar at the real snapshot. On resume the real position is being pushed every 500ms, so the bar continues from the true timestamp with no teleport.
- Motion-based play detection with YouTube-specific stall tracking. **YouTube Music play/pause is the INVERSE case and must NOT use the position-stall hack**: its raw `position` field is unreliable — it can ADVANCE while paused and FREEZE while playing — so the motion/stall detector inverted it (playing showed as paused, paused showed as playing, with the progress bar moving/freezing backwards). YT Music's reported `PlaybackState.state` IS honest, so it is trusted directly: `NowPlayingState.update()` sets `inferredIsPlaying = snapshot.isPlaying` for `com.google.android.apps.youtube.music` (skipping the stall block, which now applies ONLY to the video app `com.google.android.youtube`), and `computeDisplayedPlaying()` short-circuits `return nowPlayingReportedIsPlaying` for YT Music before any motion logic. The video app keeps the `STATE_PLAYING`+`speed=1f`-while-paused stall workaround unchanged.
- **Anchor freeze-on-pause (`maybeFreezeNowPlayingAnchor`) — kills the "+2s jump" (headset browser source)**: the progress bar extrapolates position from an anchor `(nowPlayingPositionMs, nowPlayingPositionUpdateTimeMs)`. While NOT displaying-playing, `buildNowPlayingLines` shows the frozen posSnapshot, **but the anchor TIME kept advancing** — so the instant play resumes, `pos = posSnapshot + (now - anchorTime)` SNAPS forward by the whole paused gap. A dense/accurate source (mobile Spotify/YouTube — the latter gets the continuous 500ms poll) self-corrects in a frame so it's invisible; a SPARSE, flickery source shows a stark skip. **On the Quest the only media source is the browser MediaSession (`com.oculus.browser`), which is NOT in `youtubePackages` (so it gets NO continuous poll — only sparse ~3s callbacks) and momentarily reports `isPlaying=false` between pushes** — so its flicker freezes then snaps the bar every few seconds (the tester-reported "smooth in preview, +2s jump in VRChat, consistently every ~3s"). The NowPlaying position/anchor/send code is 100% SHARED between mobile and headset — nothing was changed per-flavor; the differentiator is purely the media source. **Fix (source-agnostic, also correct for a genuine pause)**: the UI tick calls `maybeFreezeNowPlayingAnchor(displayedPlaying)` before assigning `nowPlayingIsPlaying` — while not displaying-playing it holds the bar in place AND re-stamps `nowPlayingPositionUpdateTimeMs = elapsedRealtime()` every tick, so resuming continues from where the bar visually stopped instead of snapping forward by the gap; on the play→pause edge it captures the currently-shown extrapolated position (same 1x-speed fallback as `buildNowPlayingLines`) as the frozen value so pausing doesn't lurch the bar BACKWARD to a stale snapshot either. A real seek during pause still re-anchors via the next snapshot's keepAnchor drift check (seeks aren't lost); ads/live are unaffected (their branches force displaying-playing true, so the freeze no-ops). **Paired with dense sampling (`NowPlayingListenerService.continuouslyPolledPackages` = `youtubePackages + "com.oculus.browser"`)**: the browser now gets the SAME continuous 500ms poll YouTube does, so its position/play-state is sampled every 500ms instead of on sparse ~3s callbacks — the anchor stays fresh (any correction is <500ms, invisible) and a real pause is seen within ~1s. The set gates ONLY polling (poll-start, push-every-cycle, no-10min-cap, and NOT-stopping-on-a-normal-track); the YouTube-specific stall/ad-detection logic stays keyed on `youtubePackages`, so the browser still uses normal motion-based play detection, not the YT hacks. Together: dense sampling keeps the bar glued to reality, and the freeze-on-pause anchor handles any residual flicker without a snap.
- **Last-track persistence across revival (`NowPlayingState.attach`/persist)**: the snapshot lives only in process memory, so after an OS-initiated process kill + headless ViewModel revival (`KeepAliveService` → `ensureRuntimeViewModel`) a PAUSED track had no active MediaSession to re-push and the chatbox NowPlaying line just blanked out. `NowPlayingState` now persists the last *detected, non-special* track (title/artist/duration/position/package) to SharedPreferences (`vrca_nowplaying`) inside `update()` and `pauseIfActivePackage()`, and `VrcaApplication.onCreate()` calls `NowPlayingState.attach(context)` to re-seed `_state` from disk **as paused** (re-anchoring `positionUpdateTimeMs` to `now`, `speed=0`, `isPlaying=false`) before any ViewModel collects — so the previous track shows immediately instead of disappearing. `attach` is idempotent and only restores while the in-memory state is still blank (never clobbers a live one). Ads/DJ/special windows are never persisted (restoring "AD" would be nonsense). A genuine hard clear (`clearIfActivePackage`) removes the persisted entry so a real stop doesn't resurrect a stale track. Also fixes the plain cold-start case, not just headless revival.
- **Blank-metadata push keeps the last track (close-blanking fix)**: closing the media app (vs Spotify, which keeps its MediaSession alive in the background) tears its `MediaSession` down — YouTube/YouTube Music in particular fire `onMetadataChanged(null)` / the 500ms poll reads empty metadata right around teardown, pushing a `detected=false` (blank title+artist) snapshot. The old `NowPlayingState.update()` wrote that blank straight into `_state`, instantly wiping the preview AND the OSC chatbox line ("closing YouTube removes the text immediately; Spotify doesn't"). The disk persistence above did NOT rescue this — `attach` only re-seeds when in-memory state is blank at *process start*, but here the process is still alive. **`update()` now treats a blank push for an already-`detected` track as a PAUSE: it freezes the last known title/artist (`speed=0`, `isPlaying=false`, clears `specialActive`/`adInfo`) instead of clearing**, mirroring `pauseIfActivePackage`'s keep-last behavior — so YouTube freezes the last track on close exactly like Spotify. A genuine hard stop still goes through `clearIfActivePackage()` (the only remaining blank path). **YouTube ad detection remains a known limitation**: during a YouTube ad the MediaSession keeps the VIDEO's title but reports the AD's (short) duration/position, so the progress bar shrinks to the ad then jumps back. `classifySpecial()` only flags ads when the title/artist starts with `"advertisement"` (Spotify's behavior); YouTube exposes no reliable ad flag, so this is intentionally left as-is rather than adding a fragile duration-collapse heuristic that would false-positive on Shorts / short videos / chapters.
- Crystal progress bar (preset 3) uses filled diamonds (U+25C6) before the marker position
- **Progress bar time separator is plain `/`; the PAUSED bar drops one filler slot instead** (`pausedTrim` in `renderProgressBar` / `renderSoundwaveBar`). The real overflow culprit was the PAUSE marker: `⏸` (U+23F8) gets emoji presentation in VRChat's chatbox font (and Android's), rendering ~2-3× wider than the text-glyph playing dot `◉` — so the bar+time line wrapped when paused but not when playing. Rather than swapping the emoji, the bar sheds ONE identical filler slot while `dotIsPlaying == false` (hearts 8→7 inner, presets 2/3/5 10→9, soundwave 8→7); every slot is the same glyph so it just reads one char shorter, the marker index rescales automatically, and the slot returns on resume. If in-game testing shows paused still wrapping, the tuning knob is dropping one more slot. Separator-swap attempts that FAILED before this approach (don't retry them): ASCII `|` has wide punctuation side bearings (rendered `14:47 | 20:5` with gaps, wrapped); FRACTION SLASH `⁄` U+2044 triggers fraction composition (`41:³⁰⁄₅₁:08` super/subscript digits); dental click `ǀ` U+01C0 was fine but unnecessary once the real culprit (the pause emoji) was found. **YouTube video does NOT get extra title lines** — VRChat's chatbox renders centered proportional text at ~30 average chars width, so a wider char budget just keeps the artist prefix and wraps worse visually; the single-line `TitleCleaner.fitOneLine()` (42-char cap + 30-unit visual width) with artist-drop escalation is the correct behavior for long video titles.

### UI Naming
- The "AFK" feature is renamed to "Pinned" in all user-facing strings. Internal variable/key names (`afkEnabled`, `afkMessage`, etc.) remain unchanged for backwards compatibility with DataStore and Firestore.
- Default presets use "Away" instead of "AFK" (e.g., "Away - grabbing water").
- Notification icons use a dedicated `ic_notification.xml` drawable (95% scale, less padding than adaptive launcher icon) for better visibility in the notification shade. **ALL persistent foreground-service notifications** — `VrchatPipelineService.buildPersistentNotification`, `KeepAliveService`, `DiscordRpcService.buildNotif`, and `OverlayService` — use `ic_notif_sync.xml` (the Material "sync" circular-arrows glyph, matching the Automations tab icon). Since all four services share `NOTIF_ID = 1001`, any service that rebuilds its notification overwrites the others in the shade — so they must all use the same icon or the sync glyph would revert to the old icon when another service updates. Event alert notifications (friend activity, invites, groups, etc.) still use `ic_notification.xml`. **Duplicate-`ACTION_START` notification guard**: because the services share `NOTIF_ID = 1001`, a duplicate `ACTION_START` (app reopened after pressing Back, or a sticky restart) must NOT reset the shared persistent notification to a startup state. `VrchatPipelineService.onStartCommand` only posts `"Connecting..."` when NOT already connected; when already connected it re-asserts `startForeground` with `lastConnectedNotifText` — otherwise `onOpen` never fires again to restore "Connected as X" and the notification gets permanently stuck on "Connecting..." even though presence/RPC are live. Likewise `DiscordRpcService.onStartCommand` posts `"Discord RPC active"` (not `"Discord RPC starting..."`) when already running.

### Remote Config (Admin Edits)
The public app's moderation snapshot listener on `users/{deviceHash}` also picks up admin-editable fields (feature toggles, messages, intervals, presets) and applies them in real-time. Each incoming snapshot is filtered by:
1. `initialSnapshotProcessed` — drop the very first snapshot (DataStore wins cold start)
2. Per-field `lastSyncedValues` comparison — if the remote value matches what we last wrote, it's our own echo and is skipped; if it differs, an admin changed it and it's applied

After those filters, the snapshot's content fields (afkMessage, cycleLines, presets) are written to DataStore so existing flow collectors propagate them into ViewModel state. Toggles are set directly on the ViewModel (they don't persist — see below).

### Removed Features (do not re-add)
- **Divider system**: Previously allowed inserting text dividers between chatbox components. Fully removed from UI, ViewModel, DataStore, and message construction.
- **Preset naming**: Custom names for pinned/cycle presets. Removed from ViewModel, AdminScreen, DataStore, and Firestore sync. Presets now use generic "Preset 1/2/3..." labels.
- **Redundant action buttons**: Start/stop buttons in Cycle, pin/unpin + send once in Pinned, start/stop + test in Music — all removed since toggles handle activation.

### Toggle Persistence
Feature toggles (`afkEnabled`, `spotifyEnabled`, `cycleEnabled`, `timeEnabled`) survive Android process death (memory pressure) via `SavedStateHandle`, and survive Activity destruction because the runtime `VrcaViewModel` is now **app-scoped** (owned by `VrcaApplication`'s `ViewModelStore`, not the Activity's — see "Background Survival"). Content (messages, intervals, presets) DOES persist via DataStore as normal. Admin can still flip toggles in real-time via the Firestore snapshot listener, which writes directly to ViewModel state (and SavedStateHandle) without touching DataStore.

**OSC send gate (Start/Stop) — toggles configure WHAT, Start gates WHEN**: A master `VrcaViewModel.oscSending` boolean decides whether the enabled toggles actually transmit over OSC. **Toggling a feature no longer auto-starts sending** — flipping `afkEnabled`/`cycleEnabled`/`spotifyEnabled`/`timeEnabled` while `oscSending == false` only updates the local preview and persists the config; no OSC leaves the device and the VRChat chatbox is untouched (this lets the user set up their preview first). `startSending()` (Home "Start sending" button) flips `oscSending` true and launches the sender loops (`startAfkSender`/`startCycle`/`startNowPlayingSender` + `manageKeepaliveLoop`) for whatever toggles are on; each starter early-returns unless `oscSending && <its toggle>`, and every sender `while` loop also checks `oscSending` so the master gate hard-stops them. `stopSending()` (Home "Stop sending" button, replaces the old red **KILL**) flips `oscSending` false, stops all sender loops, and clears the VRChat chatbox **WITHOUT untoggling any feature** — so pressing Start again resumes exactly what was configured. While sending, toggling a feature applies live (starts/stops just that loop). `killStopAndClear()` still exists but is now reserved for the **ban path** (`VrcaScreen.kt` ban gate) — it additionally untoggles all four features. The Home UI shows a `SendStatusChip` (amber dot "Sending" / grey dot "Idle" — **deliberately NO uptime in the chip**: "Sending · 2h 14m" squeezed the headlineSmall title into wrapping "VRChat / Preview"; the Stop button carries the uptime instead) in the header as the at-a-glance OSC indicator, and the Quick Toggles header sub-label flips between "Press Start when ready" and "Edits show live" (plus a "hold a pill to edit" hint). **Home layout (`HomePage.kt`, Phase 4 of the UI revamp — see `docs/ui-revamp.md`)**: the original **"VRChat Preview"** `SectionCard` (title enlarged via `titleStyle = headlineSmall`) keeps its original visual identity — the centered tonal preview `Surface` above the faint avatar-silhouette `Canvas` (two redesign attempts were reverted per user preference; do NOT restyle it). **The bubble text sets an explicit `color = onSurface`** (expanded bubble + collapsed chip): `Surface(color = surfaceVariant)` switches `LocalContentColor` to the muted `onSurfaceVariant`, which dimmed the preview text — the user read the dimming as "the box is rendered on top of the text". In-game chatbox text is white, so the bright onSurface matches; the minimal-pill branches don't need it (their Text is a SIBLING of the pill, inheriting the card's content color). The container is a flexible `Column` with **no height cap and no `Arrangement.Center`** (120×120 silhouette) — first the old fixed-280dp overlay `Box` overflowed, then the `heightIn(max=420.dp)` + Center replacement STILL overflowed while sending (a max-height Column with Center arrangement lets oversized content escape BOTH edges of the card); the bubble text is capped at 9 lines so letting the card grow naturally is bounded and can never spill. A **character-budget counter** ("N / 144 characters", right-aligned `labelSmall` on the row under the preview that carries the eye toggle on its left; budget drops to 142 with the invisible border on; amber within 30 of the cap, red over) shows how much of VRChat's chatbox limit the current combined output uses. The card is **collapsible**: a chevron in the card actions (persisted via `UiPrefs.previewExpanded`) toggles between the full simulation and a compact "live chip" bubble (tap to re-expand) — the chip shows the FULL chatbox content (`maxLines = 9`, same cap as the expanded bubble; the original 3-line cap cut content off and read as broken). **The invisible-border eye toggle lives BELOW the preview, on the LEFT of the character-counter row — NOT in the header actions row.** With the eye in the header, the wider "Sending" chip squeezed the headlineSmall "VRChat Preview" title into clipping; removing the eye from that row frees enough width that the title renders whole at FULL size in both Idle and Sending states (per the user: the title must look identical, including size, in both states — never shrink it). **`SectionCard` titles keep an auto-shrink safety net** (`VrcaScreen.kt`: `onTextLayout` → fontSize × 0.92 relayout loop, floor 11sp, `softWrap = false`) whose trigger is **`isLineEllipsized(0) || didOverflowWidth`** — with `overflow = Ellipsis` the truncated layout "fits" so `didOverflowWidth` alone NEVER fires on-device (the original shrink hack was dead because of this); the actions Row spacing is 4dp. Manual Send's collapsed summary is just "Type a manual message". The card also carries the `SendStatusChip`, a **"Next cycle in Ns" ticker** (driven by `VrcaViewModel.nextCycleAtMs`, set by the cycle loop before each delay; a 1s `nowTickMs` ticker runs only while sending), the full-width Start/Stop button (Stop label carries the uptime), the cycle-trim warning, and the nested **Quick Toggles** `ElevatedCard`. **Quick Toggles render as a VERTICAL full-width `TogglePill` list** (PublicUiKit) in `vm.cardOrder` order — the 2×2 grid was reverted (pill labels got clipped); long-press jumps to the feature's edit page (Pinned/Cycle → Automations, Now Playing → Media, Time → its UTC dropdown); the Time pill label shows the mode ("Time · UTC+2"). **`TogglePill` uses `combinedClickable`, NOT a raw `pointerInput(checked, …)`** — the keyed detector was cancelled+relaunched on every state flip, so taps landing mid-restart were dropped or replayed a stale `!checked` (a no-op write → no recomposition → "the dot only updates when another toggle changes"); combinedClickable keeps lambdas fresh and adds ripple. The pill's state indicator is a single dot pinned to the **RIGHT edge** (labels vary in width; a dot trailing the text looked ragged). **No ON/OFF text next to the dot** — it was added once and rejected by the user as UI clutter; do not re-add. The Edit mode (Edit/Done in the Quick Toggles header) is **shared**: it switches the list to the legacy vertical arrow list (chatbox COMPONENT order, `vm.cardOrder` → OSC output order) AND overlays slim reorder strips on the page-level cards; while active, a **full-width "Done reordering" button is pinned at the top of the page** (the header Done can be off-screen while moving upper cards). **Home page-card order** (`Preview`/`Connection`/`ManualSend`) is a separate `UiPrefs.homeCardOrder` CSV pref (Reset resets both orders). **Connection and Manual Send are `CompactSectionCard`s** (collapsed = status): Connection's collapsed summary is TWO lines — the IP with the slot's device name underneath (`"$ip\n$slotName"`; the summary Text allows 2 lines) — with a live reachability chip (20s `InetAddress.isReachable` ping while composed — green "Reachable", amber "No reply" since OSC is UDP and many devices ignore pings, red "Not set"); expanding reveals the `IpField`. **Setup health checklist** (`SetupHealthCard`) replaced both the old every-tab "Setup incomplete" banner (deleted from `VrcaScreen`) and the Home "Setup Tutorial" card (deleted — onboarding owns the walkthrough, replayable from Settings): a Home-only tertiary card listing ONLY the currently-red items (VRChat not linked, IP not set, battery optimization on, Notification Access missing **gated on `spotifyEnabled`** so it can't be permanently red for non-media users), each row deep-linking to its fix; it disappears entirely when all green. Other tabs get at most a **red `Badge` dot on the Home nav icon** (`setupNeedsAttention` = !vrcLinked || !ipSet, with `vrcLinked` now reactive to `vm.vrchatLoggedOut`). **Sending uptime (`VrcaViewModel.sendingSinceMs`)**: stamped on a fresh Start, cleared on Stop/kill, persisted in `FeatureSessionStore` (`feature_sending_since` + a 60s `feature_sending_seen` heartbeat from `startUptimeHeartbeat()`); `restoreFeatureSession()` continues the counter across an OS-kill revival only when the heartbeat is within `UPTIME_RESTORE_GRACE_MS` (20 min — spans the watchdog's ~15-min recovery gap, same pattern as the Discord RPC counter), else sending resumes with a fresh counter. A deliberate swipe disarms restore so the counter always resets.

**Auto-restore after an OS kill (`FeatureSessionStore`)**: A deliberate **swipe** still starts clean (toggles OFF, not sending), but an **OS-initiated process kill** (memory pressure / Doze / OEM killer) is made invisible — the chatbox resumes on its own **only if the user was actually sending**. `FeatureSessionStore` (`com.vrca.app`, backed by the `vrca_remote` prefs file) persists the active toggle set **plus the `sending` gate** on every toggle/Start/Stop change and **arms** restore. `VrcaViewModel.restoreFeatureSession()` runs once after initial content load: it re-applies the toggle CONFIG directly (so the preview reflects what was set up), then resumes OSC sending via `startSending()` **only when the persisted `sending` was true AND something was enabled** — toggles configured but never started come back idle (no transmission). `AppShutdown.onTaskSwiped()` calls `FeatureSessionStore.disarm()` so a real swipe (the only deliberate stop) is NOT restored — an OS kill never runs `onTaskSwiped`, so it stays armed. This is the per-session source of truth for "was this stop deliberate?"; do not key restore on the `manual_kill_at` timestamp (it persists across sessions and can't distinguish a stale prior swipe from a fresh OS kill).

### Background Survival ("works as if on-screen; only a swipe stops it")
The runtime is decoupled from the Activity so everything keeps running while backgrounded, and recovers after an OS kill:
- **App-scoped ViewModel**: `VrcaApplication` implements `ViewModelStoreOwner` with a process-lifetime `ViewModelStore` (cleared only by `AppShutdown` on swipe). `VrcaViewModel` is obtained against it (`VrcaApp.kt`, `OverlayService.kt`), so the chatbox OSC senders, moderation/kill listener, NowPlaying consumer, and Firestore sync loops survive Activity destruction (`onCleared` only fires on a real swipe). The Factory uses `VrcaApplication.instance` (CreationExtras has no `APPLICATION_KEY` against an app store) and a plain `SavedStateHandle()` (an app `ViewModelStoreOwner` has no `SavedStateRegistry`, so `createSavedStateHandle()` would throw).
- **Headless ViewModel recreation**: after an OS kill the watchdog / `START_STICKY` / `BootReceiver` bring back `KeepAliveService` with no Activity — so everything the app-scoped VM owns would stay dead. `KeepAliveService.onStartCommand` calls `VrcaApplication.ensureRuntimeViewModel()` (idempotent, main-thread `ViewModelProvider` create) **ALWAYS on revival** (after the swipe guard, which returns `START_NOT_STICKY` for a deliberate swipe — so this only runs for a genuine OS kill). **This is NOT gated on `sending && anyEnabled`** — that gate was a real bug: the VM owns the chatbox senders AND the hourly liveness heartbeat (`startHourlyHeartbeat`) AND the moderation/watch/kill listener, so withholding it from a non-OSC-sending user (e.g. a Discord-RPC-only user) left their process genuinely alive (services + Discord RPC running) but reporting itself **permanently offline** to the admin — `lastActiveAt` froze (no heartbeat), and the moderation listener never re-attached so `watcherActiveAt` was never read → `AdminWatchState.isWatched` never flipped → neither the VM live-sync loop nor the service presence loop wrote → **the admin detail never updated and Kill/toggles never reached the device** (the symptom: "user is clearly online in Discord but shows offline / detail won't update no matter how long I spectate"). Liveness + moderation are runtime concerns independent of whether OSC is sending, so the VM must always come back. **OSC transmission stays separately gated** — `restoreFeatureSession()` only calls `startSending()` when the persisted session had `sending && anyEnabled`, so always-rebuilding the VM never makes a configured-but-idle user start transmitting. The cost of an idle revived VM is trivial (1 hourly heartbeat write + 1 moderation listener — the same cost any online user pays) and the process is already awake via the foreground services + wakelock.
- **Discord RPC / presence / notifications / timer** self-restore via the service layer: `VrchatPipelineService` restarts (sticky/watchdog/boot), auto-relogins from EncryptedSharedPreferences, and on `onOpen` re-reads `discord_rpc_enabled`+`discord_session_seeded` (DataStore) to auto-start `DiscordRpcService`; the RPC online timer has its own grace-window restore.
- **Discord WebView keep-alive WITHOUT an overlay**: the Discord gateway lives in a WebView whose JS Chromium throttles when it thinks the page is hidden (gateway drops ~45s after screen-off). Rather than a 1×1 `TYPE_APPLICATION_OVERLAY` window (which forces Android's "displaying over other apps" notification and looks sketchy), `DiscordRpcService` subclasses `WebView` and overrides `onWindowVisibilityChanged` to always report `View.VISIBLE`, plus `resumeTimers()` + `dispatchWindowVisibilityChanged(VISIBLE)` after creation. No overlay window, no system notification. **Three-layer defense against background JS throttling** (the one-time calls alone weren't enough — the RPC was silently dropping after extended background): **(1) Periodic `resumeTimers()` (every 10s via `pushActivity` + every 30s via session monitor)**: the 10s native presence timer calls `wv.resumeTimers()` before every `evaluateJavascript` push, keeping JS timers alive well within Discord's ~80s server heartbeat timeout; the 30s session monitor also calls `resumeTimers()` + `dispatchWindowVisibilityChanged(VISIBLE)` as a backstop. **(2) JS-level heartbeat keepalive**: `_vrca_sendPresence()` (called from native every 10s via `VRCA_setActivity`) checks if Discord's heartbeat has stalled (`Date.now() - lastAck > interval × 1.5`) and manually sends OP 1 (Heartbeat, `d: _vrca_lastSeq`) to keep the gateway alive even when Discord's own `setInterval` is frozen by Chromium. `_vrca_lastSeq` tracks the last sequence number from readable (uncompressed) inbound frames; `null` is valid per Discord's spec. **(3) Native JS freeze detection**: `lastJsResponseMs` (`@Volatile`) tracks when the last `evaluateJavascript` callback fired (updated in every push/health/shim callback). If no callback fires for `JS_FREEZE_RELOAD_MS` (120s), the session monitor forces `onSessionExpired()` (page reload) to recover — catches the case where even `resumeTimers()` doesn't work (some OEMs). Additionally, `sessionRecoveryCount` resets after `HEALTHY_RESET_MS` (5 min) of sustained healthy checks so intermittent drops over a long session don't exhaust `MAX_SESSION_RECOVERIES`.
- **Discord RPC "socket healthy but RPC gone from Discord" — the asset-resolution dead end (`VRCA_setActivity`)**: the PRIMARY cause of the user-reported "the app says connected but the RPC isn't on my Discord profile". EVERY image (including the default VRChat image) is an `https://` URL that must be resolved to a `mp:external/...` path via Discord's `external-assets` API before it can ride the OP 3 activity. The old `VRCA_setActivity` set `_vrca_activity` for an uncached image URL but **only called `_vrca_sendPresence()` inside the resolve success callback** — so if resolution failed (token not yet grabbed from `localStorage`, `fetch` deferred under background throttling, or the image URL just won't resolve), it retried 10× then gave up and the activity was **NEVER sent to Discord** even on a perfectly healthy socket. Native treated the `'resolving'` return as success (`consecutivePushFailures = 0`), the health monitor reported `alive`, and the UI showed CONNECTED — while the RPC was silently absent. The next 10s push re-entered with the same uncached URL → same dead end → permanent. **Fix: send the activity IMMEDIATELY without the unresolved image** (a deep-cloned copy with `large_image` stripped — Discord rejects a raw `https` `large_image`), store that image-less version as `_vrca_activity` so re-pushes keep showing it, THEN resolve the image and re-send the upgraded version. On permanent resolution failure the RPC stays visible (just without the world thumbnail). The RPC can no longer be blocked by image resolution. `VRCA_setActivity` now returns `'ok'` (presence already sent) instead of `'resolving'`.
- **Discord RPC world-switch stale-resolution race (`_vrca_activityGen`)**: each `VRCA_setActivity` starts its own `tryResolve` loop (up to 10 retries × 2s = 20s) to resolve the new world's image. On RAPID world/instance hops a PREVIOUS world's resolve could complete LATE and overwrite `_vrca_activity` with the stale world, then re-send it — RPC flicker / wrong-world / re-send churn that gets worse the faster the user hops (the user-reported "RPC stops/glitches more when actively on VRChat and switching worlds"). Fix: `VRCA_setActivity` bumps a `_vrca_activityGen` token on every call and `tryResolve` captures `myGen`; a resolve that lands after a newer world took over (`myGen !== _vrca_activityGen`) is ABANDONED (no overwrite, no further retries). `VRCA_clearActivity` bumps it too so a pending resolve can't re-add an activity after a clear. The `_vrca_asset_cache` write still happens so returning to a world is instant.
- **Discord RPC renderer-process recovery (`onRenderProcessGone`)**: a long-running BACKGROUND WebView's renderer process is reclaimed by the OS under memory pressure — MUCH more likely while the user is actively on VRChat (the phone app is doing heavy VRChat REST + WebSocket + presence work, so the whole device is under load). Without an `onRenderProcessGone` override that returns `true`, the OS can terminate the whole service process; and even when it doesn't, the page goes blank and the RPC is dead until the 120s JS-freeze detector eventually reloads. The handler now destroys the dead WebView and calls `loadWebView()` (fresh gateway) immediately, recovering in seconds instead of up to two minutes — and returns `true` so a renderer reclaim can never crash the process. This is a primary contributor to "RPC stops more often when on VRChat".
- **Discord RPC JS-side presence re-assertion (`_vrca_pushInterval`) — the "works for a bit then disappears while still connected" fix**: presence was re-asserted ONLY by the native 10s `presenceTimerJob` → `evaluateJavascript`. A backgrounded WebView has those native→JS calls throttled/queued by Chromium, **while Discord's OWN internal `setInterval` heartbeat keeps the gateway socket OPEN** — so the health monitor saw `alive` and the UI showed Connected, but the instant Discord dropped our activity (an idle transition, a session RESUME, or its own status-change OP 3) our native re-push couldn't run and the RPC vanished permanently on a live socket. (This — NOT the asset-resolution dead end, which only affects the FIRST appearance since the resolved image caches — is the cause of an RPC that shows initially then disappears.) **Fix: the shim now runs a `setInterval(_vrca_sendPresence, 10000)` INSIDE the page**, coupling re-assertion to the same JS engine that keeps Discord's heartbeat alive — if the socket stays up, our re-assert stays running regardless of native→JS throttling. It's deduped against the native push (`_vrca_lastSentMs` stamped in `_vrca_sendPresence`; the interval skips if a send happened within 7s) so the foreground doesn't double OP 3 — the JS interval effectively only fires when native pushes are being throttled. Guarded against duplicate intervals on shim re-injection; dies with the WebView on teardown; no-ops while `_vrca_activity` is null (after `VRCA_clearActivity`).
- **Discord RPC activity persisted in `localStorage` (survives a page RELOAD) — the "reconnected but the RPC didn't reappear until I reopened the app" fix**: the in-page re-assert interval above only re-pushes when `_vrca_activity` is set, but a page **reload** (`onSessionExpired` session recovery / cache maintenance) starts a FRESH page with `_vrca_activity = null`. The only thing that would re-set it is the native `evaluateJavascript` push — which Chromium **throttles while the app is backgrounded** — so after a reconnect-via-reload the activity stayed null and the RPC was absent until the app was FOREGROUNDED (foreground unthrottles the native push). Compounding it: under Discord's `zlib-stream` compression the `READY`/`RESUMED` frame is binary, so the shim can't detect session-ready and the only post-reconnect re-push is the socket-`open` one-shots (+3s/+8s). **Fix: `_vrca_sendPresence` writes the activity to `localStorage['_vrca_last_activity']` on every send, and the shim init (`WS_HOOK_JS`) RESTORES it into `_vrca_activity`** — so a reloaded page has the activity immediately and the in-page `_vrca_pushInterval` (which runs even backgrounded, coupled to Discord's own JS engine) re-lands the RPC within ~10s of the gateway becoming ready, with NO native push required. `VRCA_clearActivity` removes the key so a reload can't restore a cleared RPC. (A restored activity may be image-less/slightly stale for one cycle; the next full native push upgrades it — far better than an absent RPC.)
- **Discord RPC compression-independent zombie detection (`_vrca_lastFrameMs`)**: the health probe's only zombie signal was "no heartbeat-ACK for >2.5× interval", derived from parsing op11 frames — but Discord's `zlib-stream` compression makes those frames binary/unparseable, so `_vrca_lastAckMs` never updated under compression and a dead-but-open socket falsely reported `alive` (a second "socket healthy but RPC gone" path). The shim now stamps `_vrca_lastFrameMs = Date.now()` on EVERY inbound `message` event (string OR binary — the event fires regardless of compression), and `VRCA_gatewayHealth()` returns `zombie` when no inbound frame of any kind has arrived for 90s on an open socket (a healthy gateway streams heartbeat ACKs ~every 41s plus event frames continuously). This rides the existing `zombie`/`bad` → `DEGRADED_RELOAD_THRESHOLD` (4 checks) reload path.
- **Discord WebView cache cap (`startCacheMaintenance`)**: the long-lived WebView loads the full discord.com app, which persists an HTTP cache + Code Cache that grow **unbounded** — the dominant contributor to the app's runaway on-disk "cache" size. The cap measures **ONLY `cacheDir/WebView`** (HTTP cache + Code Cache — the user-visible "Cache" number in App Info) against `WEBVIEW_CACHE_CAP_BYTES` (**64 MB** — deliberately ABOVE Discord's ~30-50 MB natural working set so the clear doesn't churn+re-download every check; do not lower it below that). Checks every 30 min with the first check 3 min after start. When over cap: clears the HTTP cache (`clearCache(true)`), best-effort deletes `cacheDir/WebView` contents directly (Code Cache is NOT cleared by `clearCache`; deleting from cacheDir is safe — the OS may wipe it anytime), then **tears down the RPC state (`shimReady=false` + `stopPresenceUpdates()` + status→RECONNECTING) BEFORE `reload()`ing**. **The teardown-before-reload is critical**: the reload destroys the page's injected shim (`window.VRCA_gatewayHealth`/`VRCA_setActivity`) and the gateway, but if `shimReady` stayed `true` the session monitor kept probing the reloading page. Its probe returned `no_probe` (health function undefined) which was grouped with `"alive"` as HEALTHY — so a reloaded/dead page kept reporting CONNECTED and **never recovered on its own; only toggling the RPC off/on fixed it** (the user-reported "RPC randomly stops after a while, a toggle fixes it, seems tied to the cache clearing"). Two coordinated fixes: (1) the cache reload now resets `shimReady`/presence like a planned reconnect (but does NOT consume a `sessionRecoveryCount` attempt — it's maintenance, not a failure); `onPageFinished` re-injects the shim and resumes presence + the monitor. (2) **`no_probe` is reclassified as a gateway-missing condition** (grouped with `no_gateway`, not `alive`) — because the health probe only runs while `shimReady==true`, an undefined `VRCA_gatewayHealth` can ONLY mean the shim was destroyed by a reload, so it re-injects then reloads after the threshold. Backstop: the JS-freeze detector (`JS_FREEZE_RELOAD_MS` 120s) runs BEFORE the `if(!shimReady) continue` guard, so even a reload that never re-injects (leaving `shimReady=false`) forces `onSessionExpired()` → full WebView recreation within 2 min. **`dataDir/app_webview` (profile data — cookies, localStorage, IndexedDB) is deliberately NEVER cleared**: Discord's `localStorage.token` is the gateway auth — the JS client uses it (not the cookie alone) for gateway Identify and API calls. The old code called `WebStorage.getInstance().deleteAllData()` which wiped `localStorage` including the token, killing the Discord session every time cache maintenance ran — the cause of "RPC shows for a bit then disappears" and the WebView not appearing as a logged-in device. Removing that call fixed it: the session now survives cache clears because only expendable HTTP/Code cache is touched. The loop is started when the WebView is created and cancelled in `teardown()`/`onDestroy()`. (Affects BOTH builds — Discord RPC isn't admin-gated.) **Coil's disk cache is also explicitly capped**: `VrchatImageLoader` sets `diskCache { maxSizeBytes(10 MB) }` at `cacheDir/image_cache` — Coil's DEFAULT is 2% of FREE disk space (hundreds of MB on a modern phone), the second silent "Cache" grower; the LRU journal trims any oversized pre-existing cache down to the cap on use.
- **`PipelineWatchdogWorker`** (WorkManager, ~15 min): if logged in and the pipeline isn't connected, restarts `KeepAliveService` + `VrchatPipelineService`. Backs off when `AppShutdown.isManualKillFresh() || AppShutdown.isSwipedAway()` — the persistent `swiped_away` flag is what prevents this worker (which fires ~15 min after a swipe, long past the 15s freshness window) from resurrecting a deliberately-swiped app. An OEM/OS kill leaves the flag false, so the watchdog still recovers that. `BootReceiver` clears the swipe flag, restarts `KeepAliveService`, and reschedules the watchdog. `onTaskSwiped` best-effort calls `PipelineWatchdogWorker.cancel()`; `MainActivity.onCreate` re-schedules it on reopen.
- **`AdminRuntime`** (`com.vrca.admin`): process-lifetime singleton owning the force-kill staleness sweep and `watcherActiveAt` watcher heartbeat — moved out of Compose `LaunchedEffect` so they keep running while the admin app is backgrounded. Compose only registers intent (`setBrowsing`/`setSelectedUser`/`updateSweepData`). (The `config/adminPresence.browsingAt` heartbeat it used to write was REMOVED — see "Browse heartbeat (FULLY REMOVED)" above; `setBrowsing` now only gates the local sweep.) **Foreground gating (`setForeground`)**: the latched intent is the problem when the admin merely BACKGROUNDS the app (no swipe / Activity destroy) — `onDispose` never fires, so `browsing`/`selectedUser` stay set and the watcher loop kept writing `watcherActiveAt` every 30s indefinitely, holding that user in 10s live-sync while no admin was actually looking. `AdminScreen`'s `LifecycleEventObserver` now calls `AdminRuntime.setForeground(true/false)` on `ON_RESUME`/`ON_PAUSE`, and the watcher heartbeat, the immediate-on-select write, and the staleness sweep all gate their WRITES on `foreground.value`. The intent stays latched (not cleared), so returning to the foreground resumes exactly where it left off — but a backgrounded admin emits **zero** periodic writes. (This deliberately narrows the earlier "admin watches while backgrounded" intent in favor of cost: an admin who isn't on-screen shouldn't keep a user in live-sync.) **Directed-release APK picker state (`pickedApkState`/`ingestPickedApk`/`clearPickedApk`)**: the admin detail view's "Pick APK" (directed release) launches the system file picker (DocumentsUI). The heavy admin app (Discord WebView + foreground services + Coil loader) is routinely EVICTED while DocumentsUI is foreground, so on return the admin **Activity is recreated** — `AdminScreen.sharedUsers` (plain `remember`) resets to empty, the detail view (gated on `if (rawRow != null)`, i.e. the selected user being present in `users`) is briefly disposed, and any plain-`remember` picker state inside it is lost AND the file-copy coroutine on the composition scope is cancelled. That was the "picking an APK doesn't apply and resets the detail view" bug. Fix: the picked APK (cache path + parsed `versionCode`/`versionName` + filename + error, keyed by `docId`) lives in `AdminRuntime` and the URI→cache copy + `parseApkInfo` run on `AdminRuntime.scope` (process lifetime), NOT the composition. `UsersTab` reads it back via `pickedApkState.collectAsState()` filtered to the open `docId`, so the rebuilt detail view re-shows the pick. `ingestPickedApk(appContext, docId, uri)` uses the APPLICATION context (the Activity may not survive); `clearPickedApk(docId)` is called after a successful push (also deletes the cached file).
- **Safe foreground promotion (`Service.startForegroundSafely`, `com.vrca.app.ForegroundUtil`) — the "OEM kill → crash-loop on every reopen until reboot" fix**: the app targets **SDK 34**. On Android 12+ starting a foreground service from the BACKGROUND throws `ForegroundServiceStartNotAllowedException`, and on Android 14 the `dataSync` FGS type is itself background-start-restricted. After an OEM process kill, three background paths re-enter the FGS-promoting services — the OS's queued `START_STICKY` restarts (every service returns `START_STICKY`), the `PipelineWatchdogWorker` (`startForegroundService` from a background Worker), and the pipeline `onOpen` Discord-start. The `startForeground()` calls in `VrchatPipelineService.onStartCommand`, `DiscordRpcService.onStartCommand`, and `OverlayService.startAsForeground` were **UNWRAPPED**, so a background-initiated start threw an uncaught exception that crashed the whole (shared) process. This became a CRASH LOOP: every queued sticky-restart re-crashed the process, which **also took down the Activity the instant the user reopened the app** — so the app "kept crashing on open" until a phone REBOOT cleared the OS's pending sticky-restart queue (after which `BootReceiver`, an FGS-start-EXEMPT boot context, started the pipeline cleanly → `isConnected` → later starts re-assert safely). **`KeepAliveService` had the SAME latent crash by a different name**: its `startForeground` WAS wrapped, but the catch did NOT `stopSelf()`, and since the service is launched via `startForegroundService()`, a caught failure that leaves the service running triggers `ForegroundServiceDidNotStartInTimeException` ~5s later → crash. **Fix**: `startForegroundSafely(id, notif, tag)` wraps `startForeground` in try/catch and, on failure, logs + calls `stopSelf()` (satisfying the `startForegroundService` 5-second contract without a crash) + returns false; each `onStartCommand` returns `START_NOT_STICKY` on a false result so the OS does not immediately sticky-restart back into the same throw. Applied to all four services (KeepAlive's catch now also `stopSelf()`s; the pipeline's 10s notif-repost loop is wrapped too). Consequence: after an OEM kill the background watchdog/sticky paths can no longer crash — they degrade to a clean no-op, and the app recovers normally on the next user-foreground launch (or once battery-optimization exemption permits a background FGS start). This is purely a crash-safety fix; it does NOT change when a foreground start is *allowed* (still gated by Android's background-FGS rules), only that a *disallowed* one no longer kills the process.
- **OEM-specific power-management guidance (`OemPowerGuidance`, `com.vrca.ui.common`)**: the four FGS + wakelock + watchdog keep the process alive on stock Android, but Samsung/Xiaomi(MIUI/HyperOS)/Huawei-Honor/Oppo-Realme(ColorOS)/Vivo-iQOO/OnePlus ship proprietary killers that force-stop background apps **regardless** of the standard battery-optimization exemption — the only fix is the user adding VRC-A to that OEM's allow-list (Autostart / Protected apps / Never sleeping apps / App launch). `OemPowerGuidance.forThisDevice()` matches `Build.MANUFACTURER`/`Build.BRAND` and returns a `Guidance(brand, steps)` with exact per-OEM menu paths, or **null** on stock/Pixel/Motorola (UI shows nothing extra). Surfaced in the onboarding Permissions step's "Turn off background restrictions" card (replaced the old Samsung-only `TrustNote`) and in Settings → Help "Stops sending with screen off" FAQ. No em dashes (onboarding copy rule). This is the highest-leverage real-world survival improvement — no app code can override these killers, so guiding the user to the right setting is the only lever.
- **Bounded re-acquired wakelock (`KeepAliveService`)**: the `PARTIAL_WAKE_LOCK` is acquired with a 6h timeout (`WAKELOCK_TIMEOUT_MS`) and **re-acquired on every 30s tick** (`TICK_MS`) instead of a single indefinite `acquire()`. Android explicitly warns against indefinitely-held wakelocks and some OEMs force-release them; a bounded acquire reset each tick is "held forever" in practice while self-releasing the moment the tick loop dies (process teardown). Non-reference-counted, so a fresh timed `acquire(timeout)` just resets the timeout — no release/re-acquire dance.
- **WiFi lock (`KeepAliveService.ensureWifiLock`, both builds)**: alongside the wakelock, KeepAliveService holds a `WifiManager.WifiLock` (`WIFI_MODE_FULL_LOW_LATENCY` on API 29+, else `FULL_HIGH_PERF`; non-ref-counted, re-asserted each 30s tick, released on destroy/swipe; `ACCESS_WIFI_STATE` in the manifest). **This mirrors the reference companion (its ChatboxService holds `PARTIAL_WAKE_LOCK` + a WiFi lock) and is the second half of the headset "chatbox/avatar-size randomly stops, reopen fixes it" fix** (the first half is the OSCQuery-host send reroute above). Root cause of the fault: Wi-Fi power management sleeping/re-associating the radio re-evaluates the PROCESS's default-network binding (fwmark/`SO_MARK`), and after that transition outbound UDP to `127.0.0.1` is stamped for the Wi-Fi routing table (no loopback route → silently dropped, `send()` still "succeeds") — per-process, so a fresh socket doesn't help but an app reopen does (fresh process re-binds to a healthy network). The WiFi lock keeps the interface awake + the binding STABLE so that transition never fires. It also keeps the VRChat WebSocket / Discord gateway / Firestore alive when backgrounded/screen-off (a general background-survival win, not headset-only). `WIFI_MODE_FULL_LOW_LATENCY` degrades to `FULL` when backgrounded — still radio-awake, which is exactly the stay-connected behavior wanted. Tradeoff: higher Wi-Fi power draw 24/7 (consistent with the app already holding a wakelock + foreground services); acceptable for a companion app whose premise is "works as if on-screen."
- **Revocable-grant ON_RESUME re-check (Home `SetupHealthCard`)**: an OEM update (notably Samsung One UI) can silently REVOKE the battery-optimization exemption or Notification Access while VRC-A is backgrounded. The Home page's `LifecycleEventObserver` re-reads `isIgnoringBatteryOptimizations` + `NotificationManagerCompat.getEnabledListenerPackages` on `ON_RESUME` (not just composition entry), so the setup-health card re-surfaces the now-red item the moment the user returns instead of staying falsely green until a full recomposition.
- **Limits (be honest with users)**: no app can guarantee the OS never kills it. Survival depends on the **battery-optimization exemption** (requested via `VrcaViewModel` / surfaced on Home + Settings) and the OEM allow-list guidance above (`OemPowerGuidance`). The architecture above maximizes survival AND makes the residual kill invisible via auto-restore.

### Storage Management
- **Firestore offline cache**: explicitly capped at 25 MB in `VrcaApplication.onCreate()` via `FirebaseFirestoreSettings.Builder().setCacheSizeBytes(25MB)` (default was 100 MB — far more than needed for the few collections the public build reads).
- **Discord WebView footprint**: `cacheDir/WebView` (HTTP cache + Code Cache) capped at 64 MB, first check 3 min after start, then every 30 min. Over-cap clear: `clearCache(true)` + direct sweep of `cacheDir/WebView`. **`dataDir/app_webview` (profile data incl. `localStorage.token`) is NEVER cleared** — wiping it kills the Discord session.
- **Coil image cache** (`VrchatImageLoader`): DISK cache capped at 10 MB (`cacheDir/image_cache`) — Coil's default is 2% of free disk space. **MEMORY cache capped at 12 MB** — Coil's default in-memory bitmap cache is **25% of app RAM**, and full-resolution event banners + world/instance thumbnails accumulating there (on top of the foreground services + Discord WebView) was a real **OEM low-memory KILL trigger** (users reported more frequent background kills after the event UI shipped); the hard 12 MB cap evicts old bitmaps instead of ballooning RAM.
- **Rich-content media (`RichMediaStore`, `filesDir/rich_media/{ann,upd}`)**: lives in DATA (not cache). `ann/` (announcement media) is culled live by `VrcaScreen`'s `gcAnnouncements` when an announcement is removed/swapped. **`upd/` (patch-note media) is wiped on every cold start** (`VrcaApplication.onCreate`) — it's view-scoped (re-downloads when the Update/What's New dialog shows), and a FORCED dialog killed mid-show never runs its `onDispose` `clearUpdateMedia`, so its cached video/images would otherwise linger forever. Culling is PURELY REFERENCE-BASED with NO size cap / LRU eviction (the `enforceTotalCap` backstop was removed): `RichMediaStore.reconcile(scope, referencedUrls)` deletes exactly the files whose URL isn't in the active set and nothing else, so ACTIVE media is NEVER evicted. This is REQUIRED because **video is download-first / local-only** (see below) — there's no URL-streaming fallback for video, so evicting an active clip would break it. `gcAnnouncements(urls, confirmed)` reconciles `ann/`; **`confirmed` MUST be true** (VrcaScreen passes `announcementsLoaded`, set once the listener delivers a snapshot) so the cull NEVER runs on the initial empty list (which would wipe all cached media before the list loads). Storage = exactly the media of your active announcements/updates, cached FULLY however big; removed/swapped content is freed on the next reconcile. **Video is DOWNLOAD-FIRST (`RichVideo`)**: the clip plays ONLY from the fully-downloaded local file (`Uri.fromFile`), NEVER streamed from the URL — the play button is replaced by a spinner + "Downloading video…" until `localFile` is set, then becomes tappable (a failed download shows a Retry). Streaming a not-yet-complete/remote clip caused playback issues, so the URL fallback was removed for video (images/gifs keep `cachedFile ?: url` — a URL load is a harmless loading state for them, and reference-culling keeps them local anyway). **`richImageLoader` (rich-content Coil loader) has its OWN capped disk cache** (`cacheDir/rich_image_cache`, 15 MB) + 12 MB memory cache — without it Coil defaults to 2% of free disk (a silent cache grower); separate dir from `VrchatImageLoader`'s `image_cache` to avoid Coil's shared-dir conflict.
- **Stale update APKs**: `VrcaApplication.cleanStaleUpdateApks()` deletes any `.apk` files from `getExternalFilesDir(DIRECTORY_DOWNLOADS)` on every app start. The update download (`vrc-a-update.apk`) was never cleaned up after the system installer ran — a ~20 MB file sitting forever until the next update.
- **Friends cache** (`vrca_friends_cache`): unbounded JSON blob keyed by userId, grows with friend count (~150-300 bytes/friend). No cap but bounded by VRChat's friend limit.
- **In-app alerts** (`vrca_in_app_alerts`): capped at 20 groups × 50 events (FIFO).
- **Seen notification IDs** (`vrca_seen_notifs`): capped at 500 (FIFO).
- **Crash log** (`vrca_crash`): capped at 80,000 chars, overwritten on next crash.

## Coding Conventions
- Use Jetpack Compose for all new UI — no XML layouts
- Follow existing file/package structure when adding new screens or components
- Admin-only features must be gated behind `BuildConfig.IS_ADMIN_BUILD`
- Firestore security rules must be updated alongside any schema changes
- Keep admin APK and public APK builds fully separated

## Git & PR Conventions
- **CRITICAL — Push ALL work to `claude/vrc-a-android-app-NUpaN`.** Commit on the local `VRC-A-Official` working branch, then push to the remote `claude/vrc-a-android-app-NUpaN` branch (e.g. `git push -u origin VRC-A-Official:claude/vrc-a-android-app-NUpaN`). **Do NOT push to `VRC-A-Official` directly — the remote consistently returns HTTP 503 on that branch.** Do NOT push to `main` or any other branch. Only deviate if the user explicitly names a different target branch in this turn.
- Commit messages: short, imperative ("Fix NowPlaying pause detection", "Add Releases tab filter")
- Include a brief description in the PR body of what changed and why
- Do NOT open pull requests automatically — the user merges the work themselves on their own schedule

## Autonomous Permissions
Claude is fully authorised to do all of the following without asking for confirmation:

- Read, create, edit, and delete any files in the repository
- Run any build, test, or lint commands
- Commit changes with descriptive commit messages
- Push branches to the remote repository
- Open pull requests with descriptions of what was changed
- Merge pull requests if all checks pass and the task is self-contained
- Create and push new releases to GitHub Releases
- Update Firestore security rules when schema changes require it
- Install dependencies or update `build.gradle` files as needed
- Fix any build errors, lint warnings, or test failures encountered along the way
- Refactor code for clarity or performance without being asked
- Add or update comments and documentation inline
- **Update this CLAUDE.md file** whenever new features, architecture changes, or conventions are added

## Keeping CLAUDE.md Up To Date
Claude must treat this file as a living document and update it as part of normal development:

- **When adding a new feature or screen:** Add a brief entry under Architecture & Stack describing what it does and any key implementation notes
- **When adding a new dependency or library:** Note it under Architecture & Stack with its purpose
- **When changing build commands or adding new Gradle tasks:** Update the Build Commands section
- **When changing Firestore schema or collections:** Update the relevant notes under Architecture & Stack
- **When establishing a new pattern or convention:** Add it to Coding Conventions so it applies to future work
- **When completing a significant PR:** Review this file and add anything that would help future sessions understand the codebase better

Claude should update CLAUDE.md in the same commit as the feature it describes, not as a separate task. The goal is that any new Claude Code session can read this file and immediately understand the full current state of the project without needing to explore the codebase from scratch.

## What Claude Should Always Do
- Run `./gradlew build` after making changes to verify nothing is broken before pushing
- Check that both admin and public build variants still compile after any change
- Never hardcode secrets, API keys, or credentials — use environment variables or Firebase config
- Keep `BuildConfig.IS_ADMIN_BUILD` gates intact on all admin-only features
- If a Firestore rule change is needed, update both the rules file and note it in the PR
- Update this CLAUDE.md file whenever the architecture, conventions, or features change

## What Claude Should Never Do
- Push to `main`, `VRC-A-Official` (it 503s), or any branch other than `claude/vrc-a-android-app-NUpaN` (always push to `claude/vrc-a-android-app-NUpaN` unless the user explicitly names a different target this turn)
- Remove or weaken `bannedDevices` Firestore security rules
- Mix admin-only UI into the public build
- Break the existing GitHub Actions CI pipeline without replacing it with something better
- Leave this CLAUDE.md file out of date after making significant changes
