name: Build APK
on:
  push:
    branches:
      - "**"
    tags:
      - "v*"
  workflow_dispatch: {}

jobs:
  build:
    name: "Build release APK (no signing)"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Grant execute permission
        run: chmod +x gradlew

      # IMPORTANT:
      # - Use the project wrapper (avoids CI forcing Gradle 9.3.1+)
      # - Skip AboutLibraries tasks that crash with "Cannot mutate dependencies..."
      - name: Assemble Release (skip AboutLibraries dependency collection)
        run: |
          ./gradlew --version
          ./gradlew clean
          ./gradlew assembleRelease \
            -x collectDependencies \
            -x prepareLibraryDefinitionsRelease \
            -x prepareLibraryDefinitionsDebug \
            -x prepareLibraryDefinitions

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-release-unsigned
          path: app/build/outputs/apk/release/*.apk
